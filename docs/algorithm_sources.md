# Algorithm Sources And Independence Log

This file tracks the origin of algorithms and defaults used by GLASS. Every
Phase 2 algorithm change should update the relevant row before becoming a
default path.

## Source Categories

- `clean-room derivation`: derived from general numerical or astronomical image
  processing principles.
- `paper`: derived from a cited public paper.
- `open documentation`: derived from publicly available format or API
  documentation.
- `compatible open source`: informed by code under a license compatible with
  this project. The exact project, license, and copied/non-copied status must be
  recorded.
- `black-box experiment`: derived from user-generated settings, logs, output
  files, or measured behavior from an external tool, without using its source
  code.

## Required Fields

Each entry should include:

- module
- source category
- source citation or experiment path
- formula or behavior summary
- implementation files
- tests
- default parameter origin
- independence statement
- known limitations

## Entries

| Module | Source category | Source or experiment | Summary | Implementation files | Tests | Defaults | Independence statement | Limitations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Unclamped Lanczos3 warp min/max skip | clean-room derivation | S2-Gate 505 real 200-light A/B at `C:\glass_runs\phase2_s2_gate505_lanczos_unclamped_warp_ab_real\runs_20260623_055211` plus GLASS CUDA warp tests | Skips local min/max tracking inside GLASS Lanczos3 CUDA warp kernels when overshoot clamping is disabled (`clamping_threshold < 0`). Native timing dictionaries and resident artifacts report `lanczos3_clamping_enabled`; default 200-light Lanczos3 stack route records `false` and remains bit-identical to Gate 504. | `cpp/cuda/warp_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass/engine/resident_cuda.py`, `tests/test_gpu_warp_vs_cpu.py`, `tests/test_resident_cuda_run.py` | `python -m pytest -q`; focused CUDA warp/resident tests; real-data Gate 505 A/B | Project-defined default keeps Lanczos3 clamping disabled unless explicitly requested by `--resident-warp-clamping-threshold >= 0` | This is a GLASS-owned kernel control-flow optimization over existing project math. It uses no external implementation source and preserves the unclamped pixel formula. | The end-to-end benchmark gain is masked by read/decode/upload and orchestration variance. Fused Lanczos3 integration remains blocked by a negative probe and is not promoted. |
| Minimal resident sigma map workspace skip | clean-room derivation | S2-Gate 506 real 200-light A/B at `C:\glass_runs\phase2_s2_gate506_sigma_master_only_maps_ab_real\runs_20260623_060330` plus GLASS resident CUDA tests | Allows nullable resident sigma/winsorized-sigma map pointers and avoids allocating or writing weight, coverage, low-rejection, and high-rejection device buffers when `download_mode=master_only`. Resident artifacts record `native_map_workspace_mode=master_only_no_weight_or_diagnostic_device_maps`. | `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass/engine/resident_cuda.py`, `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | `python -m pytest -q`; focused resident CUDA master-only/full comparison tests; real-data Gate 506 A/B | Project-defined resident minimal-output policy: write/download only the final master unless a science or audit map mode requests maps | This is a GLASS-owned CUDA memory/workspace optimization over existing project sigma integration math. It does not change rejection thresholds, frame admission, weights, calibration, registration, warp, or master pixels. | The measured component gain is small and near runtime variance; the larger remaining targets are read/decode/upload orchestration and resident registration/warp residency. |
| Contiguous resident catalog workspace | clean-room derivation | S2-Gate 507 real 200-light A/B at `C:\glass_runs\phase2_s2_gate507_catalog_contiguous_soa_ab_real\runs_20260623_061314` plus GLASS resident CUDA catalog tests | Consolidates batched resident grid/NMS star-catalog `x/y/flux` device workspaces into contiguous SoA buffers and downloads final catalog outputs with one contiguous copy. Centroid pre-refine `x/y` snapshots also use one contiguous copy. Artifacts record workspace layout, allocation counts, copy counts, and output bytes. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | `python -m pytest -q`; focused resident CUDA catalog and CLI triangle-registration tests; real-data Gate 507 A/B | Project-defined resident batch catalog workspace policy: contiguous SoA layout for multi-frame resident catalog batches | This is a GLASS-owned memory-layout and transfer-count optimization. It does not change star detection thresholds, candidate ordering, centroid math, registration decisions, warp, rejection, or master pixels. | Real 200-light output remains bit-identical to Gate506, but catalog output-download timing did not materially improve because the current component is dominated by native synchronization and centroid/refine work. |
| Fused global-mean catalog centroid sync | clean-room derivation | S2-Gate 508 real 200-light A/B at `C:\glass_runs\phase2_s2_gate508_catalog_mean_fused_sync_ab_real\runs_20260623_062032` plus GLASS resident CUDA catalog tests | Launches resident global-mean centroid-background reductions in the same per-frame catalog streams as grid/NMS candidate generation, so the initial catalog stream synchronization covers both steps. Artifacts record `catalog_centroid_mean_sync_mode=fused_with_catalog_sync`, mean block count, and the fused timing model. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | native build; `python -m pytest -q`; focused resident CUDA catalog and CLI triangle-registration tests; real-data Gate 508 A/B | Project-defined resident CUDA scheduling policy for the global-mean centroid background path | This is a GLASS-owned stream scheduling optimization. It does not change star detection thresholds, candidate ordering, centroid formulas, descriptor generation, registration decisions, warp, rejection, accepted frames, or master pixels. Validation uses only GLASS tests, GLASS run artifacts, and user-generated external reference outputs. | The real 200-light output is bit-identical to Gate507 and the sync phase count drops from 3 to 2, but native sync and registration/warp orchestration remain the next main performance bottlenecks. |
| Specialized unclamped Lanczos3 batch warp | clean-room derivation | S2-Gate 509 real 200-light A/B at `C:\glass_runs\phase2_s2_gate509_lanczos_unclamped_specialized_ab_real\runs_20260623_062926` plus GLASS CUDA warp tests | Routes batch Lanczos3 warp calls with `clamping_threshold < 0` to a dedicated unclamped CUDA kernel that omits runtime clamp branching and local min/max state. Artifacts record `lanczos3_clamp_path=unclamped_specialized`. | `cpp/cuda/warp_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass/engine/resident_cuda.py`, `tests/test_gpu_warp_vs_cpu.py`, `tests/test_resident_cuda_run.py` | native build; `python -m pytest -q`; focused CUDA warp and resident CLI tests; real-data Gate 509 A/B | Project-defined native CUDA dispatch policy: unclamped Lanczos3 uses the specialized path; clamped Lanczos3 keeps the generic runtime-clamp path | This is a GLASS-owned CUDA control-flow specialization over existing Lanczos3 math. It does not change interpolation weights, fill behavior, registration matrices, accepted frames, rejection, integration, output-map policy, or master pixels. Validation uses only GLASS tests, GLASS run artifacts, and user-generated external reference outputs. | The real 200-light master is bit-identical to Gate508 and the native warp sync component improves slightly, but the next major speed target remains broader resident registration/warp batching and I/O orchestration. |
| Resident calibration contract auto-bundle | clean-room derivation | S2-Gate 510 real 200-light A/B at `C:\glass_runs\phase2_s2_gate510_resident_calibration_contract_ab_real\runs_20260623_063856` plus GLASS contract tests | Emits `resident_calibration_contract.json` during resident CUDA runs, records it in `run_state.json`, and lets `glass stack-engine-contract --run ...` auto-discover the file so calibration and integration resident StackEngine surfaces can be audited from one run directory. | `src/glass/engine/resident_cuda.py`, `src/glass/report/stack_engine_contract.py`, `src/glass/cli.py`, `tests/test_stack_engine_contract.py`, `tests/test_resident_cuda_run.py` | focused contract tests; resident CUDA smoke; real-data Gate 510 A/B and default-ready contract audit | Project-defined run artifact self-containment policy | This is a GLASS-owned contract/artifact wiring change. It uses no external implementation source and does not change calibration, registration, warp, rejection, integration math, or output pixels. Validation uses GLASS artifacts and the existing user-generated WBPP compare metrics only after proving Gate510 output is bit-identical to Gate509. | Strict native StackEngine default remains false because these are resident CUDA StackEngine-surface contracts, not CPU StackEngine native surfaces. The gate closes default-promotion self-containment for the resident path, not future native/default engine policy work. |
| Resident throughput-v3 I/O prefetch retune | clean-room scheduling optimization | S2-Gate 511 real 200-light probe at `C:\glass_runs\phase2_s2_gate511_probe_io_registration\runs_20260623_064702` and default run at `C:\glass_runs\phase2_s2_gate511_v3_io_prefetch48_default_ab_real\runs_20260623_064914` | Retunes the default resident `throughput-v3-io` preset from 32 prefetch frames / 12 workers to 48 prefetch frames / 16 workers after a same-cache 200-light probe showed lower main-thread read wait and a small end-to-end win. Calibration batch/wave settings remain unchanged at 16/4/4 because batch-24 regressed. | `src/glass/cli.py`, `src/glass/report/benchmark_contract.py`, `tests/test_cli_smoke.py`, `tests/test_acceptance_audit.py`, `docs/phase2_algorithm_hardening.md`, `docs/algorithm_sources.md` | focused CLI/acceptance tests; real-data default 200-light run; StackEngine contract audit | Project-defined runtime scheduling policy derived from GLASS timing artifacts | This is a GLASS-owned scheduling default over existing resident prefetch and pinned-ring upload controls. It does not change calibration formulas, registration, warp, rejection, integration, DQ semantics, CUDA kernels, or output pixels. Validation uses GLASS artifacts and user-generated benchmark comparison data only. | The gain is modest on the current warm-cache benchmark (`6.6584 s` to `6.5766 s`) and read wait is not the largest remaining component. Registration/warp residency and larger batching remain the next major speed target. |
| Explicit resident warp chunk capacity probe | clean-room native scheduling experiment | S2-Gate 512 real 200-light probe at `C:\glass_runs\phase2_s2_gate512_warp_chunk_probe\runs_20260623_065637` plus GLASS CUDA warp tests | Makes explicit `--resident-warp-chunk-capacity-frames` requests able to exceed the old native preferred value of 8, then probes chunk16 and chunk24 against the Gate511 default. Both larger chunks remain bitwise identical to Gate511 but do not improve runtime, so the default stays at native preferred 8. | `cpp/src/native_bindings.cpp`, `tests/test_gpu_warp_vs_cpu.py`, `docs/phase2_algorithm_hardening.md`, `docs/algorithm_sources.md` | native build; focused CUDA warp/CLI/resident tests; real-data chunk16/chunk24 200-light A/B | Project-defined explicit tuning knob; default remains native preferred chunk 8 | This is a GLASS-owned allocator/scheduling experiment over the existing resident matrix-warp path. It uses only GLASS timing artifacts and GLASS output comparisons, and it does not change interpolation formulas, registration matrices, accepted frames, rejection, integration, DQ semantics, or default output pixels. | Larger chunks reduce warp/postprocess launch count from 24 to 12/8, but native warp total regresses from `0.4617859 s` to `0.4766107 s`/`0.4927578 s`; the next target is eliminating the workspace/scatter round trip or a bitwise-safe warp+integration fusion rather than simply enlarging chunks. |
| Resident Lanczos3 NaN footprint parity | clean-room CUDA semantic hardening | S2-Gate 513 real 200-light validation at `C:\glass_runs\phase2_s2_gate513_lanczos_nan_parity\runs_20260623_070538` plus GLASS CUDA warp/fused tests | Aligns resident batch Lanczos3 warp with the single-frame Lanczos3 warp and fused matrix sampler by skipping non-finite source samples inside the 6x6 Lanczos footprint. Fused matrix timing now records `clamping_threshold` for route auditability. | `cpp/cuda/warp_kernels.cu`, `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `tests/test_cuda_resident_stack.py`, `docs/phase2_algorithm_hardening.md`, `docs/algorithm_sources.md` | native build; focused CUDA warp/fused tests; fused CLI tests; full pytest; real 200-light stack/fused A/B | Project-defined Lanczos3 source-NaN footprint semantics shared by single-frame, batch, and fused routes | This is a GLASS-owned CUDA semantic consistency fix. It uses no external implementation source and does not change registration matrices, accepted frames, rejection thresholds, weighting, DQ policy, or default output pixels for the validated 200-light benchmark. | Gate513 default stack remains bitwise identical to Gate511, but explicit Lanczos3+winsorized `fused_matrix` remains slower and not bitwise equal (`RMS=0.460783...`), so the fused route is still not promoted for the real 200-light science path. |
| Current default real 200-light A/B audit | clean-room real-data benchmark and contract refresh over GLASS artifacts | S2-Gate 514 cold-start audit-map run at `C:\glass_runs\phase2_s2_gate514_real_200_ab\runs_20260623_071127` | Refreshes the audit-map benchmark contract required command tokens from the older Gate460 hand-tuned parity route to the current default A/B route, then validates the current resident CUDA stack path against the user-generated WBPP black-box master and timing. | `benchmarks/phase2_m38_h_200_audit_maps_contract.json`, `docs/phase2_algorithm_hardening.md`, `docs/algorithm_sources.md`, `runs/checkpoints/s2_gate_514_status.md`, `runs/checkpoints/s2_gate_514_real_200_ab_summary.json` | real 200-light GLASS run; WBPP compare; resident calibration/result contracts; pipeline and StackEngine contracts; acceptance audit; focused benchmark-contract tests; full pytest | Project-defined benchmark acceptance policy over current GLASS defaults, existing comparison scale/offset, and user-generated WBPP black-box outputs | Validation consumes GLASS outputs, GLASS contracts, the user-staged M38 H-alpha plan/manifest, and user-generated WBPP timing/reference files. It does not inspect external implementation source, change image formulas, modify input directories, or use proprietary behavior. | The cold-start audit-map route completed in `20.824213800020516 s` internally (`21.205785 s` shell), compared to WBPP `1092.541 s`, giving `52.46493387418658x` internal speedup while preserving RMS `0.0017794216505176163`, p99 abs diff `0.00042621337808668863`, and coverage fraction `0.960532609259836`. |
| Default shared resident master cache | clean-room cache/resume policy over GLASS calibration master files | S2-Gate 515 cold/warm default-auto audit-map run at `C:\glass_runs\phase2_s2_gate515_default_shared_master_cache\runs_20260623_072125` | Changes resident master-cache default policy from per-run local cache to output-parent shared cache under `--resident-master-cache-policy auto`, records requested/effective/source fields in timing and resident artifacts, and keeps `--resident-master-cache-policy run` as the legacy isolated option. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py`, `tests/test_resident_cuda_run.py`, `docs/phase2_algorithm_hardening.md`, `docs/algorithm_sources.md`, `runs/checkpoints/s2_gate_515_status.md`, `runs/checkpoints/s2_gate_515_default_shared_master_cache_summary.json` | focused ruff; focused resident CUDA cache tests; real 200-light cold/warm default-auto audit; cold/warm bitwise compare; WBPP compare; resident/pipeline/StackEngine contracts; acceptance audit | Project-defined cache policy using existing content-addressed master fingerprints and output-parent cache placement | Validation consumes GLASS cache manifests, GLASS artifacts, user-staged M38 H-alpha inputs/plans, and user-generated WBPP black-box timing/reference outputs only. It does not inspect external implementation source, change calibration formulas, registration, warp, rejection, DQ semantics, integration formulas, or input data. | Warm default-auto cache reduced master build/load from `4.839396100025624 s` to `0.4271910000243224 s`, total internal time from `21.563539599999785 s` to `16.721956100023817 s`, and preserved bitwise equality with the cold run. Warm speedup versus WBPP reached `65.33571751204656x` internal / `63.942395594326506x` shell. |
| Resident surface contract stats reuse | clean-room contract-performance optimization over GLASS DQ/map artifacts | S2-Gate 516 real 200-light cold/warm audit-map run at `C:\glass_runs\phase2_s2_gate516_surface_contract_stats_reuse\runs_20260623_073659` plus pre-change current-default A/B at `C:\glass_runs\phase2_s2_gate516_current_default_ab\runs_20260623_072959` | Reuses resident output diagnostics, DQ provenance, DQ summary, and one-pass count-map statistics when building the resident StackEngine surface contract, instead of rescanning every full-size output map inside `build_resident_integration_stack_surface_contract`. The full disk-backed `resident-result-contract --pixel-verify` remains the independent audit step. | `src/glass/engine/resident_stack_surface.py`, `src/glass/engine/resident_cuda.py`, `tests/test_resident_stack_surface.py`, `docs/phase2_algorithm_hardening.md`, `docs/algorithm_sources.md`, `runs/checkpoints/s2_gate_516_status.md`, `runs/checkpoints/s2_gate_516_surface_contract_stats_reuse_summary.json` | focused ruff; resident surface/contract tests; real 200-light cold/warm A/B; cold/warm bitwise compare; WBPP compare; resident/pipeline/StackEngine contracts with pixel verification; acceptance audit | Project-defined invariant: run-time surface contracts may consume statistics produced earlier in the same resident pass when the separate result-contract pixel verifier is kept for full FITS map audit | This is a GLASS-owned contract-construction optimization. It changes no calibration, registration, warp, rejection, DQ, integration, or output-map pixels, and consumes only GLASS-generated arrays/artifacts plus user-generated WBPP timing/reference outputs. | Warm default-auto audit time dropped from `16.68259979999857 s` pre-change to `11.345701600017492 s`, preserving bitwise cold/warm equality and the established WBPP comparison metrics. The next major bottlenecks are still light read/upload/calibration and resident registration/warp, not this contract builder. |
| Resident DQ provenance fast path | clean-room DQ/provenance map-pass consolidation over GLASS resident maps | S2-Gate 517 real 200-light cold/warm audit-map run at `C:\glass_runs\phase2_s2_gate517_dq_provenance_fastpath\runs_20260623_074736` plus profile run at `C:\glass_runs\phase2_s2_gate517_profile\runs_20260623_074248` | Reduces resident DQ/provenance overhead by batching output percentiles in one NumPy call, emitting resident DQ summary from the masks already used to build the DQ map, recording count-map contract fields without extra mean work, and reusing geometric warp coverage as finite pre-rejection coverage when source-DQ has not invalidated samples. Fallback to the older coverage+low/high path remains active when source-DQ changes samples. | `src/glass/engine/resident_cuda.py`, `tests/test_resident_cuda_run.py`, `docs/phase2_algorithm_hardening.md`, `docs/algorithm_sources.md`, `runs/checkpoints/s2_gate_517_status.md`, `runs/checkpoints/s2_gate_517_dq_provenance_fastpath_summary.json` | focused ruff; focused DQ/provenance tests; resident CUDA/StackEngine tests; real 200-light cold/warm A/B; cold/warm bitwise compare; WBPP compare; resident/pipeline/StackEngine contracts with pixel verification; acceptance audit | Project-defined DQ provenance equivalence: geometric warp coverage is a valid pre-rejection coverage source only when source-DQ applied zero invalid samples; otherwise GLASS recomputes pre-rejection coverage from post-rejection coverage plus low/high rejection maps | This is a GLASS-owned DQ/provenance optimization. It changes no calibration, registration, warp, rejection thresholds, integration math, accepted frames, DQ flag definitions, or output pixels. Validation uses GLASS artifacts and user-generated WBPP black-box timing/reference outputs only. | Warm default-auto audit time dropped from Gate516 `11.345701600017492 s` to `8.503292900044471 s` internally, with bitwise cold/warm equality and unchanged WBPP comparison metrics. The remaining warm path is now dominated by read/upload/calibration and resident registration/warp. |
| Resident DQ stats reuse in provenance | clean-room DQ/provenance stats reuse over GLASS resident maps | S2-Gate 518 real 200-light cold/warm audit-map run at `C:\glass_runs\phase2_s2_gate518_dq_stats_reuse\runs_20260623_094619` plus current-HEAD pre-change profile at `C:\glass_runs\phase2_s2_gate518_profile_current\runs_20260623_094158` | Lets `_resident_dq_map(..., return_stats=True)` return reusable post-coverage, geometric-coverage, low/high rejection-map, and rejection-mask-union stats; `_resident_dq_coverage_provenance` consumes those stats for the source-DQ-off geometric fast path instead of rescanning the same full-size maps. Fallback remains unchanged when source-DQ changes samples or callers do not provide stats. | `src/glass/engine/resident_cuda.py`, `tests/test_resident_cuda_run.py`, `docs/phase2_algorithm_hardening.md`, `docs/algorithm_sources.md`, `runs/checkpoints/s2_gate_518_status.md`, `runs/checkpoints/s2_gate_518_dq_stats_reuse_summary.json` | focused ruff; focused DQ/provenance tests; resident CUDA/StackEngine tests; real 200-light cold/warm/repeat A/B; cold/warm bitwise compare; WBPP compare; resident/pipeline/StackEngine contracts with pixel verification; acceptance audit | Project-defined DQ provenance contract: statistics produced by the same DQ-map construction pass may be reused by provenance only when the run records `precomputed_dq_stats_used=true` and source-DQ has not modified samples | This is a GLASS-owned DQ/mask contract-performance optimization. It changes no calibration, registration, warp, rejection thresholds, integration math, accepted frames, DQ flag definitions, or output pixels. Validation uses GLASS artifacts and user-generated WBPP black-box timing/reference outputs only. | Profiled current-HEAD warm time improved from `11.682714099995792 s` to `10.030273599957582 s` (`1.164745306652971x`) and cold/warm masters are bitwise equal. The non-profiled warm run reached `117.2097354043666x` versus WBPP but was slower than Gate517 because resident registration varied upward under observed GPU contention, so the next gate should target registration/warp or rerun under a quieter GPU. |
| Phase 2 core contracts | clean-room derivation | `docs/phase2_algorithm_hardening.md` | Defines `ImageSource`, `TileWindow`, `DQMask`, `FrameTransform`, stack policies, output-map policies, and `StackRequest` as project-owned execution contracts. | `src/glass/engine/contracts.py`, `src/glass/io/image_source.py` | `tests/test_phase2_contracts.py` | Project-defined | Interfaces are derived from GLASS pipeline requirements and do not encode external implementation details. | The contracts are minimal Gate 1 forms; StackEngine behavior is added in later gates. |
| Resident warp input parity attribution | clean-room derivation | S2-Gate 425 checkpoint over GLASS CPU registered-cache and resident CUDA registration artifacts | Replays CPU calibrated checkpoint frames through resident CUDA bilinear warp using CPU and resident registration matrices, compares the results to GLASS CPU `registered_cache`/`coverage_cache`, and attributes downstream rejection-map drift to either the warp kernel contract or resident registration-matrix precision. | `src/glass/report/resident_warp_input_audit.py`, `src/glass/cli.py` | `tests/test_resident_warp_input_audit.py`, `runs/checkpoints/s2_gate_425_warp_input_audit.json` | Project-defined RMS and matrix-delta thresholds for checkpoint attribution | Uses GLASS-owned CPU/resident artifacts and generic bilinear warp/matrix comparison math only. No external implementation source or proprietary behavior is used. | This is an attribution audit, not a production warp replacement. It loads checkpoint-sized frames and currently supports bilinear resident warp only. |
| Resident GPU catalog centroid refinement | clean-room derivation | S2-Gate 426 median-window centroid checkpoint over GLASS synthetic CPU/resident registration artifacts | Refines resident top-NMS/grid-NMS star catalogs on the GPU by computing a median local background in a bounded window and applying positive flux-weighted centroids before resident triangle translation refinement. Artifact summaries record refined/failed star counts and maximum centroid shift. | `cpp/cuda/star_detect_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py`, `tests/test_gpu_registration_search.py`, `runs/checkpoints/s2_gate_426_gpu_median_centroid_warp_input_audit.json` | Project-defined median-window centroid default for resident triangle catalogs; pixel NCC refine remains opt-in | The centroid formula is derived from GLASS's own CPU centroid probe and generic flux-centroid math. Validation uses GLASS-generated synthetic checkpoint artifacts only. No external implementation source or proprietary behavior is used. | This removes the CPU tile-download centroid dependency but does not yet close resident registration-matrix parity or winsorized rejection semantic parity. The current kernel clamps centroid radius to a bounded small window for predictable local-memory use. |
| Resident rejection attribution evidence bridge | clean-room derivation | S2-Gate 427 over Gate424 exact-input proof and Gate426 median-centroid resident output maps | Lets `resident-rejection-sample-audit` consume a `resident-rejection-input-audit` artifact so sample-map failures remain visible while recommendations use stronger exact-input CPU/CUDA parity evidence. Also fixes the ready recommendation to require rejected, pre-rejection, and same-pre thresholds to pass together. | `src/glass/report/resident_rejection_sample_audit.py`, `src/glass/report/resident_rejection_input_audit.py`, `src/glass/cli.py` | `tests/test_resident_rejection_sample_audit.py`, `tests/test_resident_rejection_input_audit.py`, `runs/checkpoints/s2_gate_427_rejection_sample_attributed_audit.json` | Project-defined attribution precedence: exact-input parity evidence overrides sample-only semantic guesses | Uses GLASS-owned CPU/resident artifacts and generic count-map arithmetic only. No external implementation source or proprietary behavior is used. | This is a diagnostic contract correction. It does not change rejection math, CUDA kernels, registration matrices, or output pixels; it prevents the next optimization gate from targeting the already-proven hardened winsorized kernel. |
| XISF metadata and cache-safe input | open documentation / clean-room derivation | Public XISF container structure as observed from user-generated files and GLASS test fixtures | Reads the XISF XML header without loading pixel payloads, extracts FITS keywords/properties and attachment metadata, provides an uncompressed monochrome attachment tile reader, and streams supported XISF inputs into run-local FITS cache files before calibration. | `src/glass/io/xisf_io.py`, `src/glass/metadata/xisf_reader.py`, `src/glass/io/image_source.py`, `src/glass/engine/pipeline.py` | `tests/test_xisf_io.py`, `tests/test_xisf_metadata.py`, `tests/test_pipeline_fixture.py` | Project-defined supported subset | Parser and cache path are project-owned and read user-generated XISF files as data. No proprietary implementation source is used. | Supports uncompressed attachment-backed monochrome images for pixel reads; compressed and multi-channel XISF fail explicitly while metadata scan remains header-only. |
| HTML report coverage | clean-room derivation | GLASS artifact schema and Phase 2 gate requirements | Reports stage coverage, XISF input cache, master statistics, quality, registration, LN, integration maps, output-map policy, DQ summaries, coverage provenance, resident geometric warp coverage, resident timing/memory, runtime overview, and warnings/errors from JSON artifacts. | `src/glass/report/html_report.py` | `tests/test_pipeline_fixture.py`, `tests/test_cli_smoke.py` | Project-defined | Report content is derived from GLASS-owned artifacts and gate requirements. | Report is static HTML; richer plots and embedded benchmark comparison panels remain future work. |
| S2-Gate 10 real-data regression | black-box experiment | `C:\glass_runs\phase2_s2_gate_10_200\glass_flatfloor005_20260531_183101`, compared with the user-generated external reference master at `C:\gpwbpp_runs\final_m38_h_200\pixinsight_wbpp_blackbox\master\masterLight_BIN-1_9600x6422_EXPOSURE-600.00s_FILTER-H_mono_fastIntegration.xisf` | Validates the current resident CUDA 200-light path, XISF reference-image read, scaled comparison metrics, report generation, and the Phase 1 comparison mouthful that includes `flat_floor=0.05`, coverage threshold 190, and the recorded linear scale/offset. | `src/glass/io/xisf_io.py`, `src/glass/report/compare_report.py`, `src/glass/report/html_report.py` | `python -m pytest -q`; targeted CUDA tests; real-data run/compare/report commands recorded in `runs/checkpoints/s2_gate_10_status.md` | Experiment-derived Phase 1 baseline parameters | Uses only user-generated images, JSON artifacts, timing, and output files. No proprietary implementation source is used. | Current editable-tree runtime is slower than the release baseline; the checkpoint records the regression and next optimization target. |
| Benchmark acceptance contract | clean-room derivation / black-box experiment | `benchmarks/phase2_m38_h_200_contract.json` and S2-Gate 10 real-data artifacts | Encodes the real 200-light benchmark's required frame counts, active-frame floor, resident CUDA command tokens, flat-floor setting, compare scale/offset, coverage threshold, image-difference bounds, and release-runtime envelope so acceptance audit fails on silent benchmark drift. | `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py`, `src/glass/cli.py` | `tests/test_acceptance_audit.py` | Experiment-derived from Phase 1 release and S2-Gate 10 accepted run | The contract consumes only GLASS artifacts and user-generated benchmark/reference outputs. It records validation requirements, not external implementation details. | The contract guards a specific M38 H-alpha benchmark and should be versioned when the benchmark dataset or accepted processing recipe changes. |
| DQ provenance acceptance contract | clean-room derivation | S2-Gate 28 benchmark DQ contract over GLASS `dq_provenance_summary`, resident `dq_coverage_provenance`, and resident `dq_summary` artifacts | Makes acceptance audit collect normalized DQ provenance records from integration and resident artifacts, normalizes legacy resident DQ provenance into the compact S2-Gate 27 summary shape, and lets the 200-light benchmark contract require DQ source schemas, engines, DQ map presence, active frame count, source terms, and output DQ flags. | `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py`, `benchmarks/phase2_m38_h_200_contract.json` | `tests/test_acceptance_audit.py` | Project-defined DQ audit requirements over the resident 200-light benchmark | The audit consumes GLASS-owned artifacts and does not derive from external implementation behavior. Legacy normalization is a compatibility bridge for GLASS S2-Gate 17-24 artifacts only. | The S2-Gate 28 contract verifies artifact presence and summary consistency; S2-Gate 29 adds optional FITS pixel-count verification. |
| DQ FITS map pixel verification | clean-room derivation | S2-Gate 29 DQ bitfield verification contract | Reads DQ FITS maps in tiles, counts requested bitfield flags from pixels, and lets acceptance audit compare those counts with artifact `output_dq_summary` values under an explicit tolerance. | `src/glass/report/dq_map_verify.py`, `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py`, `benchmarks/phase2_m38_h_200_contract.json` | `tests/test_dq_map_verify.py`, `tests/test_acceptance_audit.py` | Project-defined bit counts from `DQFlag` semantics | The verifier counts GLASS-owned DQ map pixels and does not use external implementation behavior. | Pixel verification is opt-in because full-size maps add audit I/O. It verifies selected summary counts, not every possible DQ semantic transition. |
| Coverage and rejection map consistency verification | clean-room derivation | S2-Gate 30 output count-map verification contract | Reads scalar coverage and rejection count maps in tiles, verifies coverage finite/zero-pixel counts against DQ provenance and DQ `NO_DATA` summary, verifies written low/high rejection maps against DQ low/high flags and rejected-sample provenance, and treats intentionally skipped rejection maps as explicit policy-driven audit results. | `src/glass/report/dq_map_verify.py`, `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py`, `benchmarks/phase2_m38_h_200_contract.json` | `tests/test_dq_map_verify.py`, `tests/test_acceptance_audit.py` | Project-defined count-map invariants from resident output-map policy and `DQFlag` semantics | The verifier consumes GLASS-owned count maps, DQ summaries, and provenance artifacts only. | Real 200-light science-mode runs skip low/high rejection FITS maps by policy, so those maps are verified as skipped unless an audit-mode run writes them. |
| Real-data audit-map rejection verification | clean-room derivation / black-box experiment | S2-Gate 31 run `C:\glass_runs\phase2_s2_gate_31_200\resident_audit_maps_20260531` and `benchmarks/phase2_m38_h_200_audit_maps_contract.json` | Runs the 200-light resident CUDA benchmark with `--resident-output-maps audit`, writes low/high rejection count maps, verifies their positive-pixel counts against DQ low/high flags, verifies their rounded sample sum against `rejected_sample_count` with explicit tolerance, and compares the audit-map master against the same user-generated reference output. | `benchmarks/phase2_m38_h_200_audit_maps_contract.json`, `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py` | `tests/test_acceptance_audit.py`, real-data acceptance audit artifact `C:\glass_runs\phase2_s2_gate_31_200\resident_audit_maps_20260531\phase2_contract_acceptance_audit_s2_gate_31.json` | Project-defined audit-map contract; comparison transform inherited from the Phase 1 benchmark record | Uses only GLASS-owned artifacts and user-generated reference/timing outputs. No external implementation source is used. | Rejection sample-sum tolerance is `4` samples to allow integer count-map write rounding; observed real-data delta is `2` over `62484984` rejected samples. |
| Resident artifact output path completeness | clean-room derivation / black-box experiment | S2-Gate 32 run `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531` and `benchmarks/phase2_m38_h_200_audit_maps_contract.json` | Mirrors master, weight, coverage, low-rejection, high-rejection, and DQ map paths from integration outputs into `resident_artifacts.json`, mirrors resident output write storage metadata, and lets the strict benchmark contract require resident artifact map paths that exist on disk. | `src/glass/engine/resident_cuda.py`, `src/glass/report/benchmark_contract.py`, `benchmarks/phase2_m38_h_200_audit_maps_contract.json` | `tests/test_resident_cuda_run.py`, `tests/test_acceptance_audit.py`, real-data acceptance audit artifact `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531\phase2_contract_acceptance_audit_s2_gate_32.json` | Project-defined resident artifact schema and audit-map contract | Uses only GLASS-owned artifacts and user-generated reference/timing outputs. No external implementation source is used. | This gate improves artifact self-containment and auditability; it does not change resident CUDA image math or output pixel values. |
| Resident output map report provenance | clean-room derivation / black-box experiment | S2-Gate 33 report regeneration over `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531` | Adds a resident output-map report table that resolves artifact paths against the run directory and displays map policy status, path existence, storage dtype, estimated MiB, and per-map write timing. | `src/glass/report/html_report.py`, `src/glass/cli.py` | `tests/test_cli_smoke.py`, regenerated report `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531\s2_gate_33_report.html` | Project-defined report presentation over GLASS artifact schema | Uses only GLASS-owned artifact JSON and output files. No external implementation source is used. | Report existence checks are diagnostic snapshots at report-generation time; moving or deleting artifacts after report creation can make the static report stale. |
| Focused HTML report summaries | clean-room derivation / black-box experiment | S2-Gate 34 report regeneration over `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531` | Replaces large nested integration-output and DQ provenance table cells with compact integration output, DQ summary, geometric coverage, and output-diagnostic tables while preserving detailed JSON artifacts on disk. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py`, regenerated report `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531\s2_gate_34_report.html` | Project-defined report presentation over GLASS artifact schema | Uses only GLASS-owned artifact JSON and output files. No external implementation source is used. | This is a presentation-only gate; it makes reports easier to inspect but does not change artifact schemas or science calculations. |
| Benchmark comparison report summary | clean-room derivation / black-box experiment | S2-Gate 35 report regeneration over `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531` plus `s2_gate_32_compare.json` and `phase2_contract_acceptance_audit_s2_gate_32.json` | Auto-discovers compare and acceptance-audit JSON artifacts for `glass report`, then surfaces speedup, elapsed times, RMS/P99 difference, coverage fraction, compared pixels, active-frame counts, calibration frame counts, acceptance status, contract name, and check counts in the main HTML report. | `src/glass/report/html_report.py`, `src/glass/cli.py` | `tests/test_cli_smoke.py`, regenerated report `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531\s2_gate_35_report.html` | Project-defined report presentation over GLASS compare/audit artifacts | Uses only GLASS-owned artifacts and user-generated reference/timing outputs already captured by compare and acceptance-audit JSON. No external implementation source is used. | The report summarizes the newest matching JSON files unless explicit CLI paths are provided; JSON artifacts remain authoritative. |
| Acceptance failure report triage | clean-room derivation | S2-Gate 36 failed-check report fixture and green real-data report regeneration | Adds a main-report table listing only failed acceptance-audit checks with check name, note, actual value, required value or threshold, and compact remaining evidence fields. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py`, regenerated report `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531\s2_gate_36_report.html` | Project-defined report presentation over GLASS acceptance-audit schema | Uses only GLASS acceptance-audit JSON produced from GLASS artifacts and user-generated reference/timing outputs. No external implementation source is used. | Green reports intentionally show no failure rows; the full check list remains in the acceptance-audit JSON. |
| Report navigation anchors | clean-room derivation / black-box experiment | S2-Gate 37 report regeneration over `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531` | Adds a static table of contents, stable section ids, and per-section anchor links to large HTML reports so timing, benchmark comparison, resident map, and acceptance-failure diagnostics are directly addressable. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py`, regenerated report `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531\s2_gate_37_report.html` | Project-defined report presentation over GLASS artifact schema | Uses only GLASS-owned report sections and artifacts. No external implementation source is used. | Static anchors only; collapsible sections, table pagination, and client-side search remain future report usability work. |
| Large report table limits | clean-room derivation / black-box experiment | S2-Gate 38 report regeneration over `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531` | Limits high-cardinality HTML tables to a first-page preview and shows displayed row count, total row count, and authoritative JSON artifact path so reports stay readable while full data remains on disk. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py`, regenerated report `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531\s2_gate_38_report.html` | Project-defined report presentation over GLASS artifact schema | Uses only GLASS-owned report sections and artifacts. No external implementation source is used. | This is a presentation-only preview limit; JSON/FITS artifacts are not truncated and remain authoritative. |
| Benchmark performance diagnostics | clean-room derivation / black-box experiment | `benchmarks/phase2_m38_h_200_contract.json`, `C:\glass_runs\cuda_version_matrix_200\v8_cuda11_m38_h_200`, and S2-Gate 10 real-data artifacts | Compares current resident CUDA per-stage timings against the recorded release baseline and emits non-blocking regression diagnostics in acceptance audit so optimization targets are explicit. | `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py` | `tests/test_acceptance_audit.py` | Experiment-derived timing baseline with project-defined warning factor | Uses GLASS timing artifacts and user-generated benchmark outputs only; it does not encode external implementation details. | Diagnostics warn on stage-level regressions but only the existing total-runtime contract is a hard acceptance gate. |
| Resident I/O overlap diagnostics | clean-room derivation / black-box experiment | S2-Gate 16 resident artifacts and `benchmarks/phase2_m38_h_200_contract.json` | Separates light-read consumer wait wall time from cumulative read-worker FITS open/decode time, records prefetch overlap savings and ratios, and marks worker-cumulative benchmark stages as informational so overlapped parallel work is not misread as wall-clock regression. | `src/glass/engine/resident_cuda.py`, `src/glass/report/benchmark_contract.py` | `tests/test_resident_cuda_run.py`, `tests/test_acceptance_audit.py` | Project-defined timing semantics, benchmark baseline from Phase 1 release artifacts | Timing semantics are derived from GLASS-owned prefetch scheduling and benchmark artifacts; no external implementation behavior is encoded. | Overlap savings are diagnostic estimates from stage accounting, not a hardware profiler trace; deeper profiling with CUDA events and OS I/O counters remains future work. |
| Resident source-DQ no-input fast skip | clean-room scheduling derivation / black-box experiment | S2-Gate 479 real M38 H-alpha 200-light run under `C:\glass_runs\phase2_s2_gate479_source_dq_fastskip_ab_real` | When resident FITS ingestion uses guarded raw-u16 GPU decode, inline source-DQ is off, and no plan/calibration sidecar DQ masks are present, the resident light loop skips per-frame source-DQ row construction while recording `no_source_dq_fast_skip` counts in `source_dq_summary` and `resident_io_pipeline`. The benchmark output is pixel-identical to the Gate478 baseline. | `src/glass/engine/resident_cuda.py`, `src/glass/engine/resident_source_dq.py` | `tests/test_resident_source_dq.py`, `tests/test_resident_cuda_run.py`, Gate479 200-light acceptance and Gate479-vs-Gate478 compare artifacts | Project-defined no-input DQ skip guard from raw integer FITS payload semantics | The optimization only bypasses empty GLASS bookkeeping when the run proves there is no source-DQ input to apply. It does not change calibration, registration, rejection, DQ output maps, or image pixels, and it uses only GLASS artifacts plus user-generated benchmark/reference outputs. | It does not reduce disk read time and measured load-loop wall time remains disk/scheduler dependent. The path is disabled whenever inline source-DQ or sidecar masks are present, and floating-source NaN detection still uses the regular per-frame path. |
| Resident light-loop master-load accounting | clean-room diagnostic derivation / black-box experiment | S2-Gate 480 real M38 H-alpha 200-light run under `C:\glass_runs\phase2_s2_gate480_lightloop_accounting_ab_real` | Splits resident light-loop timing into master calibration build/load performed inside the light loop, read wait, native H2D/calibration/store, and remaining orchestration. The corrected `light_loop_unaccounted` excludes master build/load, while `light_loop_unaccounted_without_master` preserves the legacy diagnostic value for comparison. The Gate480 output is pixel-identical to Gate479. | `src/glass/engine/resident_cuda.py`, `src/glass/engine/resident_light_pipeline_profile.py`, `src/glass/report/html_report.py` | `tests/test_resident_light_pipeline_profile.py`, `tests/test_resident_cuda_run.py`, Gate480 200-light acceptance and Gate480-vs-Gate479 compare artifacts | Project-defined timing decomposition over GLASS resident scheduler state | The timing decomposition observes GLASS-owned stages and artifacts only. It does not alter calibration, registration, rejection, integration formulas, output pixels, or use external implementation behavior. | This is a diagnostic semantics correction, not a speed optimization. It identifies master cache/build reuse as the dominant current resident load-loop target on the 200-light benchmark and leaves physical FITS read scheduling as a separate bottleneck. |
| Resident master-cache StackEngine builder | clean-room execution-path migration / black-box experiment | S2-Gate 482 real M38 H-alpha 200-light A/B after resident master-cache builder migration | Builds resident CUDA master-cache bias/dark/flat arrays through GLASS `CPUStackEngine`, records StackEngine metrics and DQ provenance summaries in resident master-cache set stats, and bumps the shared-cache fingerprint schema so legacy helper-built caches are not silently reused. The actual resident master-cache combine remains mean/no-rejection while artifacts record requested-vs-applied master rejection. | `src/glass/engine/resident_cuda.py`, `src/glass/engine/resident_calibration_artifacts.py` | `tests/test_resident_master_stack_engine.py`, `tests/test_resident_cuda_run.py`, Gate482 200-light A/B artifacts | Project-defined StackEngine/DQ contract and cache fingerprint schema | The migration uses GLASS-owned FITS readers, StackEngine contracts, calibration policy fields, and user-generated real benchmark artifacts only. It does not inspect external implementation source, derive proprietary behavior, or modify input image directories. | The resident integration engine remains CUDA resident stack; this gate closes resident master-cache construction/provenance, not full resident StackEngine parity. Robust master-frame rejection in resident cold cache is deferred until a CUDA or optimized CPU implementation is available; a direct CPU winsorized attempt timed out on the real 200-light calibration set. Flat normalization preserves the existing resident master-after-stack normalization stage for numerical continuity. |
| StackEngine streaming mean fast path | clean-room execution optimization / black-box experiment | S2-Gate 483 real M38 H-alpha 200-light cold/warm A/B under `C:\glass_runs\phase2_s2_gate483_stackengine_fast_mean_ab_real` | Adds an internal `CPUStackEngine` execution path for mean/weighted-mean with no rejection that streams one source tile at a time into weighted-sum, weight, coverage, and optional variance accumulators, avoiding per-tile 3D `np.stack` arrays while preserving StackEngine DQ provenance and result-contract output. | `src/glass/engine/stack_engine.py` | `tests/test_stack_engine.py`, `tests/test_stack_engine_result_contract.py`, `tests/test_resident_master_stack_engine.py`, Gate483 200-light A/B artifacts | Project-defined optimization of existing GLASS StackEngine mean/no-rejection semantics | The optimization is an internal GLASS execution strategy for generic mean/weighted-mean statistics. It uses no external implementation source and does not change formulas, cache keys, input directories, registration, rejection, or CUDA kernels. | Only mean/weighted-mean with rejection disabled uses the fast path. Robust master-frame rejection, median/sum, and rejected light integration still use the generic stack-tile path. Gate483 improved real cold master build/load from `46.85 s` to `32.29 s`, but Gate481's older helper cold path remains faster at `10.91 s`, so native/CUDA master building remains future work. |
| StackEngine full-frame finite-only master mean path | clean-room execution optimization / black-box experiment | S2-Gate 484 real M38 H-alpha 200-light cold/warm A/B under `C:\glass_runs\phase2_s2_gate484_finite_full_frame_mean_ab_real` | Adds a metadata-gated `CPUStackEngine` full-frame mean/weighted-mean execution path for no-rejection, no-output-map requests, and lets resident master-cache FITS sources declare `mask_from_finite_only` so the path can derive masks from `np.isfinite` without extra mask tile reads. | `src/glass/engine/stack_engine.py`; `src/glass/engine/resident_cuda.py` | `tests/test_stack_engine.py`, `tests/test_stack_engine_result_contract.py`, `tests/test_resident_master_stack_engine.py`, Gate484 200-light A/B artifacts | Project-defined optimization of existing GLASS StackEngine finite-sample mean semantics | The path is enabled only by GLASS-owned request metadata and finite-only source capability. It uses no external implementation source and does not change master formulas, calibration formulas, registration, rejection, interpolation, integration, input data, or CUDA kernels. | This path intentionally trades more CPU RAM for lower Python/tile overhead and is limited to explicit full-frame requests with rejection disabled and output maps disabled. Generic StackEngine requests keep the tile/out-of-core path. Gate484 reduced real cold master build/load from Gate483 `32.29 s` to `15.34 s` while keeping warm output pixel-identical to Gate483 and WBPP compare metrics unchanged. |
| Resident CUDA master mean builder | clean-room execution optimization / black-box experiment | S2-Gate 485 real M38 H-alpha 200-light cold/warm A/B under `C:\glass_runs\phase2_s2_gate485_resident_cuda_master_mean_ab_real` | Uses existing GLASS `ResidentCalibratedStack` as the resident master-cache mean builder when native CUDA is available: calibration frames are read by the fast/native FITS path, uploaded one at a time into a short-lived resident stack, and reduced with `ResidentCalibratedStack.integrate_mean`. The path emits StackEngine-compatible result contracts and DQ provenance; CUDA-unavailable or unsupported FITS inputs fall back to the Gate484 CPUStackEngine full-frame path. | `src/glass/engine/resident_cuda.py`; existing `src/glass_cuda.py`; existing `cpp/src/native_bindings.cpp`; existing `cpp/cuda/integration_kernels.cu` | `tests/test_resident_master_stack_engine.py`, Gate485 200-light A/B artifacts | Project-defined scheduling/dispatch over existing GLASS resident CUDA mean integration kernel and StackEngine-compatible DQ contract | The builder uses GLASS-owned CUDA kernels, FITS readers, artifact contracts, and user-generated real benchmark outputs only. It does not inspect external implementation source, modify input directories, or change calibration/registration/rejection/integration formulas. | Mean/no-rejection resident master construction only. Robust master-frame rejection is still deferred. Gate485 reduced real cold master build/load from Gate484 `15.34 s` to `7.57 s`, beating Gate481 helper `10.91 s`, while warm output remained pixel-identical to Gate484 and WBPP compare metrics stayed unchanged. |
| Resident master-cache raw-u16 GPU decode | clean-room execution optimization / black-box experiment | S2-Gate 486 real M38 H-alpha 200-light cold/warm A/B under `C:\glass_runs\phase2_s2_gate486_master_raw_u16_ab_real` | Extends the Gate485 resident CUDA master mean builder to prefer existing GLASS raw FITS u16 big-endian+BZERO GPU decode when all calibration frames are eligible. Eligible groups read compact raw bytes, decode/calibrate-with-no-masters into the resident stack through `ResidentCalibratedStack.calibrate_frames_fits_u16be_bzero_host_async_multistream_callback_release_timed`, and then reduce with `integrate_mean`; unsupported inputs fall back to the Gate485 native-direct float32 path and then CPUStackEngine if CUDA is unavailable. | `src/glass/engine/resident_cuda.py`; existing `src/glass_cuda.py`; existing `cpp/src/native_bindings.cpp`; existing `cpp/cuda/calibration_kernels.cu`; existing `cpp/cuda/integration_kernels.cu` | `tests/test_resident_master_stack_engine.py`, Gate486 200-light A/B artifacts | Project-defined dispatch over existing GLASS raw-u16 GPU decode and resident mean integration primitives | The dispatch uses GLASS-owned FITS eligibility checks, CUDA kernels, and benchmark artifacts only. It does not inspect external implementation source, modify input directories, or change master mean, flat normalization, light calibration, registration, rejection, or integration formulas. | Applies only to simple `BITPIX=16`, `BSCALE=1`, `BZERO=32768`, no-BLANK FITS calibration frames. Gate486 reduced real cold master build/load from Gate485 `7.57 s` to `6.05 s` while keeping warm output pixel-identical to Gate485 and WBPP compare metrics unchanged. |
| Resident grid catalog bulk one-sync batch | clean-room CUDA scheduling optimization / black-box experiment | S2-Gate 487 real M38 H-alpha 200-light warm A/B under `C:\glass_runs\phase2_s2_gate487_catalog_bulk_sync_ab_real` | Changes resident grid-top-NMS moving-star catalog batch execution from per-frame launch/synchronize/download to batch-owned scratch/output/count buffers, all-frame kernel enqueue, one CUDA synchronization, bulk count/catalog downloads, and one-sync centroid refinement. The local-maximum, per-cell top-k, sort, NMS, centroid, descriptor, transform, warp, rejection, and integration formulas are unchanged. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `src/glass/report/html_report.py` | `tests/test_cuda_resident_stack.py`; `tests/test_gpu_star_detect.py`; `tests/test_resident_cuda_run.py`; Gate487 200-light A/B artifacts | Project-defined native scheduling over existing GLASS grid-top-NMS and centroid kernels | The optimization only reorganizes GLASS-owned CUDA buffers, launches, synchronization, and downloads. Validation uses GLASS synthetic CUDA resident stacks plus user-owned M38 input data and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source or modify input directories. | Gate487 reduced real catalog sync from `0.8971051 s` to `0.6357767 s` and kept the final master pixel-identical to the Gate486/Gate487 baseline, but total warm runtime regressed in that single run from `20.36849359999178 s` to `21.647297300049104 s` because output writing varied from `2.2861240999773145 s` to `3.694475699972827 s`. The batch still uses default-stream serialized kernel order; later work should batch deeper catalog kernels or reduce output-map write cost. |
| Resident CUDA integration DQ map | clean-room derivation | S2-Gate 17 and S2-Gate 20 resident artifact contracts and GLASS `DQFlag` bitfield | Builds a resident integration DQ bitfield from resident master, weight, coverage, low-rejection, and high-rejection outputs; writes a FITS DQ map and records `NO_DATA`, `WARP_EDGE`, `LOW_REJECTED`, and `HIGH_REJECTED` summaries in resident and integration artifacts. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined bit assignments from `DQFlag` | The DQ map is derived only from GLASS-owned integration maps and public bitfield semantics. | Resident DQ currently marks zero-coverage warp footprint as `WARP_EDGE`; calibration, cosmetic, partial warp-edge inference, and LN-exclusion flag propagation inside the resident CUDA stack remains a later parity target. |
| Resident DQ coverage provenance | clean-room derivation | S2-Gate 21 resident artifact contract | Records post-rejection integration coverage separately from finite pre-rejection sample coverage, where finite pre-rejection coverage is derived as `coverage + low_rejection + high_rejection` when rejection maps are available. This exposes zero/partial finite-sample coverage and rejection-reduced pixels without claiming a pure geometric warp footprint map. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined coverage accounting | The provenance summary is derived from GLASS-owned resident integration maps and does not encode external implementation behavior. | It is a finite-sample provenance summary, not a true per-frame geometric warp coverage stack; partial `WARP_EDGE` marking remains deferred until that pure geometric signal exists. |
| Resident geometric warp coverage | clean-room derivation | S2-Gate 22 resident warp-footprint contract and S2-Gate 24 fused-kernel optimization | Accumulates resident CUDA warp-kernel coverage into a geometric footprint count, records full-frame coverage for active unwarped frames, adds geometric coverage stats to DQ provenance, and uses the native signal to flag partial `WARP_EDGE` DQ pixels separately from sigma rejection losses. S2-Gate 24 fuses the warped-frame accumulation into the resident translation, matrix bilinear, and matrix Lanczos warp kernels to avoid an extra post-warp accumulation launch. | `cpp/cuda/warp_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py`, `tests/test_cuda_smoke.py` | Project-defined coverage threshold of `>0.5` valid sample per frame | The signal comes from GLASS-owned warp kernels and resident artifacts; it is not derived from external implementation behavior. | The accumulator is per-run rather than per-frame. If a frame is warped and later zero-weighted by a downstream quality/LN step, artifacts report whether the geometric frame count diverges from the final active frame count. Fused accumulation assumes one CUDA thread owns each output pixel for a warp launch. |
| Resident output map policy | clean-room derivation | S2-Gate 18 resident output-map contract and 200-light benchmark artifacts | Adds explicit `audit`, `science`, and `minimal` resident output policies. `audit` writes every available diagnostic map, `science` writes master, weight, coverage, and DQ maps while skipping low/high rejection count FITS files, and `minimal` writes only the master. Artifacts record available, written, and skipped maps. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py` | Project-defined policy semantics | The policy is a GLASS-owned output scheduling contract over GLASS-generated maps; it changes artifact selection without changing master-light calculations. | `minimal` omits maps needed for coverage-masked comparison and audit trails; use `audit` for full diagnostics and `science` for validated speed runs that still need coverage and DQ maps. |
| Resident output map storage | clean-room derivation | FITS image dtype behavior and GLASS output map semantics | Keeps master and weight maps as `float32` while writing integer count maps such as coverage and rejection maps as signed FITS integer images when the frame count fits the selected dtype. Per-map write timing and estimated bytes are recorded for performance audit. | `src/glass/io/fits_io.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_fits_io.py`, `tests/test_cli_smoke.py`, `tests/test_cuda_resident_stack.py` | Project-defined dtype selection from frame count | Count maps are GLASS-owned diagnostic artifacts and the storage change does not alter master-light calculations. | Current policy uses `int16` up to 32767 frames and `int32` above that; weight maps remain float because weighted sums are not necessarily integer counts. |
| Resident parallel output writes | clean-room derivation / black-box experiment | S2-Gate 14 benchmark artifacts under `C:\glass_runs\phase2_s2_gate_14_200\parallel_outputs_cold_20260531` | Writes independent resident FITS outputs with a bounded thread pool, records mode/worker count/per-file timing/storage, and validates the cold-output 200-light run against the external reference master through the benchmark contract. | `src/glass/engine/resident_cuda.py` | `tests/test_fits_io.py`, `tests/test_cuda_resident_stack.py` | Project-defined worker bound from output count | This is an internal GLASS scheduling change over GLASS-owned output artifacts; it does not change scientific calculations or external algorithm semantics. | Parallel file writes are still storage-bandwidth limited; S2-Gate 14 shows output writing remains above the warning factor and needs a future map-output policy or faster writer. |
| Shared resident master cache | clean-room derivation / black-box experiment | S2-Gate 15 benchmark artifacts under `C:\glass_runs\phase2_s2_gate_15_200` | Adds an explicit opt-in shared resident master cache across run output directories. Cache entries are fingerprinted from calibration group ids, frame metadata, file size/mtime, image shape/filter, and calibration policy; artifacts record cache scope, key, fingerprint, hit/miss, and cache directory. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py` | Project-defined cache fingerprint and opt-in CLI semantics | Cache reuse is a GLASS-owned execution optimization over user image files treated as read-only; it does not use or encode external implementation behavior. | Fingerprints use metadata plus file size/mtime rather than full image checksums to avoid reading all calibration pixels during lookup; users can clear the shared cache when moving files between filesystems that preserve stale timestamps. |
| StackEngine combine policies | clean-room derivation | General weighted statistics | Mean, median, sum, and weighted mean over valid samples with optional maps. | `src/glass/engine/stack_engine.py` | `tests/test_stack_engine.py` | Project-defined | Implemented from basic statistical definitions. | CPU/tiled reference path only; default pipeline migration occurs in later gates. |
| StackEngine rejection policies | clean-room derivation / paper TBD | General robust statistics | Sigma, MAD, median sigma, percentile, min/max, and winsorized sigma rejection. | `src/glass/engine/stack_engine.py` | `tests/test_stack_engine.py` | Project-defined unless later cited | Implementation is independently written from public robust-statistics formulas and project derivation. | First-pass CPU behavior; edge-case tuning and CUDA parity are later gates. |
| StackEngine DQ provenance | clean-room derivation | S2-Gate 25 DQ provenance contract and S2-Gate 26 artifact/report bridge | Records source DQ flag counts, flagged sample counts, non-finite sample counts, output zero-coverage pixels, rejected-pixel counts, and output DQ summaries while preserving existing StackEngine numerical combine/rejection behavior. S2-Gate 26 writes the provenance into master calibration and CPU integration artifacts and surfaces it in the HTML report. | `src/glass/engine/stack_engine.py`, `src/glass/engine/pipeline.py`, `src/glass/engine/integration.py`, `src/glass/report/html_report.py` | `tests/test_stack_engine.py`, `tests/test_pipeline_fixture.py` | Project-defined | The provenance schema is a GLASS-owned audit contract over GLASS `DQFlag` semantics and StackEngine tile accounting. | First-pass CPU StackEngine provenance. The detailed resident CUDA DQ schema remains separate; S2-Gate 27 adds a compact common summary bridge. |
| Unified DQ provenance summary | clean-room derivation | S2-Gate 27 DQ provenance summary bridge | Adds a compact `dq_provenance_summary` emitted by CPU StackEngine calibration/integration artifacts and resident CUDA integration artifacts. The summary normalizes engine, stage, item, source schema, sample counts, zero/partial coverage, rejection pixels, and DQ counts while preserving detailed source schemas. | `src/glass/engine/dq.py`, `src/glass/engine/pipeline.py`, `src/glass/engine/integration.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_dq_provenance.py`, `tests/test_pipeline_fixture.py`, `tests/test_resident_cuda_run.py`, `tests/test_cli_smoke.py` | Project-defined | The summary schema is a GLASS-owned bridge over existing GLASS DQ artifacts and does not derive from external implementation behavior. | It is a report/audit bridge and does not yet replace detailed StackEngine or resident provenance schemas. Some fields are `null` when the source path cannot provide them. |
| StackEngine pipeline migration | clean-room derivation | `docs/phase2_algorithm_hardening.md` S2-Gate 3 | Routes CPU master calibration and non-resident CPU integration through StackEngine while retaining legacy/CUDA fast paths as fallback. | `src/glass/engine/pipeline.py`, `src/glass/engine/integration.py` | `tests/test_pipeline_fixture.py`, `tests/test_stack_engine.py` | Project-defined | Migration is an internal GLASS architecture change and preserves existing public behavior through diagnostics and fallback metadata. | Resident CUDA path is not yet StackEngine-backed; S2-Gate 3 protects the Phase 1 benchmark by keeping the CUDA fast path. |
| Data-quality mask bitfield | clean-room derivation | Project pipeline requirements | Internal bitfield for invalid, saturated, corrected, warped, LN-excluded, and rejected pixels. | `src/glass/engine/contracts.py`, `src/glass/engine/dq.py`, `src/glass/engine/pipeline.py`, `src/glass/engine/warp.py`, `src/glass/engine/local_norm.py`, `src/glass/engine/integration.py` | `tests/test_phase2_contracts.py`, `tests/test_pipeline_fixture.py` | Project-defined | Bit assignments are project-owned API and are propagated as GLASS artifacts. | S2-Gate 4 emits foundational summaries/maps; cosmetic and camera-specific flags are later gates. |
| Calibration formula and robust first pass | clean-room derivation | Standard calibration equation and robust statistics | Bias/dark/flat correction with explicit dark-bias semantics, scaling, per-flat normalization, flat floor, pedestal, overscan/trim helper, and cosmetic DQ marking. | `src/glass/cpu/calibration.py`, `src/glass/cpu/cosmetic.py`, `src/glass/engine/pipeline.py` | `tests/test_cpu_calibration.py`, `tests/test_pipeline_fixture.py` | Project-defined | Formula and first-pass robust controls are implemented from standard calibration and robust-statistics definitions. | Dark optimization and full tile-mode overscan transform remain future work. |
| Star detection and PSF metrics | paper / clean-room derivation | Stetson, P. B. 1987, "DAOPHOT - A computer program for crowded-field stellar photometry", PASP 99:191-222, doi:10.1086/131977; standard image moments | Robust background/noise estimation, thresholded local maxima, centroid and second-moment PSF estimates, median FWHM/eccentricity/star-SNR frame metrics, saturation fraction, and combined quality weighting. | `src/glass/cpu/star_detect.py`, `src/glass/cpu/metrics.py`, `src/glass/engine/quality.py`, `src/glass/engine/integration.py` | `tests/test_cpu_star_detect.py`, `tests/test_cpu_integration.py`, `tests/test_pipeline_fixture.py` | Project-defined thresholds and weight factors | The implementation is written from public photometry concepts and project-owned moment formulas; no proprietary source code is used. | It is DAOFIND-like but not a full DAOPHOT clone; deblending, aperture correction, and spatial PSF modeling remain future work. |
| Quality gate reference candidates | clean-room derivation | S2-Gate 39 quality artifact contract | Marks each calibrated light as accepted or rejected for automatic reference selection using star count, saturation fraction, optional quality score, and FWHM availability; records policy, rejection reasons, summary counts, and fallback status in `frame_quality.json` and the HTML report. | `src/glass/engine/quality.py`, `src/glass/report/html_report.py` | `tests/test_cpu_star_detect.py`, `tests/test_pipeline_fixture.py` | Project-defined default thresholds from registration policy (`min_stars`) and quality overrides (`quality_max_saturation_fraction`, `quality_min_score`, `quality_require_fwhm`) | The gate is a GLASS-owned quality-control policy over GLASS metrics and calibration DQ summaries. No external implementation source is used. | The first pass gates automatic reference selection only; downstream zero-weight/exclusion policy for integration remains future work. |
| Registration quality-gate propagation | clean-room derivation | S2-Gate 40 registration quality contract | Registration consumes `quality_gate_status` from `frame_quality.json`, skips non-reference frames rejected by the quality gate when enforcement is enabled, records quality-gate status/warnings per registration row, and reports the total quality-rejected frame count. | `src/glass/engine/registration.py`, `src/glass/engine/warp.py` | `tests/test_cpu_registration.py`, `tests/test_pipeline_fixture.py` | Project-defined enforcement flag `reject_quality_gate_failed_frames` defaulting to true for new quality artifacts | Uses only GLASS-owned quality metrics and registration artifacts. No external implementation source is used. | Older quality artifacts without gate fields remain compatible. The resident CUDA fast path still has its separate quality/weight handling and is not changed by this CPU registration gate. |
| Shared accepted-frame accounting | clean-room derivation | S2-Gate 41 frame accounting artifact contract | Builds `frame_accounting.json` by joining per-light records from the processing plan, calibration, quality gate, registration, warp, local normalization, resident artifacts, and integration frame weights; reports final states such as integrated, zero-weight, quality-rejected, registration-rejected, warp-skipped, and not-integrated. | `src/glass/engine/frame_accounting.py`, `src/glass/engine/integration.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_frame_accounting.py`, `tests/test_pipeline_fixture.py` | Project-defined diagnostic schema over GLASS artifacts | The ledger is derived only from GLASS-owned JSON artifacts and user input frame metadata. It does not encode external implementation behavior. | The artifact explains frame flow and counts; it is diagnostic-only and does not change image math. Resident local-normalization rows are summarized at group level unless future artifacts emit per-frame LN records. |
| Rejected-frame focused accounting | clean-room derivation / black-box experiment | S2-Gate 43 exception-frame artifact/report contract over the 200-light resident benchmark | Extends `frame_accounting.json` with `exception_frames` and `exception_summary`, records primary stage/reason for each non-integrated frame, surfaces a compact rejected/zero-weight table in HTML reports, and includes exception details in acceptance-audit Markdown. | `src/glass/engine/frame_accounting.py`, `src/glass/report/html_report.py`, `src/glass/report/acceptance_audit.py` | `tests/test_frame_accounting.py`, `tests/test_acceptance_audit.py`, `tests/test_pipeline_fixture.py` | Project-defined diagnostic schema over GLASS artifacts | The exception list is derived only from GLASS frame-accounting rows, integration weights, registration statuses, and user input metadata. No external implementation behavior is encoded. | This is a triage view and does not decide whether each rejection is scientifically optimal. Full warnings remain in the authoritative JSON artifact. |
| Optimization guidance contract | clean-room derivation / black-box experiment | S2-Gate 44 guidance over GLASS benchmark timing diagnostics and frame-accounting exceptions from the 200-light resident run | Builds an `optimization_guidance` audit section that ranks resident I/O/upload/calibration overlap, resident registration/warp batching, output-map writes, and master-cache reuse from current wall-clock stage timings, while preserving cumulative worker timings as informational and carrying exception-frame context into Markdown/HTML reports. | `src/glass/report/optimization_guide.py`, `src/glass/report/acceptance_audit.py`, `src/glass/report/html_report.py`, `benchmarks/phase2_m38_h_200_audit_maps_contract.json` | `tests/test_acceptance_audit.py`, `tests/test_cli_smoke.py` | Project-defined target mapping over GLASS timing keys and artifact schemas | The guidance consumes only GLASS-owned timing, resident, and frame-accounting artifacts plus user-generated benchmark outputs. It does not encode external implementation behavior. | Diagnostic only; it recommends the next optimization target but does not change scheduling, GPU kernels, frame inclusion, or image pixels. |
| Resident warp scratch reuse | clean-room derivation | S2-Gate 45 optimization of GLASS resident CUDA warp scheduling | Reuses resident device scratch buffers for translation, matrix bilinear, and matrix Lanczos warp output, per-frame coverage, and matrix inverse storage instead of allocating/freeing those buffers for every warped frame; records scratch bytes in resident artifacts and reports. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined memory reuse optimization | The change is an internal GLASS allocation strategy around existing GLASS warp kernels; it does not alter external algorithm behavior or copy external implementation details. | Scratch reuse removes allocation churn but does not yet batch descriptor scoring, pixel refinement, or warp launches across frames. |
| Async resident warp copy dispatch | clean-room derivation | S2-Gate 46 optimization of GLASS resident CUDA warp scheduling | Removes per-frame host `cudaDeviceSynchronize` calls from resident translation, matrix bilinear, and matrix Lanczos warp application, and queues default-stream asynchronous device-to-device copies from reusable warp scratch back into the resident frame stack. Artifacts report the copy mode. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA stream scheduling optimization | The change relies on CUDA default-stream ordering around GLASS-owned kernels and buffers; it does not alter scientific formulas or use external implementation details. | Launch errors remain immediate; execution errors surface at later CUDA synchronization points. Descriptor scoring, pixel refinement, and warp launches are still per-frame. |
| Batch resident triangle pixel refinement | clean-room derivation | S2-Gate 47 resident triangle-registration optimization | Adds a native batch API for one-seed-per-frame matrix translation refinement, routes `similarity_cuda_triangle` through deferred batch pixel refinement, records batch mode/timing diagnostics, and applies per-frame quality gates and resident warp after the batch returns. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined native-call batching around existing GLASS pixel-metric search | The batch API reuses GLASS-owned matrix metric scoring and CUDA warp/refine primitives. It changes scheduling and diagnostics, not the underlying scientific registration formula. | Batch refinement still executes per-frame CUDA metric searches internally; descriptor generation and descriptor fit are not yet batched. |
| Batch resident triangle descriptor fit | clean-room derivation | S2-Gate 48 resident triangle-registration optimization | Adds a native batch API for multiple moving triangle-descriptor similarity fits against a shared reference catalog and descriptor set, routes fixed-threshold resident triangle registration through the batch fit cache, and records batch mode/timing diagnostics in resident artifacts and reports. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_gpu_registration_search.py`, `tests/test_resident_cuda_run.py` | Project-defined native-call batching around existing GLASS triangle descriptor similarity scoring | The batch API reuses GLASS-owned descriptor generation and similarity-fit primitives. It changes scheduling and diagnostics, not the descriptor definition or scientific registration formula. | The first batch implementation still executes each moving fit internally with the existing CUDA primitive; moving descriptor generation remains per-frame and is only cached/batched at the resident orchestration layer. |
| Shared reference triangle descriptor device buffers | clean-room derivation | S2-Gate 49 resident triangle-registration optimization | Replaces the batch descriptor-fit wrapper's internal single-fit loop with native batch memory scheduling that uploads shared reference catalog coordinates, descriptors, and indices once, then reuses those device buffers for each moving catalog fit. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_gpu_registration_search.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA device-buffer reuse around existing GLASS triangle descriptor similarity scoring | The change reuses GLASS-owned CUDA kernel inputs and does not alter descriptor definitions, matrix fitting formulas, candidate scoring, or external algorithm behavior. | Moving catalog coordinates/descriptors and per-fit output buffers are still allocated per moving frame; moving descriptor generation is still per-frame. |
| Shared moving and output triangle descriptor device buffers | clean-room derivation | S2-Gate 50 resident triangle-registration optimization | Extends the native triangle descriptor batch fit to pre-validate moving catalogs/descriptors, allocate reusable moving-catalog and output/candidate device workspaces once per batch, and reuse those workspaces across moving-frame fits alongside the S2-Gate 49 shared reference buffers. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_gpu_registration_search.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA device-buffer reuse around existing GLASS triangle descriptor similarity scoring | The change preserves descriptor generation, candidate scoring, matrix fitting, RMS, and inlier semantics; it changes allocation scheduling and diagnostics only. | Moving descriptor generation and descriptor scoring still run once per moving frame; full-frame star candidate scanning remains the largest registration component. |
| Resident grid catalog timing decomposition | clean-room derivation | S2-Gate 51 resident triangle-registration diagnostics | Instruments the resident fixed-threshold grid/NMS moving-catalog batch path with per-frame native timings for kernel enqueue, GPU synchronization, count download, compact catalog download, and total native catalog work, then aggregates those timings into resident artifacts and reports. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined diagnostic timing around existing GLASS grid/NMS star catalog kernels | The change preserves local-maximum, grid top-k, sort, NMS, catalog coordinates, fluxes, and downstream registration behavior; it adds measurement only. | Timing decomposition is diagnostic and may vary by GPU/load. It identifies whether later optimization should target full-frame scan/sort/NMS or host/device transfer overhead. |
| Resident grid catalog bitonic shared sort | clean-room derivation | S2-Gate 52 optimization of GLASS resident grid/NMS catalog sorting | Replaces the shared-memory odd-even candidate sort with a shared bitonic sort for grid capacities up to 4096 while preserving the GLASS order comparator of flux descending, y ascending, and x ascending. Catalog sort mode is surfaced in wrapper results, resident artifacts, warnings, and reports. | `cpp/cuda/star_detect_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_gpu_registration_search.py`, `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA sorting optimization around existing GLASS local-maximum/grid/NMS semantics | The change is an internal GLASS kernel scheduling/sorting implementation and does not alter the catalog definition or use external implementation details. | Applies to shared-memory sort capacities up to 4096 candidates; larger catalogs retain the previous single-thread selection-sort fallback. |
| Resident grid top-k strict flux precheck | clean-room derivation | S2-Gate 53 optimization of GLASS resident grid top-k candidate selection | Adds a lock-free strict-flux precheck before the per-cell locked replacement loop so candidates already weaker than the current cell's weakest stored flux skip the lock after that cell reaches `candidates_per_cell`. A per-cell filled-slot counter keeps sparse cells on the locked path until all available slots can be filled. Equal-flux candidates still use the locked comparator to preserve y/x tie-break semantics. Catalog top-k mode is surfaced in wrapper results, resident artifacts, warnings, and reports. | `cpp/cuda/star_detect_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_gpu_registration_search.py`, `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA contention-reduction optimization around existing GLASS top-k semantics | The precheck is derived from the monotonic top-k threshold property and keeps the established locked comparator for all possible tie cases. It does not alter local-maximum, grid, NMS, or descriptor definitions. | The full-frame local-maximum scan still runs for every loaded frame; the precheck only reduces lock pressure after a cell's candidate slots fill. |
| Resident pixel-refine shared metric workspace | clean-room derivation | S2-Gate 54 optimization of GLASS resident triangle pixel refinement | Reuses one native CUDA workspace for matrix candidate inverses, partial metric statistics, and partial counts across the resident batch pixel-refine pass, while preserving the existing candidate grid and matrix NCC/RMS metric semantics. Workspace mode, capacity, bytes, and coarse/fine metric timings are surfaced in wrapper results, resident artifacts, warnings, and reports. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA allocation scheduling optimization around existing GLASS matrix metric refinement | The change only reuses GLASS-owned device buffers around the existing metric kernel; it does not alter candidate generation, NCC/RMS formulas, quality gates, warp behavior, or external algorithm behavior. | The metric searches still run per moving frame and per coarse/fine pass; later gates should reduce launch/synchronization frequency or batch metric scoring more deeply. |
| Resident pixel-refine flattened batch metric launch | clean-room derivation | S2-Gate 55 optimization of GLASS resident triangle pixel refinement | Flattens moving-frame/candidate matrix metric evaluation into one coarse CUDA metric launch for the full resident batch and one fine CUDA metric launch for the full resident batch, while preserving the existing candidate-grid generation and NCC/RMS metric semantics. Flattened metric mode, kernel-launch count, coarse/fine total candidates, workspace capacity, workspace bytes, and timings are surfaced in wrapper results, resident artifacts, warnings, and reports. | `cpp/cuda/warp_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA synchronization/scheduling optimization around existing GLASS matrix metric refinement | The change reuses GLASS-owned kernels and resident-frame storage; it changes launch scheduling and candidate flattening only, not the metric formula, candidate ordering, quality gates, warp behavior, or external algorithm behavior. | Fine candidate grids still depend on host-side coarse winners, so the path remains a two-pass host/native schedule; later gates can move winner selection or candidate generation further onto the device. |
| Resident triangle-registration determinism signatures | clean-room derivation | S2-Gate 56 diagnostic contract after a Gate55 real-data retry exposed acceptance-sensitive registration drift | Adds exact SHA-256 signatures over resident triangle reference/moving catalogs, triangle descriptor arrays, selected descriptor-fit matrices and metadata, and threshold trial lists. Compact combined hashes are reported in `resident_artifacts.json` and HTML; detailed per-frame hashes stay in JSON and warnings for drift triage. | `src/glass/engine/resident_cuda.py`, `src/glass/report/html_report.py` | `tests/test_resident_cuda_run.py`, `tests/test_cuda_resident_stack.py` | Project-defined diagnostic hashing over GLASS-owned arrays and registration metadata | The signatures consume only GLASS-generated arrays/artifacts and do not encode external implementation behavior. They are diagnostic and do not change catalog selection, descriptor formulas, fit scoring, or output pixels. | Hashes identify where repeated runs diverge but do not by themselves eliminate the underlying source of drift; a later gate can use the evidence to force a stricter deterministic catalog mode if needed. |
| Resident determinism audit | clean-room derivation | S2-Gate 57 diagnostic contract over two GLASS resident CUDA run directories | Compares S2-Gate 56 resident triangle-registration signatures, resident artifact combined hashes, registration result rows, frame-accounting statuses, and run timing between a baseline and candidate run. Writes JSON and optional Markdown so acceptance-sensitive drift is localizable before changing more native scheduling. | `src/glass/report/resident_determinism.py`, `src/glass/cli.py` | `tests/test_resident_determinism.py`, `tests/test_cli_smoke.py` | Project-defined comparison of GLASS-owned artifacts | The audit consumes only GLASS resident artifacts, registration results, frame-accounting ledgers, and timing JSON. It does not encode external implementation behavior. | Diagnostic only; it does not change image math or registration decisions. If it finds catalog-level drift on repeated real-data runs, a later gate should implement deterministic resident catalog compaction. |
| Deterministic resident grid top-k catalog | clean-room derivation | S2-Gate 58 and S2-Gate 60 GLASS resident CUDA catalog hardening | Adds an opt-in deterministic resident grid top-k path for triangle registration. Gate58 introduced serial per-cell candidate selection to remove atomic scheduling drift; Gate60 keeps the same flux/y/x comparator and deterministic reduction contract but scans each cell with one CUDA block, local per-thread top-k buffers, and a fixed block-level merge for standard small per-cell candidate budgets. | `cpp/cuda/star_detect_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/resident_determinism.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py`, `tests/test_resident_determinism.py` | Project-defined deterministic scheduling mode | The catalog definition, tie-breaks, and scheduling are GLASS-owned clean-room CUDA logic over GLASS resident images. It does not use external source code or proprietary implementation behavior. | The deterministic path is opt-in for audit/reproducible runs; high candidate budgets fall back to serial per-cell mode to cap shared-memory usage. |
| Resident triangle descriptor fit timing ledger | clean-room diagnostic derivation | S2-Gate 61 GLASS resident CUDA profiling hardening | Splits the native resident triangle descriptor batch-fit timing into host preparation, reference allocation/upload, reusable workspace allocation, moving uploads, kernel synchronization, output downloads, and per-frame native fit totals. The ledger is emitted in batch results, resident artifacts, fine timing components, and registration warnings. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py` | `tests/test_gpu_registration_search.py`, `tests/test_resident_cuda_run.py` | Project-defined timing instrumentation | The change observes GLASS-owned CUDA calls and Python artifacts only. It does not alter descriptor definitions, candidate scoring, fit selection, matrices, pixel refinement, warp behavior, or output pixels. | Timing numbers use host steady-clock sections around synchronous CUDA calls; a later gate can add CUDA-event timing or true multi-frame device-side batch output if needed. |
| Parallel descriptor fit best reduction | clean-room CUDA scheduling derivation | S2-Gate 62 GLASS resident CUDA descriptor-fit optimization | Replaces the single-thread scan over descriptor-fit candidate scores/RMS values with a deterministic single-block parallel reduction. The reduction preserves the original ordering: highest inlier count, then lowest RMS, then lowest candidate index for exact ties. | `cpp/cuda/registration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py` | `tests/test_gpu_registration_search.py`, `tests/test_resident_cuda_run.py`, `tests/test_resident_determinism.py` | Project-defined CUDA reduction | The candidate scoring formula, descriptor definitions, candidate index order, fit acceptance, pixel refinement, and warp behavior are unchanged; only the post-score best-selection schedule changes. | Uses one CUDA block for the final best scan. It improves the serial scan portion but does not reduce the main per-candidate scoring workload. |
| Resident pixel-refine workload ledger | clean-room diagnostic derivation | S2-Gate 63 GLASS resident CUDA pixel-refine profiling hardening | Records the batched pixel-refine coarse/fine workload as sampled pixels per candidate, total candidate sample evaluations, native pass timings, and effective metric throughput. | `cpp/src/native_bindings.cpp`, `src/glass/engine/resident_cuda.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined workload accounting over existing GLASS metric kernels | The ledger observes GLASS-owned candidate counts, sample strides, timings, and resident artifacts only. It does not alter candidate generation, NCC/RMS formulas, matrix selection, quality gates, warp behavior, or output pixels. | Diagnostic only; a later optimization gate should use the ledger to decide whether to reduce sampled pixels, candidate counts, kernel work, or host/device synchronization. |
| Explicit fast-coarse pixel refinement | clean-room performance/quality tradeoff | S2-Gate 64 GLASS resident CUDA pixel-refine optimization | Adds an opt-in fast-coarse mode that raises the coarse metric sample stride to at least the final metric sample stride, reducing the number of full-frame coarse candidate samples while preserving the existing candidate grid and metric formula. | `src/glass/cli.py`, `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined explicit sampling policy | The mode uses the existing GLASS pixel metric kernels and only changes the coarse sample stride when explicitly requested. It is surfaced in artifacts and warnings so fast runs cannot be mistaken for the conservative default. | Not bit-exact relative to the conservative Gate63 path. It is intended for audited high-throughput runs where numerical drift is measured against the conservative baseline or external reference. |
| Resident output numerical drift audit | clean-room diagnostic derivation | S2-Gate 65 GLASS resident determinism/quality audit | Extends resident determinism audits to compute numerical drift metrics for mismatched output FITS images/maps, including RMS, absolute-percentile differences, finite-pixel accounting, and RMS relative to baseline standard deviation. | `src/glass/report/resident_determinism.py` | `tests/test_resident_determinism.py` | Project-defined audit statistics over GLASS-generated outputs | The audit reads only GLASS-generated FITS outputs and reports public statistical differences. It does not change image generation, registration, rejection, weighting, or any scientific formula. | Strict hash-based pass/fail semantics remain unchanged; numerical drift explains magnitude for fast-mode and regression review. |
| HTML numerical drift reporting | clean-room reporting contract | S2-Gate 66 GLASS report hardening | Adds a dedicated HTML report section for resident output numerical drift rows carried by acceptance/resident-determinism audit JSON artifacts. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py` | Project-defined report rendering | The report only renders previously generated GLASS audit fields. It does not compute new algorithms, alter image data, or change audit pass/fail semantics. | Rows appear only when the supplied audit JSON contains `output_numerical_drifts`; the authoritative machine-readable values remain in the JSON artifact. |
| Acceptance resident drift attachment | clean-room reporting contract | S2-Gate 67 GLASS acceptance-audit hardening | Adds optional `resident-determinism` JSON attachment to acceptance-audit artifacts, carrying strict resident audit summary and output numerical drift rows into JSON/Markdown and downstream HTML reports. | `src/glass/report/acceptance_audit.py`, `src/glass/cli.py` | `tests/test_acceptance_audit.py` | Project-defined artifact composition | The acceptance audit copies GLASS-generated resident determinism evidence without changing image processing, comparison formulas, or default acceptance checks. | Drift evidence is informational until a benchmark contract explicitly adds thresholds for it. |
| Resident drift contract thresholds | clean-room acceptance contract | S2-Gate 68 GLASS benchmark-contract hardening | Adds optional `resident_determinism` benchmark-contract checks for resident determinism artifact presence, strict pass status, drift-row count, relative RMS drift, absolute RMS drift, and mean absolute drift. | `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py`, `benchmarks/phase2_m38_h_200_resident_drift_contract.json` | `tests/test_acceptance_audit.py` | Project-defined contract checks over GLASS-generated audit statistics | The contract consumes GLASS resident-determinism JSON and GLASS/WBPP comparison metadata; it does not alter image processing or derive formulas from external implementation source. | Checks run only when the contract declares `resident_determinism`; default acceptance behavior remains unchanged. |
| Resident calibration batch scheduler | clean-room CUDA scheduling derivation | S2-Gate 71 optimization of GLASS resident pinned-ring light calibration | Adds an opt-in native resident calibration batch path that enqueues multiple raw-light host-to-device copies and calibration kernels on the resident calibration stream before one synchronization. Batch mode, requested frame count, actual batch count, frame count, stream elapsed time, sync time, and native wall time are surfaced in resident artifacts. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined explicit opt-in batch size | The scheduler changes only GLASS-owned CUDA enqueue/synchronization behavior around the existing calibration kernel and policy semantics. It does not use external implementation details or change calibration formulas. | The first batch path uses one reusable raw device buffer and one stream, so H2D and calibration are serialized within a batch; later gates need multiple raw device buffers/streams for true copy/compute overlap. |
| Resident calibration multistream lanes | clean-room CUDA scheduling derivation | S2-Gate 72 optimization of GLASS resident pinned-ring light calibration | Adds an opt-in native batch path with multiple reusable raw-light device buffers, CUDA streams, and lane timing events so separate frames can enqueue H2D plus calibration work on independent lanes before one synchronization. Requested streams, actual streams, lane buffer bytes, multistream enablement, and timing model are surfaced in resident artifacts. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined explicit opt-in stream count | The scheduler changes only GLASS-owned CUDA resource allocation and enqueue behavior around the existing calibration kernel and policy semantics. It does not change calibration formulas or use external implementation details. | Host prefetch slots are still released after the native batch returns, so large batch sizes can starve prefetch; later gates should release slots after H2D completion or split batch scheduling across smaller waves. |
| Resident calibration wave scheduler | clean-room CUDA/Python scheduling derivation | S2-Gate 73 optimization of GLASS resident pinned-ring light calibration | Adds an opt-in wave size that splits a larger requested resident calibration batch into smaller native waves, releasing pinned prefetch slots after each wave synchronization. Wave size, effective wave size, release mode, prefetch no-slot blocked count, release count, and maximum in-flight slots are surfaced in resident artifacts. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py` | Project-defined explicit opt-in wave size | The scheduler changes only GLASS-owned batch segmentation and prefetch-slot accounting. It does not change CUDA kernels, calibration formulas, or image math. | The wave scheduler still synchronizes at wave boundaries; a future native ingest queue or H2D completion event path is needed for true per-frame early release while calibration continues. |
| Resident calibration H2D event release | clean-room CUDA/Python scheduling derivation | S2-Gate 74 optimization of GLASS resident pinned-ring light calibration | Adds an opt-in release mode that synchronizes reusable per-lane H2D-completion events, returns pinned host prefetch slots after copies finish, and then waits for the pending calibration kernels/store path. Release mode, support, enablement, release count, H2D-release timing, H2D event timing, and pending wait timing are surfaced in resident artifacts. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined explicit opt-in release mode | The scheduler changes only GLASS-owned CUDA event synchronization and pinned-slot lifetime management around the existing calibration kernel and policy semantics. It does not change calibration formulas, registration, rejection, or image math. | The first H2D-event path requires one frame per calibration lane for safe host-buffer release and still waits immediately after releasing slots; a later native queue can overlap multiple pending waves more aggressively. |
| Resident calibration release auto policy | clean-room CUDA/Python scheduling derivation / black-box experiment | S2-Gate 75 policy over S2-Gate 73 and S2-Gate 74 200-light benchmark evidence | Adds an opt-in `auto` release mode that chooses `h2d_event` only when the effective calibration wave fills the native stream lanes; underfilled waves remain on the synchronized multistream path. Requested/effective mode, capability, recommendation, policy, and reason are surfaced in resident artifacts. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py` | Project-defined policy informed by GLASS 200-light benchmark artifacts | The policy uses only GLASS-generated timing artifacts and GLASS-owned scheduler state. It does not derive formulas or implementation details from external software and does not change calibration image math. | The policy is opt-in and heuristic. It guards the first H2D-event path from underfilled-lane use, but it is not a replacement for a deeper native calibration queue. |
| Resident calibration callback release queue | clean-room CUDA/Python scheduling derivation | S2-Gate 76 optimization of GLASS resident pinned-ring light calibration | Adds an explicit native callback queue mode that accepts a larger Python batch, schedules smaller native H2D waves, synchronizes each wave's H2D completion events, calls back into Python to release completed prefetch slots, then performs one final stream synchronization for calibration completion. Callback support, capability, enablement, release count/time, internal wave count, fetch batch size, and timing model are surfaced in resident artifacts. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined explicit opt-in callback queue mode | The scheduler changes only GLASS-owned native enqueue/callback timing and pinned-slot lifetime management around the existing calibration kernel and policy semantics. It does not change calibration formulas, registration, rejection, or image math. | The callback queue still uses Python callbacks for slot release and remains explicit. It reduces Python/native batch calls but is not yet a fully device-resident ingest scheduler. |
| Batched resident prefetch slot release | clean-room Python scheduling derivation | S2-Gate 77 optimization of GLASS resident pinned-ring prefetch scheduling | Replaces repeated per-index slot releases with `release_many`, so a completed batch returns all pinned slots before triggering a single prefetch fill pass. Fill call count, submitted frame count, release batch count, and release/fill model are surfaced in resident artifacts. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined prefetch scheduling policy | The change only alters GLASS-owned host prefetch bookkeeping and task-submission cadence. It does not change CUDA kernels, calibration formulas, registration, rejection, or image math. | It reduces fill-call overhead and improves diagnostics, but disk/decode variability can still dominate `light_read_wait_wall`. |
| Resident prefetch parameter sweep | clean-room benchmark harness | S2-Gate 78 benchmark plan over Gate70B/Gate73/Gate77 resident timing artifacts | Adds a reusable resident CUDA sweep harness that expands prefetch depth, worker count, native batch size, stream count, wave size, and release mode into auditable GLASS run commands, parses resident timing artifacts, ranks variants by wall time and blocked prefetch slots, and optionally generates per-variant compare reports. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined benchmark ranking over GLASS artifacts | The harness consumes and compares GLASS-generated timing/artifact files only. It does not change image math or derive behavior from external implementation source. | Sweep rankings are hardware, disk-cache, and dataset dependent; they guide optimization policy but do not by themselves promote a new default. |
| Queued resident prefetch refill | clean-room Python scheduling derivation | S2-Gate 79 optimization of GLASS resident pinned-ring prefetch scheduling | Adds an explicit `--resident-prefetch-refill-mode` policy. `queued` releases H2D-complete pinned slots in the callback path, then asks a lightweight refill executor to submit replacement read tasks outside native callback timing; artifacts record queued submit/execute/coalescing counts and wait time. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py`, `src/glass/report/resident_sweep.py` | `tests/test_resident_cuda_run.py`, `tests/test_benchmarks.py` | Project-defined explicit opt-in refill policy | The change only alters GLASS-owned host scheduling around the existing prefetch ring. It preserves H2D completion safety and does not change CUDA kernels, calibration formulas, registration, rejection, or image math. | The policy is opt-in because real 200-light benefit is expected to be small and disk-cache dependent; sweep evidence decides whether it should become recommended later. |
| Batched resident warp matrix upload | clean-room CUDA scheduling derivation | S2-Gate 80 optimization of native resident matrix warp dispatch | Replaces per-frame inverse-matrix H2D uploads in native resident matrix bilinear/Lanczos3 batch warp with one host inverse preparation pass, one device inverse batch allocation, and one H2D upload per batch. Timing fields record upload mode, preparation, allocation, bytes, enqueue, sync, and total time. | `cpp/src/native_bindings.cpp`, `src/glass/engine/resident_cuda.py` | `tests/test_gpu_warp_vs_cpu.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA upload scheduling around existing GLASS warp kernels | The change preserves GLASS-owned interpolation kernels, matrix inversion semantics, coverage/DQ accounting, accepted-frame decisions, and output pixels. It changes only how inverse matrices are staged for the batch. | The gate still launches one warp kernel per frame and uses one reusable output scratch buffer; later gates should consider true multi-frame warp kernels or stream-level scheduling. |
| Chunked multi-frame resident matrix warp | clean-room CUDA scheduling derivation | S2-Gate 81 optimization of native resident matrix warp dispatch | Adds chunked multi-frame matrix bilinear/Lanczos3 warp kernels that write warped pixels into a temporary chunk workspace, reduce per-frame coverage bytes into the resident geometric coverage accumulator, and scatter chunk outputs back into resident frame slots. Timing fields record chunk/workspace sizes, index and inverse uploads, warp/reduce/scatter launches, enqueue timings, sync, and total time. | `cpp/cuda/warp_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass/engine/resident_cuda.py` | `tests/test_gpu_warp_vs_cpu.py`, `tests/test_resident_cuda_run.py` | Project-defined CUDA batch scheduling around existing GLASS interpolation formulas | The change preserves GLASS-owned interpolation formulas, matrix inversion semantics, coverage/DQ accounting, accepted-frame decisions, and output pixels. It changes only resident warp dispatch and staging. | The first implementation uses a fixed preferred chunk size with adaptive fallback on allocation failure. Larger speedups likely require reusable workspace, CUDA graph dispatch, or fusing warp output directly into downstream integration. |
| Fused resident matrix-warp weighted mean primitive | clean-room CUDA fusion derivation | S2-Gate 82 native primitive for future fused resident warp plus integration | Computes a weighted mean directly from unwarped resident frames and one registration matrix per frame, using the same bilinear/Lanczos3 sampling and matrix inversion semantics as GLASS warp kernels while emitting finite-sample and geometric footprint coverage maps. | `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py` | `tests/test_cuda_resident_stack.py` | Project-defined CUDA fusion primitive | Uses GLASS-owned sampling formulas and public weighted-mean definitions. No external implementation behavior is encoded. | Prototype covers no-rejection weighted mean only; rejection, LN, DQ map routing, and pipeline-level fused execution remain future gates. |
| Fused resident matrix-warp sigma rejection primitive | clean-room CUDA fusion derivation | S2-Gate 83 native primitive extending Gate82 to sigma and winsorized-sigma rejection | Computes sigma or winsorized-sigma rejection directly from unwarped resident frames and one registration matrix per frame, using GLASS-owned bilinear/Lanczos3 sampling while emitting master, weight, coverage, low/high rejection, and geometric footprint coverage maps. | `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py` | `tests/test_cuda_resident_stack.py` | Project-defined CUDA fusion primitive | Uses GLASS-owned sampling formulas and public population mean/std plus winsorized clipping definitions. No external implementation behavior is encoded. | Primitive is not yet wired into the real pipeline; LN, DQ map construction, report artifacts, and 200-light routing remain later gates. |
| Opt-in fused resident matrix integration route | clean-room CUDA fusion routing | S2-Gate 84 pipeline route over Gate82/Gate83 fused primitives | Adds an explicit resident integration dispatch that feeds identity or external registration matrices directly into the fused matrix-warp mean/sigma integration primitives, avoiding registered full-frame stack scatter for that route. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py`, `tests/test_cuda_resident_stack.py` | Project-defined opt-in execution route | Uses GLASS-owned resident artifacts and GLASS registration matrices. The route consumes only user-generated `registration_results.json` artifacts for external-matrix mode and does not encode external implementation behavior. | Gate84 route supports `registration=off` and `registration=external_matrix`, and requires local normalization to be off. Gate86 extends fused dispatch to resident triangle matrices; resident LN fusion remains future work. |
| Fused minimal diagnostic download mode | clean-room CUDA transfer scheduling | S2-Gate 85 output-transfer optimization for Gate82/Gate83 fused primitives | Adds a native fused `download_mode` so `resident-output-maps minimal` downloads only master and weight maps while `audit` and `science` keep full coverage/rejection/geometric diagnostics. | `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py`, `tests/test_cuda_resident_stack.py` | Project-defined output-transfer policy | The change only alters which GLASS-owned diagnostic arrays are allocated and copied back for minimal-output fused runs. It does not change integration formulas, accepted-frame decisions, or default audit/science diagnostics. | Minimal fused artifacts have unavailable coverage/rejection/DQ/geometric diagnostics by design. Use `science` or `audit` when those maps are required. |
| Triangle fused resident matrix dispatch | clean-room CUDA fusion routing | S2-Gate 86 pipeline route over resident triangle registration and Gate82/Gate83 fused primitives | Routes accepted `similarity_cuda_triangle` matrices into fused matrix-warp integration, leaving calibrated frames resident and skipping registration-stage stack scatter for those accepted frames. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined opt-in execution route | Uses GLASS-owned triangle catalog/descriptor/pixel-refine results and GLASS fused integration kernels. The gate changes scheduling after accepted matrices pass existing quality gates; it does not change descriptor matching, matrix fitting, rejection formulas, or source data. | Requires local normalization off and fused dispatch opt-in. Diagnostic maps remain governed by `resident-output-maps`; minimal mode intentionally skips coverage/rejection/DQ downloads. |
| Resident integration auto dispatch policy | clean-room routing policy from GLASS benchmark evidence | S2-Gate 87 resident dispatch hardening over Gate86 benchmark results | Adds an `auto` resident integration dispatch that selects fused matrix integration only for verified bilinear matrix routes and keeps stack dispatch for non-bilinear routes such as Lanczos3 rejection until a future gate proves speed and numerical parity. | `src/glass/cli.py`, `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined policy based on GLASS-generated benchmark artifacts | The policy does not change registration, interpolation, rejection, or integration formulas. It selects among existing GLASS routes and records requested/effective mode plus reason in artifacts. | Auto is intentionally conservative. It does not promote Lanczos3 fused rejection because Gate86 measured slower runtime and nonzero differences against the existing Lanczos3 stack baseline. |
| StackEngine default contract audit | clean-room artifact contract | S2-Gate 88 audit over GLASS `calibration_artifacts.json` and `integration_results.json` | Adds a standalone audit that verifies master calibration records and tile/CPU light integration outputs use StackEngine by default, include StackEngine DQ provenance, and expose normalized `dq_provenance_summary` records. | `src/glass/report/stack_engine_contract.py`, `src/glass/cli.py` | `tests/test_stack_engine_contract.py`, `tests/test_cli_smoke.py` | Project-defined contract over GLASS artifacts | The audit consumes GLASS-owned JSON artifacts only and does not change science calculations or derive behavior from external software. | Resident CUDA integration is intentionally explicit; the audit can check resident provenance separately but defaults to the tile/CPU StackEngine route. |
| StackEngine contract reporting | clean-room report presentation | S2-Gate 89 report integration over Gate88 StackEngine contract artifacts | Lets `glass report` auto-discover or explicitly consume StackEngine contract audit JSON, then surfaces pass/fail status, expected engine, failed checks, and audited calibration/integration surfaces in the main HTML report. | `src/glass/report/html_report.py`, `src/glass/cli.py` | `tests/test_cli_smoke.py` | Project-defined report presentation over GLASS artifacts | The report consumes GLASS-owned contract JSON only and does not change science calculations, routing, or external reference behavior. | The report is a triage layer; `stack_engine_contract.json` remains the authoritative artifact. |
| Pipeline invariant contract audit | clean-room artifact contract | S2-Gate 90 audit over GLASS warp, local-normalization, and integration artifacts | Adds a standalone audit for structural pipeline invariants: integration output maps and rejection-map policy, DQ map summaries and provenance summaries, LN crop-box/coefficient/DQ records, and warp coverage/DQ/skipped-frame records. | `src/glass/report/pipeline_contract.py`, `src/glass/cli.py` | `tests/test_pipeline_contract.py`, `tests/test_cli_smoke.py` | Project-defined invariant contract over GLASS artifacts | The audit consumes GLASS-owned JSON/FITS artifact paths only and does not change image calculations, routing, or external reference behavior. | It verifies artifact presence and schema-level invariants, not numerical image agreement; compare and acceptance audits remain responsible for benchmark image differences. |
| Pipeline contract reporting | clean-room report presentation | S2-Gate 91 report integration over Gate90 pipeline contract artifacts | Lets `glass report` auto-discover or explicitly consume pipeline invariant contract JSON, then surfaces pass/fail status, failed checks, integration map policy rows, LN rows, and warp rows in the main HTML report. | `src/glass/report/html_report.py`, `src/glass/cli.py` | `tests/test_cli_smoke.py` | Project-defined report presentation over GLASS artifacts | The report consumes GLASS-owned pipeline contract JSON only and does not change image calculations, routing, or external reference behavior. | The report is a triage layer; `pipeline_contract.json` remains the authoritative artifact for automated checks. |
| Pipeline contract pixel verification | clean-room artifact contract | S2-Gate 92 tiled FITS pixel verification over GLASS integration output maps | Adds explicit `pipeline-contract --pixel-verify` mode that reads DQ, coverage, and rejection count maps in tiles, compares DQ pixel counts with integration `dq_summary`, compares coverage no-data pixels and rejection positive pixels with DQ summaries, and surfaces the rows in HTML reports. | `src/glass/report/pipeline_contract.py`, `src/glass/cli.py`, `src/glass/report/html_report.py` | `tests/test_pipeline_contract.py`, `tests/test_cli_smoke.py` | Project-defined pixel-count invariants from `DQFlag` and GLASS output-map policy | The verifier consumes GLASS-owned FITS maps and JSON summaries only. It does not derive behavior from external implementation details and does not alter science pixels. | Pixel verification is explicit because full-size 200-light maps add audit I/O; it verifies selected integration map invariants, not every possible DQ semantic transition. |
| Pipeline pixel delta reporting | clean-room report presentation | S2-Gate 93 report rendering over Gate92 pipeline-contract pixel verification artifacts | Expands the HTML pipeline-contract section with per-map/per-flag actual, summary, delta, and pass/fail rows for DQ, coverage, and rejection-map pixel verification. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py` | Project-defined report presentation over GLASS artifacts | The report consumes GLASS-owned `pipeline_contract.json` only and does not change image calculations, routing, or external reference behavior. | The report is a readable review layer; `pipeline_contract.json` remains authoritative for automated checks. |
| Run guardrail bundle | clean-room audit orchestration | S2-Gate 94 guardrail bundle over existing GLASS run artifacts | Adds `glass guardrails`, which generates StackEngine contract JSON/Markdown, Pipeline contract JSON/Markdown with optional pixel verification, a compact guardrail summary, and a report wired to those artifacts. | `src/glass/cli.py` | `tests/test_cli_smoke.py` | Project-defined audit orchestration | The command consumes GLASS-owned run artifacts and invokes existing GLASS contract builders. It does not change image calculations, routing, or external reference behavior. | The bundle is only as complete as the artifacts present in the audited run; missing run artifacts fail through the underlying contracts. |
| Guarded resident sweep | clean-room benchmark orchestration | S2-Gate 95 resident sweep guardrail integration over GLASS benchmark artifacts | Extends the resident CUDA sweep harness with optional per-variant `glass guardrails` execution, dry-run guardrail command planning, guardrail pass/fail fields in the sweep summary, and ranking that does not select contract-broken variants as best. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined benchmark orchestration | The sweep consumes GLASS run artifacts and GLASS guardrail summaries only. It does not change image calculations, CUDA kernels, or external reference behavior. | Guardrail checks add I/O and report generation time when enabled; benchmark timing still comes from each GLASS run's own `run_timing.json`. |
| Resident sweep runtime guard | clean-room benchmark orchestration | S2-Gate 96 timeout/failure handling after an interrupted 200-light guarded sweep attempt | Adds optional per-variant and per-guardrail timeout controls to the resident CUDA sweep harness, records timeout/failure status in the sweep summary, skips guardrails when the run itself fails, and prevents timed-out or failed variants from becoming the selected best result. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined benchmark orchestration | The guard consumes process exit/timeout state and GLASS-owned run artifacts only. It does not change image calculations, CUDA kernels, benchmark inputs, or external reference behavior. | Python's subprocess timeout is a process-level guard; if a future launcher creates detached grandchildren, the harness may need a Windows job-object based kill path. The interrupted 200-light attempt is recorded as motivation, not as an accepted benchmark result. |
| Resident sweep baseline command import | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 97 import of a proven GLASS `run_command.txt` for bounded resident sweeps | Adds `--common-run-args-from-command` to import shared science and tuning flags from a known-good GLASS run while filtering out sweep-managed plan/out/backend/memory/prefetch/batch/stream/release flags. A bounded 200-light single-variant sweep imported from the Gate32 audit-map command completed with guardrails and reference comparison. | `benchmarks/bench_resident_prefetch_sweep.py` | `tests/test_benchmarks.py` | Project-defined command import and filtering rules | The importer consumes GLASS-owned `run_command.txt` artifacts only. It preserves GLASS science/tuning parameters and does not read or encode external implementation behavior. | Imported command files are trusted local GLASS artifacts; if a command contains shell constructs beyond plain GLASS tokens, the importer intentionally keeps simple `shlex` token parsing and should be extended before accepting such files. |
| Resident sweep provenance and mini matrix | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 98 provenance summary plus 200-light imported sweep matrix | Records common-run-argument provenance in resident sweep JSON/Markdown summaries and runs a four-variant 200-light sweep over prefetch depth and refill mode using the imported Gate32 command, timeout guards, pixel-verifying guardrails, and external-reference comparison for the best variant. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined provenance schema and benchmark matrix | The gate consumes GLASS-owned command, timing, guardrail, and compare artifacts only. It does not change image calculations, CUDA kernels, input data, or external reference behavior. | The measured 15-16 s variants used shared master cache and hot local data. They are valid for relative parameter tuning in this session, but do not replace cold/package release baselines. |
| Resident registration component sweep reporting | clean-room benchmark reporting over GLASS artifacts | S2-Gate 99 promotion of resident registration component timings into sweep summaries | Pulls resident triangle registration component timings from `resident_artifacts.json` into resident sweep JSON/Markdown summaries: total registration/warp, moving catalog, descriptor fit, moving descriptor generation, pixel refinement, warp, and native batch warp. The latest 200-light mini matrix was regenerated from existing runs to expose the component table. | `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined reporting schema over existing GLASS timing keys | The gate consumes GLASS-owned timing artifacts only. It does not alter image calculations, CUDA kernels, registration choices, input data, or external reference behavior. | Component timings are as accurate as the resident artifact instrumentation. Some component totals are native/cumulative diagnostics and should be interpreted alongside the aggregate `resident_registration_warp` wall time. |
| Resident registration parameter sweep | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 100 sweep dimensions for triangle pixel-refine and star-candidate tuning | Extends the resident sweep harness with registration-tuning dimensions for triangle fast-coarse mode, coarse/final pixel-refine strides, and star max candidates. A two-variant 200-light sweep compared baseline refinement with fast-coarse refinement under guardrails and external-reference comparison. | `src/glass/report/resident_sweep.py`, `benchmarks/bench_resident_prefetch_sweep.py` | `tests/test_benchmarks.py` | Project-defined sweep dimensions over existing GLASS CLI options | The gate orchestrates existing GLASS parameters and consumes GLASS-owned timing/compare artifacts. It does not change CUDA kernels, registration formulas, or image calculations. | Fast-coarse improved pixel-refine time in this run, but it changes the raw GLASS output versus baseline refinement. It remains a candidate path requiring broader numerical confirmation, not a default. |
| Resident sweep compare contract | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 101 per-variant reference comparison metrics in resident sweeps | Adds per-variant compare options and summary extraction so resident sweep JSON/Markdown records reference compare JSON/HTML paths, shape match, RMS, relative RMS, p99 absolute difference, compared pixels, coverage fraction, and external speedup alongside timing and guardrails. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined benchmark reporting contract over GLASS compare artifacts | The gate consumes GLASS-owned compare outputs and resident sweep artifacts only. It does not alter image calculations, CUDA kernels, registration formulas, input data, or reference outputs. | Compare metrics depend on the supplied normalization, coverage-map, and minimum-coverage settings. They are benchmark evidence for candidate ranking, not a claim of universal numerical equivalence. |
| Compare-gated resident sweep ranking | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 102 ranking gate over S2-Gate 101 compare metrics | Adds optional compare-gate thresholds to resident sweep ranking: require shape match, maximum RMS difference, maximum relative RMS difference, and maximum p99 absolute difference. Per-variant pass/fail reasons and top-level gate counts are recorded in JSON/Markdown. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined benchmark selection policy over GLASS compare artifacts | The gate consumes GLASS-owned compare metrics and resident sweep artifacts only. It does not alter image calculations, CUDA kernels, registration formulas, input data, or reference outputs. | Thresholds are benchmark-contract choices. A variant can remain scientifically interesting even when it is demoted by a strict compare gate for a specific benchmark. |
| Resident triangle catalog sweep knobs | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 103 CLI and sweep exposure for resident triangle catalog capacity/NMS controls | Adds resident CLI overrides and sweep dimensions for triangle grid top candidates per cell, non-grid NMS scan candidate count, and NMS minimum separation. Effective values are recorded in resident artifacts and registration warnings. A two-variant 200-light sweep tested `triangle_grid_top_per_cell=2,4` with guardrails and compare-gated ranking. | `src/glass/cli.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/resident_sweep.py`, `benchmarks/bench_resident_prefetch_sweep.py` | `tests/test_benchmarks.py`, `tests/test_resident_cuda_run.py` | Project-defined benchmark/tuning controls over existing GLASS catalog kernels | The gate changes parameter exposure, orchestration, and artifact reporting only. It does not change star-catalog kernel definitions, descriptor matching, warp, integration, input data, or reference outputs. | Lower grid top-per-cell reduced 200-light moving-catalog time in the tested run but failed the strict reference compare gate by a small margin. It remains a candidate requiring further agreement tuning, not a default. |
| Resident sweep analysis artifact | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 104 analysis layer over resident sweep summaries | Writes `resident_prefetch_sweep_analysis.json` and Markdown for each sweep, identifying fastest variants, lowest moving-catalog/registration-warp variants, promotion candidates, and recommendation status/reasons under enabled guardrail and compare gates. The Gate103 200-light catalog sweep analysis records the lower-catalog candidate as blocked by compare-gated image agreement. | `src/glass/report/resident_sweep.py`, `benchmarks/bench_resident_prefetch_sweep.py` | `tests/test_benchmarks.py` | Project-defined benchmark analysis over GLASS sweep artifacts | The analysis consumes GLASS-owned sweep summaries, guardrail results, and compare metrics only. It does not change image calculations, CUDA kernels, registration formulas, input data, or reference outputs. | This is a decision-support artifact, not an automatic default-changing mechanism. Promotion still requires an explicit follow-up gate. |
| Resident sweep frame-accounting gate | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 105 frame-accounting promotion gate over resident sweep summaries | Adds optional exact/minimum frame-count requirements to resident sweep ranking and analysis so variants that change input light, active/integrated light, or zero-weight frame counts cannot be promoted solely because they are faster. A frame-gated 200-light catalog sweep analysis is regenerated from existing Gate103 artifacts. | `src/glass/report/resident_sweep.py`, `benchmarks/bench_resident_prefetch_sweep.py` | `tests/test_benchmarks.py` | Project-defined benchmark safety policy over the Phase 1/2 frame-count baseline | The gate consumes GLASS-owned frame-accounting and sweep artifacts only. It does not change image calculations, CUDA kernels, registration formulas, input data, or reference outputs. | Frame accounting protects benchmark comparability; it does not by itself decide whether a rejected frame is scientifically optimal. |
| Resident sweep contract frame-gate import | clean-room benchmark orchestration over GLASS artifacts | S2-Gate 106 import of GLASS benchmark-contract `frame_accounting` requirements into resident sweeps | Adds `--frame-gate-from-contract` so resident sweeps can derive frame-count promotion requirements from the existing benchmark contract instead of retyping expected counts. Explicit CLI values override imported defaults, and provenance is recorded in summary Markdown/JSON. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined mapping from GLASS benchmark-contract fields to GLASS sweep ranking policy | The importer reads GLASS-owned benchmark contract JSON only. It does not consume external source code, change image calculations, or alter CUDA kernels. | Only frame-accounting requirements are imported in this gate; compare thresholds and compare normalization remain explicit sweep arguments. |
| Resident sweep contract compare import | clean-room benchmark orchestration over GLASS artifacts | S2-Gate 107 import of GLASS benchmark-contract `comparison` requirements into resident sweeps | Adds `--compare-from-contract` so resident sweeps can derive shape-match, RMS, p99, scale, offset, minimum coverage, and reference-runtime compare settings from an existing benchmark contract. Explicit CLI values override imported defaults, and provenance is recorded in summary Markdown/JSON. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py` | Project-defined mapping from GLASS benchmark-contract fields to GLASS sweep ranking and compare-command policy | The importer reads GLASS-owned benchmark contract JSON only. It does not consume external source code, change image calculations, or alter CUDA kernels. | The current benchmark contract's numerical limits are acceptance thresholds, not necessarily the stricter exploratory thresholds used for promotion of registration-tuning candidates. |
| Contract-gated 200-light catalog sweep | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 108 real 200-light sweep at `C:\glass_runs\phase2_s2_gate_108_contract_gated_catalog_sweep` | Runs a two-variant resident CUDA sweep over `triangle_grid_top_per_cell=2,4` using imported Gate32 science arguments, benchmark-contract frame/compare defaults, strict compare overrides, per-variant guardrails with pixel verification, candidate coverage maps, and external-reference comparison. Both variants preserved the expected 200/193/7 frame counts and passed guardrails, but both were blocked by strict compare-gate image agreement. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | `tests/test_benchmarks.py`, real-data sweep artifacts | Project-defined benchmark promotion policy over GLASS-owned artifacts and user-generated reference output | The gate consumes GLASS-owned command, timing, guardrail, frame-accounting, coverage, and compare artifacts only. It does not change image calculations, CUDA kernels, input data, or reference outputs. | No catalog-capacity variant is promoted. The next optimization target is reducing RMS/p99 drift while retaining the lower moving-catalog time. |
| Contract-gated 200-light NMS separation sweep | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 109 real 200-light sweep at `C:\glass_runs\phase2_s2_gate_109_nms_separation_sweep` | Runs a four-variant resident CUDA sweep over `triangle_nms_min_separation_px=48,64,80,96` while keeping `triangle_grid_top_per_cell=2`, imported Gate32 science arguments, benchmark-contract frame/compare defaults, strict compare overrides, per-variant guardrails with pixel verification, candidate coverage maps, and external-reference comparison. All variants preserved 200/193/7 frame counts and passed guardrails, but all were blocked by strict compare-gate image agreement. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | Real-data sweep artifacts, full test suite | Project-defined benchmark promotion policy over GLASS-owned artifacts and user-generated reference output | The gate consumes GLASS-owned command, timing, guardrail, frame-accounting, coverage, and compare artifacts only. It does not change image calculations, CUDA kernels, input data, or reference outputs. | No NMS separation variant is promoted. `sep48` nearly satisfies RMS while preserving safe p99; `sep96` satisfies RMS but slightly exceeds p99, suggesting the next search should combine separation with grid shape or candidate density. |
| Contract-gated 200-light grid-shape sweep | clean-room benchmark orchestration / black-box experiment over GLASS artifacts | S2-Gate 110 real 200-light sweep at `C:\glass_runs\phase2_s2_gate_110_grid_shape_sweep` | Runs a four-variant resident CUDA sweep over `star_grid_cols=20,28`, fixed `star_grid_rows=16`, `triangle_nms_min_separation_px=48,96`, and `triangle_grid_top_per_cell=2`, using imported Gate32 science arguments, benchmark-contract frame/compare defaults, strict compare overrides, per-variant guardrails with pixel verification, candidate coverage maps, and external-reference comparison. All variants preserved 200/193/7 frame counts and passed guardrails, but all were blocked by strict compare-gate image agreement. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_sweep.py` | Real-data sweep artifacts, full test suite | Project-defined benchmark promotion policy over GLASS-owned artifacts and user-generated reference output | The gate consumes GLASS-owned command, timing, guardrail, frame-accounting, coverage, and compare artifacts only. It does not change image calculations, CUDA kernels, input data, or reference outputs. | No grid-shape variant is promoted. Reducing grid columns to 20 worsened catalog time and RMS; 28 columns kept runtime healthy, but RMS remained above the strict compare threshold. |
| Resident registration candidate audit | clean-room artifact parsing / benchmark observability | S2-Gate 111 audit over S2-Gate 110 resident registration outputs at `C:\glass_runs\phase2_s2_gate_111_candidate_audit` | Adds `glass resident-registration-audit`, which converts resident CUDA triangle registration warning strings into a stable JSON/Markdown audit: per-frame triangle trial counts, candidate counts, fit RMS, pixel-refine RMS/NCC, quality-gate status, catalog parameters, determinism signatures, and aggregate timing/candidate statistics. Running it over the four S2-Gate 110 variants showed 200 triangle frames and zero failed triangle frames in each variant, so the strict compare failures are not explained by outright registration failure. | `src/glass/report/resident_registration_audit.py`, `src/glass/cli.py` | `tests/test_resident_registration_audit.py`, `tests/test_cli_smoke.py`, real-data audit artifacts | Project-defined artifact schema over GLASS-owned registration artifacts | The audit reads GLASS-owned `registration_results.json` and `resident_artifacts.json` only. It does not change registration scoring, CUDA kernels, image calculations, input data, or reference outputs. | This is an observability gate. It identifies candidate/refine statistics for follow-up scoring work, but it does not by itself prove which scoring change should become default. |
| Resident candidate/compare correlation | clean-room artifact analysis / benchmark observability | S2-Gate 112 join over S2-Gate 110 sweep summary and S2-Gate 111 candidate audits at `C:\glass_runs\phase2_s2_gate_112_candidate_compare` | Adds `glass resident-registration-compare`, which joins per-variant compare metrics from `resident_prefetch_sweep_summary.json` with candidate-audit statistics, writes JSON/Markdown, and ranks simple Pearson correlations between candidate/refine metrics and compare RMS/p99/relative RMS. The 200-light join found four compare-gate failures with zero registration hard-failure variants, and highlighted pixel NCC/RMS statistics as the strongest small-sample drift indicators. | `src/glass/report/resident_registration_compare.py`, `src/glass/cli.py` | `tests/test_resident_registration_compare.py`, `tests/test_cli_smoke.py`, real-data joined artifacts | Project-defined correlation/reporting schema over GLASS-owned benchmark artifacts | The analysis reads GLASS-owned sweep and candidate-audit artifacts only. It does not change registration scoring, CUDA kernels, image calculations, input data, or reference outputs. | Correlations are based on a four-variant tuning slice and are decision-support evidence, not proof of causation. The next gate must validate any scoring/refine change with guardrails and compare gates. |
| Resident triangle agreement gate | clean-room scoring control / benchmark observability | S2-Gate 113 audit-only default score over resident triangle pixel-refine NCC/RMS | Adds a project-defined `triangle_agreement_score = pixel_ncc / (1 + max(pixel_rms_adu, 0) / rms_scale_adu)` for resident triangle registration. The score, status, reason, RMS scale, and optional minimum threshold are emitted in per-frame warnings and resident artifacts. Defaults are audit-only; an explicit plan or CLI minimum score is required before a low-agreement frame is rejected. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py`, `tests/test_cli_smoke.py` | Project-defined scalar diagnostic derived from GLASS-owned pixel-refine metrics | The gate changes GLASS scoring/reporting only. It does not read or depend on external implementation source, alter descriptor matching kernels, or change default registration decisions. | The score is a control surface for follow-up sweeps, not a universal scientific threshold. Any non-default threshold still needs real-data guardrail, frame-count, and image-comparison validation before promotion. |
| Agreement-threshold sweep dimension | clean-room benchmark orchestration / scoring-control sweep setup | S2-Gate 114 resident sweep dimensions for `triangle_min_agreement_score` and `triangle_agreement_rms_scale` | Extends the resident sweep harness so agreement thresholds and RMS scales are first-class variant dimensions. Variant ids encode `agr*` and `agrs*`, and generated commands append the matching resident CLI overrides. Dry-run validation can now plan bounded 200-light threshold sweeps without changing registration kernels or default behavior. | `src/glass/report/resident_sweep.py`, `benchmarks/bench_resident_prefetch_sweep.py` | `tests/test_benchmarks.py` | Project-defined benchmark orchestration over the S2-Gate 113 agreement score | This gate changes sweep planning and command generation only. It does not alter image calculations, CUDA kernels, default thresholds, input data, or reference outputs. | It enables threshold experiments but does not promote a threshold. Promotion still requires executed 200-light sweeps with frame-count, guardrail, and strict image-compare gates. |
| Contract-gated agreement-threshold sweep | clean-room benchmark orchestration / scoring-control experiment over GLASS artifacts | S2-Gate 115 real 200-light sweep at `C:\glass_runs\phase2_s2_gate_115_agreement_threshold_sweep` | Runs a bounded resident CUDA sweep over `triangle_min_agreement_score` using the latest known-good grid-shape command as common science/tuning input, benchmark-contract frame and compare gates, candidate coverage maps, and per-variant guardrails with pixel verification. The executed variants all passed guardrails: `inherit` preserved 200/193/7 frame accounting but failed strict RMS, `0.05` nearly reached the strict RMS gate but rejected one extra frame, and `0.1` rejected four extra frames so no >=190-coverage comparison region remained. Each completed variant is audited with `glass resident-registration-audit` and joined with compare metrics using `glass resident-registration-compare`. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_registration_audit.py`, `src/glass/report/resident_registration_compare.py` | Real-data sweep artifacts, full test suite | Project-defined benchmark promotion policy over GLASS-owned registration agreement scores and user-generated reference output | The gate consumes GLASS-owned command, timing, frame-accounting, guardrail, coverage, compare, and candidate-audit artifacts only. It does not change image calculations, CUDA kernels, input data, or reference outputs. | No agreement threshold is promoted by this gate. Agreement filtering can reduce RMS on this dataset, but the current scalar gate drops required frames before passing all benchmark contracts. |
| Agreement rejection triage | clean-room artifact analysis / benchmark observability | S2-Gate 116 triage over S2-Gate 115 candidate audits at `C:\glass_runs\phase2_s2_gate_116_agreement_rejection_triage` | Adds `glass resident-registration-triage`, which compares an audit-only baseline candidate audit against one or more threshold candidate audits. It reports newly rejected frames, recovered frames, agreement score/RMS/NCC details, and determinism-signature changes for reference catalogs, descriptors, moving catalogs, selected fits, and trials. The S2-Gate 115 triage identifies extra threshold rejections together with reference catalog signature drift, so agreement-threshold promotion is blocked until resident catalog determinism is isolated. | `src/glass/report/resident_registration_triage.py`, `src/glass/cli.py` | `tests/test_resident_registration_triage.py`, real-data triage artifacts | Project-defined comparison over GLASS-owned candidate-audit artifacts | The triage reads GLASS-owned audit JSON only. It does not change registration scoring, CUDA kernels, image calculations, input data, or reference outputs. | Diagnostic only. It points the next work toward deterministic resident catalog hardening before further agreement-threshold tuning. |
| Deterministic catalog sweep control | clean-room benchmark orchestration / deterministic-mode control | S2-Gate 117 resident sweep dimension and repeated 200-light determinism audit at `C:\glass_runs\phase2_s2_gate_117_deterministic_catalog` | Adds `--star-catalog-deterministic-modes inherit,off,on` to the resident sweep harness. Variant ids encode `catfast` and `catdet`, generated commands append `--resident-star-catalog-deterministic` only for deterministic variants, and explicit deterministic-mode sweeps strip any imported deterministic flag from common run args so `off` is real. A 200-light dry run verified `off/on` command generation, then two 200-light `catdet` runs were compared with `glass resident-determinism --fail-on-mismatch` and matched artifact signatures, frame signatures, registration results, frame accounting, and output pixels/maps. | `src/glass/report/resident_sweep.py`, `benchmarks/bench_resident_prefetch_sweep.py` | `tests/test_benchmarks.py`, real-data determinism artifacts | Project-defined benchmark control over the existing GLASS deterministic resident grid/NMS catalog mode | This gate changes sweep command generation and deterministic-mode selection only. It does not change star catalog formulas, registration scoring, CUDA kernels, input data, or reference outputs. | Default fast catalog behavior is unchanged. Use `catdet` for follow-up agreement-threshold experiments where candidate drift must be attributable to the threshold rather than catalog nondeterminism. |
| Deterministic agreement-threshold sweep | clean-room benchmark orchestration / scoring-control experiment over GLASS artifacts | S2-Gate 118 real 200-light deterministic sweep at `C:\glass_runs\phase2_s2_gate_118_deterministic_agreement_sweep` | Reruns the agreement-threshold matrix with `--star-catalog-deterministic-modes on`, imported benchmark contract gates, candidate coverage maps, strict compare overrides, and per-variant guardrails. The deterministic audit-only baseline preserved 200/193/7 and passed RMS but narrowly failed p99; the `0.05` and `0.1` thresholds passed strict image compare but rejected required frames. Triage showed no reference catalog or descriptor drift and isolated extra hard rejections to `F000061` and `F000159`. | `benchmarks/bench_resident_prefetch_sweep.py`, `src/glass/report/resident_registration_audit.py`, `src/glass/report/resident_registration_compare.py`, `src/glass/report/resident_registration_triage.py` | Real-data sweep artifacts, full test suite | Project-defined benchmark promotion policy over GLASS-owned agreement scores and deterministic catalog controls | The gate consumes GLASS-owned command, timing, frame-accounting, guardrail, coverage, compare, candidate-audit, and triage artifacts only. It does not change image calculations, CUDA kernels, input data, or reference outputs. | No threshold is promoted. Deterministic control proves the extra frame drops are threshold decisions, so the next work should replace scalar hard rejection with a less blunt policy such as flagging, down-weighting, or multi-condition rejection. |
| Agreement downweight policy | clean-room scoring-control experiment over GLASS artifacts | S2-Gate 119 optional resident agreement action and real 200-light downweight sweeps at `C:\glass_runs\phase2_s2_gate_119_agreement_downweight*` | Adds explicit agreement actions: default `fail`, plus `downweight` and `flag`. Downweighting keeps low-agreement registered frames active with a `score / threshold` multiplier applied after resident frame-quality weighting, while recording action, multiplier, and downweighted-frame counts in resident artifacts and registration audits. Deterministic 200-light sweeps over thresholds `0.05,0.1,0.2,0.3,0.5,0.8,1.0` preserved 200/193/7 and produced zero registration hard failures, but linear downweighting still missed the strict p99 compare gate. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py`, `src/glass/report/resident_registration_audit.py` | `tests/test_resident_cuda_run.py`, `tests/test_resident_registration_audit.py`, real-data sweep artifacts | Project-defined agreement weighting policy over GLASS-owned agreement scores | The gate changes optional GLASS scoring/weighting behavior only. Default hard-fail behavior is unchanged, and no external source code or implementation behavior is used. | No downweight threshold is promoted. The best tested p99 was still above the strict benchmark gate, so the next work should try a non-linear or multi-signal agreement weighting model instead of a simple score/threshold multiplier. |
| Compare tail outlier audit | clean-room residual localization over GLASS and user-generated reference artifacts | S2-Gate 120 audit at `C:\glass_runs\phase2_s2_gate_120_compare_outliers` | Adds `glass compare-outliers`, which applies the same GLASS-side scale/offset, clipping, border exclusion, and coverage-mask policy as benchmark comparison, then localizes p99 tail pixels and optional strict target-threshold exceedance pixels into top residual tiles/pixels with signed-tail and edge-band summaries. The 200-light audit showed that the best Gate119 downweight candidate reduced strict-threshold exceedance pixels from 612,930 to 599,340 but the tail remained localized in the same residual tiles and overwhelmingly negative. | `src/glass/report/compare_outliers.py`, `src/glass/cli.py` | `tests/test_compare_outliers.py`, `tests/test_cli_smoke.py`, real-data audit artifacts | Project-defined residual statistics and triage recommendations over generated comparison products | The audit reads GLASS output images/maps and a user-generated external reference image only. It does not use external implementation source code and does not change image processing, registration, weighting, or integration math. | Diagnostic only. The next work should inspect localized residual tiles or per-frame contribution/rejection maps before changing global agreement weighting again. |
| Localized compare tile package | clean-room diagnostic packaging over GLASS and user-generated reference artifacts | S2-Gate 121 tile package at `C:\glass_runs\phase2_s2_gate_121_tile_pack` | Adds `glass compare-tile-pack`, which consumes a `compare-outliers` audit and exports top residual tiles as GLASS/reference/signed-diff/absolute-diff FITS cutouts with optional PNG previews and a manifest containing coordinates, source top-tile metadata, and tile diff statistics. Running it on the Gate120 `agr0p5` audit exported the top three localized residual tiles for visual and numerical review. | `src/glass/report/compare_tile_pack.py`, `src/glass/cli.py` | `tests/test_compare_tile_pack.py`, `tests/test_cli_smoke.py`, real-data tile-package artifacts | Project-defined artifact packaging and preview rendering over generated comparison products | The package reads GLASS outputs, coverage maps, and a user-generated external reference image only. It does not use external source code and does not change scientific image processing. | Diagnostic only. The top signed-diff previews show localized negative residual structures around stars/nebulosity; the next gate should attribute those regions to frame contribution/rejection or LN/registration behavior. |
| Localized tile map attribution | clean-room diagnostic join over GLASS output maps and frame accounting | S2-Gate 122 attribution at `C:\glass_runs\phase2_s2_gate_122_tile_attribution` | Adds `glass compare-tile-attribution`, which consumes a tile-pack manifest and GLASS run directory, then summarizes coverage, weight, DQ, low-rejection, and high-rejection maps for each localized residual tile while joining global frame-accounting rows for integrated, zero-weight, low-weight, and agreement-downweighted frames. The Gate121 `agr0p5` attribution found no warp-edge pixels in the top tiles, mild rejection fractions around 0.42%-0.50%, coverage variation from 185 to 193, and 73 agreement-downweighted frames. | `src/glass/report/compare_tile_attribution.py`, `src/glass/cli.py` | `tests/test_compare_tile_attribution.py`, `tests/test_cli_smoke.py`, real-data attribution artifacts | Project-defined artifact join and map statistics over generated GLASS products | The attribution reads GLASS output maps, GLASS frame-accounting JSON, and tile-package metadata only. It does not use external source code and does not change image processing. | Diagnostic only. Existing resident artifacts do not include per-frame registered tile samples, so this gate cannot prove individual frame causality; the next gate should add a bounded per-frame tile replay or capture path. |
| Bounded per-frame tile replay | clean-room diagnostic replay over GLASS inputs, masters, matrices, and tile metadata | S2-Gate 123 replay at `C:\glass_runs\phase2_s2_gate_123_tile_replay` | Adds `glass compare-tile-replay`, which consumes a tile-pack manifest and GLASS run directory, discovers the resident master cache, reads only the source windows needed for each residual tile, applies GLASS master calibration semantics, applies registration matrices, and ranks replayed frames by signed/weighted delta to the final GLASS master tile. The `agr0p5` all-positive-weight replay covered 193 selected frames and replayed 193 frames in each top tile; F000102, F000100, F000105, and F000101 consistently ranked as the largest positive local contributors across all three residual tiles. | `src/glass/report/compare_tile_replay.py`, `src/glass/cli.py` | `tests/test_compare_tile_replay.py`, `tests/test_cli_smoke.py`, real-data replay artifacts | Project-defined bounded CPU diagnostic replay using GLASS-generated matrices and calibration masters | The replay reads GLASS input paths, GLASS master cache files, GLASS registration results, and GLASS final master tiles only. It does not use external implementation source code and does not change image processing. | Diagnostic only. The replay currently uses bilinear CPU sampling while the resident CUDA benchmark used Lanczos3, and its sigma proxy is not a byte-exact resident winsorized rejection replay. A later gate can add exact Lanczos3 CPU replay or native capture for promoted decisions. |
| Lanczos3 replay parity | clean-room diagnostic replay parity over GLASS inputs, masters, and matrices | S2-Gate 124 Lanczos3 replay at `C:\glass_runs\phase2_s2_gate_124_lanczos_replay` | Extends `glass compare-tile-replay` with selectable replay interpolation and adds vectorized CPU Lanczos3 sampling for bounded residual-tile replay. Running the `agr0p5` all-positive-weight replay with Lanczos3 covered 193 selected frames and replayed 193 frames in each top tile. The top-10 contributor ordering matched the S2-Gate 123 bilinear replay exactly for all three tiles, preserving the same F000102/F000100/F000105/F000101 frame-family finding under closer interpolation parity with the resident Lanczos3 benchmark path. | `src/glass/report/compare_tile_replay.py`, `src/glass/cli.py` | `tests/test_compare_tile_replay.py`, `tests/test_cli_smoke.py`, real-data Lanczos3 replay artifacts | Project-defined vectorized Lanczos3 sampling from the public sinc/Lanczos mathematical definition | The replay reads GLASS input paths, GLASS master cache files, GLASS registration results, and GLASS final master tiles only. It does not use external implementation source code and does not change image processing defaults. | Diagnostic only. This is still CPU replay and does not reproduce every native CUDA detail or the resident winsorized rejection implementation byte-for-byte; it confirms that the frame-family attribution is not a bilinear replay artifact. |
| Residual frame-family audit | clean-room diagnostic join over GLASS frame accounting, registration results, and replay artifacts | S2-Gate 125 frame-family audit at `C:\glass_runs\phase2_s2_gate_125_frame_family` | Adds `glass compare-frame-family`, which selects a focus frame family and neighboring controls, then joins Lanczos3 replay tile ranks/deltas, integration weights, agreement warning fields, registration RMS, and registration matrix translation metrics. Running it on F000100-F000110 against neighboring controls found all 11 focus frames downweighted, top focus frame F000102, focus mean translation_x 44.879 px versus 5.318 px for controls, and focus mean local weighted-delta 94.782 ADU versus 37.800 ADU for controls. | `src/glass/report/compare_frame_family.py`, `src/glass/cli.py` | `tests/test_compare_frame_family.py`, `tests/test_cli_smoke.py`, real-data frame-family audit artifacts | Project-defined diagnostic grouping and summary statistics over GLASS-owned artifacts | The audit reads GLASS frame accounting, GLASS registration results, and GLASS replay diagnostics only. It does not use external implementation source code and does not change image processing defaults. | Diagnostic only. Neighboring controls are sequence neighbors, not a matched observational control population; the next gate should either improve this coherent motion-family handling or capture exact resident per-frame tile contributions before changing defaults. |
| Resident tile capture parity | clean-room diagnostic capture over GLASS resident CUDA kernels and artifacts | S2-Gate 126 resident tile capture at `C:\glass_runs\phase2_s2_gate_126_resident_tile_capture` | Adds `ResidentCalibratedStack.download_frame_tile` and `glass resident-tile-capture`. The diagnostic rebuilds selected positive-weight frames into a small resident CUDA stack, applies GLASS master calibration cache and registration matrices, captures localized post-warp resident frame tiles, and compares weighted-delta statistics with the S2-Gate 124 CPU Lanczos3 replay. On F000100-F000110 across the top three residual tiles, resident capture matched CPU replay within about 4.6e-5 ADU max absolute weighted-delta mean difference, confirming replay interpolation/calibration is not the cause of the frame-family residual finding. | `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/report/resident_tile_capture.py`, `src/glass/cli.py` | `tests/test_resident_tile_capture.py`, `tests/test_cli_smoke.py`, real-data resident capture artifacts | Project-defined diagnostic capture over GLASS-owned CUDA kernels, registration matrices, and master cache files | The capture uses only GLASS resident CUDA kernels and GLASS artifacts. It does not use external source code and does not change image processing defaults. | Diagnostic only. It captures selected post-warp frame tiles before integration rejection decisions and does not yet capture exact per-frame accepted/rejected contribution inside the resident winsorized integration kernel. |
| Localized integration contribution audit | clean-room diagnostic replay over GLASS inputs, masters, matrices, and resident rejection semantics | S2-Gate 127 integration audit at `C:\glass_runs\phase2_s2_gate_127_tile_integration` | Adds `glass compare-tile-integration`, which replays selected residual tiles for all positive-weight frames, recomputes GLASS resident rejection semantics locally, and reports per-frame accepted/rejected pixels, accepted weighted deltas, normalized contribution, focus/control summaries, and diagnostic reconstructed-master delta. On the Gate121 top three `agr0p5` tiles, the diagnostic master matched the GLASS master at around 1e-5 ADU mean delta while F000100-F000110 remained mostly accepted after winsorized rejection and contributed more than neighboring controls. | `src/glass/report/compare_tile_integration.py`, `src/glass/cli.py` | `tests/test_compare_tile_integration.py`, `tests/test_cli_smoke.py`, real-data integration audit artifacts | Project-defined diagnostic replay of GLASS-owned resident CUDA rejection formulas | The audit reads GLASS input paths, master cache files, registration results, integration outputs, and frame-accounting artifacts only. It does not use external source code and does not change image processing defaults. | Diagnostic only. It confirms the localized frame-family signal survives rejection, but policy changes are deferred to a later gate so they can be tested against the 200-light benchmark contract. |
| Registration-motion weighting candidate | clean-room opt-in weighting policy over GLASS registration matrices | S2-Gate 128 candidate at `C:\glass_runs\phase2_s2_gate_128_motion_weighting` | Adds explicit resident CLI controls for registration-motion weighting. The `translation_mad` mode groups registration matrices by orientation cluster, computes robust translation centers and MAD scales per cluster, and applies a smooth multiplier floor to high-score motion outliers after normal frame weighting and triangle agreement downweighting. A 200-light candidate preserved 193 integrated / 7 zero-weight frames and downweighted only F000061 after cluster correction, but reduced the strict target exceedance count by only 16 pixels versus the Gate120 `agr0p5` baseline, so the policy is not promoted. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py`, real-data candidate artifacts | Project-defined robust statistics over GLASS-owned registration matrices | The policy consumes GLASS registration matrices, GLASS frame weights, and GLASS artifact state only. It does not use external source code or proprietary implementation behavior. | Default remains `off`. This gate is infrastructure plus negative evidence: simple registration-motion outlier weighting is insufficient for the F000100-F000110 localized residual family, so the next corrective gate should use localized contribution/family evidence rather than global motion distance alone. |
| Localized frame-weight proposal | clean-room opt-in weighting experiment over GLASS diagnostic contribution artifacts | S2-Gate 129 candidate derived from `C:\glass_runs\phase2_s2_gate_127_tile_integration\agr0p5_f100_f110_integration_audit.json` | Adds `glass frame-weight-proposal`, which converts localized `compare-tile-integration` focus/control contribution summaries into an explicit per-frame multiplier proposal. Resident CUDA can consume the proposal with `--resident-frame-weight-proposal`, applying multipliers after normal frame weighting, triangle agreement downweighting, and registration-motion weighting while recording proposal provenance and per-frame warnings. | `src/glass/report/frame_weight_proposal.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_frame_weight_proposal.py`, `tests/test_resident_cuda_run.py`, real-data candidate artifacts | Project-defined diagnostic-to-experiment bridge over GLASS-owned contribution summaries | The proposal consumes GLASS diagnostic artifacts, registration results, and frame weights only. It does not use external source code or proprietary implementation behavior, and it is never enabled by default. | This is an explicit experiment artifact, not a promoted scientific weighting policy. It can overfit a known localized residual family, so benchmark acceptance must judge global residuals and timing before any default behavior changes. |
| Frame-weight proposal direction audit | clean-room diagnostic direction check over GLASS residual and contribution artifacts | S2-Gate 130 audit over the S2-Gate 129 F000100-F000110 proposal and S2-Gate 121/127 localized residual artifacts | Adds `glass frame-weight-proposal-audit`, which joins tile-pack signed GLASS-minus-reference residual summaries with `compare-tile-integration` focus contribution summaries and estimates whether a proposed focus-frame multiplier would move each residual tile toward or away from the reference. | `src/glass/report/frame_weight_proposal_audit.py`, `src/glass/cli.py` | `tests/test_frame_weight_proposal_audit.py`, `tests/test_cli_smoke.py`, real-data direction-audit artifact | Project-defined first-order sign audit over GLASS-owned diagnostics | The audit reads GLASS comparison tile packs, GLASS localized integration diagnostics, and GLASS frame-weight proposal artifacts only. It does not use external source code or proprietary implementation behavior. | This is a first-order tile summary, not a full reintegration or per-pixel native kernel. It explains why a candidate should be rejected before running another full 200-light experiment, but later gates still need tile-local resident integration support for promoted policies. |
| Resident tile contribution capture | clean-room near-native diagnostic over GLASS resident CUDA post-warp tile pixels | S2-Gate 131 capture over S2-Gate 121 localized residual tiles and the S2-Gate 119/120 resident benchmark run | Adds `glass resident-tile-contribution`, which rebuilds selected positive-weight frames into a resident CUDA stack, applies GLASS master calibration and registration matrices, downloads bounded post-warp resident tile pixels, and computes rejection/contribution summaries from those captured tiles. | `src/glass/report/resident_tile_contribution.py`, `src/glass/cli.py` | `tests/test_resident_tile_contribution.py`, `tests/test_cli_smoke.py`, real-data resident tile contribution artifact | Project-defined diagnostic replay over GLASS-owned CUDA post-warp pixels and public weighted-mean/sigma formulas | The command uses only GLASS CUDA kernels, GLASS registration matrices, GLASS frame accounting, GLASS master cache files, and user-generated comparison tile manifests. It does not use external source code or proprietary implementation behavior. | This avoids CPU interpolation replay but still downloads selected tiles and computes contribution/rejection summaries on the CPU; it is not yet an inside-kernel per-pixel trace of the native integration kernel. |
| Tile-local policy proposal | clean-room artifact-only policy proposal over GLASS resident contribution and residual summaries | S2-Gate 132 proposal over `C:\glass_runs\phase2_s2_gate_131_resident_tile_contribution\agr0p5_f100_f110_resident_contribution.json` | Adds `glass tile-local-policy-proposal`, which joins resident-captured tile contribution summaries with signed GLASS-minus-reference residual tile summaries and solves a bounded tile-local multiplier for a target frame family. The artifact records whether the proposal should boost, downweight, hold, or reject each localized tile direction. | `src/glass/report/tile_local_policy.py`, `src/glass/cli.py` | `tests/test_tile_local_policy.py`, `tests/test_cli_smoke.py`, real-data tile-local proposal artifact | Project-defined first-order tile-local residual correction estimate | The proposal consumes only GLASS resident contribution artifacts, GLASS comparison tile packs, and user-generated reference residual summaries. It does not use external source code or proprietary implementation behavior and does not modify any image output. | Artifact-only. It uses tile-level means rather than per-pixel native weights, and boost multipliers require a future tile-local integration implementation before they can affect production results. |
| Tile-local policy replay | clean-room summary replay over GLASS resident contribution and policy artifacts | S2-Gate 133 replay over S2-Gate 132 F000100-F000110 proposal artifacts | Adds `glass tile-local-policy-replay`, which applies a tile-local proposal to resident contribution summaries, records selected target-frame row changes, canonical contribution deltas, before/after signed residual estimates, and conservative promotion status. | `src/glass/report/tile_local_policy_replay.py`, `src/glass/cli.py` | `tests/test_tile_local_policy_replay.py`, `tests/test_cli_smoke.py`, real-data tile-local replay artifacts | Project-defined first-order summary replay | The replay consumes only GLASS resident contribution artifacts and GLASS tile-local policy proposal artifacts. It does not use external source code or proprietary implementation behavior and does not modify image data. | Summary-only. It is a handoff contract for future native tile-local integration and cannot prove per-pixel output parity until the actual resident implementation exists. |
| Resident tile-local policy contract | clean-room resident artifact contract over GLASS tile-local replay artifacts | S2-Gate 134 interface lock for future resident tile-local integration | Adds a resident-run input for `tile-local-policy-replay` artifacts, validates target frames, tile extents, multipliers, and residual-direction summary fields, then records the contract in resident integration weighting diagnostics as `validated_not_applied`. | `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py`, `tests/test_cli_smoke.py` | Project-defined artifact validation and opt-in resident run metadata | The contract consumes only GLASS tile-local replay artifacts. It does not use external source code, proprietary behavior, or external output internals, and it does not change image pixels. | Interface-only. It deliberately does not apply tile-local multipliers until a native integration kernel can preserve DQ, coverage, rejection maps, and output parity. |
| Opt-in resident tile-local mean | clean-room resident CUDA weighted-mean primitive over GLASS replay contracts | S2-Gate 135 native stack-dispatch route for `tile-local-policy-replay` with `rejection=none` | Adds `ResidentCalibratedStack.integrate_tile_local_mean` plus `--resident-tile-local-policy-mode apply_mean`, applying target-frame tile multipliers inside non-overlapping half-open extents while preserving ordinary frame-global weights elsewhere. | `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined weighted mean extension over GLASS-owned resident stack frames and tile-local replay artifacts | The primitive consumes only resident GLASS frame pixels, frame weights, and GLASS tile-local replay contracts. It does not use external source code, proprietary behavior, or external output internals. Default mode remains record-only. | Opt-in and mean-only. Sigma and winsorized rejection still record the replay contract without applying tile-local multipliers; DQ and rejection-map parity for tile-local policies remain future gates. |
| Opt-in resident tile-local rejection | clean-room resident CUDA rejection primitive over GLASS replay contracts | S2-Gate 136 native stack-dispatch route for `tile-local-policy-replay` with `sigma_clip` and `winsorized_sigma` | Adds `ResidentCalibratedStack.integrate_tile_local_sigma_clip` and the forward `--resident-tile-local-policy-mode apply` route, applying target-frame tile multipliers before resident rejection accumulation while still producing weight, coverage, low-rejection, and high-rejection maps. | `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined extension of the existing GLASS resident two-pass sigma/winsorized rejection approximation | The primitive consumes only resident GLASS frame pixels, frame weights, and GLASS tile-local replay contracts. It does not use external source code, proprietary behavior, or external output internals. Default mode remains record-only. | Opt-in only. Fused-matrix integration does not support tile-local policy application yet, and broad 200-light promotion still requires benchmark-contract and image-agreement validation. |
| Bounded tile-local apply experiment audit | clean-room audit over GLASS resident artifacts and black-box reference outputs | S2-Gate 137 200-light candidate `C:\glass_runs\phase2_s2_gate_137_tile_local_apply\agr0p8_apply_signed_mean_tile0_v3` | Adds `glass tile-local-apply-experiment`, which verifies that a candidate run applied a tile-local replay contract natively, preserved frame accounting, stayed within runtime/speedup contracts, and passed reference compare thresholds. | `src/glass/report/tile_local_apply_experiment.py`, `src/glass/cli.py`, `src/glass/engine/resident_cuda.py` | `tests/test_tile_local_apply_experiment.py`, `tests/test_resident_cuda_run.py`, real-data Gate137 audit artifacts | Project-defined acceptance logic over GLASS artifacts and benchmark contract thresholds | The audit consumes GLASS run artifacts, GLASS compare artifacts, and user-generated reference outputs only. It does not use external source code or proprietary implementation behavior. | Audit-only. It validates a bounded candidate and does not enable tile-local apply by default. |
| Non-overlapping tile-local replay subset | clean-room artifact transformation over GLASS tile-local replay contracts | S2-Gate 138 subset `C:\glass_runs\phase2_s2_gate_138_tile_local_subset\f100_f110_signed_mean_nonoverlap_subset.json` | Adds `glass tile-local-policy-subset`, which greedily selects non-overlapping tile extents from a replay artifact by canonical delta, residual reduction, or tile index, records dropped overlap/limit tiles, and recomputes replay summary statistics. | `src/glass/report/tile_local_policy_subset.py`, `src/glass/cli.py` | `tests/test_tile_local_policy_subset.py`, `tests/test_cli_smoke.py`, real-data Gate138 subset artifacts | Project-defined native-safety transformation over half-open tile extents | The subset command consumes GLASS replay artifacts only and preserves the replay artifact type for opt-in resident CUDA application. It does not use external source code or proprietary behavior. | Greedy and not globally optimal. Subsets are experiment artifacts and do not promote a default policy. |
| Localized tile-local apply verification | clean-room measured residual verification over GLASS and reference images | S2-Gate 139 verification `C:\glass_runs\phase2_s2_gate_139_tile_local_verify\subset2_local_verify.json` | Adds `glass tile-local-apply-verify`, which reads baseline, candidate, and reference master images for selected replay tile extents, applies the benchmark GLASS-to-reference scale/offset, masks by coverage when requested, and measures signed mean, mean absolute residual, RMS, tail statistics, and improvement flags. | `src/glass/report/tile_local_apply_verify.py`, `src/glass/cli.py` | `tests/test_tile_local_apply_verify.py`, `tests/test_cli_smoke.py`, real-data Gate139 verification artifact | Project-defined residual metrics over bounded image tiles | The verifier consumes GLASS outputs, GLASS replay artifacts, and user-generated reference output images. It does not use external source code or proprietary implementation behavior. | Diagnostic only. It validates selected tile extents and does not prove an unbounded global tile-local policy is safe. |
| Measured tile-local policy decision | clean-room promotion-control artifact over measured verification and GLASS acceptance audits | S2-Gate 140 decision over Gate139 verification, Gate138 apply experiment, and Gate138 acceptance audit | Adds `glass tile-local-policy-decision`, which ranks measured tile-local verification artifacts and accepts or rejects the top candidate under configurable signed-mean, RMS, mean-absolute-residual, apply-experiment, and acceptance-audit gates. | `src/glass/report/tile_local_policy_decision.py`, `src/glass/cli.py` | `tests/test_tile_local_policy_decision.py`, `tests/test_cli_smoke.py`, real-data Gate140 decision artifact | Project-defined promotion thresholds over GLASS-owned metrics and benchmark contracts | The decision consumes GLASS JSON artifacts only. It does not read external implementation source and does not alter image processing defaults. | Promotion-control only. An accepted subset remains a sweep candidate until broader measured validation exists. |
| Measured tile-local policy sweep summary | clean-room promotion-summary artifact over measured tile-local policy decisions | S2-Gate 141 sweep over Gate137 single-tile and Gate138 two-tile candidates | Adds `glass tile-local-policy-sweep`, which ranks multiple measured `tile-local-policy-decision` artifacts, reports accepted/rejected counts, top decision, top score, tile count, and per-decision residual summaries. | `src/glass/report/tile_local_policy_sweep.py`, `src/glass/cli.py` | `tests/test_tile_local_policy_sweep.py`, `tests/test_cli_smoke.py`, real-data Gate141 sweep artifact | Project-defined ranking over GLASS decision artifacts | The sweep consumes GLASS JSON artifacts only. It does not read external implementation source, does not run image processing, and does not alter image processing defaults. | Summary-only. It identifies measured candidates for broader validation; it does not promote tile-local apply by default. |
| Tile-local sweep plan and three-tile candidate | clean-room benchmark orchestration over GLASS tile-local replay and decision artifacts | S2-Gate 142 plan and three-tile candidate over `C:\glass_runs\phase2_s2_gate_133_tile_local_replay\f100_f110_signed_mean_replay.json` | Adds `glass tile-local-sweep-plan`, which turns explicit benchmark inputs into a reproducible command queue for subset generation, resident run, compare, apply-experiment, acceptance-audit, local verification, decision, and final sweep commands. Gate142 also measures the three-tile candidate from the F000100-F000110 replay ladder. | `src/glass/report/tile_local_sweep_plan.py`, `src/glass/cli.py` | `tests/test_tile_local_sweep_plan.py`, `tests/test_cli_smoke.py`, real-data Gate142 plan and three-tile artifacts | Project-defined command orchestration over existing GLASS tools and measured artifacts | The plan consumes GLASS replay/decision artifacts, GLASS benchmark paths, and user-generated reference outputs only. It does not read external implementation source, does not execute hidden logic, and does not change defaults. | The command queue is explicit and opt-in. Generated plans can become stale if benchmark paths move; measured candidates still require acceptance and broader validation before promotion. |
| Residual tile candidate mining | clean-room artifact mining over GLASS compare-outlier audits | S2-Gate 143 candidate manifest over Gate120, Gate128, and Gate129 outlier artifacts with Gate121 known tile pack overlap labels | Adds `glass residual-tile-candidates`, which ranks top residual tiles from one or more `compare-outliers` artifacts, greedily drops overlapping selected candidates, marks overlap with known tile packs, and emits a tile-pack-like manifest for downstream resident contribution capture. | `src/glass/report/residual_tile_candidates.py`, `src/glass/cli.py` | `tests/test_residual_tile_candidates.py`, `tests/test_cli_smoke.py`, real-data Gate143 residual candidate artifact | Project-defined ranking and non-overlap selection over GLASS residual audit JSON | The miner consumes GLASS compare-outlier JSON and known GLASS tile-pack manifests only. It does not read image pixels, external implementation source, or proprietary behavior, and it does not change defaults. | Artifact-mining only. The selected tiles identify where to run future resident contribution capture; they are not by themselves a tile-local policy or proof of a correction. |
| New-region resident contribution capture | clean-room CUDA diagnostic over GLASS residual candidate manifests | S2-Gate 144 new-region manifest and contribution capture under `C:\glass_runs\phase2_s2_gate_144_new_region_contribution` | Extends `glass residual-tile-candidates` with `known-overlap-mode` and runs `glass resident-tile-contribution` on new residual regions excluded from the Gate121 known tile pack, measuring F000100-F000110 focus contribution against neighboring control frames under winsorized sigma rejection. | `src/glass/report/residual_tile_candidates.py`, `src/glass/cli.py` | `tests/test_residual_tile_candidates.py`, `tests/test_cli_smoke.py`, real-data Gate144 resident contribution artifact | Project-defined filtering over known tile-pack overlap and existing GLASS resident contribution diagnostics | The gate consumes GLASS outlier/candidate/run artifacts and GLASS CUDA capture paths only. It does not use external implementation source, proprietary behavior, or change pipeline defaults. | Diagnostic only. It establishes that the known focus frame family contributes strongly in newly mined regions, but does not yet solve or apply a new tile-local policy. |
| New-region tile-local proposal replay | clean-room summary replay over GLASS resident contribution artifacts | S2-Gate 145 proposal and replay under `C:\glass_runs\phase2_s2_gate_145_new_region_policy` | Runs existing `glass tile-local-policy-proposal` and `glass tile-local-policy-replay` on the S2-Gate 144 new-region contribution artifact using the F000100-F000110 focus group and tail-signed-mean residuals. | `src/glass/report/tile_local_policy.py`, `src/glass/report/tile_local_policy_replay.py`, `src/glass/cli.py` | Existing proposal/replay tests plus real-data Gate145 proposal/replay artifacts | Project-defined first-order tile-local summary replay over GLASS contribution and residual artifacts | The gate consumes GLASS candidate, contribution, proposal, and replay JSON artifacts only. It does not read external implementation source, run image integration, or change defaults. | Proposal/replay only. It confirms all new-region tiles move toward the reference under a clamped boost proposal, but the measured summary reduction is small and needs a future native apply experiment before promotion. |
| Tile-local multiplier saturation diagnostics | clean-room diagnostic summary over GLASS tile-local proposal artifacts | S2-Gate 146 saturation proposal and replay under `C:\glass_runs\phase2_s2_gate_146_multiplier_saturation` | Extends `glass tile-local-policy-proposal` summary output with unconstrained multiplier stats, applied multiplier stats, required boost/downweight stats, applied-to-required boost ratio, downweight depth ratio, clamped fraction, and mean residual reduction fraction. Re-runs the S2-Gate 145 new-region proposal/replay to explain universal clamping. | `src/glass/report/tile_local_policy.py` | `tests/test_tile_local_policy.py`, real-data Gate146 proposal/replay artifacts | Project-defined arithmetic over GLASS residual and contribution summaries | The diagnostic consumes GLASS proposal inputs only. It does not read image pixels, external implementation source, proprietary behavior, or alter resident integration defaults. | Diagnostic only. The S2-Gate 146 real artifact shows the 2.0 boost cap reaches only a small fraction of the estimated required boost, so future work should revisit the model or run bounded native apply experiments rather than simply promoting the clamped proposal. |
| Tile-local frame-family search | clean-room diagnostic search over GLASS resident contribution artifacts | S2-Gate 147 frame-family search under `C:\glass_runs\phase2_s2_gate_147_frame_family_search` | Adds `glass tile-local-frame-family-search`, scanning sorted contiguous frame windows and scoring each window by bounded tile-local residual correction potential with the same scale, residual stat, multiplier bounds, and saturation summary used by tile-local proposal artifacts. | `src/glass/report/tile_local_frame_family_search.py`, `src/glass/cli.py` | `tests/test_tile_local_frame_family_search.py`, `tests/test_cli_smoke.py`, real-data Gate147 top-ranked and all-candidate search artifacts | Project-defined arithmetic over GLASS resident contribution and residual candidate manifests | The search consumes GLASS contribution and tile-pack JSON artifacts only. It does not read image pixels, external implementation source, proprietary behavior, or alter resident integration defaults. | Diagnostic only. On the new-region artifact, F000100-F000110 remains the best 11-frame window and global top candidate, but its bounded reduction is still small, indicating the current frame-family hypothesis is directionally right but not sufficiently explanatory. |
| Tile-local residual source audit | clean-room diagnostic audit over GLASS contribution/search artifacts | S2-Gate 148 residual-source audit under `C:\glass_runs\phase2_s2_gate_148_residual_source` | Adds `glass tile-local-residual-source-audit`, summarizing per-tile coverage fraction, rejection-map means, focus/control high-rejection excess, top frame-family explained fraction, and recommendation routing for residual-source follow-up. | `src/glass/report/tile_local_residual_source_audit.py`, `src/glass/cli.py` | `tests/test_tile_local_residual_source_audit.py`, `tests/test_cli_smoke.py`, real-data Gate148 residual-source audit artifact | Project-defined diagnostic aggregation over GLASS JSON artifacts | The audit consumes GLASS contribution, residual tile-pack, and frame-family search JSON artifacts only. It does not read image pixels, external implementation source, proprietary behavior, or alter resident integration defaults. | Diagnostic only. On the new-region artifact, coverage is healthy and the best frame-family explains less than one percent of total residual, while all audited tiles show focus high-rejection excess, so the next work should prioritize rejection/registration diagnostics. |
| Tile-local rejection/registration audit | clean-room frame-level diagnostic aggregation over GLASS contribution artifacts | S2-Gate 149 rejection/registration audit under `C:\glass_runs\phase2_s2_gate_149_rejection_registration` | Adds `glass tile-local-rejection-registration-audit`, aggregating resident contribution frame rows by frame id and ranking high-rejection frames with triangle agreement score, NCC, registration RMS, agreement weight multiplier, focus/control membership, and top frame-family membership. | `src/glass/report/tile_local_rejection_registration_audit.py`, `src/glass/cli.py` | `tests/test_tile_local_rejection_registration_audit.py`, `tests/test_cli_smoke.py`, real-data Gate149 rejection/registration artifact | Project-defined diagnostic aggregation over GLASS resident contribution and frame-family search JSON artifacts | The audit consumes GLASS JSON artifacts only. It does not read image pixels, external implementation source, proprietary behavior, or alter resident integration defaults. | Diagnostic only. On the new-region artifact, the top frame family has 11/11 high-rejection-excess frames and a lower agreement-score group mean than controls, supporting a next gate focused on registration-agreement/rejection interaction rather than tile-local boost promotion. |
| Tile-local rejection/registration experiment plan | clean-room command planning over GLASS audit artifacts | S2-Gate 150 experiment plan under `C:\glass_runs\phase2_s2_gate_150_rejection_registration_plan` | Adds `glass tile-local-rejection-registration-plan`, which consumes a Gate149 audit plus a baseline resident `run_command.txt` and emits candidate run/compare/acceptance command queues for agreement flag-only, soft downweight, strict downweight, and hotspot-frame exclusion experiments. | `src/glass/report/tile_local_rejection_registration_plan.py`, `src/glass/cli.py` | `tests/test_tile_local_rejection_registration_plan.py`, `tests/test_cli_smoke.py`, real-data Gate150 plan artifact | Project-defined command planning over GLASS JSON artifacts and existing GLASS CLI commands | The plan consumes GLASS audit artifacts and user-provided command/reference paths only. It does not execute integration, read image pixels, use external implementation source, or alter resident integration defaults. | Planning only. The exclude-hotspot candidate is a diagnostic control, not a production policy; all candidates require measured run, compare, acceptance, DQ, frame-accounting, and runtime validation before any promotion. |
| Soft agreement-downweight measured candidate | clean-room measured GLASS candidate execution | S2-Gate 151 retry artifacts under `C:\glass_runs\phase2_s2_gate_151_repair` | Executes the S2-Gate 150 `agreement_soft_downweight` candidate, lowering `--resident-triangle-min-agreement-score` to `0.6` while keeping the 200-light resident CUDA benchmark contract, DQ maps, frame accounting, compare scale/offset, and WBPP black-box reference fixed. | Existing `glass run`, `glass compare`, and `glass acceptance-audit` commands | Full test suite at S2-Gate 151 repair, real-data candidate run/compare/acceptance artifacts | Project-defined measured candidate over GLASS resident CUDA pipeline | The gate uses GLASS commands and user-generated WBPP black-box outputs only. It does not use external implementation source or alter defaults. | Candidate measurement only. The first run was preserved as a failed anomalous runtime artifact; retry passed acceptance with `26.005688100121915 s` runtime and `42.011616681462776x` speedup, but the candidate still requires broader comparison before promotion. |
| Measured candidate comparison | clean-room decision artifact over GLASS benchmark outputs | S2-Gate 152 comparison artifacts under `C:\glass_runs\phase2_s2_gate_152_candidate_comparison` | Adds `glass candidate-comparison`, which consumes baseline/candidate run timing, frame accounting, compare JSON, candidate-vs-baseline drift JSON, and acceptance-audit JSON to decide whether a measured candidate is eligible for broader sweeps. | `src/glass/report/candidate_comparison.py`, `src/glass/cli.py` | `tests/test_candidate_comparison.py`, `tests/test_cli_smoke.py`, real-data Gate152 comparison artifact | Project-defined promotion-control thresholds over GLASS artifacts and user-generated reference outputs | The command reads GLASS-owned JSON/FITS comparison outputs and user-generated black-box reference compare artifacts only. It does not use external implementation source, execute integration, or change defaults. | Decision artifact only. The Gate151 soft downweight retry passed required checks and kept reference RMS within 0.0615% of baseline with p99 1.04% lower, but was 1.45x slower than the historical GLASS baseline, so it remains eligible for runtime sweeps rather than default promotion. |
| Candidate comparison sweep summary | clean-room decision artifact over measured candidate comparisons | S2-Gate 153 sweep artifacts under `C:\glass_runs\phase2_s2_gate_153_candidate_sweep` | Adds `glass candidate-comparison-sweep`, which ranks multiple `candidate-comparison` artifacts, preserves failed attempts, records failed required checks, and reports the top candidate plus next recommendation. | `src/glass/report/candidate_comparison_sweep.py`, `src/glass/cli.py` | `tests/test_candidate_comparison_sweep.py`, `tests/test_cli_smoke.py`, real-data Gate153 sweep artifact | Project-defined ranking over GLASS comparison artifacts | The sweep consumes GLASS-owned comparison JSON only. It does not read image pixels, use external implementation source, execute integration, run image comparison, or alter defaults. | Decision artifact only. The Gate153 real sweep ranks the Gate151 retry above the failed first attempt and recommends a runtime-focused follow-up sweep before any default promotion. |
| Candidate runtime sweep plan | clean-room benchmark orchestration plan over GLASS candidate artifacts | S2-Gate 154 plan under `C:\glass_runs\phase2_s2_gate_154_runtime_sweep_plan` | Adds `glass candidate-runtime-sweep-plan`, which derives runtime-only variants from the accepted candidate `run_command.txt` and emits run, compare, acceptance, candidate-comparison, and final sweep-summary commands. | `src/glass/report/candidate_runtime_sweep_plan.py`, `src/glass/cli.py` | `tests/test_candidate_runtime_sweep_plan.py`, `tests/test_cli_smoke.py`, real-data Gate154 plan artifact | Project-defined orchestration variants over GLASS runtime knobs | The planner consumes GLASS command/comparison artifacts and user-provided benchmark paths only. It does not read image pixels, use external implementation source, execute integration, or alter defaults. | Planning only. The Gate154 variants keep science settings inherited from the accepted soft-downweight candidate and vary only resident prefetch/calibration orchestration knobs; execution remains a future gate. |
| Candidate runtime sweep execution | clean-room benchmark execution over GLASS runtime variants | S2-Gate 155 measured sweep under `C:\glass_runs\phase2_s2_gate_154_runtime_sweep_plan` | Executes the Gate154 runtime-only variants and ranks them with `glass candidate-comparison-sweep`, preserving per-variant run timing, compare, acceptance, and candidate-comparison artifacts. | Existing `glass run`, `glass compare`, `glass acceptance-audit`, `glass candidate-comparison`, `glass candidate-comparison-sweep` commands | Full test suite at S2-Gate 155, real-data 200-light runtime sweep artifacts | Project-defined benchmark over GLASS runtime orchestration knobs | The execution uses GLASS commands and user-generated reference outputs only. It does not read external implementation source, modify input data, or change resident defaults. | Benchmark evidence only. `prefetch12_workers6` ranked first at `19.55820359988138 s` and `55.8610096484846x`, but remains a measured candidate until a separate promotion gate updates defaults. |
| Prefetch/worker runtime matrix planning | clean-room benchmark orchestration plan over GLASS runtime variants | S2-Gate 156 matrix plan under `C:\glass_runs\phase2_s2_gate_156_prefetch_matrix` | Extends `glass candidate-runtime-sweep-plan` with generated prefetch-frame/prefetch-worker matrix variants and emits full run/compare/acceptance/comparison/sweep command chains for each matrix cell. | `src/glass/report/candidate_runtime_sweep_plan.py`, `src/glass/cli.py` | `tests/test_candidate_runtime_sweep_plan.py`, `tests/test_cli_smoke.py`, real-data Gate156 matrix plan artifact | Project-defined orchestration matrix over GLASS runtime knobs | The planner consumes GLASS command/comparison artifacts and user-provided benchmark paths only. It does not read image pixels, use external implementation source, execute integration, or alter defaults. | Planning/preflight only in Gate156. Real execution was deferred because an unrelated ASTERIS training process occupied most GPU memory; the generated plan remains the next executable artifact when GPU memory is available. |
| Resumable runtime sweep executor | clean-room benchmark orchestration executor over GLASS runtime variants | S2-Gate 157 dry-run execution artifact under `C:\glass_runs\phase2_s2_gate_157_runtime_executor` | Adds `glass candidate-runtime-sweep-execute`, which consumes a runtime sweep plan and records/resumes run, compare, acceptance, candidate-comparison, and final sweep-summary steps with dry-run, skip-existing, variant selection, start/stop, executable substitution, and working-directory controls. | `src/glass/report/candidate_runtime_sweep_execute.py`, `src/glass/cli.py`, `src/glass/capabilities.py` | `tests/test_candidate_runtime_sweep_execute.py`, `tests/test_cli_smoke.py`, `tests/test_capabilities.py`, `tests/test_cuda_skip_policy.py`, full test suite at Gate157 | Project-defined command orchestration and environment diagnostics | The executor consumes GLASS command-plan artifacts and launches GLASS commands only when not in dry-run mode. It does not read image pixels, use external implementation source, alter scientific parameters, or change resident defaults. | Gate157 validated the executor by dry-run because the measured matrix belongs to the next gate. It also hardens local Windows/GPU test diagnostics by adding CUDA probe skipping, GPU-busy CUDA test skipping, astroalign smoke-test skipping, and benchmark subprocess timeouts. |
| Prefetch/worker runtime matrix execution | clean-room measured benchmark over GLASS runtime variants | S2-Gate 158 measured matrix under `C:\glass_runs\phase2_s2_gate_156_prefetch_matrix` and execution audit under `C:\glass_runs\phase2_s2_gate_158_prefetch_matrix_execution` | Executes the S2-Gate 156 prefetch-frame/prefetch-worker matrix with `glass candidate-runtime-sweep-execute`, preserving per-variant run, reference compare, baseline compare, acceptance, candidate-comparison, and final sweep-summary artifacts. | Existing `glass run`, `glass compare`, `glass acceptance-audit`, `glass candidate-comparison`, `glass candidate-comparison-sweep`, `src/glass/report/candidate_runtime_sweep_execute.py` | Full test suite at S2-Gate 158, real-data 200-light matrix artifacts | Project-defined benchmark over GLASS runtime orchestration knobs | The execution uses GLASS commands and user-generated reference outputs only. It does not read external implementation source, modify input data, change scientific parameters, or alter resident defaults. | All 9 variants passed. `prefetch12_workers7` ranked first at `17.101234800000043 s`, `63.88667325940681x` versus the black-box reference, and `0.9549992718308159` runtime ratio versus the historical GLASS baseline. A separate promotion gate is required before defaults change. |
| Throughput runtime preset | clean-room runtime preset derived from measured GLASS benchmark artifacts | S2-Gate 159 explicit preset based on S2-Gate 158 `prefetch12_workers7` | Adds opt-in `--resident-runtime-preset throughput-v1` to `glass run` and `glass audit`, applying Gate158's prefetch/H2D/calibration scheduling values unless the user explicitly provides an individual runtime option. | `src/glass/cli.py` | `tests/test_cli_smoke.py`, full test suite at S2-Gate 159 | Project-defined preset over GLASS runtime knobs | The preset only changes resident orchestration values: prefetch frames/workers, queued refill, pinned-ring H2D, and calibration batch/stream/wave/release settings. It does not change calibration formulas, registration model, rejection, weighting, output maps, image pixels by itself, or defaults. | Default remains `manual`. The preset is benchmark-derived from one 200-light M38 H-alpha case and should remain opt-in until broader hardware/data confirmation supports changing defaults. |
| Throughput preset confirmation and command-token contract groups | clean-room benchmark contract hardening over GLASS artifacts | S2-Gate 160 preset confirmation under `C:\glass_runs\phase2_s2_gate_160_preset_confirmation` | Adds `required_command_token_groups` to benchmark contracts and updates the M38 200-light contract so equivalent command evidence can pass, including either explicit pinned-ring H2D flags or `--resident-runtime-preset throughput-v1`. Runs a 200-light confirmation using the preset command path. | `src/glass/report/benchmark_contract.py`, `benchmarks/phase2_m38_h_200_contract.json` | `tests/test_acceptance_audit.py`, full test suite at S2-Gate 160, real-data Gate160 preset confirmation artifacts | Project-defined benchmark contract logic | The contract consumes GLASS run commands and GLASS artifacts only. It does not change image processing, calibration formulas, registration, weighting, rejection, or output pixels. | The preset confirmation passed acceptance and preserved reference agreement/frame accounting, but ran at `23.330858499999977 s` (`46.82815250883293x` versus reference), slower than the Gate158 winner due to higher read/decode wait. The preset remains opt-in and is not promoted to default. |
| Resident runtime variance compare | clean-room timing audit over GLASS resident artifacts | S2-Gate 161 comparison under `C:\glass_runs\phase2_s2_gate_161_runtime_compare` | Adds `glass resident-runtime-compare`, which reads `run_timing.json` and resident CUDA artifacts from existing runs, ranks elapsed times, records effective resident I/O settings, and compares stage timing ratios for read/decode, H2D+calibration, registration/warp, integration, output, and GC. | `src/glass/report/resident_runtime_compare.py`, `src/glass/cli.py` | `tests/test_resident_runtime_compare.py`, `tests/test_cli_smoke.py`, real-data Gate161 timing artifact | Project-defined artifact comparison over GLASS outputs | The comparison consumes GLASS-owned timing and resident artifact JSON only. It does not read image pixels, use external implementation source, modify input data, rerun image processing, or alter defaults. | Diagnostic only. Gate161 confirmed Gate158 and Gate160 used the same effective throughput settings; the slower Gate160 run is dominated by read/decode variance, so the next benchmark should repeat in a warm-cache or dedicated I/O window before any default promotion. |
| Resident runtime repeat plan | clean-room benchmark command planning over GLASS run artifacts | S2-Gate 162 plan under `C:\glass_runs\phase2_s2_gate_162_repeat_plan` | Adds `glass resident-runtime-repeat-plan`, which reads a GLASS `run_command.txt`, preserves its science/runtime settings, replaces only per-repeat `--out` directories, and emits a final `resident-runtime-compare` command for repeated timing analysis. | `src/glass/report/resident_runtime_repeat_plan.py`, `src/glass/cli.py` | `tests/test_resident_runtime_repeat_plan.py`, `tests/test_cli_smoke.py`, real-data Gate162 repeat-plan artifact | Project-defined command planning over GLASS-owned run commands | The planner consumes GLASS-owned command text only. It does not execute image processing, read image pixels, use external implementation source, alter calculations, modify input data, or change defaults. | Planning only. Gate162 generated a three-repeat dedicated-I/O-window plan for the Gate160 `throughput-v1` command while the local GPU was externally loaded; measured execution remains a future gate. |
| Resident runtime repeat executor | clean-room benchmark orchestration over GLASS repeat plans | S2-Gate 163 dry-run audit under `C:\glass_runs\phase2_s2_gate_163_repeat_executor` | Adds `glass resident-runtime-repeat-execute`, which executes or dry-runs a `resident_runtime_repeat_plan`, records per-repeat command status, supports skip-existing based on `run_timing.json`, and optionally records the final `resident-runtime-compare` step. | `src/glass/report/resident_runtime_repeat_execute.py`, `src/glass/cli.py` | `tests/test_resident_runtime_repeat_execute.py`, `tests/test_cli_smoke.py`, real-data Gate163 dry-run execution artifact | Project-defined command orchestration over GLASS-owned plans | The executor consumes GLASS repeat-plan JSON and launches recorded GLASS commands only outside dry-run mode. It does not alter image calculations, generate new science settings, read external implementation source, or modify input data. | Gate163 validated the real Gate162 plan in dry-run mode because the local GPU was externally loaded. Measured execution remains the next runtime benchmark gate. |
| Resident runtime repeat preflight | clean-room benchmark readiness audit over GLASS repeat plans | S2-Gate 164 preflight under `C:\glass_runs\phase2_s2_gate_164_repeat_preflight` | Adds `glass resident-runtime-repeat-preflight`, which checks a repeat plan's output directories, resume markers, compare output, CUDA/resident command intent, and point-in-time GPU memory/utilization before recommending heavy benchmark execution. | `src/glass/report/resident_runtime_repeat_preflight.py`, `src/glass/cli.py` | `tests/test_resident_runtime_repeat_preflight.py`, `tests/test_cli_smoke.py`, real-data Gate164 preflight artifact | Project-defined readiness policy over GLASS plans and local `nvidia-smi` telemetry | The preflight consumes GLASS repeat-plan JSON and local GPU telemetry only. It does not execute image processing, read image pixels, use external implementation source, alter algorithms, modify input data, or change defaults. | Gate164 found the three planned repeat outputs clean but the GPU externally busy, so it recommends `wait_for_controlled_window` before running the 200-light repeats. |
| Preflight-gated resident repeat executor | clean-room benchmark execution guard over GLASS repeat plans | S2-Gate 165 guarded dry-run under `C:\glass_runs\phase2_s2_gate_165_guarded_repeat_executor` | Extends `glass resident-runtime-repeat-execute` with `--require-preflight-ready`, blocking execution before any subprocess starts when repeat preflight does not recommend `execute_repeat_plan`, and optionally writing the embedded preflight to `--preflight-out`. | `src/glass/report/resident_runtime_repeat_execute.py`, `src/glass/cli.py` | `tests/test_resident_runtime_repeat_execute.py`, focused CLI tests, real-data Gate165 guarded execution artifact | Project-defined execution guard over GLASS preflight policy | The guard consumes GLASS repeat-plan JSON and local preflight telemetry. It does not alter science commands, image calculations, external references, input data, or defaults. | Gate165 guarded dry-run correctly produced `preflight_blocked` with zero recorded runs while the GPU was externally busy, preventing contaminated 200-light repeat timing. |
| StackEngine result DQ contract | clean-room in-memory invariant audit over StackEngine outputs | S2-Gate 166 synthetic result-contract artifact under `C:\glass_runs\phase2_s2_gate_166_stack_result_contract` | Adds `build_stack_engine_result_contract`, validates requested map presence, map shapes, finite master pixels, coverage/no-data agreement, rejection-map/DQ-flag agreement, DQ summary/provenance agreement, coverage-sum/metrics agreement, and request-shape/input-sample agreement, then embeds the contract in CPU StackEngine result provenance. | `src/glass/engine/stack_contract.py`, `src/glass/engine/stack_engine.py` | `tests/test_stack_engine_result_contract.py`, `tests/test_stack_engine.py`, synthetic Gate166 contract artifact | Project-defined StackEngine invariant contract | The contract consumes in-memory GLASS StackEngine results only. It does not use external implementation source, alter pixel formulas, read user image directories, or change CUDA behavior. | This strengthens DQ/mask provenance at the engine boundary. It currently validates CPU StackEngine results; resident CUDA result-contract parity remains future work. |
| StackEngine contract result-contract requirement | clean-room artifact audit over GLASS StackEngine outputs | S2-Gate 167 synthetic CPU audit under `C:\glass_runs\phase2_s2_gate_167_stack_contract_result_gate` | Extends `glass stack-engine-contract` so CPU StackEngine calibration and integration records must include embedded `result_contract.passed=true`, and reports `result_contract_passed` for each master and integration output. | `src/glass/report/stack_engine_contract.py` | `tests/test_stack_engine_contract.py`, synthetic Gate167 CPU audit/contract artifact | Project-defined artifact-level enforcement of the S2-Gate166 in-memory contract | The audit consumes GLASS-generated calibration/integration JSON only. It does not read external implementation source, alter pixel calculations, modify input data, or change resident CUDA behavior. | Gate167 makes the CPU StackEngine result contract mandatory in default StackEngine contract audits while leaving resident CUDA parity as future work. |
| Pipeline contract StackEngine result-contract exposure | clean-room pipeline invariant audit over GLASS artifacts | S2-Gate 168 synthetic CPU audit under `C:\glass_runs\phase2_s2_gate_168_pipeline_result_contract` | Extends `glass pipeline-contract` to expose per-output `stack_result_contract` status and add `integration_stack_result_contract`, requiring CPU StackEngine integration artifacts to carry embedded `result_contract.passed=true`. | `src/glass/report/pipeline_contract.py` | `tests/test_pipeline_contract.py`, synthetic Gate168 pixel-verifying pipeline-contract artifact | Project-defined pipeline-level enforcement of StackEngine result contracts | The audit consumes GLASS integration JSON and optional FITS output maps only. It does not read external implementation source, alter pixel calculations, modify input data, or change resident CUDA behavior. | CPU StackEngine outputs are now checked at both StackEngine-contract and pipeline-contract layers; resident CUDA output parity remains future work. |
| Resident CUDA result-contract audit | clean-room resident artifact audit over GLASS CUDA outputs | S2-Gate 169 JSON-only audit under `C:\glass_runs\phase2_s2_gate_169_resident_result_contract` | Adds `glass resident-result-contract`, validating resident identity, required output maps, DQ summary/provenance agreement, active-frame counts, coverage provenance, source terms, geometric warp coverage agreement, and optional tiled DQ/count-map pixel verification. | `src/glass/report/resident_result_contract.py`, `src/glass/cli.py` | `tests/test_resident_result_contract.py`, `tests/test_cli_smoke.py`, Gate160 real resident artifact audit | Project-defined resident CUDA artifact contract | The audit consumes GLASS resident integration JSON and optional GLASS output FITS maps only. It does not read external implementation source, alter CUDA kernels, rerun image processing, modify input data, or change defaults. | Gate169 passed on the Gate160 `throughput-v1` run in JSON-only mode with one H output, active frame count 193, and input frame count 200. Pixel verification is available for focused/smaller map checks and future controlled runs. |
| Pipeline contract resident result-contract exposure | clean-room pipeline invariant audit over GLASS resident artifacts | S2-Gate 170 Gate160 resident audit under `C:\glass_runs\phase2_s2_gate_170_pipeline_resident_contract` | Extends `glass pipeline-contract` to expose per-output `resident_result_contract` status and add `integration_resident_result_contract`, requiring resident CUDA integration outputs to satisfy the S2-Gate169 resident result-contract audit. | `src/glass/report/pipeline_contract.py`, `src/glass/report/resident_result_contract.py` | `tests/test_pipeline_contract.py`, Gate160 real resident pipeline-contract artifact | Project-defined pipeline-level enforcement of resident CUDA result contracts | The audit consumes GLASS integration JSON and existing output paths in JSON-only mode. It does not read external implementation source, alter CUDA kernels, rerun image processing, modify input data, or change defaults. | Gate170 passed on the Gate160 `throughput-v1` resident run with one required resident output, active frame count 193, and all nine resident output checks passing. |
| Acceptance audit pipeline-contract evidence | clean-room acceptance contract over GLASS artifacts | S2-Gate 171 Gate160 acceptance audit under `C:\glass_runs\phase2_s2_gate_171_acceptance_pipeline_contract` | Extends `glass acceptance-audit` with `--pipeline-contract-json`, requiring a supplied pipeline invariant contract to exist, identify as `pipeline_invariant_contract`, and pass before the overall acceptance audit can pass. The acceptance artifact records pipeline contract status, failed checks, integration evidence, and pixel-verification metadata. | `src/glass/report/acceptance_audit.py`, `src/glass/cli.py` | `tests/test_acceptance_audit.py`, Gate160 real acceptance artifact with Gate170 pipeline contract | Project-defined acceptance-level enforcement of GLASS pipeline invariant contracts | The audit consumes GLASS acceptance inputs, GLASS pipeline-contract JSON, and user-generated benchmark/reference artifacts only. It does not read external implementation source, alter image calculations, rerun processing, modify input data, or change defaults. | Gate171 passed with 92 acceptance checks, zero failed pipeline-contract checks, and the same measured `46.82815250883293x` speedup versus the black-box reference. |
| Benchmark contract pipeline-contract requirement | clean-room release/benchmark contract over GLASS artifacts | S2-Gate 172 Gate160 release-contract audit under `C:\glass_runs\phase2_s2_gate_172_release_contract_pipeline` | Extends benchmark contracts with a `pipeline_contract` requirement block and updates candidate runtime sweep plans to generate `glass pipeline-contract` before acceptance, so release-grade 200-light acceptance requires both benchmark checks and pipeline invariant checks. | `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py`, `src/glass/report/candidate_runtime_sweep_plan.py`, `src/glass/report/candidate_runtime_sweep_execute.py`, `benchmarks/phase2_m38_h_200_contract.json`, `benchmarks/phase2_m38_h_200_audit_maps_contract.json` | `tests/test_acceptance_audit.py`, `tests/test_candidate_runtime_sweep_plan.py`, `tests/test_candidate_runtime_sweep_execute.py`, Gate160 real release-contract artifact and dry-run runtime plan | Project-defined release-grade benchmark policy over GLASS acceptance and pipeline-contract artifacts | The gate consumes GLASS-owned benchmark contracts, GLASS pipeline-contract JSON, and user-generated benchmark/reference artifacts only. It does not read external implementation source, alter image calculations, rerun processing, modify input data, or change defaults. | Gate172 passed with 98 acceptance checks and all six benchmark pipeline-contract checks passing. New runtime sweep plans include a `pipeline_contract` step before `acceptance_audit`; the executor remains compatible with older plans that lack the step. |
| Release evidence report surface | clean-room report/audit presentation over GLASS artifacts | S2-Gate 173 Gate160 release evidence artifacts under `C:\glass_runs\phase2_s2_gate_173_release_evidence_report` | Adds `release_contract_evidence.pipeline_contract` to acceptance-audit JSON, emits a `Pipeline Contract Evidence` section in acceptance Markdown, and adds a `Release contract evidence` section to HTML reports so passing release-grade pipeline-contract checks are visible, not only failures. | `src/glass/report/acceptance_audit.py`, `src/glass/report/html_report.py` | `tests/test_acceptance_audit.py`, `tests/test_cli_smoke.py`, Gate160 real acceptance Markdown/JSON and HTML report artifacts | Project-defined release evidence presentation over GLASS acceptance and pipeline-contract checks | The gate reads GLASS acceptance inputs and GLASS pipeline-contract JSON only. It does not read external implementation source, alter image calculations, rerun processing, modify input data, or change defaults. | Gate173 passed with release evidence status `passed`, two direct pipeline checks, six benchmark pipeline checks, zero failed pipeline checks, and visible Markdown/HTML evidence rows. |
| StackEngine adoption evidence | clean-room report/audit presentation over GLASS artifacts | S2-Gate 174 Gate160 resident adoption audit under `C:\glass_runs\phase2_s2_gate_174_stack_engine_adoption` | Extends `glass stack-engine-contract` with an adoption summary that classifies audited master/integration surfaces by engine family, result-contract readiness, fallback state, and Phase 2 default gap reason, then surfaces the recommendation in Markdown and HTML reports. | `src/glass/report/stack_engine_contract.py`, `src/glass/report/html_report.py` | `tests/test_stack_engine_contract.py`, `tests/test_cli_smoke.py`, Gate160 real resident adoption JSON/Markdown/HTML artifacts | Project-defined artifact-level adoption audit over GLASS StackEngine and resident CUDA outputs | The gate reads GLASS calibration/integration/report contract artifacts only. It does not read external implementation source, alter image calculations, rerun processing, modify input data, or change defaults. | Gate174 passed the resident CUDA integration contract while explicitly reporting one `resident_cuda_surface` Phase 2 default gap and recommendation `resident_cuda_surfaces_remain`. |
| StackEngine default promotion guard | clean-room release/CI guard over GLASS contract artifacts | S2-Gate 175 CPU ready and Gate160 resident blocked artifacts under `C:\glass_runs\phase2_s2_gate_175_default_promotion_guard` | Adds a `default_promotion` block to StackEngine contract audits, `--require-default-ready` to `glass stack-engine-contract`, and `--require-stack-default-ready` to `glass guardrails`, so promotion to full StackEngine default is machine-gated on all-surface scope, present calibration/integration surfaces, passing checks, zero adoption gaps, and recommendation `stack_engine_default_ready`. | `src/glass/report/stack_engine_contract.py`, `src/glass/cli.py`, `src/glass/report/html_report.py` | `tests/test_stack_engine_contract.py`, `tests/test_cli_smoke.py`, synthetic CPU ready artifact, Gate160 resident blocked artifact | Project-defined promotion policy over GLASS-owned contract evidence | The guard consumes GLASS StackEngine/adoption contract fields and GLASS guardrail artifacts only. It does not read external implementation source, alter image calculations, rerun heavy processing, modify input data, or change defaults by itself. | Gate175 verified that a small CPU StackEngine run is promotion-ready while the Gate160 resident CUDA contract, although passing for resident integration, is blocked from default promotion with exit code `3` and explicit blocker rows. |
| Release contract StackEngine default-promotion requirement | clean-room release/benchmark contract over GLASS artifacts | S2-Gate 176 Gate160 release audit under `C:\glass_runs\phase2_s2_gate_176_release_stack_default_contract` | Extends benchmark contracts and acceptance audits so release-grade 200-light acceptance requires StackEngine default-promotion evidence, including a passing StackEngine contract, `default_promotion.ready=true`, all-surface scope, `stack_engine_default_ready` recommendation, zero default gaps, and zero blockers. Runtime sweep plans now emit StackEngine contract artifacts and pass them to acceptance. | `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py`, `src/glass/report/html_report.py`, `src/glass/report/candidate_runtime_sweep_plan.py`, `src/glass/report/candidate_runtime_sweep_execute.py`, `src/glass/cli.py`, `benchmarks/phase2_m38_h_200_contract.json`, `benchmarks/phase2_m38_h_200_audit_maps_contract.json` | `tests/test_acceptance_audit.py`, `tests/test_candidate_runtime_sweep_plan.py`, `tests/test_candidate_runtime_sweep_execute.py`, `tests/test_cli_smoke.py`, Gate160 real failed release audit and dry-run runtime plan artifacts | Project-defined release-grade promotion policy over GLASS contract evidence | The gate consumes GLASS benchmark contracts, StackEngine contract JSON, pipeline-contract JSON, GLASS run artifacts, and user-generated benchmark/reference metadata only. It does not read external implementation source, alter image calculations, rerun heavy processing, or modify input data. | Gate176 intentionally fails the current Gate160 resident release audit on StackEngine default-promotion checks while preserving pipeline-contract pass status and the measured `46.82815250883293x` speedup, making the remaining default gap a release blocker instead of a footnote. |
| Resident result-contract parity in StackEngine audit | clean-room artifact parity over GLASS resident CUDA contracts | S2-Gate 177 Gate160 resident parity audit under `C:\glass_runs\phase2_s2_gate_177_resident_stack_parity` | Extends StackEngine contract audits with optional resident-result-contract input so resident CUDA integration outputs can prove result-contract parity and become adoption-ready without weakening all-surface default-promotion blockers. Candidate runtime sweep plans now generate resident-result-contract artifacts before StackEngine contract audits. | `src/glass/report/stack_engine_contract.py`, `src/glass/report/html_report.py`, `src/glass/report/candidate_runtime_sweep_plan.py`, `src/glass/report/candidate_runtime_sweep_execute.py`, `src/glass/cli.py` | `tests/test_stack_engine_contract.py`, `tests/test_candidate_runtime_sweep_plan.py`, `tests/test_candidate_runtime_sweep_execute.py`, Gate160 real resident parity JSON/Markdown/HTML artifacts and dry-run runtime plan | Project-defined parity bridge over GLASS resident CUDA result-contract evidence | The gate consumes GLASS resident-result-contract JSON, GLASS StackEngine contract JSON, GLASS run artifacts, and user-generated benchmark/reference metadata only. It does not read external implementation source, alter image calculations, rerun heavy processing, or modify input data. | Gate177 reduced the Gate160 resident adoption gap count from `1` to `0` and removed the resident CUDA surface gap from release acceptance; release remains blocked only by scope/calibration all-surface promotion requirements. |
| Resident calibration-contract parity in StackEngine audit | clean-room artifact parity over GLASS resident CUDA calibration evidence | S2-Gate 178 Gate160 resident calibration parity audit under `C:\glass_runs\phase2_s2_gate_178_resident_calibration_parity` | Adds `glass resident-calibration-contract`, validating resident master-calibration evidence from `resident_artifacts.json`, then extends StackEngine contract audits so a passing resident calibration contract supplies the master-calibration surface required for all-surface default promotion. Candidate runtime sweep plans now emit resident calibration/result contracts before StackEngine audits. | `src/glass/report/resident_calibration_contract.py`, `src/glass/report/stack_engine_contract.py`, `src/glass/report/html_report.py`, `src/glass/report/candidate_runtime_sweep_plan.py`, `src/glass/report/candidate_runtime_sweep_execute.py`, `src/glass/cli.py` | `tests/test_resident_calibration_contract.py`, `tests/test_stack_engine_contract.py`, `tests/test_candidate_runtime_sweep_plan.py`, `tests/test_candidate_runtime_sweep_execute.py`, Gate160 real resident calibration/result/StackEngine/acceptance artifacts and dry-run runtime plan | Project-defined parity bridge over GLASS resident CUDA calibration artifacts | The gate consumes GLASS resident artifacts, resident calibration/result contract JSON, StackEngine contract JSON, and user-generated benchmark/reference metadata only. It does not read external implementation source, alter image calculations, rerun heavy processing, modify input data, or claim equivalence beyond measured evidence. | Gate178 passed release acceptance for the Gate160 resident CUDA run with `default_promotion.ready=true`, zero StackEngine blockers, one resident calibration surface, one resident integration surface, and the measured `46.82815250883293x` speedup versus the black-box reference. |
| Release promotion decision audit | clean-room release/default decision over GLASS artifacts | S2-Gate 179 decision artifact under `C:\glass_runs\phase2_s2_gate_179_release_promotion_decision` | Adds `glass release-promotion-decision`, separating release-candidate readiness from default-change readiness and requiring stable repeat/runtime evidence before default promotion even when acceptance and StackEngine default-promotion contracts pass. | `src/glass/report/release_promotion_decision.py`, `src/glass/cli.py` | `tests/test_release_promotion_decision.py`, `tests/test_cli_smoke.py`, Gate178/Gate161/Gate164 real decision artifact | Project-defined release/default promotion policy over GLASS contract and runtime evidence | The gate consumes GLASS acceptance, StackEngine, pipeline, runtime-compare, and repeat-preflight artifacts only. It does not read external implementation source, alter image calculations, rerun heavy processing, modify input data, or change defaults. | Gate179 marks the Gate178 result as `release_candidate_ready=true` but `default_change_ready=false`, with recommendation `wait_for_controlled_window` because existing runtime evidence still recommends a warm-cache/dedicated-I/O repeat and the preflight GPU sample is busy. |
| Controlled repeat promotion evidence | clean-room release/default decision over measured GLASS repeat artifacts | S2-Gate 180 repeat execution and decision artifacts under `C:\glass_runs\phase2_s2_gate_180_controlled_repeat` | Extends `glass release-promotion-decision` with explicit `--ignore-warmup-runs` and executes the three-run resident repeat plan, allowing default-change readiness to be evaluated on stable post-warm-up observations while preserving the cold first run in evidence. | `src/glass/report/release_promotion_decision.py`, `src/glass/cli.py` | `tests/test_release_promotion_decision.py`, Gate180 real repeat execution/runtime-compare/release-decision artifacts | Project-defined warm-up handling over GLASS repeat timing artifacts | The gate consumes GLASS repeat execution, runtime-compare, acceptance, StackEngine, and pipeline artifacts only. It does not read external implementation source, alter image calculations, modify input data, or silently remove timing data. | Gate180 completed 3 repeat runs; the first cold-cache run was explicitly ignored for stability, the remaining two runs had slowest/best ratio `1.0021999002314625`, and the final decision reports `default_change_ready=true` with recommendation `promote_default_candidate`. |
| Resident throughput preset default | clean-room runtime default promotion over measured GLASS artifacts | S2-Gate 181 no-preset resident CUDA run and decision artifacts under `C:\glass_runs\phase2_s2_gate_181_default_runtime` | Promotes `throughput-v1` to the default resident runtime preset for `glass run` and `glass audit`, while retaining explicit `manual` as the conservative fallback and preserving user-supplied option overrides. Acceptance token-group checks can now verify defaulted runtime scheduling from `resident_artifacts.json` effective `resident_io_pipeline` evidence. | `src/glass/cli.py`, `src/glass/report/benchmark_contract.py` | `tests/test_cli_smoke.py`, `tests/test_acceptance_audit.py`, Gate181 real no-preset run, compare, acceptance, runtime-compare, StackEngine, and release-decision artifacts | Project-defined default chosen from Gate180 controlled repeat evidence and Gate181 no-preset validation | The gate consumes GLASS command, timing, resident artifact, contract, compare, and user-generated benchmark/reference metadata only. It does not read external implementation source, alter image calculations, modify input data, or hide command overrides. | Gate181 no-preset run elapsed `18.80478299999959 s`, effective schedule matched throughput-v1, acceptance passed with `58.099101701945926x` speedup, and release decision reports `default_change_ready=true` without ignored warm-up runs. |
| Windows CUDA release matrix guard | clean-room release/package compatibility audit over GLASS artifacts | S2-Gate 182 matrix artifact under `C:\glass_runs\phase2_s2_gate_182_windows_release_matrix` | Adds `glass windows-release-matrix`, joining `glass doctor` package recommendation evidence with release-promotion and optional acceptance artifacts to decide whether the Windows CPU/CUDA package matrix is ready for publication with the promoted default resident runtime. | `src/glass/report/windows_release_matrix.py`, `src/glass/cli.py`, `src/glass/gpu/compatibility.py` | `tests/test_windows_release_matrix.py`, `tests/test_cuda_package_compatibility.py`, `tests/test_cli_smoke.py`, Gate182 real matrix artifact | Project-defined packaging compatibility policy over detected compute capability, planned CUDA package targets, and release-decision evidence | The gate consumes GLASS doctor, release-decision, acceptance, and package target metadata only. It does not build binaries, alter image calculations, read external implementation source, rerun heavy processing, or modify input data. | Gate182 passed on the local RTX PRO 6000 Blackwell machine with primary `cuda13`, try order `cuda13,cuda12,cuda11,cpu`, native-cubin `cuda13`, PTX forward-JIT `cuda12/cuda11`, CPU fallback, default `throughput-v1`, and zero failed checks. |
| Windows portable package smoke | clean-room package startup audit over built GLASS artifacts | S2-Gate 183 CPU portable package smoke under `C:\glass_runs\phase2_s2_gate_183_windows_package_smoke` | Adds `glass windows-package-smoke`, auditing a Windows portable folder and zip for required files, source stamp, non-empty zip, and optional command execution of `glass-doctor.cmd` plus `glass.cmd --help`. | `src/glass/report/windows_package_smoke.py`, `src/glass/cli.py`, `packaging/windows/build_portable.ps1` | `tests/test_windows_package_smoke.py`, `tests/test_cli_smoke.py`, real CPU portable build and smoke artifact | Project-defined release smoke policy over GLASS-built package structure and startup commands | The gate consumes a GLASS-built portable package and GLASS doctor output only. It does not alter image calculations, read external implementation source, modify input data, or prove CUDA package loading for GPU builds. | Gate183 built a CPU portable zip of `290938309` bytes, smoke-tested `glass-doctor.cmd` and `glass.cmd --help` with return code `0`, recorded source stamp `4b54e77`, and passed with zero failed checks. |
| CUDA13 portable package smoke | clean-room CUDA package startup audit over built GLASS artifacts | S2-Gate 184 CUDA13 portable package smoke under `C:\glass_runs\phase2_s2_gate_184_cuda13_package_smoke` | Extends the portable builder with package labels, package manifest metadata, isolated user-site launchers, Visual Studio environment import, and no-BOM manifest writes; extends `glass windows-package-smoke` with package-label and CUDA-required checks. | `packaging/windows/build_portable.ps1`, `src/glass/report/windows_package_smoke.py`, `src/glass/cli.py`, `src/glass/io/json_io.py` | `tests/test_windows_package_smoke.py`, `tests/test_windows_packaging_scripts.py`, real CUDA13 portable build and smoke artifact | Project-defined release smoke policy over GLASS-built package metadata, startup commands, and GLASS doctor CUDA evidence | The gate consumes a GLASS-built portable package and GLASS doctor output only. It does not alter image calculations, read external implementation source, modify input data, or prove CUDA11/CUDA12 package loading. | Gate184 built a CUDA13 portable zip of `339732356` bytes with static CUDA runtime intent, source stamp `245c2f9`, package label `cuda13`, native extension loaded, CUDA available on RTX PRO 6000 Blackwell cc 12.0, and zero failed checks. |
| Windows package build preflight | clean-room release build planning over local Toolkit availability | S2-Gate 185 package build plan under `C:\glass_runs\phase2_s2_gate_185_windows_package_build_plan` | Adds `glass windows-package-build-plan`, a non-mutating preflight that detects installed CUDA Toolkit roots, plans CPU/cuda11/cuda12/cuda13 portable package commands, records package labels, zip paths, CMake architecture strings, and missing-toolkit reasons. | `src/glass/report/windows_package_build_plan.py`, `src/glass/cli.py` | `tests/test_windows_package_build_plan.py`, real local package build-plan artifact | Project-defined release planning policy over GLASS package labels and local filesystem Toolkit detection | The gate reads local CUDA Toolkit directory metadata and emits GLASS build-plan artifacts only. It does not install toolkits, build packages, alter image calculations, read external implementation source, or modify input data. | Gate185 detected only CUDA Toolkit `v13.2` on the local machine, marked `cuda13` and `cpu` ready, marked `cuda12` and `cuda11` missing, and passed in non-strict mode with recommendation to build ready variants or install missing toolkits. |
| Minimal CUDA Toolkit installer plan | clean-room release environment preparation | S2-Gate 186 plan-only installer artifacts under `C:\glass_runs\phase2_s2_gate_186_cuda_toolkit_minimal_installer` | Adds `packaging/windows/install_cuda_toolkit_minimal.ps1`, a plan/download/install helper for CUDA12/CUDA11 that records NVIDIA installer URL/SHA256, installs only `nvcc` and `cudart` components when explicitly requested, and never requests display driver components. Build-plan artifacts now include download/install commands for missing CUDA variants. | `packaging/windows/install_cuda_toolkit_minimal.ps1`, `src/glass/report/windows_package_build_plan.py` | `tests/test_windows_packaging_scripts.py`, `tests/test_windows_package_build_plan.py`, plan-only cuda12/cuda11 artifacts | Project-defined environment-preparation guard using official NVIDIA installer metadata surfaced by winget and NVIDIA installation-guide component names | The gate does not install or download Toolkits by default. It emits GLASS plan artifacts and records that driver components are excluded. It does not alter image calculations, read external implementation source, or modify input data. | Gate186 produced plan-only cuda12/cuda11 artifacts with `driver_component_included=false`, `install_requested=false`, and refreshed build-plan Markdown containing minimal download/install commands for the missing variants. |
| CUDA12 minimal Toolkit install | clean-room release environment preparation | S2-Gate 187 CUDA12 install artifacts under `C:\glass_runs\phase2_s2_gate_187_cuda12_toolkit_install` | Uses the Gate186 helper and BITS download to install only CUDA12 `nvcc_12.4` and `cudart_12.4`, verifies SHA256, records install exit code, confirms nvcc/runtime/header files, and refreshes the Windows package build plan. | `packaging/windows/install_cuda_toolkit_minimal.ps1`, `src/glass/report/windows_package_build_plan.py` | Gate187 download/install/build-plan artifacts, full test suite after install | Project-defined environment-preparation guard using verified NVIDIA installer metadata | The gate installs CUDA Toolkit compiler/runtime components only and records `driver_component_included=false`; it does not request display driver components, alter image calculations, read external implementation source, or modify input data. | Gate187 installed CUDA12 Toolkit `v12.4`, kept NVIDIA driver `596.21`, and refreshed build-plan readiness to `cuda13,cuda12,cpu` with only `cuda11` still missing. |
| CUDA12 portable package smoke | clean-room CUDA package startup audit over built GLASS artifacts | S2-Gate 188 CUDA12 portable package smoke under `C:\glass_runs\phase2_s2_gate_188_cuda12_package_smoke` | Builds `GLASS-Portable-win64-cuda12.zip` with CUDA Toolkit 12.4, static CUDA runtime intent, and CMake architectures `75;80;86;89;90`, then smoke-tests the portable package with CUDA required and package label/source checks. | `packaging/windows/build_portable.ps1`, `src/glass/report/windows_package_smoke.py`, `src/glass/report/windows_package_build_plan.py` | Gate188 CUDA12 portable build and smoke artifact, full test suite | Project-defined release smoke policy over GLASS-built package metadata, startup commands, and GLASS doctor CUDA evidence | The gate consumes a GLASS-built portable package and GLASS doctor output only. It does not alter image calculations, read external implementation source, modify input data, or claim CUDA11 package readiness. | Gate188 built a CUDA12 portable zip of `341206870` bytes, recorded source stamp `fbf454a`, loaded the native extension on Blackwell via CUDA availability checks, and passed package smoke with zero failed checks. |
| CUDA11 minimal Toolkit install | clean-room release environment preparation | S2-Gate 189 CUDA11 install artifacts under `C:\glass_runs\phase2_s2_gate_189_cuda11_toolkit_install` | Uses the Gate186 helper and BITS download to install only CUDA11 `nvcc_11.8` and `cudart_11.8`, verifies SHA256, records install exit code, confirms nvcc/runtime/header files, and refreshes the Windows package build plan with strict missing-toolkit failure enabled. | `packaging/windows/install_cuda_toolkit_minimal.ps1`, `src/glass/report/windows_package_build_plan.py` | Gate189 download/install/build-plan artifacts, full test suite after install | Project-defined environment-preparation guard using verified NVIDIA installer metadata | The gate installs CUDA Toolkit compiler/runtime components only and records `driver_component_included=false`; it does not request display driver components, alter image calculations, read external implementation source, or modify input data. | Gate189 installed CUDA11 Toolkit `v11.8`, kept NVIDIA driver `596.21`, and refreshed build-plan readiness to all variants: `cuda13,cuda12,cuda11,cpu`. |
| CUDA11 portable package smoke | clean-room CUDA package startup audit over built GLASS artifacts | S2-Gate 190 CUDA11 portable package smoke under `C:\glass_runs\phase2_s2_gate_190_cuda11_package_smoke` | Builds `GLASS-Portable-win64-cuda11.zip` with CUDA Toolkit 11.8, static CUDA runtime intent, and CMake architectures `50;52;60;61;70;75;80;86`, then smoke-tests the portable package with CUDA required and package label/source checks. | `packaging/windows/build_portable.ps1`, `src/glass/report/windows_package_smoke.py`, `src/glass/report/windows_package_build_plan.py` | Gate190 CUDA11 portable build and smoke artifact, full test suite | Project-defined release smoke policy over GLASS-built package metadata, startup commands, and GLASS doctor CUDA evidence | The gate consumes a GLASS-built portable package and GLASS doctor output only. It does not alter image calculations, read external implementation source, modify input data, or claim native Blackwell cubin coverage for CUDA11. | Gate190 built a CUDA11 portable zip of `342183616` bytes, recorded source stamp `260c832`, loaded the native extension on Blackwell via CUDA availability checks, and passed package smoke with zero failed checks. |
| Labeled CPU portable package smoke | clean-room package startup audit over built GLASS artifacts | S2-Gate 191 CPU portable package smoke under `C:\glass_runs\phase2_s2_gate_191_cpu_package_smoke` | Builds `GLASS-Portable-win64-cpu.zip` with package label `cpu` and smoke-tests the portable package with source/label checks but without requiring CUDA. | `packaging/windows/build_portable.ps1`, `src/glass/report/windows_package_smoke.py` | Gate191 CPU portable build and smoke artifact, full test suite | Project-defined release smoke policy over GLASS-built package metadata and startup commands | The gate consumes a GLASS-built portable package and GLASS doctor output only. It does not alter image calculations, read external implementation source, modify input data, or require CUDA. | Gate191 built a labeled CPU portable zip of `296215418` bytes, recorded source stamp `a1604b0`, and passed package smoke with zero failed checks. |
| Windows package suite readiness | clean-room release-suite audit over GLASS package smoke artifacts | S2-Gate 192 suite artifact under `C:\glass_runs\phase2_s2_gate_192_windows_package_suite` | Adds `glass windows-package-suite`, which aggregates the cuda13, cuda12, cuda11, and cpu package-smoke artifacts, validates smoke pass status, package-label agreement, and non-empty zip sizes, and emits JSON/Markdown publish-readiness evidence. | `src/glass/report/windows_package_suite.py`, `src/glass/cli.py` | `tests/test_windows_package_suite.py`, package-suite artifact, full test suite | Project-defined release readiness policy over GLASS-owned package smoke artifacts | The gate consumes only GLASS package-smoke artifacts. It does not rebuild, sign, upload, alter image calculations, read external implementation source, or modify input data. | Gate192 reports `package_suite_ready` with zero failed checks for `cuda13,cuda12,cuda11,cpu`; the historical package set has mixed source stamps, so formal release packages should be rebuilt from one final tag or checked with `--require-same-source-stamp`. |
| Windows release manifest | clean-room release-file manifest over GLASS package suite and zip artifacts | S2-Gate 193 manifest artifact under `C:\glass_runs\phase2_s2_gate_193_windows_release_manifest` | Adds `glass windows-release-manifest`, which consumes the package-suite artifact, reads each release zip, records size and SHA256, and validates that zip sizes still match the package-smoke evidence. | `src/glass/report/windows_release_manifest.py`, `src/glass/cli.py` | `tests/test_windows_release_manifest.py`, release-manifest artifact, full test suite | Project-defined release-file audit over GLASS-owned package artifacts | The gate consumes only GLASS suite metadata and local GLASS zip files. It does not rebuild, sign, upload, alter image calculations, read external implementation source, or modify input data. | Gate193 reports `release_manifest_ready` and records SHA256 digests for `cuda13,cuda12,cuda11,cpu`; historical package source stamps remain mixed until a formal rebuild from one final tag. |
| Strict same-source Windows packages | clean-room release rebuild and artifact audit over GLASS package variants | S2-Gate 194 strict package artifacts under `C:\glass_runs\phase2_s2_gate_194_strict_windows_packages` | Rebuilds `cuda13`, `cuda12`, `cuda11`, and `cpu` Windows portable zips from source stamp `aa63510`, smoke-tests every variant, then generates strict same-source package-suite and release-manifest artifacts. | `packaging/windows/build_portable.ps1`, `src/glass/report/windows_package_smoke.py`, `src/glass/report/windows_package_suite.py`, `src/glass/report/windows_release_manifest.py` | Gate194 build-plan, package-smoke, strict-suite, strict-manifest artifacts, full test suite | Project-defined release publication evidence over GLASS-owned package artifacts | The gate builds GLASS packages and consumes their GLASS smoke artifacts only. It does not sign, upload, alter image calculations, read external implementation source, or modify input data. | Gate194 reports source stamp `aa63510` for all four packages, strict suite `package_suite_ready`, strict manifest `release_manifest_ready`, and SHA256 digests for every rebuilt zip. |
| GitHub release handoff plan | clean-room release publication planning over GLASS manifest artifacts | S2-Gate 195 release-plan artifacts under `C:\glass_runs\phase2_s2_gate_195_github_release_plan` | Adds `glass windows-github-release-plan`, which converts a strict Windows release manifest into GitHub release notes, a package asset table, and an explicit `gh release create` upload command. | `src/glass/report/windows_github_release_plan.py`, `src/glass/cli.py` | `tests/test_windows_github_release_plan.py`, Gate195 release-plan artifacts, full test suite | Project-defined publication handoff over GLASS-owned release manifest metadata | The gate consumes only GLASS manifest metadata and local GLASS zip paths. It does not create tags, push commits, upload assets, alter image calculations, read external implementation source, or modify input data. | Gate195 reports `release_plan_ready` for tag `v0.1.0-windows-gpu.9`, source stamp `aa63510`, and `publication_ready=false` because GitHub CLI is missing on the checkpoint machine. |
| GitHub CLI auth preflight | clean-room release publication preflight over GLASS manifest artifacts and GitHub CLI status | S2-Gate 196 auth-preflight artifacts under `C:\glass_runs\phase2_s2_gate_196_github_cli_auth_preflight` | Extends `glass windows-github-release-plan` with explicit `--gh-path` and `--check-gh-auth` so publication readiness requires both valid package assets and authenticated GitHub CLI when requested. | `src/glass/report/windows_github_release_plan.py`, `src/glass/cli.py` | `tests/test_windows_github_release_plan.py`, Gate196 auth-preflight artifacts, full test suite | Project-defined release tooling preflight over GLASS-owned release metadata plus local `gh` command status | The gate consumes GLASS manifest metadata, local GLASS zip paths, and `gh --version`/`gh auth status` output only. It does not create tags, push commits, upload assets, alter image calculations, read external implementation source, or modify input data. | Gate196 installed GitHub CLI 2.94.0, detected `gh.available=true`, detected `gh.auth_ok=false`, and kept the release plan passed but not publication-ready until authentication succeeds. |
| Frame accounting acceptance contract | clean-room derivation / black-box experiment | S2-Gate 42 benchmark contracts over `C:\glass_runs\phase2_s2_gate_32_200\resident_artifact_paths_20260531` | Adds benchmark-contract checks requiring `frame_accounting.json`, exact 200-light/193-integrated/7-zero-weight counts for the M38 H-alpha 200-light run, and consistency with integration weights, speedup summary, DQ active-frame provenance, and resident registration status counts. | `src/glass/report/benchmark_contract.py`, `src/glass/report/acceptance_audit.py`, `benchmarks/phase2_m38_h_200_contract.json`, `benchmarks/phase2_m38_h_200_audit_maps_contract.json` | `tests/test_acceptance_audit.py` | Project-defined contract over measured Phase 1/2 benchmark artifacts | Uses only GLASS-owned artifacts and user-generated benchmark/reference outputs. No external implementation source is used. | This is an audit gate. It catches frame-count drift but does not decide whether a specific rejected frame is scientifically optimal. |
| Registration model ladder | clean-room derivation / compatible open source bridge | General geometric transforms; optional MIT `astroalign` bridge described in `docs/registration_model.md` | Translation, similarity, affine, and homography fitting with greedy one-to-one inlier scoring, descriptor-seeded hypotheses, and explicit validation of matrix shape, determinant, inliers, RMS, and solution source. | `src/glass/cpu/registration.py`, `src/glass/engine/registration.py` | `tests/test_cpu_registration.py`, `tests/test_pipeline_fixture.py` | Project-defined | Uses standard least-squares, SVD, DLT homography, and robust scoring concepts; optional astroalign remains an open-source bridge, not copied into GLASS. | Homography is a first-pass global model; local distortion surfaces remain future work. |
| Warp interpolators | clean-room derivation | Standard interpolation kernels | Tile-mode warp interpolator registry with nearest, bilinear, bicubic Catmull-Rom, and Lanczos 3 entries, plus DQ/coverage maps for invalid resampling footprints. | `src/glass/engine/warp.py`, `src/glass/cli.py` | `tests/test_cpu_warp.py`, `tests/test_pipeline_fixture.py` | Project-defined | Kernels implemented from public mathematical definitions. | Bicubic/Lanczos tile-mode paths are CPU correctness paths; CUDA resident warp already has bilinear/Lanczos variants. Drizzle added later. |
| Local normalization | clean-room derivation | General background matching and bilinear interpolation | Per-tile masked source/reference mean and standard deviation estimates are repaired for empty cells, interpolated into continuous coefficient fields, and applied as `O(x,y)=a(x,y)S(x,y)+b(x,y)`. Coefficient grids, optional full-resolution scale/offset fields, residual maps, DQ summaries, and crop-box metadata are emitted for audit. | `src/glass/cpu/local_norm.py`, `src/glass/engine/local_norm.py` | `tests/test_cpu_local_norm.py`, `tests/test_pipeline_fixture.py`, `tests/test_gpu_local_norm_vs_cpu.py` | Project-defined tile size and full-field diagnostic threshold | The continuous field, empty-cell repair, and diagnostic artifact contract are implemented from project-owned numerical definitions and do not rely on proprietary source. | Current fit uses mean/std per tile and bilinear smoothing; robust nebulosity/background masks and a fully resident CUDA field-apply path remain future work. |
| Variance-aware integration | clean-room derivation | Inverse-variance weighting and weighted population variance | Tile-mode integration can use per-frame inverse variance weights derived from `noise_sigma` or `background_rms`, normalizes positive weights by their median, and writes a variance map from the shared StackEngine/FITS integration output contract. | `src/glass/engine/integration.py`, `src/glass/engine/stack_engine.py`, `src/glass/models.py`, `src/glass/cli.py` | `tests/test_cpu_integration.py`, `tests/test_stack_engine.py`, `tests/test_pipeline_fixture.py` | Project-defined first-pass variance proxy and map defaults | Implemented from public statistical definitions: inverse variance weighting and weighted population variance. No proprietary source code is used. | Uses frame-level variance proxies from quality metrics; camera gain/read-noise model and per-pixel variance propagation remain future work. |
| StackEngine rejection sample accounting | clean-room derivation | S2-Gate 231 StackEngine result-contract semantics | Validates rejection count maps as rejected-sample counts while preserving DQ maps as per-pixel touched-by-rejection flags. Result contracts now require finite, non-negative, near-integer low/high rejection maps, rounded sample sums matching metrics, and positive-pixel counts matching DQ provenance. | `src/glass/engine/stack_contract.py`, `src/glass/engine/stack_engine.py` | `tests/test_stack_engine_result_contract.py`, `tests/test_stack_engine.py` | Project-defined artifact semantics | The contract is derived from GLASS-owned output-map and DQ bitfield semantics and does not rely on proprietary source or external implementation behavior. | This gate hardens CPU/StackEngine contract evidence; resident CUDA parity remains enforced by resident result contracts and should carry the same sample-vs-pixel distinction. |
| StackEngine invalid sample accounting contract | clean-room derivation | S2-Gate 242 StackEngine DQ/rejection closure semantics | Adds explicit `input_valid_samples_before_rejection` and `input_invalid_samples_before_rejection` provenance plus contract checks proving input valid/invalid samples close over total input samples and final valid plus rejected samples closes over initially valid samples. | `src/glass/engine/stack_engine.py`, `src/glass/engine/stack_contract.py` | `tests/test_stack_engine.py`, `tests/test_stack_engine_result_contract.py` | Project-defined artifact semantics | The contract is derived from GLASS-owned DQ bitfield and StackEngine rejection semantics only; it does not read image pixels outside tests, use external implementation behavior, or rely on proprietary source. | This gate improves DQ/rejection auditability only. Image math, CUDA kernels, runtime defaults, release packages, and benchmark outputs are unchanged. |
| Resident source-DQ invalid sample parity | clean-room derivation | S2-Gate 447 CPU StackEngine vs resident CUDA source-DQ parity validation | Converts explicit source invalid masks to NaN in resident GPU memory before integration, preserving CPU StackEngine's `finite and DQ==0` valid-sample semantics for finite bad/hot/no-data pixels. Resident provenance keeps source-DQ counts separate from post-warp coverage closure. | `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py`, `src/glass/engine/resident_source_dq.py`, `src/glass/engine/resident_cuda.py`, `src/glass/engine/dq.py`, `src/glass/engine/resident_dq_pixel_closure.py` | `tests/test_resident_source_dq.py`, `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py`, synthetic artifact `runs/checkpoints/s2_gate_447_perf/synthetic_source_dq_cpu_vs_resident.json` | Project-defined DQMask and StackEngine validity semantics | The behavior is derived from GLASS-owned DQ bitfield and StackEngine contracts. It does not use external implementation source, proprietary behavior, or user data beyond GLASS-generated validation artifacts. | Current resident run path extracts nonfinite source samples from float arrays and supports explicit DQ bitmasks through the resident source-DQ helper; raw compact FITS payloads have no sidecar DQ extraction at this layer and are recorded as unsupported-with-zero-invalid unless future input-DQ plumbing supplies masks. |
| Resident input-DQ sidecar plumbing | clean-room derivation | S2-Gate 448 resident plan/file DQ sidecar regression | Lets resident CUDA runs consume FITS DQ sidecar paths declared on light-frame records, converts bitfield sidecars into source invalid masks, unions them with source-array nonfinite samples, and applies the union before integration so finite bad/hot/no-data pixels are excluded from master, weight, and coverage outputs. | `src/glass/engine/resident_source_dq.py`, `src/glass/engine/resident_cuda.py` | `tests/test_resident_source_dq.py`, `tests/test_resident_cuda_run.py` | Project-defined DQ bitfield and StackEngine valid-sample semantics | Validation consumes GLASS-generated FITS fixtures, plan JSON, resident CUDA artifacts, and source-DQ summaries only. It does not inspect external implementation source, use proprietary behavior, modify user image directories, change CUDA kernels, create package releases, or upload assets. | Runtime DQ/mask input plumbing. It does not change calibration math, registration fitting, rejection math, or resident integration kernels; it makes existing Gate447 resident invalid-mask semantics reachable from file-level DQ sidecars. |
| Resident calibration-artifact DQ cache routing | clean-room derivation | S2-Gate 449 resident calibration/cosmetic DQ cache routing | Indexes GLASS `calibration_artifacts.json` `calibrated_lights[].dq_mask_path` entries by frame id before resident execution and uses them as source-DQ sidecars when the light-frame plan has no explicit sidecar, preserving sidecar source and artifact-path provenance in resident `source_dq_summary`. | `src/glass/engine/resident_cuda.py`, `src/glass/engine/resident_source_dq.py` | `tests/test_resident_cuda_run.py`, `tests/test_resident_source_dq.py` | Project-defined GLASS calibration artifact schema and DQ bitfield semantics | Validation consumes only GLASS-generated FITS fixtures, GLASS calibration artifact JSON, resident CUDA outputs, and source-DQ provenance. It does not inspect external implementation source, use proprietary behavior, modify user image directories, change CUDA kernels, create release assets, or upload artifacts. | Runtime DQ/mask cache plumbing. It does not change calibration math, registration fitting, rejection math, or resident integration kernels; it makes existing CPU/tile calibration and cosmetic DQ cache outputs reachable by the resident default path. |
| Two-phase cosmetic DQ cache resident regression | clean-room derivation | S2-Gate 450 CPU/tile cosmetic calibration to resident CUDA DQ cache workflow | Validates the real GLASS workflow where CPU/tile calibration with cosmetic correction generates DQ sidecars, then resident CUDA consumes those pre-existing calibration artifacts on the same run directory before rewriting resident artifacts. Also fixes relative `dq_mask_path` handling when GLASS calibration artifacts store paths relative to the current working directory. | `src/glass/engine/resident_cuda.py`, `tests/test_resident_cuda_run.py` | `tests/test_resident_cuda_run.py`, synthetic artifact `runs/checkpoints/s2_gate_450_perf/two_phase_cpu_cosmetic_calibration_cache_to_resident_cuda.json` | Project-defined GLASS two-phase workflow over calibration artifacts and DQ bitfields | Validation uses only GLASS-generated FITS fixtures, GLASS CPU/tile calibration outputs, and GLASS resident CUDA outputs. It does not inspect external implementation source, use proprietary behavior, modify user image directories, change CUDA kernels, create release assets, or upload artifacts. | Workflow regression and path handling fix. It does not change calibration math, registration fitting, rejection math, or resident integration kernels; it proves existing cosmetic DQ cache products can drive resident valid-sample semantics. |
| Resident source-DQ cache runtime route | clean-room derivation | S2-Gate 451 first-class `glass run --resident-source-dq-cache generate-calibration` workflow | Adds an explicit guarded resident runtime route that first runs GLASS CPU/tile calibration to generate `calibration_artifacts.json` and DQ sidecars, writes `resident_source_dq_cache_route.json`, then lets resident CUDA consume those generated sidecars before integration. Timing records DQ-cache generation separately from resident CUDA integration. | `src/glass/cli.py`, `tests/test_resident_cuda_run.py` | `tests/test_resident_cuda_run.py`, synthetic artifact `runs/checkpoints/s2_gate_451_perf/resident_source_dq_cache_route_two_phase.json` | Project-defined GLASS runtime route over calibration artifacts and DQ bitfields | Validation uses only GLASS-generated FITS fixtures, GLASS CPU/tile calibration outputs, and GLASS resident CUDA outputs. It does not inspect external implementation source, use proprietary behavior, modify user image directories, alter CUDA kernels, create release assets, or upload artifacts. | Runtime orchestration and auditability route. The route is default-off until a dedicated 200-light benchmark proves its overhead and benefit; image math, registration fitting, rejection math, and resident kernels are unchanged. |
| DQ provenance sample closure handoff | clean-room derivation | S2-Gate 243 DQ provenance summaries and resident result-contract bridge | Carries StackEngine valid/invalid/rejected sample closure into DQ provenance summaries, adds resident coverage sample sums, and lets resident result contracts enforce optional supplied sample-closure evidence while preserving compatibility with older artifacts that lack it. | `src/glass/engine/dq.py`, `src/glass/engine/stack_engine.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/resident_result_contract.py` | `tests/test_dq_provenance.py`, `tests/test_resident_result_contract.py`, `tests/test_stack_engine.py` | Project-defined DQ/provenance semantics over GLASS artifacts | The handoff consumes GLASS-owned DQ provenance and resident output metadata only; it does not use external implementation behavior or proprietary source. | This gate changes DQ/report-contract visibility only. Image math, CUDA kernels, runtime defaults, package builds, uploads, and benchmark outputs are unchanged. |
| Pipeline sample closure report surface | clean-room derivation | S2-Gate 244 pipeline-contract and HTML report surface over DQ provenance closure | Surfaces `sample_accounting_closure` in pipeline-contract integration rows, adds `integration_sample_accounting_closure`, expands pipeline-contract Markdown, and exposes valid/invalid/rejected sample closure in the main HTML report. | `src/glass/report/pipeline_contract.py`, `src/glass/report/html_report.py` | `tests/test_pipeline_contract.py`, `tests/test_resident_result_contract.py`, `tests/test_dq_provenance.py` | Project-defined report/contract policy over GLASS artifacts | The surface consumes GLASS-owned DQ provenance and pipeline-contract JSON only; it does not read external implementation behavior or proprietary source. | This gate changes report and contract visibility only. Image math, CUDA kernels, runtime defaults, package builds, uploads, and benchmark outputs are unchanged. |
| Acceptance/status sample closure handoff | clean-room derivation | S2-Gate 245 acceptance and Phase 2 status handoff over pipeline-contract sample closure | Carries `integration_sample_accounting_closure` and per-output `sample_accounting_closure` rows into acceptance-audit release evidence, Phase 2 status summaries, Markdown, and status-compare regression checks. | `src/glass/report/acceptance_audit.py`, `src/glass/report/phase2_status.py` | `tests/test_acceptance_audit.py`, `tests/test_phase2_status.py` | Project-defined acceptance/status policy over GLASS artifacts | The handoff consumes GLASS-owned pipeline contract and status JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes acceptance/status visibility only. Image math, CUDA kernels, runtime defaults, package builds, uploads, and benchmark outputs are unchanged. |
| Promotion sample closure blocker | clean-room derivation | S2-Gate 246 release/default-promotion readiness over Phase 2 sample-closure evidence | Adds `pipeline_sample_accounting_closure_passed` blockers to release-promotion decisions and default-promotion manifests, carrying failed `sample_accounting_closure` rows into readiness evidence and Markdown summaries. | `src/glass/report/release_promotion_decision.py`, `src/glass/report/default_promotion_manifest.py` | `tests/test_release_promotion_decision.py`, `tests/test_default_promotion_manifest.py` | Project-defined promotion-control policy over GLASS artifacts | The blocker consumes GLASS-owned acceptance, pipeline-contract, release-decision, and Phase 2 status JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes promotion readiness only. Image math, CUDA kernels, runtime defaults, Windows release matrices, package builds, uploads, and benchmark outputs are unchanged. |
| Windows release matrix sample closure handoff | clean-room derivation | S2-Gate 247 Windows release-matrix readiness over default-promotion sample closure | Carries default-promotion `sample_accounting_closure` evidence into `glass windows-release-matrix`, adds `default_promotion_sample_accounting_closure_passed`, and reports closure status/counts in Markdown. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined release-matrix policy over GLASS artifacts | The handoff consumes GLASS-owned default-promotion and release matrix JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes Windows release-matrix readiness only. Image math, CUDA kernels, runtime defaults, package builds, uploads, publish preflight, and benchmark outputs are unchanged. |
| GitHub release plan sample closure handoff | clean-room derivation | S2-Gate 248 publication handoff over Phase 2 and release-matrix sample closure | Carries Phase 2 and Windows release-matrix `sample_accounting_closure` evidence into `glass windows-github-release-plan`, release notes, Markdown, generated PowerShell dry-run validation, and publication-plan checks. | `src/glass/report/windows_github_release_plan.py` | `tests/test_windows_github_release_plan.py` | Project-defined release-handoff policy over GLASS artifacts | The handoff consumes GLASS-owned Phase 2 status, release matrix, and release manifest JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes publication-plan readiness only. Image math, CUDA kernels, runtime defaults, package builds, uploads, publish preflight, and benchmark outputs are unchanged. |
| Windows publish preflight sample closure handoff | clean-room derivation | S2-Gate 249 final publish-preflight handoff over release-plan, release-matrix, and default-promotion sample closure | Carries sample-closure evidence into `glass windows-publish-preflight`, adding hard checks for GitHub plan Phase 2 evidence, GitHub plan release-matrix evidence, direct release-matrix evidence, direct default-promotion evidence, and plan/matrix sample-closure agreement. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publish-preflight policy over GLASS artifacts | The handoff consumes GLASS-owned release-plan, release-matrix, release-manifest, and default-promotion JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes final publish-preflight readiness only. Image math, CUDA kernels, runtime defaults, package builds, uploads, package releases, and benchmark outputs are unchanged. |
| Phase 2 publish-preflight sample closure status | clean-room derivation | S2-Gate 250 Phase 2 status/compare over publish-preflight sample closure | Carries publish-preflight sample-closure evidence into `glass phase2-status`, adds `windows_publish_preflight_sample_accounting_closure_passed`, and extends `glass phase2-status-compare` to catch candidates that lose a previously passing publish-preflight sample-closure chain. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS artifacts | The handoff consumes GLASS-owned publish-preflight and Phase 2 status JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes Phase 2 status/compare readiness only. Image math, CUDA kernels, runtime defaults, package builds, uploads, package releases, and benchmark outputs are unchanged. |
| Phase 2 StackEngine default contract handoff | clean-room derivation | S2-Gate 251 Phase 2 status/compare over StackEngine default-contract readiness | Carries StackEngine default-contract audit evidence into `glass phase2-status`, adds `stack_engine_default_contract_ready`, and extends `glass phase2-status-compare` to catch candidates that lose a previously ready StackEngine default contract or increase the recorded default-gap count. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS artifacts | The handoff consumes GLASS-owned StackEngine contract and Phase 2 status JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes Phase 2 status/compare readiness only. Image math, CUDA kernels, StackEngine audit behavior, runtime defaults, package builds, uploads, package releases, and benchmark outputs are unchanged. |
| Default promotion StackEngine contract handoff | clean-room derivation | S2-Gate 252 default-promotion readiness over Phase 2 StackEngine default-contract evidence | Carries Phase 2 `stack_engine_contract` evidence into `glass default-promotion-manifest`, adds `phase2_stack_engine_default_contract_ready`, and exposes StackEngine default-contract readiness, gap count, and blockers in manifest JSON and Markdown. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS artifacts | The handoff consumes GLASS-owned Phase 2 status and StackEngine contract summaries only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes default-promotion readiness/reporting only. Image math, CUDA kernels, StackEngine audit behavior, runtime defaults, release matrix, package builds, uploads, package releases, and benchmark outputs are unchanged. |
| Windows release matrix StackEngine contract handoff | clean-room derivation | S2-Gate 253 Windows release-matrix readiness over default-promotion StackEngine default-contract evidence | Carries default-promotion `stack_engine_contract` evidence into `glass windows-release-matrix`, adds `default_promotion_stack_engine_contract_ready`, and exposes StackEngine readiness, Phase2 check state, gap count, and blockers in matrix JSON and Markdown. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release-matrix policy over GLASS artifacts | The handoff consumes GLASS-owned default-promotion manifests and StackEngine contract summaries only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes Windows release-matrix readiness/reporting only. Image math, CUDA kernels, StackEngine audit behavior, runtime defaults, package builds, uploads, publish-preflight, and benchmark outputs are unchanged. |
| GitHub release plan StackEngine contract handoff | clean-room derivation | S2-Gate 254 GitHub release-plan readiness over Phase 2 and Windows release-matrix StackEngine default-contract evidence | Carries StackEngine default-contract evidence into `glass windows-github-release-plan`, adds Phase2 and release-matrix StackEngine readiness checks plus a Phase2/matrix agreement check, and exposes the evidence in release notes, Markdown, and the generated PowerShell dry-run script. | `src/glass/report/windows_github_release_plan.py` | `tests/test_windows_github_release_plan.py` | Project-defined GitHub release-handoff policy over GLASS artifacts | The handoff consumes GLASS-owned Phase 2 status, Windows release matrix, and release manifest JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes GitHub release-plan readiness/reporting only. Image math, CUDA kernels, StackEngine audit behavior, runtime defaults, package builds, uploads, package releases, publish-preflight, and benchmark outputs are unchanged. |
| Windows publish preflight StackEngine contract handoff | clean-room derivation | S2-Gate 255 final publish-preflight readiness over GitHub release-plan, Windows release-matrix, and default-promotion StackEngine default-contract evidence | Carries StackEngine default-contract evidence into `glass windows-publish-preflight`, adding hard checks for GitHub plan Phase2 evidence, GitHub plan release-matrix evidence, direct release-matrix evidence, direct default-promotion evidence, and cross-artifact agreement. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined final publish-preflight policy over GLASS artifacts | The handoff consumes GLASS-owned GitHub release-plan, Windows release-matrix, default-promotion, and release-manifest JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes final publish-preflight readiness/reporting only. Image math, CUDA kernels, StackEngine audit behavior, runtime defaults, package builds, uploads, package releases, GitHub release creation, and benchmark outputs are unchanged. |
| Phase 2 publish-preflight StackEngine status handoff | clean-room derivation | S2-Gate 256 Phase 2 status/compare over final publish-preflight StackEngine default-contract evidence | Carries final publish-preflight StackEngine default-contract evidence into `glass phase2-status`, adds `windows_publish_preflight_stack_engine_default_contract_ready`, and extends `glass phase2-status-compare` to catch candidates that lose a previously passing final publish-preflight StackEngine chain. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined Phase 2 status policy over GLASS artifacts | The handoff consumes GLASS-owned publish-preflight and Phase 2 status JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes Phase 2 status/compare readiness/reporting only. Image math, CUDA kernels, StackEngine audit behavior, runtime defaults, package builds, uploads, package releases, GitHub release creation, and benchmark outputs are unchanged. |
| StackEngine publication evidence chain audit | clean-room derivation | S2-Gate 257 audit over StackEngine default-contract publication evidence chain | Adds `glass stack-engine-publication-audit`, validating source StackEngine contract, Phase 2 direct handoff, default-promotion manifest, Windows release matrix, GitHub release plan, publish preflight, and Phase 2 publish-preflight handoff all report ready StackEngine evidence, zero gaps/blockers, and adjacent-artifact agreement. | `src/glass/report/stack_engine_publication_audit.py`, `src/glass/cli.py` | `tests/test_stack_engine_publication_audit.py`, `tests/test_cli_smoke.py` | Project-defined release-chain audit over GLASS artifacts | The audit consumes GLASS-owned JSON artifacts only; it does not read image pixels, external implementation behavior, proprietary source, packages, or GitHub release state. | This gate adds an audit artifact only. Image math, CUDA kernels, StackEngine runtime behavior, runtime defaults, package builds, uploads, GitHub release creation, and benchmark outputs are unchanged. |
| Resident rejection sample accounting parity | clean-room derivation | S2-Gate 232 resident CUDA result-contract semantics | Extends resident result contracts and tiled pixel verification so low/high rejection FITS maps are finite non-negative near-integer rejected-sample count maps whose rounded total matches resident JSON rejected-sample provenance, while DQ low/high flags continue to count touched pixels. | `src/glass/report/resident_result_contract.py`, `src/glass/report/dq_map_verify.py` | `tests/test_resident_result_contract.py`, `tests/test_dq_map_verify.py` | Project-defined artifact semantics | The contract consumes GLASS-owned resident artifacts and DQ/count-map bitfield semantics only; it does not use external implementation behavior or proprietary source. | This gate verifies artifact parity but does not change CUDA rejection kernels or rerun the 200-light benchmark. |
| Pipeline rejection sample pixel handoff | clean-room derivation | S2-Gate 233 pipeline guardrail semantics | Extends pipeline pixel verification so low/high rejection FITS maps are audited as rejected-sample count maps, their rounded total is compared against resident provenance or StackEngine metrics, and guardrails expose `rejection_sample_accounting` beside DQ touched-pixel checks. | `src/glass/report/pipeline_contract.py`, `src/glass/report/dq_map_verify.py` | `tests/test_pipeline_contract.py`, `tests/test_resident_result_contract.py`, `tests/test_dq_map_verify.py` | Project-defined artifact semantics | The handoff consumes GLASS-owned pipeline, resident, StackEngine, DQ, and count-map artifacts only; it does not use external implementation behavior or proprietary source. | This gate improves guardrail reporting and failure detection; it does not change integration math, CUDA kernels, runtime defaults, or benchmark outputs. |
| Pipeline rejection sample report surface | clean-room derivation | S2-Gate 234 HTML report presentation over pipeline contract artifacts | Surfaces pipeline `rejection_sample_accounting` rows in HTML reports, including sample totals from low/high rejection maps, provenance source counts, failed source deltas, and the DQ touched-pixel versus rejected-sample-count semantics. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py`, `tests/test_pipeline_contract.py` | Project-defined report presentation over GLASS artifacts | The report consumes GLASS-owned pipeline contract JSON only; it does not use external implementation behavior or proprietary source. | This gate changes report visibility only; JSON artifacts remain authoritative and image math/runtime behavior is unchanged. |
| Acceptance rejection sample triage | clean-room derivation | S2-Gate 235 acceptance release-evidence summary over pipeline contract artifacts | Adds compact rejection sample accounting triage to acceptance audit JSON and Markdown, including `integration_rejection_sample_counts_match_maps` status, pixel-verification state, row counts, failed output items, low/high rejection-map sample totals, provenance source counts, and failed source deltas. | `src/glass/report/acceptance_audit.py` | `tests/test_acceptance_audit.py`, `tests/test_pipeline_contract.py` | Project-defined acceptance summary over GLASS artifacts | The triage consumes GLASS-owned pipeline contract JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes acceptance/report visibility only; image math, CUDA kernels, runtime defaults, and benchmark outputs are unchanged. |
| Phase 2 rejection sample status handoff | clean-room derivation | S2-Gate 236 Phase 2 status and compare summaries over pipeline contract artifacts | Adds rejection sample accounting summary and regression checks to `glass phase2-status` and `glass phase2-status-compare`, preserving the `integration_rejection_sample_counts_match_maps` check, row counts, failed output items, map sample totals, provenance source counts, and failed deltas across release-readiness status artifacts. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py`, `tests/test_pipeline_contract.py` | Project-defined status summary over GLASS artifacts | The handoff consumes GLASS-owned pipeline contract and Phase 2 status JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes status/compare visibility only; image math, CUDA kernels, runtime defaults, release packages, and benchmark outputs are unchanged. |
| Promotion rejection sample blocker | clean-room derivation | S2-Gate 237 release/default promotion control over pipeline contract artifacts | Adds `pipeline_rejection_sample_accounting_passed` blockers to release promotion and default-promotion manifests so default-route promotion cannot proceed when rejected-sample count maps drift from provenance or when the `integration_rejection_sample_counts_match_maps` evidence fails. | `src/glass/report/release_promotion_decision.py`, `src/glass/report/default_promotion_manifest.py` | `tests/test_release_promotion_decision.py`, `tests/test_default_promotion_manifest.py`, `tests/test_phase2_status.py` | Project-defined promotion-control policy over GLASS artifacts | The blocker consumes GLASS-owned acceptance, pipeline contract, and Phase 2 status JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes promotion readiness only; image math, CUDA kernels, runtime defaults, release packages, and benchmark outputs are unchanged. |
| Windows release matrix rejection sample handoff | clean-room derivation | S2-Gate 238 Windows release-matrix readiness over default-promotion artifacts | Carries default-promotion rejection sample accounting into `glass windows-release-matrix` and adds `default_promotion_rejection_sample_accounting_passed` so Windows release matrices cannot pass when rejected-sample count maps drift from provenance. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py`, `tests/test_default_promotion_manifest.py` | Project-defined release-matrix policy over GLASS artifacts | The handoff consumes GLASS-owned default-promotion and release matrix JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes release-matrix readiness only; image math, CUDA kernels, runtime defaults, release packages, uploads, and benchmark outputs are unchanged. |
| GitHub release plan rejection sample handoff | clean-room derivation | S2-Gate 239 publication handoff over Phase 2 and release-matrix artifacts | Carries Phase 2 and Windows release-matrix rejection sample accounting into `glass windows-github-release-plan`, release notes, Markdown, and generated PowerShell dry-run validation so publication plans fail when rejected-sample accounting evidence is missing or failed. | `src/glass/report/windows_github_release_plan.py` | `tests/test_windows_github_release_plan.py`, `tests/test_phase2_status.py`, `tests/test_windows_release_matrix.py` | Project-defined release-handoff policy over GLASS artifacts | The handoff consumes GLASS-owned Phase 2 status, release matrix, and release manifest JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes publication-plan readiness only; image math, CUDA kernels, runtime defaults, package builds, uploads, and benchmark outputs are unchanged. |
| Windows publish preflight rejection sample handoff | clean-room derivation | S2-Gate 240 final publication preflight over release-plan, release-matrix, and default-promotion artifacts | Carries rejected-sample accounting into `glass windows-publish-preflight`, adds hard checks for Phase 2 plan evidence, matrix plan evidence, direct matrix evidence, direct default-promotion evidence, and cross-artifact matrix accounting agreement. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py`, `tests/test_windows_github_release_plan.py` | Project-defined publication-preflight policy over GLASS artifacts | The handoff consumes GLASS-owned release-plan, release-matrix, release-manifest, and default-promotion JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes publish-preflight readiness only; image math, CUDA kernels, runtime defaults, package builds, uploads, and benchmark outputs are unchanged. |
| Phase 2 publish-preflight rejection status | clean-room derivation | S2-Gate 241 Phase 2 status/compare over publish-preflight artifacts | Carries publish-preflight rejected-sample accounting into `glass phase2-status`, adds `windows_publish_preflight_rejection_sample_accounting_passed`, and extends `glass phase2-status-compare` to catch candidates that lose a previously passing publish-preflight accounting chain. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py`, `tests/test_windows_publish_preflight.py` | Project-defined status/compare policy over GLASS artifacts | The handoff consumes GLASS-owned publish-preflight and Phase 2 status JSON only; it does not read image pixels, external implementation behavior, or proprietary source. | This gate changes status/compare readiness only; image math, CUDA kernels, runtime defaults, package builds, uploads, and benchmark outputs are unchanged. |
| Winsorized sigma stack rejection semantics | clean-room derivation | S2-Gate 258 CPU StackEngine rejection baseline hardening | Makes `winsorized_sigma` a distinct CPU StackEngine baseline by deriving first-pass winsorization bounds from median/IQR sigma estimates, falling back to standard deviation only for zero robust scale, then using winsorized mean/std for rejection. Records the rejection scale estimator and policy provenance in StackEngine metrics/DQ provenance. | `src/glass/engine/stack_engine.py` | `tests/test_stack_engine.py`, `tests/test_stack_engine_result_contract.py` | Project-defined robust rejection semantics over GLASS in-memory StackEngine tiles | The gate uses synthetic in-memory arrays only. It does not read image pixels from user data, use external implementation behavior, inspect proprietary source, alter CUDA kernels, or rerun benchmarks. | CPU StackEngine rejection math changes for `winsorized_sigma`; CUDA/resident parity and real-data benchmark confirmation remain future work. |
| CPU integration rejection baseline unification | clean-room derivation | S2-Gate 259 shared CPU rejection statistics | Adds `glass.engine.rejection` and routes both CPU StackEngine and legacy `glass.cpu.integration.weighted_integrate_stack` through the same center/scale estimators, including the S2-Gate 258 median/IQR-guided `winsorized_sigma` baseline. | `src/glass/engine/rejection.py`, `src/glass/engine/stack_engine.py`, `src/glass/cpu/integration.py` | `tests/test_cpu_integration.py`, `tests/test_stack_engine.py`, `tests/test_stack_engine_result_contract.py` | Project-defined CPU baseline semantics over GLASS in-memory arrays | The gate uses synthetic in-memory arrays only. It does not read user image data, use external implementation behavior, inspect proprietary source, alter CUDA kernels, change resident runtime behavior, or rerun benchmarks. | This unifies CPU baseline behavior for future CUDA parity tests. Resident CUDA still reports its current two-stage mean/std winsorized approximation and remains a future parity target. |
| Resident winsorized rejection semantics disclosure | clean-room derivation | S2-Gate 260 resident CUDA artifact/contract guard over winsorized semantics | Adds shared descriptor constants for CPU winsorized sigma and resident CUDA's current mean/std two-stage approximation; writes descriptors into resident artifacts and integration outputs; requires resident result contracts to disclose `cpu_baseline_parity=false` and pending CUDA parity for `winsorized_sigma`. | `src/glass/engine/rejection.py`, `src/glass/engine/resident_cuda.py`, `src/glass/report/resident_result_contract.py` | `tests/test_resident_result_contract.py`, `tests/test_pipeline_contract.py` | Project-defined artifact semantics over GLASS resident CUDA outputs | The gate uses synthetic JSON/FITS contract fixtures only. It does not read user image data, use external implementation behavior, inspect proprietary source, alter CUDA kernels, change resident runtime behavior, or rerun benchmarks. | This prevents resident CUDA `winsorized_sigma` from being mistaken for hardened CPU baseline parity. Actual CUDA kernel parity with the median/IQR baseline remains future work. |
| Resident hardened winsorized CUDA parity prototype | clean-room derivation | S2-Gate 261 optional native CUDA method for hardened winsorized rejection | Adds `ResidentCalibratedStack.integrate_hardened_winsorized_sigma`, a prototype resident CUDA kernel that sorts up to 256 valid samples per pixel, computes median/q25/q75 with linear percentile interpolation, applies the S2-Gate 258/259 median-IQR winsorized baseline, and accumulates weighted mean plus rejection maps. | `cpp/cuda/integration_kernels.cu`, `cpp/src/native_bindings.cpp`, `src/glass_cuda.py` | `tests/test_cuda_resident_stack.py`, `tests/test_cpu_integration.py`, `tests/test_stack_engine.py` | Project-defined CUDA parity prototype over GLASS synthetic arrays | The gate uses synthetic in-memory arrays and the local GLASS CUDA extension only. It does not read user image data, use external implementation behavior, inspect proprietary source, alter resident runtime defaults, publish packages, or rerun benchmarks. | The prototype is correctness-oriented and limited to 256 resident frames. Existing fast resident `integrate_sigma_clip(..., winsorize=true)`, tile-local integration, and matrix-warped integration remain on the prior mean/std approximation until future parity gates migrate them. |
| Opt-in hardened winsorized resident runtime | clean-room derivation | S2-Gate 262 runtime wiring for the Gate261 hardened CUDA method | Adds `--resident-winsorized-mode hardened_cpu_parity`, routes stack-dispatch resident winsorized integration through the Gate261 median/IQR CUDA prototype, records hardened parity semantics in resident artifacts and integration results, and keeps `fast_approx` as the default throughput path. | `src/glass/engine/rejection.py`, `src/glass/report/resident_result_contract.py`, `src/glass/engine/resident_cuda.py`, `src/glass/cli.py` | `tests/test_resident_cuda_run.py`, `tests/test_resident_result_contract.py`, `tests/test_cuda_resident_stack.py` | Project-defined explicit opt-in mode; default remains `fast_approx` | The gate only connects GLASS-owned formulas, native kernels, JSON contracts, and synthetic FITS tests. It does not read external implementation source, alter input data, rerun the real benchmark, publish packages, or claim optimized hardened performance. | Hardened mode is stack-dispatch only, inherits the 256-frame prototype limit, and rejects unsupported fused-matrix or tile-local policy combinations until dedicated parity kernels exist. |
| Hardened winsorized resident guardrail | clean-room runtime contract | S2-Gate 263 guardrail over the Gate261/262 hardened resident prototype | Adds a shared hardened frame-limit constant and resident winsorized runtime contract, validates hardened mode before allocating resident VRAM, and records mode, implementation, frame count, frame limit, stack-dispatch requirement, and guardrail status in resident artifacts and integration outputs. | `src/glass/engine/rejection.py`, `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined capacity contract matching the Gate261 native prototype | The guardrail is derived from GLASS's own prototype capacity and artifact schema. It does not read external implementation source, change image math, alter input data, rerun real data, publish packages, or promote hardened mode by default. | The guardrail prevents opaque over-limit failures but does not optimize the hardened kernel, raise the native 256-frame limit, or add fused-matrix/tile-local hardened parity. |
| Hardened winsorized timing surface | clean-room timing instrumentation | S2-Gate 264 timing wrapper around the Gate261 hardened resident method | Adds a timed Python wrapper around `ResidentCalibratedStack.integrate_hardened_winsorized_sigma` and records timing model, method name, frame count, image shape, pixel count, sigma thresholds, and wall time in resident dispatch artifacts and integration output rows. | `src/glass_cuda.py`, `src/glass/engine/resident_cuda.py` | `tests/test_cuda_resident_stack.py`, `tests/test_resident_cuda_run.py` | Project-defined timing metadata for future benchmark diagnostics | The gate records GLASS runtime timing around an existing GLASS-owned CUDA method. It does not change image math, read external implementation source, alter input data, rerun real data, build packages, or promote hardened mode by default. | Timing is wall-clock around the wrapper call and includes device synchronization/download performed by the native method. It is for audit and benchmark attribution, not a CUDA event profiler replacement. |
| Resident winsorized microbenchmark artifact | clean-room synthetic benchmark | S2-Gate 265 deterministic synthetic resident winsorized benchmark | Adds `glass resident-winsorized-benchmark`, generating an in-memory synthetic stack with gradients, frame variation, weights, and outliers, then comparing resident fast approximation, resident hardened CUDA, and the CPU winsorized baseline while emitting JSON/Markdown timing and difference evidence. | `src/glass/report/resident_winsorized_benchmark.py`, `src/glass/cli.py` | `tests/test_resident_winsorized_benchmark.py`, `tests/test_cli_smoke.py` | Project-defined synthetic data and pass/fail tolerances | The benchmark uses GLASS-generated arrays and GLASS CPU/CUDA code only. It does not read user image directories, external implementation source, proprietary behavior, package artifacts, or real-data reference outputs. | Microbenchmark only. It does not replace the 200-light benchmark, does not prove end-to-end runtime, and does not optimize or promote the hardened path by default. |
| Resident winsorized microbenchmark contract audit | clean-room synthetic benchmark contract | S2-Gate 266 contract over the Gate265 resident winsorized benchmark artifact | Adds a default machine-readable contract and `glass resident-winsorized-benchmark-audit`, validating benchmark pass status, CUDA availability, deterministic synthetic configuration, hardened-vs-CPU difference tolerances, required timing metadata, and fast-approximation context. | `benchmarks/resident_winsorized_microbenchmark_contract.json`, `src/glass/report/resident_winsorized_benchmark_contract.py`, `src/glass/cli.py` | `tests/test_resident_winsorized_benchmark_contract.py`, `tests/test_cli_smoke.py` | Project-defined contract thresholds matching the Gate265 synthetic benchmark tolerances | The audit consumes GLASS-generated benchmark JSON and a GLASS-owned contract only. It does not read user image directories, external implementation source, proprietary behavior, packages, or real-data reference outputs. | Contract audit only. It catches microbenchmark drift but does not replace the 200-light benchmark, optimize kernels, change runtime defaults, or validate end-to-end throughput. |
| Phase 2 resident winsorized audit status handoff | clean-room status handoff | S2-Gate 267 Phase 2 status handoff over the Gate266 audit artifact | Adds optional `glass phase2-status --resident-winsorized-benchmark-audit`, summarizes the Gate266 audit artifact, and fails Phase 2 status when a supplied hardened winsorized microbenchmark audit did not pass. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined status handoff over GLASS-owned audit artifacts | The handoff consumes GLASS status/checkpoint JSON and the GLASS-owned resident winsorized benchmark audit only. It does not read user image directories, external implementation source, proprietary behavior, packages, or real-data reference outputs. | Status handoff only. It does not change CUDA kernels, image math, runtime defaults, release artifacts, status-compare behavior, or benchmark outputs. |
| Resident winsorized frame-count sweep | clean-room synthetic benchmark | S2-Gate 268 deterministic frame-count sweep over Gate265 resident winsorized benchmark | Adds `glass resident-winsorized-sweep`, reusing the Gate265 benchmark over multiple frame counts including a required 200-frame row by default, and reports per-row CPU/fast CUDA/hardened CUDA timings plus hardened-vs-CPU differences. | `src/glass/report/resident_winsorized_sweep.py`, `src/glass/cli.py` | `tests/test_resident_winsorized_sweep.py`, `tests/test_cli_smoke.py` | Project-defined synthetic frame counts, required 200-frame row, `5e-5` sweep RMS tolerance, and `2e-4` max-absolute tolerance | The sweep uses GLASS-generated arrays and GLASS CPU/CUDA code only. It does not read user image directories, external implementation source, proprietary behavior, package artifacts, or real-data reference outputs. | Synthetic scale probe only. It validates 200-frame sample-count parity on a small in-memory image but does not replace the 200-light real-data benchmark or prove end-to-end I/O, registration, LN, or output-write performance. |
| Resident winsorized sweep contract audit | clean-room synthetic benchmark contract | S2-Gate 269 contract over the Gate268 resident winsorized frame-count sweep artifact | Adds a default machine-readable contract and `glass resident-winsorized-sweep-audit`, validating sweep pass status, deterministic frame-count configuration, required 200-frame row, hardened-vs-CPU difference tolerances, timing metadata, and hardened native method/mode. | `benchmarks/resident_winsorized_sweep_contract.json`, `src/glass/report/resident_winsorized_sweep_contract.py`, `src/glass/cli.py` | `tests/test_resident_winsorized_sweep_contract.py`, `tests/test_cli_smoke.py` | Project-defined contract thresholds matching the Gate268 sweep-scale tolerances | The audit consumes GLASS-generated sweep JSON and a GLASS-owned contract only. It does not read user image directories, external implementation source, proprietary behavior, packages, or real-data reference outputs. | Contract audit only. It catches synthetic 200-frame sweep drift but does not replace the 200-light benchmark, optimize kernels, change runtime defaults, update release artifacts, or validate end-to-end throughput. |
| Phase 2 resident winsorized sweep audit status handoff | clean-room status handoff | S2-Gate 270 Phase 2 status handoff over the Gate269 sweep audit artifact | Adds optional `glass phase2-status --resident-winsorized-sweep-audit`, summarizes the Gate269 sweep audit artifact, and fails Phase 2 status when a supplied required-200-frame winsorized sweep audit did not pass. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined status handoff over GLASS-owned audit artifacts | The handoff consumes GLASS status/checkpoint JSON and the GLASS-owned resident winsorized sweep audit only. It does not read user image directories, external implementation source, proprietary behavior, packages, or real-data reference outputs. | Status handoff only. It does not change CUDA kernels, image math, runtime defaults, release artifacts, status-compare behavior, or benchmark outputs. |
| Phase 2 resident winsorized sweep status-compare guard | clean-room status regression guard | S2-Gate 271 Phase 2 status-compare guard over Gate270 status summaries | Extends `glass phase2-status-compare` so a candidate cannot lose a previously passing resident winsorized sweep audit, lose the required 200-frame row pass, or reduce the sweep-audit check count while comparing Phase 2 status artifacts. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined regression policy over GLASS-owned status artifacts | The guard consumes GLASS Phase 2 status JSON only. It does not read user image directories, external implementation source, proprietary behavior, packages, or real-data reference outputs. | Compare guard only. It does not change CUDA kernels, image math, runtime defaults, release artifacts, promotion policy, or benchmark outputs. |
| Default promotion resident winsorized sweep guard | clean-room default-promotion policy guard | S2-Gate 272 default-promotion manifest guard over Gate270 status summaries | Extends `glass default-promotion-manifest` so default-route promotion requires a passing resident winsorized sweep audit, the required 200-frame row pass, and the expected sweep-audit check count from Phase 2 status evidence. | `src/glass/report/default_promotion_manifest.py`, `src/glass/cli.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned Phase 2 status artifacts | The guard consumes GLASS Phase 2 status JSON and release/default-promotion metadata only. It does not read user image directories, external implementation source, proprietary behavior, packages, or real-data reference outputs. | Promotion guard only. It does not change CUDA kernels, image math, runtime defaults, release artifacts, release-promotion decisions, or benchmark outputs. |
| Windows release matrix resident winsorized sweep guard | clean-room release-matrix policy guard | S2-Gate 273 Windows release matrix guard over Gate272 default-promotion evidence | Extends `glass windows-release-matrix` so Windows release readiness requires default-promotion evidence for a passing resident winsorized sweep audit, the required 200-frame row pass, and the expected sweep-audit check count. | `src/glass/report/windows_release_matrix.py`, `src/glass/cli.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned default-promotion artifacts | The guard consumes GLASS doctor, release-decision, and default-promotion manifest JSON only. It does not read user image directories, external implementation source, proprietary behavior, packages, or real-data reference outputs. | Release-matrix guard only. It does not change CUDA kernels, image math, runtime defaults, package artifacts, GitHub release handoff, publish preflight, or benchmark outputs. |
| Windows publish preflight resident winsorized sweep guard | clean-room publication-preflight policy guard | S2-Gate 274 publish-preflight guard over Gate273 matrix/default-promotion evidence | Extends `glass windows-publish-preflight` so final Windows publication readiness requires release-matrix and default-promotion evidence for a passing resident winsorized sweep audit, the required 200-frame row pass, and an agreeing sweep-audit check count. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publication policy over GLASS-owned release artifacts | The guard consumes GLASS release-manifest, GitHub release-plan, Windows release-matrix, and default-promotion JSON only. It does not read user image directories, external implementation source, proprietary behavior, package contents, or real-data reference outputs. | Publication-preflight guard only. It does not change CUDA kernels, image math, runtime defaults, package artifacts, GitHub release creation, or benchmark outputs. |
| Phase 2 publish-preflight resident winsorized sweep status handoff | clean-room status regression guard | S2-Gate 275 Phase 2 status handoff over Gate274 publish-preflight evidence | Carries Windows publish-preflight resident winsorized sweep evidence into `glass phase2-status`, adds a green-status blocker for missing or failed publish-preflight sweep evidence, and extends `glass phase2-status-compare` to preserve that chain. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned status artifacts | The handoff consumes GLASS Phase 2 status and publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, or real-data reference outputs. | Status/compare guard only. It does not change CUDA kernels, image math, runtime defaults, package artifacts, publish-preflight behavior, GitHub release creation, or benchmark outputs. |
| StackEngine publication-audit resident winsorized sweep guard | clean-room publication-audit policy guard | S2-Gate 276 publication audit over Gate275 Phase 2 status and Gate274 publish-preflight evidence | Extends `glass stack-engine-publication-audit` so StackEngine default-publication readiness also requires raw publish-preflight and Phase 2 status handoff evidence for a passing resident winsorized sweep audit, the required 200-frame row pass, non-empty sweep check counts, and agreement between both artifacts. | `src/glass/report/stack_engine_publication_audit.py`, `src/glass/cli.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned release/status artifacts | The audit consumes GLASS StackEngine contract, Phase 2 status, default-promotion, release-matrix, GitHub release-plan, and publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, or real-data reference outputs. | Publication-audit guard only. It does not change CUDA kernels, image math, runtime defaults, package artifacts, publish-preflight behavior, GitHub release creation, or benchmark outputs. |
| Strict StackEngine master calibration default | clean-room runtime contract | S2-Gate 277 default master-calibration StackEngine fallback policy | Makes master bias/dark/flat StackEngine failures fatal by default, adds `CalibrationPolicy.allow_legacy_stack_fallback=false`, and preserves the legacy streaming accumulator fallback only for explicit diagnostic compatibility runs. | `src/glass/models.py`, `src/glass/engine/pipeline.py` | `tests/test_pipeline_fixture.py` | Project-defined default contract requiring the StackEngine master-frame path to stay authoritative | The gate changes GLASS-owned fallback policy only. It does not read image pixels outside synthetic tests, use external implementation source, inspect proprietary behavior, alter CUDA kernels, or rerun real-data benchmarks. | Default strictness surfaces StackEngine failures earlier; users needing legacy diagnostics must explicitly set `allow_legacy_stack_fallback=true` in the calibration policy. |
| Explicit non-resident CUDA integration fast path | clean-room runtime contract | S2-Gate 278 default light-integration engine selection policy | Keeps non-resident light integration on `stack_engine_cpu` for `backend=auto` by default, adds `IntegrationPolicy.allow_cuda_streaming_accumulator_fast_path=false`, and requires policy opt-in or explicit `--backend cuda` before using the older non-resident CUDA streaming accumulator fast path. | `src/glass/models.py`, `src/glass/engine/integration.py` | `tests/test_pipeline_fixture.py` | Project-defined default contract requiring the StackEngine light-integration path to stay authoritative unless a CUDA fast path is explicitly requested | The gate uses synthetic registered-frame FITS fixtures and a monkeypatched GLASS CUDA integration module. It does not read user image directories, external implementation source, proprietary behavior, package contents, or real-data reference outputs. | This gate only changes the non-resident integration engine-selection policy. Resident CUDA integration and CUDA kernels are unchanged; real-data benchmark confirmation remains future work. |
| Pipeline contract integration engine policy guard | clean-room audit contract | S2-Gate 279 pipeline-contract guard over S2-Gate 278 artifacts | Extends `glass pipeline-contract` so non-resident integration outputs must preserve `integration_engine_policy`, per-output `engine_selection`, `default_engine=stack_engine_cpu`, and explicit opt-in evidence before using the older CUDA streaming accumulator fast path. | `src/glass/report/pipeline_contract.py` | `tests/test_pipeline_contract.py` | Project-defined audit policy over GLASS-owned integration artifacts | The gate consumes GLASS `integration_results.json` artifacts and synthetic FITS fixture maps only. It does not alter image calculations, read user image directories, inspect external implementation source, or use proprietary behavior. | Resident CUDA stack outputs remain governed by the resident result contract; this guard only blocks implicit non-resident CUDA fast-path artifacts and confirms StackEngine remains the audited default. |
| Acceptance audit integration engine policy handoff | clean-room acceptance contract | S2-Gate 280 acceptance handoff over S2-Gate 279 pipeline-contract evidence | Extends `glass acceptance-audit` so supplied pipeline contracts must preserve a passing `integration_default_engine_policy` check, and acceptance JSON/Markdown summarizes engine-policy status, resident/non-resident counts, failed rows, and failure reasons. | `src/glass/report/acceptance_audit.py` | `tests/test_acceptance_audit.py` | Project-defined evidence handoff over GLASS-owned pipeline-contract artifacts | The gate consumes GLASS acceptance fixtures, benchmark-contract metadata, and pipeline-contract JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, or use proprietary behavior. | This prevents stale acceptance artifacts from dropping the Gate279 default-route guard while keeping runtime behavior, CUDA kernels, package artifacts, and real-data benchmarks unchanged. |
| Phase 2 status integration engine policy handoff | clean-room status regression guard | S2-Gate 281 Phase 2 status handoff over S2-Gate 280 acceptance and S2-Gate 279 pipeline-contract evidence | Extends `glass phase2-status` and `glass phase2-status-compare` so acceptance and pipeline-contract integration engine-policy evidence remains visible in status JSON/Markdown and cannot regress in candidate status comparisons. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned status, acceptance, and pipeline-contract artifacts | The handoff consumes GLASS Phase 2 status JSON, acceptance-audit JSON, and pipeline-contract JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, or use proprietary behavior. | Status/compare guard only. It does not change CUDA kernels, runtime defaults, package artifacts, release publication, or real-data benchmark outputs. |
| Default promotion integration engine policy guard | clean-room default-promotion policy guard | S2-Gate 282 default-promotion manifest guard over S2-Gate 281 status evidence | Extends `glass default-promotion-manifest` so default-route promotion requires both acceptance-side integration engine-policy handoff evidence and raw pipeline-contract default engine-policy evidence from Phase 2 status. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned Phase 2 status artifacts | The guard consumes GLASS release-decision, Phase 2 status, and optional doctor JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, or use proprietary behavior. | Promotion guard only. It does not change CUDA kernels, runtime defaults, package artifacts, release publication, or real-data benchmark outputs. |
| Windows release matrix integration engine policy guard | clean-room release-matrix policy guard | S2-Gate 283 Windows release-matrix guard over S2-Gate 282 default-promotion evidence | Extends `glass windows-release-matrix` so Windows release readiness requires default-promotion evidence for both acceptance-side integration engine-policy handoff and pipeline-side default engine-policy evidence. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release/default-promotion artifacts | The guard consumes GLASS doctor, release-decision, acceptance, and default-promotion manifest JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, or use proprietary behavior. | Release-matrix guard only. It does not change CUDA kernels, runtime defaults, package artifacts, GitHub release publication, or real-data benchmark outputs. |
| Windows publish preflight integration engine policy guard | clean-room publication-preflight policy guard | S2-Gate 284 publish-preflight guard over S2-Gate 283 release-matrix and S2-Gate 282 default-promotion evidence | Extends `glass windows-publish-preflight` so final Windows publication readiness requires release-matrix and default-promotion evidence for both acceptance-side integration engine-policy handoff and pipeline-side default engine-policy evidence. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publication policy over GLASS-owned release artifacts | The guard consumes GLASS release-manifest, GitHub release-plan, Windows release-matrix, and default-promotion manifest JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, or create a release. | Publication-preflight guard only. It does not change CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Phase 2 publish-preflight integration engine policy handoff | clean-room status regression guard | S2-Gate 285 Phase 2 status handoff over S2-Gate 284 publish-preflight evidence | Carries Windows publish-preflight integration engine-policy evidence into `glass phase2-status`, adds a green-status blocker for missing or failed publish-preflight policy evidence, and extends `glass phase2-status-compare` to preserve that chain. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned status artifacts | The handoff consumes GLASS Phase 2 status JSON and Windows publish-preflight JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, or create a release. | Status/compare guard only. It does not change CUDA kernels, runtime defaults, package artifacts, publish-preflight generation, GitHub release creation, or real-data benchmark outputs. |
| StackEngine publication-audit integration engine policy guard | clean-room publication-audit policy guard | S2-Gate 286 publication audit over S2-Gate 285 Phase 2 status and S2-Gate 284 publish-preflight evidence | Extends `glass stack-engine-publication-audit` so StackEngine publication readiness requires raw publish-preflight and Phase 2 status handoff evidence for the integration engine-policy chain, plus agreement between both artifacts. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned release/status artifacts | The audit consumes GLASS StackEngine contract, Phase 2 status, default-promotion, release-matrix, GitHub release-plan, and publish-preflight JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, or create a release. | Publication-audit guard only. It does not change CUDA kernels, runtime defaults, package artifacts, publish-preflight generation, GitHub release creation, or real-data benchmark outputs. |
| Phase 2 StackEngine publication-audit handoff | clean-room status regression guard | S2-Gate 287 Phase 2 status handoff over S2-Gate 286 publication-audit evidence | Extends `glass phase2-status` and `glass phase2-status-compare` so Phase 2 status can carry StackEngine publication-audit evidence, block failed publication-audit handoffs, and preserve both integration engine-policy and resident winsorized chains. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned publication-audit artifacts | The handoff consumes GLASS Phase 2 status and StackEngine publication-audit JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, or create a release. | Status/compare guard only. It does not change CUDA kernels, runtime defaults, package artifacts, publication-audit generation, GitHub release creation, or real-data benchmark outputs. |
| Default promotion StackEngine publication-audit guard | clean-room default-promotion policy guard | S2-Gate 288 default-promotion manifest guard over S2-Gate 287 Phase 2 publication-audit evidence | Extends `glass default-promotion-manifest` so default-route promotion preserves a passing StackEngine publication-audit plus its integration engine-policy and resident winsorized chain evidence. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned Phase 2 status artifacts | The guard consumes GLASS release-decision and Phase 2 status JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, or create a release. | Default-promotion guard only. It does not change CUDA kernels, runtime defaults, package artifacts, publication-audit generation, GitHub release creation, or real-data benchmark outputs. |
| Windows release matrix StackEngine publication-audit guard | clean-room release-matrix policy guard | S2-Gate 289 Windows release-matrix guard over S2-Gate 288 default-promotion evidence | Extends `glass windows-release-matrix` so Windows CUDA release readiness preserves default-promotion StackEngine publication-audit evidence, including the publication audit itself, the integration engine-policy chain, and the resident winsorized chain. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release/default-promotion artifacts | The guard consumes GLASS doctor, release-decision, acceptance, and default-promotion manifest JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, or create a release. | Release-matrix guard only. It does not change CUDA kernels, runtime defaults, package artifacts, publish-preflight generation, GitHub release creation, or real-data benchmark outputs. |
| Windows publish preflight StackEngine publication-audit guard | clean-room publication-preflight policy guard | S2-Gate 290 publish-preflight guard over S2-Gate 289 release-matrix and S2-Gate 288 default-promotion evidence | Extends `glass windows-publish-preflight` so final Windows publication readiness preserves StackEngine publication-audit evidence from both release-matrix and default-promotion artifacts, including the publication audit itself, the integration engine-policy chain, and the resident winsorized chain. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publication policy over GLASS-owned release artifacts | The guard consumes GLASS release-manifest, GitHub release-plan, Windows release-matrix, and default-promotion manifest JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, create a release, or upload assets. | Publish-preflight guard only. It does not change CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publication-audit generation, or real-data benchmark outputs. |
| Phase 2 publish-preflight StackEngine publication-audit handoff | clean-room status regression guard | S2-Gate 291 Phase 2 status handoff over S2-Gate 290 publish-preflight evidence | Extends `glass phase2-status` and `glass phase2-status-compare` so Phase 2 green status and candidate comparisons preserve final publish-preflight StackEngine publication-audit evidence from release-matrix/default-promotion handoffs. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned release artifacts | The handoff consumes GLASS Phase 2 status JSON and Windows publish-preflight JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, create a release, or upload assets. | Status/compare guard only. It does not change CUDA kernels, runtime defaults, package artifacts, publish-preflight generation, GitHub release creation, publication-audit generation, or real-data benchmark outputs. |
| StackEngine publication-audit Phase 2 publish-preflight handoff | clean-room publication-audit policy guard | S2-Gate 292 publication audit over S2-Gate 291 Phase 2 status and S2-Gate 290 publish-preflight evidence | Extends `glass stack-engine-publication-audit` so final StackEngine publication readiness requires raw publish-preflight publication-audit evidence, matching Phase 2 status handoff evidence, and agreement across matrix/default-promotion status, policy-chain agreement, resident winsorized-chain agreement, and matrix/default agreement. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned release/status artifacts | The audit consumes GLASS StackEngine contract, Phase 2 status, default-promotion, release-matrix, GitHub release-plan, and publish-preflight JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, create a release, or upload assets. | Publication-audit guard only. It does not change CUDA kernels, image math, runtime defaults, package artifacts, publish-preflight generation, GitHub release creation, or real-data benchmark outputs. |
| Pipeline StackEngine runtime-default surface | clean-room runtime artifact contract | S2-Gate 293 pipeline-contract hardening over calibration and integration runtime artifacts | Extends `glass pipeline-contract` with `stack_engine_runtime_default_path`, a combined summary proving master calibration surfaces use StackEngine CPU or resident CUDA calibration contracts and integration outputs remain StackEngine default, resident CUDA, or an explicitly recorded CUDA fast-path override. | `src/glass/report/pipeline_contract.py` | `tests/test_pipeline_contract.py` | Project-defined runtime artifact policy over GLASS-owned run artifacts | The contract consumes GLASS `calibration_artifacts.json`, `integration_results.json`, optional resident calibration contracts, and synthetic FITS fixture outputs only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, build packages, create a release, or upload assets. | Runtime-contract guard only. It blocks legacy master accumulator artifacts while preserving explicit CUDA fast-path diagnostics; CUDA kernels, math defaults, release artifacts, and real-data benchmark outputs are unchanged. |
| Acceptance/Phase2 StackEngine runtime-default handoff | clean-room status and acceptance policy | S2-Gate 294 handoff over S2-Gate 293 pipeline-contract runtime-default evidence | Carries `stack_engine_runtime_default_path` into acceptance release evidence, Phase 2 status summaries, Markdown reports, and status-compare regression checks so the runtime-default master/integration guard cannot be dropped after pipeline-contract generation. | `src/glass/report/acceptance_audit.py`, `src/glass/report/phase2_status.py` | `tests/test_acceptance_audit.py`, `tests/test_phase2_status.py` | Project-defined handoff policy over GLASS-owned pipeline-contract and status artifacts | The handoff consumes GLASS acceptance-audit, Phase 2 status, and pipeline-contract JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/acceptance guard only. It changes no image math, CUDA kernels, runtime defaults, package artifacts, release publication behavior, or real-data benchmark outputs. |
| Default promotion StackEngine runtime-default guard | clean-room default-promotion policy | S2-Gate 295 default-promotion guard over S2-Gate 294 Phase 2 runtime-default handoff | Extends `glass default-promotion-manifest` so resident CUDA default promotion requires acceptance-side and direct pipeline runtime-default evidence, including zero legacy master rows and zero failed runtime-default integration rows. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned Phase 2 status artifacts | The guard consumes GLASS release-decision and Phase 2 status JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, create a release, or upload assets. | Default-promotion guard only. It changes no image math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows release matrix StackEngine runtime-default guard | clean-room Windows release policy | S2-Gate 296 release-matrix guard over S2-Gate 295 default-promotion runtime-default evidence | Extends `glass windows-release-matrix` so Windows CUDA release readiness preserves default-promotion runtime-default evidence for acceptance-side and pipeline-side StackEngine paths, including zero legacy master rows and zero failed runtime-default integration rows. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release/default-promotion artifacts | The guard consumes GLASS doctor, release-decision, optional acceptance-audit, and default-promotion manifest JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, build packages, create a release, or upload assets. | Release-matrix guard only. It changes no image math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows publish preflight StackEngine runtime-default guard | clean-room publication-preflight policy | S2-Gate 297 publish-preflight guard over S2-Gate 296 release-matrix and S2-Gate 295 default-promotion runtime-default evidence | Extends `glass windows-publish-preflight` so final Windows publication readiness preserves StackEngine runtime-default evidence from both release-matrix and default-promotion artifacts, including zero legacy master rows, zero failed runtime-default integration rows, and matrix/default-promotion agreement. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publication-preflight policy over GLASS-owned release artifacts | The guard consumes GLASS release-manifest, GitHub release-plan, Windows release-matrix, and default-promotion manifest JSON only. It does not alter image calculations, read user image directories, inspect external implementation source, use proprietary behavior, build packages, create a release, or upload assets. | Publish-preflight guard only. It changes no image math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Warp science residual contract | clean-room tiled residual audit over GLASS warp outputs | S2-Gate 321 small checkpoint guardrail over GLASS `warp_results.json` and registered/coverage FITS artifacts | Extends `warp_quality_contract.json` and `glass guardrails` with optional registered science-image residual verification against the reference warp output, recording common valid pixels, mean, mean absolute residual, RMS, maximum absolute residual, and threshold pass/fail evidence. | `src/glass/report/warp_quality.py`, `src/glass/cli.py`, `src/glass/report/html_report.py` | `tests/test_warp_quality_contract.py`, `tests/test_cli_smoke.py` | Project-defined residual formula over GLASS-owned registered outputs | The audit streams GLASS registered and coverage FITS artifacts tile-by-tile and compares only common finite, covered pixels. It does not read external implementation source, alter image calculations, modify input directories, change CUDA kernels, or change runtime defaults. | Diagnostic/guardrail only. It is opt-in through `--warp-science-residual-verify` or science residual thresholds and should be interpreted as an internal consistency check, not a claim of PixInsight-equivalent registration. |
| Release decision resident fastpath handoff | clean-room release/default policy over GLASS artifacts | S2-Gate 328 release-promotion handoff over Gate327 acceptance fastpath evidence and Gate326 repeat-runtime evidence | Carries `release_contract_evidence.resident_registration_fastpath` into `glass release-promotion-decision`, adds a `resident_registration_fastpath_handoff` summary, and blocks release-candidate readiness when benchmark-required resident registration fastpath evidence is present but failed. | `src/glass/report/release_promotion_decision.py` | `tests/test_release_promotion_decision.py` | Project-defined release policy over GLASS-owned acceptance and runtime artifacts | The handoff consumes GLASS acceptance-audit and runtime-repeat JSON artifacts only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-decision guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Phase2 release resident fastpath handoff | clean-room status handoff over GLASS release artifacts | S2-Gate 329 Phase2 handoff over Gate328 release-promotion resident fastpath evidence | Carries release-decision `resident_registration_fastpath_handoff` into `glass phase2-status`, adds a Phase2 readiness check, and surfaces the resident fastpath release handoff in Phase2 Markdown. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status policy over GLASS-owned release-decision artifacts | The handoff consumes GLASS Phase2 checkpoint, release-decision, acceptance-audit, and doctor JSON artifacts only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, release-decision generation, or real-data benchmark outputs. |
| Default promotion resident fastpath handoff | clean-room default-promotion policy over GLASS release artifacts | S2-Gate 330 default-promotion guard over Gate328 release decision and Gate329 Phase2 status | Carries raw release-decision and Phase2 resident fastpath handoff evidence into `glass default-promotion-manifest`, requires both sides to be ready and agreeing, and blocks default-promotion readiness on failed or missing fastpath release handoff evidence. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned release/status artifacts | The guard consumes GLASS release-decision, Phase2 status, and optional doctor JSON artifacts only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Promotion guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, release-decision generation, Phase2 generation, or real-data benchmark outputs. |
| Windows release matrix resident fastpath handoff | clean-room Windows release policy over GLASS promotion artifacts | S2-Gate 331 release-matrix guard over Gate330 default-promotion resident fastpath handoff | Carries `resident_registration_fastpath_release_handoff` from `glass default-promotion-manifest` into `glass windows-release-matrix`, requires the raw release-decision and Phase2 resident fastpath handoff chain to remain ready and agreeing, and blocks Windows CUDA release readiness on failed or missing handoff evidence. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release/default-promotion artifacts | The guard consumes GLASS doctor, release-decision, acceptance-audit, and default-promotion manifest JSON artifacts only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-matrix guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, release-decision/default-promotion generation, or real-data benchmark outputs. |
| GitHub release plan resident fastpath handoff | clean-room publication-plan policy over GLASS release artifacts | S2-Gate 332 release-plan guard over Gate331 Windows release-matrix resident fastpath handoff | Carries Windows release-matrix resident fastpath release handoff evidence into `glass windows-github-release-plan`, requires the matrix/default-promotion handoff chain to remain ready, required, agreeing, and failed-check-free, and mirrors the evidence into release notes, Markdown, and generated PowerShell dry-run validation. | `src/glass/report/windows_github_release_plan.py` | `tests/test_windows_github_release_plan.py` | Project-defined GitHub release-plan policy over GLASS-owned release artifacts | The guard consumes GLASS Windows release-matrix and default-promotion JSON artifacts only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publication-plan guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, release-matrix generation, or real-data benchmark outputs. |
| Publish preflight resident fastpath handoff | clean-room final publication-preflight policy over GLASS release artifacts | S2-Gate 333 publish-preflight guard over Gate332 release-plan and Gate331 release-matrix resident fastpath handoff | Carries resident fastpath release handoff evidence into `glass windows-publish-preflight`, requiring GitHub release-plan, Windows release-matrix, and default-promotion handoff readiness plus cross-artifact agreement before final local publication preflight passes. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined final publish-preflight policy over GLASS-owned release artifacts | The guard consumes GLASS release manifest, GitHub release-plan, Windows release-matrix, and default-promotion JSON artifacts only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publish-preflight guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, release-plan generation, or real-data benchmark outputs. |
| Phase2 publish-preflight resident fastpath handoff | clean-room status/compare policy over GLASS release artifacts | S2-Gate 334 Phase2 status handoff over Gate333 publish-preflight resident fastpath evidence | Carries final publish-preflight resident fastpath handoff readiness into `glass phase2-status` and `glass phase2-status-compare`, requiring plan/matrix/default-promotion readiness, agreement, passed raw statuses, passed Phase2 handoff statuses, and nonzero raw check counts. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned release artifacts | The handoff consumes GLASS Phase2 status JSON and Windows publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, publish-preflight generation, or real-data benchmark outputs. |
| HTML resident fastpath release evidence | clean-room report surface over GLASS acceptance artifacts | S2-Gate 335 HTML report surface over acceptance-audit resident fastpath release evidence | Surfaces `release_contract_evidence.resident_registration_fastpath` in the main HTML report, including resident registration mode, descriptor/pixel/warp batch evidence, warp-copy and scratch-memory evidence, pass/fail counts, failed-check names, and the underlying resident fastpath release-contract checks. | `src/glass/report/html_report.py` | `tests/test_pipeline_contract.py` | Project-defined report presentation over GLASS-owned acceptance artifacts | The report consumes GLASS acceptance-audit JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Report-surface guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, acceptance-audit generation, publication behavior, or real-data benchmark outputs. |
| HTML release contract failure evidence columns | clean-room report surface over GLASS acceptance artifacts | S2-Gate 336 HTML report readability over failed release-contract checks | Adds explicit `actual`, `required`, `status`, `available`, `failed_checks`, and `blockers` columns to the HTML release-contract check table so failed resident fastpath and pipeline/default checks can be diagnosed without parsing compact evidence text. | `src/glass/report/html_report.py` | `tests/test_pipeline_contract.py` | Project-defined report presentation over GLASS-owned acceptance artifacts | The report consumes GLASS acceptance-audit JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Report-surface guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, acceptance-audit generation, publication behavior, or real-data benchmark outputs. |
| HTML resident result-contract failure rows | clean-room report surface over GLASS pipeline-contract artifacts | S2-Gate 337 HTML report readability over resident CUDA result-contract failures | Adds resident result-contract failure rows to the HTML Pipeline contract audit section, expanding nested failed resident integration checks such as missing DQ source terms with output item, backend, contract status, actual/required values, and compact evidence. | `src/glass/report/html_report.py` | `tests/test_pipeline_contract.py` | Project-defined report presentation over GLASS-owned pipeline-contract artifacts | The report consumes GLASS pipeline-contract JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Report-surface guard only. It does not change registration math, CUDA kernels, runtime defaults, pipeline-contract generation, package artifacts, publication behavior, or real-data benchmark outputs. |
| Phase2 resident result-contract handoff | clean-room status/compare guard over GLASS pipeline-contract artifacts | S2-Gate 338 Phase2 status and compare over resident CUDA result-contract failures | Carries resident result-contract rows from `pipeline_contract` into Phase2 status summaries, adds `pipeline_resident_result_contract_passed`, and extends status-compare to flag dropped checks, failed resident result contracts, or increased resident result-contract failure counts. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status policy over GLASS-owned pipeline-contract and Phase2 status artifacts | The handoff consumes GLASS pipeline-contract JSON and Phase2 status JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change registration math, CUDA kernels, runtime defaults, pipeline-contract generation, package artifacts, publication behavior, or real-data benchmark outputs. |
| Default promotion resident result-contract guard | clean-room default-promotion policy over GLASS Phase2 artifacts | S2-Gate 339 default-promotion guard over Gate338 resident result-contract handoff | Carries Phase2 resident result-contract evidence into `glass default-promotion-manifest`, adding `pipeline_resident_result_contract_handoff_passed` so resident CUDA default promotion is blocked when resident result-contract checks are missing, failed, not Phase2-checked, or have failed output rows. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned Phase2 status and release-decision artifacts | The guard consumes GLASS Phase2 status JSON and release-decision JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Default-promotion guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows release matrix resident result-contract guard | clean-room Windows release policy over GLASS default-promotion artifacts | S2-Gate 340 release-matrix guard over Gate339 default-promotion resident result-contract evidence | Carries default-promotion resident result-contract evidence into `glass windows-release-matrix`, adding `default_promotion_resident_result_contract_handoff_passed` so Windows CUDA package readiness is blocked when default-promotion resident result-contract evidence is missing, failed, not Phase2-checked, or has failed output rows. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release-matrix policy over GLASS-owned default-promotion and release-decision artifacts | The guard consumes GLASS doctor, release-decision, acceptance-audit, and default-promotion manifest JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-matrix guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| GitHub release plan resident result-contract guard | clean-room publication-plan policy over GLASS release artifacts | S2-Gate 341 GitHub release-plan guard over Gate340 Windows release-matrix resident result-contract evidence | Carries Windows release-matrix resident result-contract evidence into `glass windows-github-release-plan`, adding `windows_release_matrix_resident_result_contract_handoff_passed` so GitHub release plans are blocked when matrix/default-promotion resident result-contract evidence is missing, failed, not Phase2-checked, or has failed output rows. | `src/glass/report/windows_github_release_plan.py` | `tests/test_windows_github_release_plan.py` | Project-defined GitHub publication-plan policy over GLASS-owned release-matrix and release-manifest artifacts | The guard consumes GLASS Windows release-matrix and release-manifest JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publication-plan guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Publish preflight resident result-contract guard | clean-room final publication-preflight policy over GLASS release artifacts | S2-Gate 342 publish-preflight guard over Gate341 GitHub release-plan and Gate340 release-matrix resident result-contract evidence | Carries resident result-contract evidence into `glass windows-publish-preflight`, requiring GitHub release-plan, Windows release-matrix, and default-promotion readiness plus cross-artifact agreement before final local publication preflight passes. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined final publication-preflight policy over GLASS-owned release artifacts | The guard consumes GLASS release-manifest, GitHub release-plan, Windows release-matrix, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publish-preflight guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Phase2 publish-preflight resident result-contract handoff | clean-room status/compare policy over GLASS release artifacts | S2-Gate 343 Phase2 status handoff over Gate342 publish-preflight resident result-contract evidence | Carries final publish-preflight resident result-contract readiness into `glass phase2-status` and `glass phase2-status-compare`, requiring plan/matrix/default-promotion readiness, agreement, passed statuses, passed Phase2 checks, nonzero required counts, and zero failed output rows. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned release artifacts | The handoff consumes GLASS Phase2 status JSON and Windows publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, publish-preflight generation, or real-data benchmark outputs. |
| StackEngine publication-audit resident result-contract handoff | clean-room publication-audit policy over GLASS release artifacts | S2-Gate 344 publication audit over Gate343 Phase2 status and Gate342 publish-preflight resident result-contract evidence | Extends `glass stack-engine-publication-audit` so final publication readiness requires raw publish-preflight resident result-contract evidence, matching Phase2 status handoff evidence, plan/matrix/default-promotion readiness, passed statuses, Phase2-checked state, nonzero required output counts, zero failed output rows, and cross-artifact agreement. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned release/status artifacts | The audit consumes GLASS StackEngine contract, Phase2 status, default-promotion, release-matrix, GitHub release-plan, and publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publication-audit guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, publish-preflight generation, or real-data benchmark outputs. |
| Phase2 StackEngine publication-audit resident result-contract handoff | clean-room status/compare policy over GLASS publication-audit artifacts | S2-Gate 345 Phase2 status/compare over Gate344 StackEngine publication-audit resident result-contract evidence | Carries StackEngine publication-audit raw and Phase2 resident result-contract layers into `glass phase2-status`, adds a top-level resident result-contract publication-audit chain check, surfaces the evidence in Markdown, and extends `glass phase2-status-compare` to flag loss of a previously passing publication-audit resident result-contract chain. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned publication-audit artifacts | The handoff consumes GLASS Phase2 status JSON and StackEngine publication-audit JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change registration math, CUDA kernels, runtime defaults, package artifacts, publication-audit generation, publication behavior, or real-data benchmark outputs. |
| Phase2 compare failed-check evidence details | clean-room report presentation over GLASS status artifacts | S2-Gate 346 Markdown diagnostics over Gate345 Phase2 status-compare regressions | Adds a failed-check details section to `glass phase2-status-compare` Markdown, preserving the existing JSON schema while expanding baseline/candidate evidence for failed compare checks such as StackEngine publication-audit resident result-contract regressions. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined report presentation over GLASS-owned status artifacts | The report consumes GLASS Phase2 status-compare payloads only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Report presentation only. It does not change status pass/fail logic, registration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Frame accounting integration-conflict guard | clean-room accounting and acceptance-contract semantics over GLASS run artifacts | S2-Gate 347 frame-accounting guard for rejected frames with positive integration weight | Adds the `integration_conflict` final status, per-frame conflict evidence, summary counts, HTML reporting, and acceptance-contract check `contract_frame_accounting_no_integration_conflicts` so quality/registration/warp/LN rejected frames cannot be silently counted as integrated when they still have positive integration weight. | `src/glass/engine/frame_accounting.py`, `src/glass/report/benchmark_contract.py`, `src/glass/report/html_report.py` | `tests/test_frame_accounting.py`, `tests/test_acceptance_audit.py` | Project-defined pipeline-accounting invariant over GLASS-owned stage JSON artifacts | The guard consumes GLASS processing-plan, quality, registration, warp, local-normalization, integration, and frame-accounting JSON artifacts only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Accounting/contract guard only. It does not change registration math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Pipeline contract frame admission guard | clean-room pipeline-contract and quality-admission semantics over GLASS artifacts | S2-Gate 348 pipeline-contract guard over rejected frames with positive integration weight | Carries `frame_accounting.json` admission consistency into `glass pipeline-contract`, adds `frame_accounting_no_integration_conflicts`, records frame admission summary/conflict details in pipeline-contract JSON and Markdown, and records an adaptive effective `min_stars` threshold for tiny synthetic quality-gate fixtures so CPU audit smoke runs do not falsely enter the all-quality-rejected fallback path. | `src/glass/report/pipeline_contract.py`, `src/glass/engine/quality.py` | `tests/test_pipeline_contract.py`, `tests/test_cpu_star_detect.py` | Project-defined pipeline-contract invariant over GLASS-owned frame-accounting JSON artifacts and GLASS quality-gate policy metadata | The guard consumes GLASS frame-accounting, quality, and pipeline contract artifacts only. It does not read image pixels outside normal GLASS quality measurement, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Pipeline-contract and quality-admission guard only. It does not change registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Registration reference admission policy | clean-room registration-stage admission semantics over GLASS quality artifacts | S2-Gate 349 registration reference admission policy for quality-rejected fallback references | Adds `reference_admission` metadata to `registration_results.json`, blocks quality-rejected references from becoming accepted registration outputs by default when quality-gate rejection is enforced, preserves an explicit `registration_policy.allow_quality_rejected_reference=true` override for diagnostic experiments, and keeps tiny synthetic benchmark frames detectable by using fewer separated stars below 64 px. | `src/glass/engine/registration.py`, `src/glass/synthetic/generator.py` | `tests/test_cpu_registration.py`, `tests/test_synthetic_generator.py` | Project-defined registration admission policy over GLASS-owned quality and calibration artifacts plus GLASS synthetic fixture generation | The policy consumes GLASS frame-quality, calibration, and processing-plan JSON artifacts plus calibrated frames through existing GLASS registration paths. Synthetic changes generate GLASS-owned test data only. It does not read user image directories outside the run cache, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Registration-admission and synthetic-fixture guard only. It does not change registration transform math, star matching math for real data, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Registration admission run-state guard | clean-room CLI/run-state semantics over GLASS registration artifacts | S2-Gate 350 CLI run-state guard for blocked registration reference admission | Carries `reference_admission.status=blocked` into `glass run` and `glass audit` control flow so both commands stop at registration, write `run_state.json` with `failed_stage=registration`, record failed timing evidence, return exit code 2, and avoid producing integration artifacts. | `src/glass/cli.py` | `tests/test_cli_smoke.py` | Project-defined CLI failure policy over GLASS-owned registration artifacts | The guard consumes GLASS registration payloads produced by GLASS and writes GLASS run-state/timing artifacts. It does not read user image directories outside normal scan/run inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | CLI/run-state guard only. It does not change registration transform math, quality metric math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Resume registration admission guard | clean-room resume/run-state semantics over GLASS registration artifacts | S2-Gate 351 resume guard for existing blocked registration reference admission | Extends the S2-Gate350 admission guard to `glass resume`: existing `registration_results.json` artifacts with `reference_admission.status=blocked` now stop resume at registration, write `run_state.json`, append a failed resume timing record with `resume_existing_artifact=true`, return exit code 2, and avoid warp/integration fall-through. | `src/glass/cli.py` | `tests/test_cli_smoke.py` | Project-defined resume failure policy over GLASS-owned registration artifacts | The guard consumes existing GLASS registration artifacts and writes GLASS run-state/timing artifacts. It does not read user image directories outside normal resume inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Resume-control guard only. It does not change registration transform math, quality metric math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Registration admission report surface | clean-room report surface over GLASS registration artifacts | S2-Gate 352 HTML report surface for blocked registration reference admission | Adds a dedicated Registration admission section to the main HTML report, summarizing `reference_admission` status, reference id, quality-gate state, fallback/override flags, block reason, and affected `quality_reference_admission` rows. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py` | Project-defined report presentation over GLASS-owned registration artifacts | The report consumes GLASS registration, quality, timing, manifest, and plan artifacts only. It does not read image pixels, user image directories outside normal report inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Report-surface guard only. It does not change registration transform math, quality metric math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Phase2 registration admission handoff | clean-room status/compare policy over GLASS registration artifacts | S2-Gate 353 Phase2 status handoff for registration reference admission | Adds optional `registration_results.json` ingestion to `glass phase2-status`, surfaces `reference_admission` status and affected admission rows in JSON/Markdown, adds `registration_reference_admission_not_blocked`, and extends `glass phase2-status-compare` to flag accepted-to-blocked registration admission regressions. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned registration artifacts | The handoff consumes GLASS registration_results JSON artifacts only when explicitly supplied. It does not read image pixels, user image directories outside normal status inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare handoff only. It does not change registration transform math, quality metric math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Streaming saturation quality metric | clean-room quality metric over GLASS-calibrated frame pixels | S2-Gate 354 tile-scanned saturation fraction for quality gating | Adds optional threshold-based saturation counting to streaming quality measurement, records saturated pixel count/level/source in frame quality rows, and wires `registration_policy.quality_saturation_level` into calibrated quality gates while preserving existing DQ summary saturation counts. | `src/glass/engine/quality.py` | `tests/test_cpu_star_detect.py` | Project-defined saturation threshold policy; default remains disabled unless configured or DQ summary provides saturated counts | The metric reads GLASS calibrated-frame pixels through the existing tile reader and GLASS run artifacts only. It does not read user image directories outside normal pipeline inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Quality-metric guard only. It does not change star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Quality saturation report surface | clean-room report presentation over GLASS quality artifacts | S2-Gate 355 HTML report surface for saturation quality evidence | Adds a dedicated Quality saturation section to the main HTML report, summarizing saturation counts, rejection counts, threshold/source metadata, worst frame id, and saturated/rejected frame rows from `frame_quality.json`. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py` | Project-defined report presentation over GLASS-owned quality artifacts | The report consumes GLASS `frame_quality.json` artifacts only. It does not read image pixels, user image directories outside normal report inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Report-surface guard only. It does not change quality metric math, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Phase2 quality saturation handoff | clean-room status/compare policy over GLASS quality artifacts | S2-Gate 356 Phase2 status handoff for saturation quality evidence | Adds optional `frame_quality.json` ingestion to `glass phase2-status`, surfaces saturation status/counts/threshold/source/worst-frame evidence in JSON and Markdown, adds `quality_saturation_no_rejections`, and extends `glass phase2-status-compare` to flag loss of previously passing saturation handoff evidence. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned quality artifacts | The handoff consumes GLASS `frame_quality.json` artifacts only when explicitly supplied. It does not read image pixels, user image directories outside normal status inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare handoff only. It does not change quality metric math, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Quality metric distribution report surface | clean-room report presentation over GLASS quality artifacts | S2-Gate 357 HTML report surface for frame-quality distributions | Adds a dedicated Quality metrics section to the main HTML report, summarizing distributions and worst-frame rows for `star_count`, `fwhm_px`, `eccentricity`, `background_rms`, `snr`, `quality_score`, and `weight` from `frame_quality.json`. | `src/glass/report/html_report.py` | `tests/test_cli_smoke.py` | Project-defined report presentation over GLASS-owned quality artifacts | The report consumes GLASS `frame_quality.json` artifacts only. It does not read image pixels, user image directories outside normal report inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Report-surface guard only. It does not change quality metric math, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Phase2 quality metric summary handoff | clean-room status/compare policy over GLASS quality artifacts | S2-Gate 358 Phase2 status handoff for core quality metric distributions | Reuses `glass phase2-status --quality-results` to summarize core frame-quality metric distributions in JSON/Markdown, adds `quality_metric_summary_available`, and extends `glass phase2-status-compare` to flag loss of previously available metric summary evidence. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned quality artifacts | The handoff consumes GLASS `frame_quality.json` artifacts only when explicitly supplied. It does not read image pixels, user image directories outside normal status inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare handoff only. It does not change quality metric math, quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Quality metric compare artifact | clean-room comparison policy over GLASS quality artifacts | S2-Gate 359 baseline/candidate frame-quality metric comparison | Adds `glass quality-metrics-compare`, which compares baseline/candidate `frame_quality.json` metric availability, frame counts, quality-gate status counts, median/mean deltas, bad-direction ratios, and worst-frame ids with optional experiment thresholds. | `src/glass/report/quality_metrics_compare.py`, `src/glass/cli.py` | `tests/test_quality_metrics_compare.py` | Project-defined quality-regression comparison policy over GLASS-owned quality artifacts; optional thresholds are user-supplied and disabled by default | The compare consumes GLASS `frame_quality.json` artifacts only. It does not read image pixels, user image directories outside explicitly supplied artifact paths, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Quality-regression artifact only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Phase2 quality metric compare handoff | clean-room status/compare policy over GLASS quality compare artifacts | S2-Gate 360 Phase2 status handoff for quality-regression compare evidence | Carries `glass quality-metrics-compare` artifacts into `glass phase2-status`, surfaces pass/fail and threshold-failure evidence in JSON/Markdown, and extends `glass phase2-status-compare` to flag loss of a previously passing quality-metrics compare handoff. | `src/glass/report/phase2_status.py`, `src/glass/cli.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned quality compare artifacts | The handoff consumes GLASS `quality-metrics-compare` JSON artifacts only when explicitly supplied. It does not read image pixels, user image directories outside status inputs, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare handoff only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Default promotion quality metric compare guard | clean-room default-promotion policy over GLASS Phase2 status artifacts | S2-Gate 361 default-promotion guard for quality-regression compare evidence | Carries Phase2 `quality_metrics_compare` evidence into `glass default-promotion-manifest`, blocking default promotion if an explicitly supplied quality-metrics compare handoff fails while keeping older Phase2 artifacts without that optional evidence non-blocking. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned Phase2 status artifacts | The guard consumes GLASS release-decision and Phase2 status JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Default-promotion guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows release matrix quality metric compare guard | clean-room Windows release policy over GLASS default-promotion artifacts | S2-Gate 362 release-matrix guard for quality-regression compare evidence | Carries default-promotion `quality_metrics_compare` evidence into `glass windows-release-matrix`, blocking Windows release readiness if an explicitly supplied quality-metrics compare handoff fails while keeping older default-promotion artifacts without that optional evidence non-blocking. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned default-promotion artifacts | The guard consumes GLASS doctor, release-decision, and default-promotion manifest JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-matrix guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows publish preflight quality metric compare guard | clean-room publication-preflight policy over GLASS release artifacts | S2-Gate 363 publish-preflight guard for quality-regression compare evidence | Carries release-matrix and default-promotion `quality_metrics_compare` evidence into `glass windows-publish-preflight`, blocking local publication readiness if explicitly supplied quality-metrics compare evidence fails or diverges while keeping older artifacts without that optional evidence non-blocking. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publication-preflight policy over GLASS-owned release artifacts | The guard consumes GLASS release-manifest, GitHub release-plan, Windows release-matrix, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publish-preflight guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Phase2 publish-preflight quality metric compare handoff | clean-room status/compare policy over GLASS publish-preflight artifacts | S2-Gate 364 Phase2 status and status-compare guard for publish-preflight quality-regression evidence | Carries `windows-publish-preflight` quality-metrics-compare evidence into `glass phase2-status`, blocks failed handoffs when present, and extends `glass phase2-status-compare` so candidates cannot lose a previously passing publish-preflight quality compare chain. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned publish-preflight and Phase2 status artifacts | The guard consumes GLASS Phase2 status JSON and GLASS publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| StackEngine publication-audit quality metric compare guard | clean-room publication-audit policy over GLASS publish-preflight and Phase2 artifacts | S2-Gate 365 StackEngine publication-audit guard for quality-regression compare handoff evidence | Carries raw `windows-publish-preflight` and Phase2 status quality-metrics-compare evidence into `glass stack-engine-publication-audit`, blocking failed handoffs or raw/Phase2 mismatches when evidence is present while keeping older artifacts without that optional evidence non-blocking. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned publish-preflight and Phase2 status artifacts | The guard consumes GLASS stack/default-promotion/release/github/publish-preflight/Phase2 JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publication-audit guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Release promotion decision quality metric compare guard | clean-room release-decision policy over GLASS StackEngine publication-audit artifacts | S2-Gate 366 final release-decision guard for quality-regression compare handoff evidence | Carries StackEngine publication-audit quality-metrics-compare evidence into `glass release-promotion-decision`, blocking release-candidate readiness for failed or raw/Phase2-mismatched quality compare handoffs while keeping older publication-audit artifacts without that optional evidence non-blocking. | `src/glass/report/release_promotion_decision.py` | `tests/test_release_promotion_decision.py` | Project-defined release-decision policy over GLASS-owned publication-audit JSON | The guard consumes GLASS acceptance, contract, runtime, preflight, and StackEngine publication-audit JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-decision guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Default promotion release quality metric compare guard | clean-room default-promotion policy over GLASS release-decision artifacts | S2-Gate 367 default-promotion guard for final publication quality-regression evidence | Carries release-decision StackEngine publication quality-metrics-compare evidence into `glass default-promotion-manifest`, blocking default promotion for failed or raw/Phase2-mismatched publication quality compare guards while keeping older release-decision artifacts without that optional guard non-blocking. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned release-decision JSON | The guard consumes GLASS release-decision and Phase2 status JSON only, with optional doctor JSON for existing package checks. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Default-promotion guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Windows release matrix quality publication guard | clean-room Windows release policy over GLASS release/default-promotion artifacts | S2-Gate 368 Windows release-matrix guard for final publication quality-regression evidence | Carries release-decision and default-promotion quality publication guard evidence into `glass windows-release-matrix`, blocking Windows release readiness for failed release-decision quality guards or default-promotion manifests that lose/mismatch that guard while keeping older artifacts without the optional guard non-blocking. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release-decision/default-promotion JSON | The guard consumes GLASS doctor, release-decision, optional acceptance, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Windows release-matrix guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Windows publish preflight quality publication guard | clean-room publication-preflight policy over GLASS release/default-promotion artifacts | S2-Gate 369 publish-preflight guard for final publication quality-regression evidence | Carries Windows release-matrix and default-promotion quality publication guard evidence into `glass windows-publish-preflight`, blocking final local publication preflight for failed matrix/default quality guards or cross-artifact mismatches while keeping older artifacts without the optional guard non-blocking. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publication-preflight policy over GLASS-owned release artifacts | The guard consumes GLASS release-manifest, GitHub release-plan, Windows release-matrix, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publish-preflight guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Phase2 publish-preflight quality publication guard handoff | clean-room status/compare policy over GLASS publish-preflight artifacts | S2-Gate 370 Phase2 status and status-compare guard for final publication quality-regression evidence | Carries `windows-publish-preflight` release quality publication guard evidence into `glass phase2-status`, blocks failed handoffs when present, and extends `glass phase2-status-compare` so candidates cannot lose a previously passing publish-preflight quality publication guard chain. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned publish-preflight and Phase2 status artifacts | The guard consumes GLASS Phase2 status JSON and GLASS publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| StackEngine publication-audit quality publication guard | clean-room publication-audit policy over GLASS publish-preflight and Phase2 artifacts | S2-Gate 371 StackEngine publication-audit guard for final publication quality-regression evidence | Carries raw `windows-publish-preflight` and Phase2 status release quality publication guard evidence into `glass stack-engine-publication-audit`, blocking failed handoffs or raw/Phase2 mismatches when evidence is present while keeping older artifacts without that optional guard non-blocking. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned publish-preflight and Phase2 status artifacts | The guard consumes GLASS stack/default-promotion/release/github/publish-preflight/Phase2 JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publication-audit guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Release promotion decision quality publication guard | clean-room release-decision policy over GLASS StackEngine publication-audit artifacts | S2-Gate 372 final release-decision guard for release quality publication evidence | Carries StackEngine publication-audit release quality publication guard evidence into `glass release-promotion-decision`, blocking release-candidate readiness for failed, one-sided missing, or raw/Phase2-mismatched quality publication guard handoffs while keeping older publication-audit artifacts without that optional guard non-blocking. | `src/glass/report/release_promotion_decision.py` | `tests/test_release_promotion_decision.py` | Project-defined release-decision policy over GLASS-owned publication-audit JSON | The guard consumes GLASS acceptance, contract, runtime, preflight, and StackEngine publication-audit JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-decision guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Default promotion release quality publication guard | clean-room default-promotion policy over GLASS release-decision artifacts | S2-Gate 373 default-promotion guard for final release quality publication evidence | Carries release-decision StackEngine publication release quality guard evidence into `glass default-promotion-manifest`, blocking default promotion for failed, one-sided missing, or raw/Phase2-mismatched release quality guard handoffs while keeping older release-decision artifacts without that optional guard non-blocking. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned release-decision JSON | The guard consumes GLASS release-decision and Phase2 status JSON only, with optional doctor JSON for existing package checks. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Default-promotion guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Windows release matrix release quality publication guard | clean-room Windows release policy over GLASS release/default-promotion artifacts | S2-Gate 374 Windows release-matrix guard for final release quality publication evidence | Carries release-decision and default-promotion StackEngine publication release quality guard evidence into `glass windows-release-matrix`, blocking Windows release readiness for failed release-decision guards, missing default-promotion handoff objects, or raw/Phase2/default-promotion mismatches while keeping explicitly compatible missing guard evidence non-blocking. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release-decision/default-promotion JSON | The guard consumes GLASS doctor, release-decision, optional acceptance, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Windows release-matrix guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Windows publish preflight release quality publication guard | clean-room publication-preflight policy over GLASS release/default-promotion artifacts | S2-Gate 375 publish-preflight guard for final release quality publication evidence | Carries Windows release-matrix and default-promotion StackEngine publication release quality guard evidence into `glass windows-publish-preflight`, blocking local publication readiness for failed matrix/default guard checks or cross-artifact mismatches while keeping older artifacts that omit the optional guard on both sides non-blocking. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publish-preflight policy over GLASS-owned release-matrix/default-promotion JSON | The guard consumes GLASS release manifest, GitHub release plan, Windows release matrix, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publish-preflight guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Phase2 publish-preflight release quality publication guard | clean-room status/compare policy over GLASS publish-preflight artifacts | S2-Gate 376 Phase2 status guard for final release quality publication evidence | Carries Gate375 final release quality publication checks into `glass phase2-status`, blocks failed final checks when present, and extends `glass phase2-status-compare` so candidates cannot lose previously passing final check evidence. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned publish-preflight and Phase2 status JSON | The guard consumes GLASS Phase2 status JSON and GLASS windows-publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| StackEngine publication-audit release quality publication guard | clean-room publication-audit policy over GLASS publish-preflight and Phase2 artifacts | S2-Gate 377 publication-audit guard for final release quality publication evidence | Carries Gate375/Gate376 final release quality checks into `glass stack-engine-publication-audit`, blocking failed, mismatched, or missing Phase2 final-check handoff evidence while keeping older artifacts that omit final checks on both sides compatible. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned publish-preflight and Phase2 status JSON | The guard consumes GLASS windows-publish-preflight JSON and GLASS Phase2 status JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publication-audit guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Release decision publication-audit release quality guard | clean-room release-decision policy over GLASS publication-audit artifacts | S2-Gate 378 release-decision guard for final release quality publication evidence | Carries StackEngine publication-audit final release quality checks into `glass release-promotion-decision`, blocking failed, mismatched, or one-sided missing final-check evidence while keeping older publication-audit artifacts that omit final checks on both sides compatible. | `src/glass/report/release_promotion_decision.py` | `tests/test_release_promotion_decision.py` | Project-defined release-decision policy over GLASS-owned publication-audit JSON | The guard consumes GLASS StackEngine publication-audit JSON only, alongside existing GLASS acceptance/runtime artifacts. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-decision guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Default promotion release quality final guard | clean-room default-promotion policy over GLASS release-decision artifacts | S2-Gate 379 default-promotion guard for final release quality publication evidence | Carries Gate378 final release quality publication evidence into `glass default-promotion-manifest`, blocking failed, mismatched, or one-sided missing final-check evidence while keeping older release-decision artifacts with compatible-missing or absent final checks non-blocking. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned release-decision JSON | The guard consumes GLASS release-promotion-decision JSON and Phase2 status JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Default-promotion guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows release matrix release quality final guard | clean-room Windows release policy over GLASS release/default-promotion artifacts | S2-Gate 380 Windows release-matrix guard for final release quality publication evidence | Carries Gate379 final release quality publication evidence into `glass windows-release-matrix`, blocking failed direct final checks, failed default-promotion final checks, or loss of final-check evidence during default promotion while keeping explicitly compatible-missing final checks non-blocking. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release-decision and default-promotion JSON | The guard consumes GLASS doctor, release-promotion-decision, optional acceptance-audit, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Windows release-matrix guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows publish preflight release quality final guard | clean-room publication-preflight policy over GLASS release/default-promotion artifacts | S2-Gate 381 publish-preflight guard for final release quality publication evidence | Carries Gate380 final release quality publication evidence into `glass windows-publish-preflight`, blocking failed matrix final checks, failed default-promotion final checks, or loss of final-check evidence between release matrix and default-promotion manifest while keeping explicitly compatible-missing final checks non-blocking. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publish-preflight policy over GLASS-owned release-matrix and default-promotion JSON | The guard consumes GLASS release manifest, GitHub release plan, Windows release matrix, and default-promotion manifest JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publish-preflight guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Phase2 publish-preflight release quality final evidence handoff | clean-room status/compare policy over GLASS publish-preflight artifacts | S2-Gate 382 Phase2 status guard for publish-preflight final release quality evidence | Carries Gate381 publish-preflight final release quality evidence into `glass phase2-status`, blocking failed final evidence when present and extending `glass phase2-status-compare` so candidates cannot lose previously passing final evidence while keeping older final check names. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned publish-preflight JSON | The guard consumes GLASS Phase2 status JSON and GLASS windows-publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| StackEngine publication-audit release quality final evidence handoff | clean-room publication-audit policy over GLASS publish-preflight and Phase2 status artifacts | S2-Gate 383 StackEngine publication-audit guard for publish-preflight final release quality evidence | Carries Gate382 Phase2 publish-preflight final release quality evidence into `glass stack-engine-publication-audit`, blocking failed raw evidence, failed Phase2 evidence, or Phase2 loss of raw final-evidence fields while keeping older artifacts without the optional evidence non-blocking. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned publish-preflight and Phase2 status JSON | The guard consumes GLASS stack/default-promotion/release/github/publish-preflight/Phase2 JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publication-audit guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Release promotion decision final evidence guard | clean-room release-decision policy over GLASS StackEngine publication-audit artifacts | S2-Gate 384 release-promotion guard for final release quality evidence | Carries Gate383 StackEngine publication-audit final release quality evidence into `glass release-promotion-decision`, blocking failed raw evidence, failed Phase2 evidence, or Phase2 loss of raw final-evidence fields while keeping older publication-audit artifacts without the optional evidence non-blocking. | `src/glass/report/release_promotion_decision.py` | `tests/test_release_promotion_decision.py` | Project-defined release-decision policy over GLASS-owned StackEngine publication-audit JSON | The guard consumes GLASS acceptance, runtime, and StackEngine publication-audit JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-decision guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Default promotion final evidence guard | clean-room default-promotion policy over GLASS release-decision artifacts | S2-Gate 385 default-promotion guard for final release quality evidence | Carries Gate384 release-promotion final release quality evidence into `glass default-promotion-manifest`, blocking failed raw evidence, failed Phase2 evidence, or Phase2 loss of raw final-evidence fields while keeping older release-decision artifacts without the optional evidence non-blocking. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned release-decision JSON | The guard consumes GLASS release-decision, Phase2 status, and optional doctor JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Default-promotion guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows release matrix final evidence guard | clean-room Windows release policy over GLASS release/default-promotion artifacts | S2-Gate 386 Windows release-matrix guard for final release quality evidence | Carries Gate385 default-promotion final release quality evidence into `glass windows-release-matrix`, blocking failed direct final evidence, failed default-promotion final evidence, or loss of final-evidence fields during default promotion while keeping older artifacts without the optional evidence non-blocking. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release-decision and default-promotion JSON | The guard consumes GLASS doctor, release-promotion-decision, optional acceptance-audit, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Windows release-matrix guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows publish preflight final evidence guard | clean-room publication-preflight policy over GLASS release/default-promotion artifacts | S2-Gate 387 publish-preflight guard for final release quality evidence | Carries Gate386 Windows release-matrix final release quality evidence into `glass windows-publish-preflight`, blocking failed matrix final evidence, failed default-promotion final evidence, or loss of final-evidence fields between the matrix and standalone default-promotion manifest while keeping older artifacts without the optional evidence non-blocking. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publication-preflight policy over GLASS-owned release-matrix and default-promotion JSON | The guard consumes GLASS release manifest, GitHub release plan, Windows release matrix, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publish-preflight guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |

| Phase2 publish-preflight final evidence detail handoff | clean-room status/compare policy over GLASS publish-preflight artifacts | S2-Gate 388 Phase2 status guard for publish-preflight final evidence detail | Carries Gate387 publish-preflight `final_evidence_*` release quality detail into `glass phase2-status`, blocks failed detail evidence when present, preserves legacy `final_checks_*` compatibility, and extends `glass phase2-status-compare` so candidates cannot lose previously passing detail evidence. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned publish-preflight JSON | The guard consumes GLASS Phase2 status JSON and GLASS windows-publish-preflight JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Status/compare guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| StackEngine publication-audit final evidence detail handoff | clean-room publication-audit policy over GLASS publish-preflight and Phase2 status artifacts | S2-Gate 389 StackEngine publication-audit guard for publish-preflight final evidence detail | Carries Gate388 Phase2 publish-preflight `final_evidence_*` release quality detail into `glass stack-engine-publication-audit`, blocks failed raw/Phase2 detail evidence, preserves legacy `final_checks_*` compatibility, and flags Phase2 loss of detail fields carried by raw publish-preflight. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned publish-preflight and Phase2 status JSON | The guard consumes GLASS stack/default-promotion/release/github/publish-preflight/Phase2 JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publication-audit guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Release-promotion final evidence detail handoff | clean-room release-decision policy over GLASS StackEngine publication-audit artifacts | S2-Gate 390 release-promotion guard for publication-audit final evidence detail | Carries Gate389 StackEngine publication-audit `final_evidence_*` release quality detail into `glass release-promotion-decision`, blocks failed raw/Phase2 detail evidence, preserves legacy `final_checks_*` compatibility, and flags Phase2 loss of detail fields carried by raw publish-preflight evidence. | `src/glass/report/release_promotion_decision.py` | `tests/test_release_promotion_decision.py` | Project-defined release-decision policy over GLASS-owned StackEngine publication-audit JSON | The guard consumes GLASS acceptance/runtime/StackEngine publication-audit JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Release-decision guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Default-promotion final evidence detail handoff | clean-room default-promotion policy over GLASS release-decision artifacts | S2-Gate 391 default-promotion guard for release-decision final evidence detail | Carries Gate390 release-promotion `final_evidence_*` release quality detail into `glass default-promotion-manifest`, blocks failed detail evidence, preserves legacy `final_checks_*` compatibility, and flags Phase2-side detail loss while raw release-decision detail is present. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned release-decision JSON | The guard consumes GLASS release-decision, Phase2 status, and optional doctor JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Default-promotion guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows release matrix final evidence detail handoff | clean-room Windows release policy over GLASS release/default-promotion artifacts | S2-Gate 392 Windows release-matrix guard for final evidence detail | Carries Gate391 default-promotion `final_evidence_*` release quality detail into `glass windows-release-matrix`, blocks failed direct/default detail evidence, preserves legacy `final_checks_*` compatibility, and flags detail loss between release-decision and default-promotion artifacts. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release policy over GLASS-owned release-decision and default-promotion JSON | The guard consumes GLASS doctor, release-promotion-decision, optional acceptance-audit, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Windows release-matrix guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, publication behavior, or real-data benchmark outputs. |
| Windows publish-preflight final evidence detail handoff | clean-room publish-preflight policy over GLASS release-matrix/default-promotion artifacts | S2-Gate 393 publish-preflight guard for final evidence detail | Carries Gate392 release-matrix `final_evidence_*` release quality detail into `glass windows-publish-preflight`, blocks failed matrix/default detail evidence, preserves legacy `final_checks_*` compatibility, and flags detail loss between release-matrix and standalone default-promotion artifacts. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined publication-preflight policy over GLASS-owned release-matrix and default-promotion JSON | The guard consumes GLASS release manifest, GitHub release plan, Windows release matrix, and default-promotion JSON only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, or benchmark reference outputs. | Publish-preflight guard only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, or real-data benchmark outputs. |
| Release quality evidence shared contract | clean-room report contract helper over GLASS-owned release evidence fields | S2-Gate 394 shared release-quality evidence helper | Extracts the release-quality `final_evidence_*` detail field list and readiness evaluator into `glass.report.release_quality_evidence`, then moves `glass windows-publish-preflight` onto that shared helper without changing emitted field names or pass/fail semantics. | `src/glass/report/release_quality_evidence.py`; `src/glass/report/windows_publish_preflight.py` | `tests/test_release_quality_evidence.py`; `tests/test_windows_publish_preflight.py` | Project-defined report-contract helper over GLASS-owned release-matrix/default-promotion evidence fields | The helper consumes in-memory GLASS report dictionaries only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Shared report-contract helper only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Windows release matrix shared evidence contract | clean-room report contract helper integration over GLASS-owned release evidence fields | S2-Gate 395 release-matrix migration to shared final-evidence helper | Moves Windows release-matrix final-evidence field lists and detail readiness evaluation onto `glass.report.release_quality_evidence` while preserving existing release-matrix present/ready semantics and emitted JSON/Markdown field names. | `src/glass/report/windows_release_matrix.py`; `src/glass/report/release_quality_evidence.py` | `tests/test_windows_release_matrix.py`; `tests/test_release_quality_evidence.py` | Project-defined report-contract migration over GLASS-owned release-decision/default-promotion evidence fields | The migration consumes in-memory GLASS release-decision/default-promotion report dictionaries only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Release-matrix report-contract migration only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Default promotion shared evidence contract | clean-room report contract helper integration over GLASS-owned release evidence fields | S2-Gate 396 default-promotion migration to shared final-evidence helper | Moves default-promotion final-evidence field lists and detail readiness evaluation onto `glass.report.release_quality_evidence` while preserving existing default-promotion present/ready semantics and emitted JSON/Markdown field names. | `src/glass/report/default_promotion_manifest.py`; `src/glass/report/release_quality_evidence.py` | `tests/test_default_promotion_manifest.py`; `tests/test_release_quality_evidence.py` | Project-defined report-contract migration over GLASS-owned release-decision evidence fields | The migration consumes in-memory GLASS release-decision/Phase2 report dictionaries only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Default-promotion report-contract migration only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Release promotion shared evidence readiness | clean-room report contract helper integration over GLASS-owned publication-audit evidence fields | S2-Gate 397 release-promotion migration to shared final-evidence readiness | Adds strict paired raw/Phase2 readiness mode to `glass.report.release_quality_evidence` and moves release-promotion final-evidence detail readiness onto that helper while preserving existing legacy `final_checks_*` compatibility behavior. | `src/glass/report/release_promotion_decision.py`; `src/glass/report/release_quality_evidence.py` | `tests/test_release_promotion_decision.py`; `tests/test_release_quality_evidence.py` | Project-defined report-contract migration over GLASS-owned publication-audit evidence fields | The migration consumes in-memory GLASS publication-audit report dictionaries only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Release-promotion report-contract migration only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| StackEngine publication-audit shared evidence readiness | clean-room report contract helper integration over GLASS-owned publish-preflight and Phase2 evidence fields | S2-Gate 398 publication-audit migration to shared final-evidence readiness | Moves StackEngine publication-audit final-evidence detail readiness onto `glass.report.release_quality_evidence`, promotes publication-audit release-quality field groups to local constants, and preserves strict paired raw/Phase2 detail semantics plus legacy `final_checks_*` compatibility. | `src/glass/report/stack_engine_publication_audit.py`; `src/glass/report/release_quality_evidence.py` | `tests/test_stack_engine_publication_audit.py`; `tests/test_release_quality_evidence.py` | Project-defined report-contract migration over GLASS-owned publish-preflight and Phase2 evidence fields | The migration consumes in-memory GLASS publish-preflight and Phase2 report dictionaries only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Publication-audit report-contract migration only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Publication evidence profile contract | clean-room shared report profile over GLASS-owned release-quality evidence fields | S2-Gate 399 shared publication-layer final-evidence field profile | Adds shared publication-layer final-evidence constants for the `matrix/matrix_default/default_promotion` field profile and moves release-promotion plus StackEngine publication-audit onto those constants while preserving strict paired raw/Phase2 readiness and legacy `final_checks_*` compatibility. | `src/glass/report/release_quality_evidence.py`; `src/glass/report/release_promotion_decision.py`; `src/glass/report/stack_engine_publication_audit.py` | `tests/test_release_quality_evidence.py`; `tests/test_release_promotion_decision.py`; `tests/test_stack_engine_publication_audit.py` | Project-defined shared report-contract profile over GLASS-owned publication-audit/release-promotion evidence fields | The profile consumes in-memory GLASS report dictionaries only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Shared report-contract profile only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Release evidence chain fixture | clean-room shared-profile chain test over GLASS-owned release-quality evidence fields | S2-Gate 400 publication-profile to release-matrix profile chain fixture | Adds a focused chain fixture that maps publication-layer final-evidence detail into the release-matrix/publish-preflight detail profile, validating ready preservation, failed-detail propagation, and source-present/target-missing detail loss detection. | `tests/test_release_quality_evidence_chain.py`; `src/glass/report/release_quality_evidence.py` | `tests/test_release_quality_evidence_chain.py`; `tests/test_release_quality_evidence.py` | Project-defined shared-profile regression fixture over GLASS-owned release-quality evidence dictionaries | The fixture uses in-memory GLASS evidence dictionaries only. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Shared-profile chain fixture only. It does not change quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Resident DQ provenance contract profile | clean-room benchmark-contract profile over GLASS-owned resident DQ artifacts | S2-Gate 401 shared resident CUDA DQ provenance profile | Adds a reusable `dq_provenance` benchmark-contract profile for resident CUDA coverage, DQ, low-rejection, high-rejection, source terms, and artifact map-path evidence, then moves acceptance-audit DQ contract setup onto that profile without changing emitted check names or pass/fail semantics. | `src/glass/report/dq_contract_profile.py`; `tests/test_acceptance_audit.py` | `tests/test_dq_contract_profile.py`; `tests/test_acceptance_audit.py` | Project-defined resident DQ/mask pipeline contract over GLASS-owned integration artifacts | The profile consumes in-memory GLASS benchmark-contract dictionaries and GLASS run artifact metadata/maps created by GLASS tests or runs. It does not read image pixels outside those artifacts, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Report-contract profile only. It does not change DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Resident benchmark contract profile generator | clean-room contract-artifact generator over GLASS-owned benchmark policy fields | S2-Gate 402 resident CUDA benchmark-contract generator | Adds a reusable resident CUDA benchmark-contract generator and `glass benchmark-contract-profile` CLI that embeds the shared DQ provenance profile, 200-light dataset minima, resident route requirements, speedup threshold, and compare tolerances so acceptance-audit can consume generated policy artifacts instead of hand-authored DQ JSON. | `src/glass/report/benchmark_contract_profile.py`; `src/glass/cli.py` | `tests/test_benchmark_contract_profile.py`; `tests/test_dq_contract_profile.py` | Project-defined benchmark-contract policy over GLASS-owned acceptance-audit fields | The generator writes JSON contract dictionaries only. Its tests consume GLASS-owned synthetic acceptance fixtures and GLASS-generated artifact metadata/maps. It does not read user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Contract-artifact generator only. It does not change DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Acceptance-audit benchmark profile shortcut | clean-room acceptance workflow over GLASS-owned benchmark profiles | S2-Gate 403 direct resident CUDA profile consumption in acceptance-audit | Adds `glass acceptance-audit --benchmark-contract-profile resident_cuda_dq_v1` and matching Python API support so acceptance audits can consume the built-in resident CUDA DQ benchmark profile directly while preserving existing `--benchmark-contract` path behavior and emitted check semantics. | `src/glass/report/acceptance_audit.py`; `src/glass/cli.py`; `src/glass/report/benchmark_contract_profile.py` | `tests/test_benchmark_contract_profile.py`; `tests/test_acceptance_audit.py` | Project-defined acceptance workflow over GLASS-owned benchmark-contract profiles | The shortcut builds an in-memory GLASS contract dictionary and consumes GLASS-owned synthetic acceptance fixtures plus GLASS-generated artifact metadata/maps in tests. It does not read user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Acceptance workflow shortcut only. It does not change benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Runtime sweep acceptance profile handoff | clean-room planning workflow over GLASS-owned acceptance commands | S2-Gate 404 resident CUDA profile handoff in candidate runtime sweep plans | Makes `glass candidate-runtime-sweep-plan` write planned `acceptance-audit` commands with `--benchmark-contract-profile resident_cuda_dq_v1` when no explicit benchmark contract file is supplied, while preserving explicit file-path contracts and recording the effective source/profile in plan artifacts. | `src/glass/report/candidate_runtime_sweep_plan.py`; `src/glass/cli.py` | `tests/test_candidate_runtime_sweep_plan.py` | Project-defined planning policy over GLASS-owned candidate runtime sweep artifacts | The planner reads GLASS candidate-comparison JSON, GLASS run-command text, and GLASS baseline artifact metadata supplied by tests or users. It writes command-plan JSON only; it does not execute runs, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Planning workflow only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Phase2 status benchmark profile evidence | clean-room status/compare policy over GLASS-owned acceptance artifacts | S2-Gate 405 acceptance benchmark profile evidence in Phase2 status | Surfaces acceptance benchmark contract source/profile/name/schema fields in Phase2 status and Markdown, then extends `glass phase2-status-compare` so candidate statuses cannot lose an acceptance benchmark contract profile present in the baseline. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined status/compare policy over GLASS-owned acceptance-audit JSON and Phase2 status JSON | The status builder reads GLASS acceptance-audit JSON and GLASS Phase2 status JSON supplied by tests or users. It does not execute runs, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Status/report-contract guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Release promotion benchmark profile guard | clean-room release-promotion policy over GLASS-owned acceptance artifacts | S2-Gate 406 release-promotion guard for resident CUDA benchmark profile evidence | Carries acceptance benchmark contract source/path/profile/name/schema fields into `glass release-promotion-decision`, adds release-blocking `acceptance_benchmark_contract_profile`, and surfaces the required `resident_cuda_dq_v1` profile in release Markdown. | `src/glass/report/release_promotion_decision.py` | `tests/test_release_promotion_decision.py` | Project-defined release-promotion policy over GLASS-owned acceptance-audit JSON | The decision builder reads GLASS acceptance-audit JSON and optional GLASS release contract artifacts supplied by tests or users. It does not execute runs, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Release-policy guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Default promotion benchmark profile handoff | clean-room default-promotion policy over GLASS-owned release/status artifacts | S2-Gate 407 default-promotion guard for resident CUDA benchmark profile handoff | Carries release-decision acceptance benchmark profile evidence into `glass default-promotion-manifest`, requires release decision, Phase2 acceptance summary, and default-route acceptance summary to agree on `resident_cuda_dq_v1`, and surfaces the handoff in Markdown. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned release-decision and Phase2 status JSON | The manifest builder reads GLASS release-decision JSON, Phase2 status JSON, and optional GLASS doctor JSON supplied by tests or users. It does not execute runs, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Default-promotion guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Windows release matrix benchmark profile handoff | clean-room release-matrix policy over GLASS-owned release/default-promotion artifacts | S2-Gate 408 Windows release-matrix guard for resident CUDA benchmark profile handoff | Carries release-decision acceptance benchmark profile evidence into `glass windows-release-matrix`, requires the release decision profile guard and default-promotion benchmark profile handoff to agree on `resident_cuda_dq_v1`, and surfaces the handoff in Markdown. | `src/glass/report/windows_release_matrix.py` | `tests/test_windows_release_matrix.py` | Project-defined Windows release-matrix policy over GLASS-owned doctor, release-decision, acceptance-audit, and default-promotion JSON | The matrix builder reads GLASS release artifacts supplied by tests or users. It does not execute runs, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Release-matrix guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Windows publish-preflight benchmark profile handoff | clean-room publish-preflight policy over GLASS-owned release/default-promotion artifacts | S2-Gate 409 Windows publish-preflight guard for resident CUDA benchmark profile handoff | Carries the Windows release-matrix release-decision benchmark profile and default-promotion benchmark profile handoff into `glass windows-publish-preflight`, blocks release-profile or handoff drift, and surfaces the handoff in Markdown. | `src/glass/report/windows_publish_preflight.py` | `tests/test_windows_publish_preflight.py` | Project-defined Windows publish-preflight policy over GLASS-owned release manifest, GitHub release plan, Windows release matrix, and default-promotion JSON | The preflight builder reads GLASS release artifacts supplied by tests or users. It does not execute runs, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Publish-preflight guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Phase2 publish-preflight benchmark profile handoff | clean-room status/compare policy over GLASS-owned publish-preflight artifacts | S2-Gate 410 Phase2 status handoff for resident CUDA benchmark profile evidence | Carries the Windows publish-preflight benchmark profile handoff into `glass phase2-status`, surfaces the release/default handoff in JSON and Markdown, and extends `glass phase2-status-compare` so candidate statuses cannot lose or fail present publish-preflight `resident_cuda_dq_v1` benchmark profile evidence. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status policy over GLASS-owned Windows publish-preflight and Phase2 status JSON | The handoff consumes GLASS Phase2 status JSON and Windows publish-preflight JSON supplied by tests or users. It does not execute runs, read image pixels, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Status/compare guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| StackEngine publication-audit benchmark profile handoff | clean-room publication-audit policy over GLASS-owned publish-preflight and Phase2 status artifacts | S2-Gate 411 publication audit over Gate410 resident CUDA benchmark profile evidence | Carries raw Windows publish-preflight and Phase2 publish-preflight benchmark profile handoff evidence into `glass stack-engine-publication-audit`, requiring present fields to resolve to `resident_cuda_dq_v1`, requiring Phase2/raw agreement, and blocking publication when Phase2 loses raw profile evidence. | `src/glass/report/stack_engine_publication_audit.py` | `tests/test_stack_engine_publication_audit.py` | Project-defined publication-audit policy over GLASS-owned publish-preflight and Phase2 status JSON | The audit consumes GLASS publication/status artifacts supplied by tests or users. It does not execute runs, read image pixels, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Publication-audit guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Phase2 publication-audit benchmark profile handoff | clean-room status/compare policy over GLASS-owned StackEngine publication-audit artifacts | S2-Gate 412 Phase2 status/compare over Gate411 publication-audit benchmark profile evidence | Carries StackEngine publication-audit benchmark profile handoff layers and checks into `glass phase2-status`, adds a top-level publication-audit benchmark profile chain check, surfaces the evidence in Markdown, and extends `glass phase2-status-compare` so candidate statuses cannot lose a previously passing publication-audit `resident_cuda_dq_v1` profile chain. | `src/glass/report/phase2_status.py` | `tests/test_phase2_status.py` | Project-defined Phase2 status/compare policy over GLASS-owned StackEngine publication-audit JSON | The handoff consumes GLASS Phase2 status JSON and StackEngine publication-audit JSON supplied by tests or users. It does not execute runs, read image pixels, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Status/compare guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Default-promotion publication-audit benchmark profile handoff | clean-room default-promotion policy over GLASS-owned Phase2 status artifacts | S2-Gate 413 default-promotion guard for publication-audit benchmark profile evidence | Carries the Phase2 StackEngine publication-audit benchmark profile chain into `glass default-promotion-manifest`, requiring raw and Phase2 publication-audit profile readiness, agreement, and the Phase2 chain check before default promotion can pass. | `src/glass/report/default_promotion_manifest.py` | `tests/test_default_promotion_manifest.py` | Project-defined default-promotion policy over GLASS-owned release-decision and Phase2 status JSON | The manifest builder consumes GLASS release-decision and Phase2 status artifacts supplied by tests or users. It does not execute runs, read image pixels, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Default-promotion guard only. It does not change runtime execution, benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Resident parity summary artifact | clean-room runtime-validation harness over GLASS-owned CPU/resident run artifacts | S2-Gate 415 CPU tiled vs CUDA resident parity summary | Adds `glass resident-parity-summary`, which reads a CPU tiled run, a CUDA resident run, and a GLASS compare JSON artifact, then separates numerical parity from resident result-contract readiness across timing, registration reference, frame count, DQ/rejection accounting, and master-image compare metrics. | `src/glass/report/resident_parity_summary.py`; `src/glass/cli.py` | `tests/test_resident_parity_summary.py` | Project-defined runtime validation policy over GLASS-owned run and compare artifacts | The summary builder consumes GLASS `run_timing.json`, `registration_results.json`, `integration_results.json`, optional `resident_result_contract.json`, and GLASS compare JSON supplied by tests or generated runs. It does not execute runs, read image pixels directly, read user image directories, inspect external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Runtime-validation harness only. It does not change benchmark contract check semantics, DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Resident result-contract path resolution | clean-room runtime-validation contract over GLASS-owned integration artifacts | S2-Gate 416 cwd-relative resident map path handling | Resolves resident result-contract map paths as absolute paths, run-root-relative paths, or current-working-directory-relative GLASS artifact paths, then regenerates the external-matrix CUDA resident contract/parity artifacts so the near-parity synthetic validation is contract-ready. | `src/glass/report/resident_result_contract.py` | `tests/test_resident_result_contract.py` | Project-defined artifact path resolution over GLASS-owned integration output paths | The resolver consumes GLASS `integration_results.json`, output-map paths, and FITS maps generated by GLASS tests or runs. It does not read user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Runtime-validation contract fix only. It does not change DQ pixel semantics, quality metric math, default quality thresholds, star detection, registration transform math, integration math, CUDA kernels, runtime defaults, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Resident quality reference handoff | clean-room runtime reference policy over GLASS-owned quality artifacts | S2-Gate 417 resident CUDA registration reference handoff | Lets resident CUDA registration consume an existing GLASS `frame_quality.json` reference frame when no explicit resident reference or external-matrix reference is supplied, records the selected source/status/path in resident registration artifacts, and validates that synthetic resident triangle registration now matches the CPU quality reference frame. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined reference priority policy over GLASS quality artifacts | The handoff reads only GLASS-generated `frame_quality.json` from the run directory plus GLASS plan frames. Validation uses GLASS synthetic data and GLASS-generated CPU/resident artifacts. It does not read user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Runtime reference-policy fix only. It does not change quality metric math, star detection thresholds, triangle descriptor formulas, registration transform fitting, integration math, DQ pixel semantics, CUDA kernels, runtime packaging, GitHub release creation, or real-data benchmark outputs. Full image parity remains blocked by transform/rejection deltas after the reference-frame mismatch is removed. |
| Resident registration matrix compare | clean-room runtime-validation artifact over GLASS registration matrices | S2-Gate 418 CPU/external versus resident matrix triage | Adds `glass resident-registration-matrix-compare`, which compares CPU or external registration results against resident CUDA registration rows by frame id, reference/status agreement, translation-vector delta, matrix Frobenius delta, RMS, inliers, and matched-star counts. Gate418 validates exact external-matrix handoff and shows quality-reference resident triangle matrices still exceed a strict 0.1 px synthetic parity threshold. | `src/glass/report/resident_registration_matrix_compare.py`; `src/glass/cli.py` | `tests/test_resident_registration_matrix_compare.py` | Project-defined matrix-delta audit over GLASS registration artifacts | The comparison reads only GLASS `registration_results.json` artifacts generated by tests or runs. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Runtime-validation diagnostic only. It does not change quality metric math, star detection, registration transform fitting, triangle descriptor formulas, CUDA kernels, warp behavior, integration math, DQ semantics, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Resident subpixel refinement sweep audit | clean-room runtime-experiment summary over GLASS matrix/parity artifacts | S2-Gate 419 resident refinement variant ranking | Adds `glass resident-registration-matrix-sweep`, which ranks matrix-compare variants and optional parity summaries by strict matrix pass state, max/mean translation delta, RMS, p99, and rejected-sample delta. Gate419 uses it to record that finer triangle fine-step and translation-NCC controls do not improve the 16-frame synthetic strict matrix/parity blocker. | `src/glass/report/resident_registration_matrix_sweep.py`; `src/glass/cli.py` | `tests/test_resident_registration_matrix_sweep.py` | Project-defined experiment ranking over GLASS-owned registration/parity artifacts | The sweep reads only GLASS matrix-compare JSON and resident-parity-summary JSON artifacts generated by tests or runs. It does not read image pixels, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Runtime-experiment diagnostic only. It does not change quality metric math, star detection, registration transform fitting, triangle descriptor formulas, CUDA kernels, warp behavior, integration math, DQ semantics, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. |
| Resident triangle pixel-refine default guard | clean-room default policy over GLASS resident triangle registration artifacts | S2-Gate 420 default-safe resident triangle registration | Changes the resident triangle registration default so `cuda_triangle_pixel_refine` is disabled unless explicitly requested by the processing plan or `--resident-triangle-pixel-refine`. Gate420 validates that the no-refine default passes the synthetic strict matrix contract against the S2-Gate 414 CPU registration baseline and reduces image RMS while preserving the old pixel-refine path as opt-in. | `src/glass/engine/resident_cuda.py`; `src/glass/engine/registration.py`; `src/glass/cli.py` | `tests/test_resident_cuda_run.py` | Project-defined default guard derived from GLASS Gate418/Gate419 matrix and parity artifacts | The change and validation use only GLASS synthetic data, GLASS CPU/resident registration artifacts, and GLASS comparison reports. It does not read image pixels outside generated test runs, user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Runtime default/policy change only. It does not change triangle descriptor formulas, star detection kernels, CUDA pixel-refine kernels, warp kernels, integration math, DQ semantics, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. Rejection sample parity remains blocked and is explicitly deferred to the next substantive gate. |
| Resident rejection sample accounting audit | clean-room tiled runtime-validation audit over GLASS CPU/resident output maps | S2-Gate 421 rejection/coverage sample attribution | Adds `glass resident-rejection-sample-audit`, which streams CPU tiled and CUDA resident coverage, DQ, low-rejection, and high-rejection FITS maps tile-by-tile, then separates rejected-sample deltas into post-rejection coverage drift, pre-rejection sample drift, same-pre-rejection rejection semantic drift, compare-region splits, and hotspot tiles. | `src/glass/report/resident_rejection_sample_audit.py`; `src/glass/cli.py` | `tests/test_resident_rejection_sample_audit.py` | Project-defined sample-accounting decomposition over GLASS-owned StackEngine maps | The audit reads only GLASS-generated CPU/resident run artifacts and optional GLASS compare JSON. It does not read user image directories, external implementation source, proprietary behavior, package contents, GitHub releases, benchmark reference outputs, or external release data. | Runtime diagnostic only. It does not change CUDA kernels, registration transform fitting, warp math, integration math, DQ semantics, package artifacts, GitHub release creation, publish behavior, or real-data benchmark outputs. Gate421 shows the Gate420 global rejected-sample delta is dominated by resident geometric/coverage input-sample drift, so the next substantive fix should target resident coverage/edge or transform parity before tightening rejection semantics. |
| Guarded resident triangle translation refinement | clean-room resident CUDA registration guard over GLASS star catalogs | S2-Gate 428 resident translation-refine stability | Preserves the Gate426 first median-translation refinement as the default resident triangle registration behavior, adds optional bounded post-median refinement iterations, records initial seed-pair RMS and accepted iteration counts, and prevents future multi-iteration experiments from silently falling back to worse general similarity seeds or large pair-set jumps. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined robust translation-refinement guard derived from GLASS Gate426-Gate428 synthetic CUDA validation artifacts | The guard consumes only GLASS resident star catalogs and GLASS-generated synthetic CPU/resident validation artifacts. It does not read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, create releases, or upload assets. | Runtime algorithm guard only. It does not change CUDA kernels, DQ pixel semantics, winsorized rejection math, release artifacts, package publication, or the 200-light benchmark output. Gate428 validates that unguarded extra iterations can worsen output deltas and that the guarded default preserves Gate426 output while exposing the next blocker: resident star-catalog/transform precision. |
| Resident GPU global-mean centroid background | clean-room resident CUDA star-centroid refinement over GLASS calibrated stacks | S2-Gate 429 resident centroid background precision | Adds a resident CUDA finite-frame mean reduction and uses global-mean background for resident top-NMS centroid refinement by default in triangle registration, while preserving local-median centroid background as an explicit override. | `cpp/cuda/star_detect_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_gpu_star_detect.py`; `tests/test_resident_cuda_run.py` | Project-defined centroid-background choice derived from GLASS CPU-vs-resident catalog probes and Gate429 synthetic runtime validation artifacts | The change consumes only GLASS-generated calibrated frames, resident CUDA catalogs, and GLASS CPU/resident audit artifacts. It does not read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, create releases, or upload assets. | Runtime algorithm change only. It changes resident CUDA star-centroid background handling and artifacts, not DQ pixel semantics, winsorized rejection formulas, release artifacts, package publication, or the 200-light benchmark output. Gate429 improves 16-frame matrix/warp/rejection deltas but leaves resident catalog performance and warp-value parity as the next blockers. |
| Resident triangle auto grid-batch catalog default | clean-room resident CUDA registration default over GLASS star catalogs | S2-Gate 430 resident catalog batch performance | Makes resident `similarity_cuda_triangle` automatically use deterministic 8x8 grid catalogs with 8 candidates per cell when no explicit star grid is supplied, preserving explicit user overrides and recording auto-grid provenance in artifacts and warnings. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py` | Project-defined runtime default derived from Gate429/Gate430 GLASS synthetic CUDA timing and parity artifacts | The change consumes only GLASS resident catalog APIs and GLASS-generated CPU/resident validation artifacts. It does not read user image directories, inspect external implementation source, use proprietary behavior, inspect package contents, create releases, or upload assets. | Runtime default/performance change only. It does not alter CUDA kernels, DQ pixel semantics, winsorized rejection formulas, package publication, or the 200-light benchmark output. Gate430 reduces the 16-frame resident registration/catalog hot path from about 80.232 s to about 0.047995 s while preserving matrix/RMS parity and slightly improving output-map deltas. |
| Resident registration quality admission gate | clean-room resident CUDA frame-admission policy over GLASS registration diagnostics | S2-Gate 433 automatic resident frame-quality gate | Adds an auditable resident registration quality decision contract. In `auto` mode, resident `similarity_cuda_triangle` rejects accepted fits with fewer than 4 inliers before integration, records per-frame thresholds, inliers, RMS, transform/refine diagnostics, action, final status, and reasons, and preserves explicit `off`/`warn`/`exclude` overrides. | `src/glass/engine/resident_registration_quality.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_resident_registration_quality.py` | Project-defined default derived from GLASS Gate431/Gate432 real M38 H 200-light artifacts: the historical explicit-mask frames had 2-3 triangle inliers, while a retained low-confidence comparison frame had 5 inliers. | Validation consumes only GLASS-generated registration artifacts and GLASS real-run outputs from the user's local dataset. It does not inspect external implementation source, use proprietary algorithms, modify input image directories, change CUDA kernels, or create release artifacts. | Runtime admission-policy change only. It changes which low-confidence resident triangle frames are assigned zero integration weight under the default `auto` policy and writes `resident_registration_quality.json`; registration fitting, warp kernels, DQ pixel semantics, winsorized rejection math, packaging, and release behavior are unchanged. |
| Resident frame-level mask admission contract | clean-room resident CUDA DQ/mask execution contract over GLASS frame weights and registration diagnostics | S2-Gate 434 resident StackEngine DQ mask contract | Adds `resident_frame_masks.json` and a pre-integration validator: every resident frame with zero or non-finite integration weight must have a structured reason such as manual exclude, registration-quality exclude, registration failure, local-normalization failure, or weighting failure. Frame accounting now promotes registration-quality mask decisions to `quality_rejected` instead of anonymous `zero_weight`. | `src/glass/engine/resident_frame_mask.py`; `src/glass/engine/resident_cuda.py`; `src/glass/engine/frame_accounting.py` | `tests/test_resident_frame_mask_contract.py`; `tests/test_resident_cuda_run.py` | Project-defined pipeline contract derived from Gate433 real 200-light resident quality decisions and existing GLASS DQ map semantics. | Validation consumes only GLASS-generated registration quality, integration weight, frame accounting, DQ, and real-run artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change CUDA kernels, package releases, or upload assets. | Runtime execution-contract change only. It does not change registration fitting, warp kernels, winsorized rejection math, or pixel DQ bit definitions; it prevents silent frame drops before resident StackEngine integration and records how frame-level masks relate to pixel-level warp/rejection maps. Gate434 real M38 H 200-light output is bitwise identical to Gate433 auto-quality output while classifying 7 masked frames as registration-quality masks and 0 unaudited zero-weight frames. |
| Resident DQ pixel closure contract | clean-room resident CUDA DQ/mask execution contract over GLASS frame masks, geometric coverage, rejection maps, and DQ provenance | S2-Gate 435 resident StackEngine DQ pixel closure | Adds `resident_dq_pixel_closure.json` and a resident CUDA validator that closes frame-level admission against pixel-level provenance: active frame count, geometric warp coverage frame count, post-rejection coverage source terms, low/high rejected sample counts, output DQ summary, and valid-sample arithmetic must agree before the run is accepted. | `src/glass/engine/resident_dq_pixel_closure.py`; `src/glass/engine/resident_cuda.py` | `tests/test_resident_dq_pixel_closure.py`; `tests/test_resident_frame_mask_contract.py`; `tests/test_resident_cuda_run.py` | Project-defined closure derived from GLASS DQ map semantics and Gate434 real 200-light frame-mask artifacts. | Validation consumes only GLASS-generated resident frame masks, DQ summaries, coverage provenance, rejection maps, and real-run artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change CUDA kernels, package releases, or upload assets. | Runtime validation contract only. It does not change registration fitting, warp kernels, winsorized rejection math, output pixels, or frame admission; Gate435 real M38 H 200-light output is bitwise identical to Gate434 while proving 193 active frames, 193 geometric coverage frames, and 61,680,274 rejected samples close exactly. |
| Resident shared master-cache manifest | clean-room resident CUDA throughput/audit contract over GLASS calibration master cache files | S2-Gate 436 cold/warm shared master-cache benchmark | Adds `resident_master_cache.json`, which records each resident master set's cache key, fingerprint, scope, hit/miss state, required `.npy` and stats files, byte totals, and completeness. Resident artifacts and integration results link to the manifest summary so cold/warm benchmark cache behavior is auditable. | `src/glass/engine/resident_master_cache.py`; `src/glass/engine/resident_cuda.py` | `tests/test_resident_master_cache.py`; `tests/test_resident_cuda_run.py` | Project-defined content-addressed cache evidence over GLASS calibration groups, input stat tokens, policy, and cache files. | Validation consumes only GLASS-generated master-cache stats and files plus user-provided benchmark run artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change CUDA kernels, package releases, or upload assets. | Runtime cache-audit/throughput gate only. It does not change calibration math, registration fitting, warp kernels, winsorized rejection math, output pixels, or frame admission. Gate436 warm-cache real M38 H 200-light output is bitwise identical to Gate435 while reducing total time from 29.058 s cold to 17.407 s warm and reducing `master_build_or_load` from 11.177 s to 0.396 s. |
| Resident light pipeline profile | clean-room resident CUDA throughput diagnostic over GLASS timing artifacts | S2-Gate 437 warm-cache light read/upload/calibration profile | Adds a `resident_light_pipeline_profile` block to resident artifacts and integration outputs, decomposing `light_read_upload_calibrate` into consumer read/decode wait, native H2D+calibrate/store, sync, and unaccounted orchestration, with knob snapshots and a recommendation string. | `src/glass/engine/resident_light_pipeline_profile.py`; `src/glass/engine/resident_cuda.py` | `tests/test_resident_light_pipeline_profile.py`; `tests/test_resident_cuda_run.py` | Project-defined timing decomposition over GLASS resident timing fields | Validation consumes only GLASS-generated timing, I/O overlap, cache, DQ, and real-run artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change CUDA kernels, package releases, or upload assets. | Runtime diagnostic contract only. It does not change calibration math, registration fitting, warp kernels, winsorized rejection math, output pixels, frame admission, or default runtime preset. Gate437 records that aggressive prefetch lowers read-wait/light-bucket time but increases native/host contention enough that throughput-v1 remains the best same-window total-runtime setting. |
| Resident FITS fast-reader probe | clean-room resident CUDA I/O probe over simple primary FITS images | S2-Gate 438 bounded FITS decode cost probe | Adds a bounded simple-primary FITS reader that parses primary headers, memory maps common BITPIX image payloads, applies BSCALE/BZERO/BLANK semantics, and exposes resident `--resident-fits-read-mode astropy|auto|fast` with backend counts and fallback reasons in resident artifacts. | `src/glass/io/fits_fast.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_fits_io.py`; `tests/test_resident_cuda_run.py`; `tests/test_cli_smoke.py` | Project-defined FITS primary-image parser using public FITS format semantics already represented by GLASS FITS I/O tests | Validation consumes only GLASS-generated FITS fixtures and the user's real M38 H 200-light benchmark run artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, change CUDA kernels, package releases, or upload assets. | Runtime I/O probe only. Gate438 proves 200/200 real light frames can use `fast_simple` with zero output delta against Gate437 and same-code astropy control, and reduces the same-code light pipeline by about 0.061 s, but total runtime is not improved. Therefore the conservative default remains `astropy`; `auto`/`fast` are explicit probes for future decode/upload optimization. |
| Resident native-direct FITS decode probe | clean-room resident CUDA I/O probe over simple primary FITS images | S2-Gate 439 direct decode into resident host buffers | Adds native `read_simple_fits_into_f32`, Python wrapper `glass_cuda.read_simple_fits_into_f32`, and resident `--resident-fits-read-mode native_direct`, with explicit native file-read/decode/bytes timing and a fast path for the benchmark's `BITPIX=16, BZERO=32768` unsigned-FITS convention. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/io/fits_fast.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_fits_io.py`; `tests/test_resident_cuda_run.py`; `tests/test_cli_smoke.py`; `tests/test_cuda_resident_stack.py`; `tests/test_gpu_calibration_vs_cpu.py` | Project-defined native implementation of public FITS primary-image BSCALE/BZERO/BLANK semantics | Validation consumes only GLASS-generated FITS fixtures and GLASS real M38 H 200-light benchmark artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or upload assets. | Runtime I/O probe only. Gate439 proves native-direct output is bitwise identical to Gate438 astropy control with DQ closure passing at 193 active/7 masked frames, but the same-code light read/upload/calibrate bucket remains slower (6.818247 s vs 6.624954 s). The default remains `astropy`; the next substantive gate should target fused native decode-to-upload or GPU-side decode staging. |
| Resident raw-u16 GPU FITS decode staging | clean-room resident CUDA I/O and calibration staging over simple primary FITS images | S2-Gate 440 compact raw FITS upload with GPU decode/calibration | Adds pinned `uint8` host staging, native raw FITS payload reads, a CUDA kernel for `BITPIX=16, BSCALE=1, BZERO=32768, no BLANK` big-endian payloads, and resident `--resident-fits-read-mode native_u16_gpu` so compatible lights upload compact raw bytes and decode/calibrate on GPU. | `cpp/cuda/calibration_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/io/fits_fast.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_fits_io.py`; `tests/test_cuda_resident_stack.py`; `tests/test_resident_cuda_run.py`; `tests/test_cli_smoke.py`; `tests/test_gpu_calibration_vs_cpu.py` | Project-defined CUDA staging implementation of public FITS unsigned-16 `BZERO=32768` semantics plus existing GLASS calibration math | Validation consumes only GLASS-generated FITS fixtures and GLASS real M38 H 200-light benchmark artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or upload assets. | Runtime I/O/calibration staging probe. Gate440 is bitwise identical to Gate438 astropy control and Gate439 native-direct fastpath with DQ closure passing at 193 active/7 masked frames, while reducing real 200-light total runtime from 17.288359 s to 13.938977 s and the light read/upload/calibrate bucket from 6.624954 s to 3.501538 s. The path remains explicit until Gate441 adds guarded compatibility auto-selection with fallback. |
| Guarded resident raw-u16 GPU FITS auto-selection | clean-room resident CUDA I/O policy over public FITS headers | S2-Gate 441 guarded compatible raw-GPU FITS auto path | Adds a header-only eligibility probe for compact unsigned-16 FITS lights and lets resident `--resident-fits-read-mode auto` select `native_u16_gpu` only when every light in the group matches `BITPIX=16`, `BSCALE=1`, `BZERO=32768`, no `BLANK`, and the runtime supports callback-release batch calibration. Artifacts record requested/effective mode, runtime eligibility, checked/eligible frame counts, fallback reason counts, and sample ineligible frames. | `src/glass/io/fits_fast.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_fits_io.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_442_guarded_auto_regression.json` | Project-defined guard over public FITS header semantics and GLASS resident runtime capabilities | Validation consumes only GLASS-generated FITS fixtures, GLASS resident artifacts, and user-owned real benchmark outputs. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or upload assets. | Runtime I/O selection policy only. It preserves `astropy` as the conservative default and keeps explicit `fast`, `native_direct`, and `native_u16_gpu` semantics. Gate442 proves the guarded auto path on the real 200-light benchmark before broader default promotion. |
| Guarded resident FITS auto regression contract | clean-room runtime regression audit over GLASS resident artifacts | S2-Gate 442 guarded auto 200-light regression | Adds `glass resident-fits-auto-regression`, which verifies that resident `auto` selected raw-u16 GPU decode, all checked light frames were eligible, DQ/frame-mask closure passed, compare JSON remains within zero-diff limits, and total/light-pipeline timing remains inside explicit/control baseline envelopes. | `src/glass/report/resident_fits_auto_regression.py`; `src/glass/cli.py` | `tests/test_resident_fits_auto_regression.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_442_guarded_auto_regression.json` | Project-defined regression thresholds over GLASS Gate438/Gate440/Gate442 runtime artifacts | Validation consumes only GLASS-generated JSON/FITS artifacts, GLASS compare reports, and user-owned real benchmark outputs. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or upload assets. | Runtime mainline regression contract. It does not alter image math; it prevents the guarded auto path from being treated as proven unless raw-GPU selection, DQ closure, zero-diff output, and speed envelope all pass together. |
| Resident guarded-auto FITS default routing | clean-room runtime default policy over GLASS CLI and resident artifacts | S2-Gate 443 resident guarded-auto default path | Makes resident CUDA `glass run`/`glass audit` resolve omitted `--resident-fits-read-mode` to guarded `auto`, records `resident_cuda_guarded_auto_default` provenance in run timing and resident I/O artifacts, and preserves explicit `astropy`, `fast`, `native_direct`, and `native_u16_gpu` overrides. | `src/glass/cli.py`; `src/glass/engine/resident_cuda.py`; `src/glass/report/resident_fits_auto_regression.py` | `tests/test_resident_cuda_run.py`; `tests/test_resident_fits_auto_regression.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_443_default_guarded_auto_regression.json` | Project-defined default-routing policy derived from Gate440-Gate442 zero-diff 200-light benchmark evidence | Validation consumes only GLASS-generated synthetic fixtures and user-owned real benchmark outputs. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or upload assets. | Runtime default change only. Image math is unchanged; compatible data now reaches the already-guarded raw-u16 GPU path by default in resident CUDA, while explicit `astropy` remains the compatibility escape hatch. |
| Resident FITS default compatibility matrix | clean-room runtime compatibility audit over GLASS run artifacts | S2-Gate 444 default fallback compatibility matrix | Adds `glass resident-fits-default-matrix`, which reads GLASS run artifacts and verifies that resident CUDA default routing selects raw-u16 GPU only for compatible light groups, records fallback reasons for incompatible simple FITS groups, preserves explicit astropy, and leaves CPU/tile paths non-resident. | `src/glass/report/resident_fits_default_matrix.py`; `src/glass/cli.py` | `tests/test_resident_fits_default_matrix.py`; `tests/test_resident_cuda_run.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_444_default_fallback_matrix.json` | Project-defined compatibility matrix over public FITS header semantics and GLASS default-routing artifacts | Validation consumes only GLASS-generated 24x24 FITS fixtures, GLASS run artifacts, and GLASS CLI default-resolution metadata. It does not inspect external implementation source, use proprietary behavior, modify user image directories, change calibration/registration/rejection math, create package releases, or upload assets. | Runtime compatibility audit only. It does not change image math; it prevents the resident guarded-auto default from silently dropping the conservative fallback path or overriding explicit astropy/CPU tile behavior. |
| StackEngine strict default-path audit | clean-room runtime contract over GLASS StackEngine and resident artifacts | S2-Gate 445 StackEngine default path audit and gap closure | Adds a `default_path` block to `glass stack-engine-contract`, distinguishing native `stack_engine_cpu` default surfaces from `cuda_resident_stack` contract-emulation surfaces, adds `--require-native-stack-engine-default` exit-code gating, fixes resident calibration contract path resolution for cwd-relative artifacts, and prevents attached resident calibration contracts from duplicating per-master calibration surfaces. | `src/glass/report/stack_engine_contract.py`; `src/glass/report/resident_calibration_contract.py`; `src/glass/cli.py` | `tests/test_stack_engine_contract.py`; `tests/test_resident_calibration_contract.py`; `runs/checkpoints/s2_gate_445_cpu_native_stackengine_contract.json`; `runs/checkpoints/s2_gate_445_resident_contract_emulation.json` | Project-defined default-path classification and contract gate over GLASS-owned StackEngine/resident artifacts | Validation consumes only GLASS synthetic CPU runs, GLASS-generated resident CUDA run artifacts, and GLASS contract JSON. It does not inspect external implementation source, use proprietary behavior, modify user image directories, change calibration/registration/rejection math, change CUDA kernels, create package releases, or upload assets. | Runtime contract hardening only. It does not change image math or performance. It makes the current state explicit: CPU/tiled surfaces are strict native StackEngine defaults, while resident CUDA surfaces are contract-audited emulation surfaces and still block strict native StackEngine default readiness. |
| Resident CUDA StackEngine surface contract | clean-room runtime contract emitted by GLASS resident execution | S2-Gate 446 resident StackEngine surface closure | Emits `resident_stack_engine_surface_contract` payloads from resident CUDA integration while arrays are still in memory, attaches StackRequest/StackResult-shaped contracts to resident integration outputs and resident master-calibration artifacts, records real master source frame ids in resident master cache stats, and teaches `glass stack-engine-contract` to distinguish StackEngine-shaped resident surfaces from older resident contract-emulation surfaces. | `src/glass/engine/resident_stack_surface.py`; `src/glass/engine/resident_cuda.py`; `src/glass/engine/resident_calibration_artifacts.py`; `src/glass/report/stack_engine_contract.py` | `tests/test_resident_stack_surface.py`; `tests/test_resident_cuda_run.py`; `tests/test_stack_engine_contract.py`; `runs/checkpoints/s2_gate_446_stack_engine_contract.json`; `runs/checkpoints/s2_gate_446_cpu_vs_resident_metrics.json` | Project-defined StackRequest/StackResult surface schema over GLASS resident CUDA artifacts and existing CPU StackEngine semantics | Validation consumes only GLASS-generated clean-star synthetic CPU/resident runs, resident CUDA artifacts, GLASS FITS outputs, and GLASS contract JSON. It does not inspect external implementation source, use proprietary behavior, modify user image directories, change CUDA kernels, create package releases, or upload assets. | Runtime artifact/contract closure. It does not change CUDA pixel math or 200-light output; it makes resident master and integration surfaces directly auditable as StackEngine-shaped contracts. Gate446 also exposes the next substantive gap: dirty synthetic source-DQ invalid samples still differ until resident CUDA consumes the CPU StackEngine source-DQ model. |
| Resident frame-accounting zero-weight closure | clean-room audit contract over GLASS resident frame masks, integration weights, and 200-light acceptance artifacts | S2-Gate 452 real 200-light resident regression and frame-accounting closure | Counts zero-weight frames from `integration_status_counts` while preserving specific `final_status_counts` such as `quality_rejected`, and lets the acceptance contract validate legacy zero-weight requirements against integration status when final status carries a more diagnostic rejection reason. Adds source-DQ cache disk preflight for guarded resident runs. | `src/glass/engine/frame_accounting.py`; `src/glass/report/benchmark_contract.py`; `src/glass/cli.py` | `tests/test_frame_accounting.py`; `tests/test_resident_frame_mask_contract.py`; `tests/test_acceptance_audit.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_452_real_regression_summary.json` | Project-defined audit policy over GLASS frame accounting, resident frame masks, integration weights, and disk-budget preflight. | Validation consumes only GLASS-generated artifacts and user-owned M38 H-alpha 200-light run outputs. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change CUDA kernels, create package releases, or upload assets. | Runtime audit/preflight gate only. It does not change calibration, registration fitting, warp, rejection, integration pixel math, or frame admission. Gate452 proves the contract-parity resident CUDA path still passes real 200-light acceptance at `30.900435 s` versus WBPP black-box `1092.541 s`, while documenting that the current stricter default quality gate integrates `191/200` instead of the contract-parity `193/200`. |
| Resident source-DQ strategy estimator | clean-room runtime strategy over GLASS processing plans and resident DQ semantics | S2-Gate 453 resident source-DQ strategy artifact | Estimates calibrated+DQ cache disk usage and resident invalid-mask streaming memory from plan metadata, writes `resident_source_dq_strategy.json` for resident CUDA runs, keeps cache preflight compatibility, and recommends `resident_in_vram_mask_streaming` when cache materialization is over disk budget. | `src/glass/engine/resident_source_dq_strategy.py`; `src/glass/cli.py` | `tests/test_resident_source_dq_strategy.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_453_real_source_dq_strategy.json` | Project-defined runtime strategy derived from GLASS resident source-DQ semantics: source invalid samples are applied to resident calibrated frames in memory and skipped by CUDA integration as NaN samples. | Validation consumes only GLASS processing plans, GLASS-generated synthetic resident runs, and user-owned M38 H-alpha 200-light metadata. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change CUDA kernels, create package releases, or read real pixels for the 200-light strategy artifact. | Runtime strategy/preflight gate only. It does not change calibration, registration, warp, rejection, integration pixel math, or frame admission. Gate453 makes the next DQ/mask path explicit: the real 200-light calibrated+DQ cache is too large for current disk budget, while an 8-frame resident invalid-mask streaming batch fits within a 96 GB VRAM budget. |
| Resident source-DQ execution contract | clean-room runtime execution contract over GLASS resident source-DQ rows and CUDA stack application | S2-Gate 454 resident source-DQ execution contract | Emits `resident_source_dq_execution.json` from actual resident CUDA source-DQ rows, proving invalid samples are applied through `ResidentCalibratedStack.apply_invalid_mask_frame`, the execution route is `resident_in_memory_mask_streaming`, and no calibrated+DQ disk cache is required for integration. | `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_cuda.py` | `tests/test_resident_source_dq.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_454_real_regression_summary.json` | Project-defined execution contract over GLASS resident source-DQ semantics and the project-owned CUDA resident stack API. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, or create package releases. | Runtime execution contract only. It does not change calibration, registration, warp, rejection, integration pixel math, or frame admission. Gate454 proves the real 200-light contract-parity path remains accepted while recording source-DQ execution as resident in-memory mask streaming with no calibrated+DQ disk cache materialization. |
| Resident inline source-DQ cosmetic mask | clean-room resident mask-generation contract over GLASS CPU cosmetic baseline and resident CUDA stack application | S2-Gate 455 resident inline source-DQ masks | Adds opt-in `--resident-inline-source-dq cosmetic`, generating hot/cold/nonfinite source invalid masks while resident light frames are loaded and applying them through `ResidentCalibratedStack.apply_invalid_mask_frame` without materializing a calibrated+DQ disk cache. | `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_source_dq_strategy.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_resident_source_dq.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_455_real_regression_summary.json` | Project-defined integration of the existing GLASS CPU cosmetic detector into the resident source-DQ mask contract. The mode excludes flagged samples from integration and does not replace pixels. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime DQ/mask gate only. It does not change default image math because inline mode defaults to `off`; synthetic resident CUDA tests prove finite hot-pixel exclusion when enabled, and Gate455 proves the real 200-light default contract-parity path remains accepted at `20.611956 s` and `53.005208x` versus WBPP black-box timing. |
| Resident CUDA inline source-DQ threshold mask | clean-room CUDA resident detector/application over GLASS scalar threshold contract | S2-Gate 456 resident CUDA inline source-DQ thresholds | Adds opt-in `--resident-inline-source-dq cosmetic_cuda` and native `ResidentCalibratedStack.apply_cosmetic_threshold_mask_frame`, which applies CPU-derived median/MAD scalar thresholds on resident CUDA frame pixels, marks hot/cold/nonfinite samples as NaN, returns device-side counts, and records `mask_upload_s=0` evidence. | `cpp/cuda/integration_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_source_dq_strategy.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_resident_source_dq.py`; `tests/test_cuda_resident_stack.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_456_real_regression_summary.json` | Project-defined CUDA kernel and GLASS CPU scalar-threshold contract. Per-pixel detection and invalid-sample application are project-owned resident CUDA behavior; the CPU threshold source is explicitly recorded as `cpu_median_mad_scalar`. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime DQ/mask gate only. It does not change default image math because inline mode defaults to `off`; focused CUDA tests prove hot/cold/nonfinite resident exclusion without full-mask upload, and Gate456 proves the real 200-light audit-map path remains accepted at `31.352762 s` and `34.846723x` versus WBPP black-box timing. |
| Resident CUDA sampled source-DQ threshold stats | clean-room resident CUDA sampling plus GLASS robust scalar threshold contract | S2-Gate 457 resident CUDA sampled source-DQ threshold stats | Adds native `ResidentCalibratedStack.frame_sampled_robust_stats`, which samples an already calibrated resident frame without downloading the full frame, computes bounded-sample median/MAD scalar thresholds, and switches opt-in `cosmetic_cuda` to record `threshold_source=cuda_resident_sampled_median_mad_scalar` before the existing CUDA threshold-apply kernel. | `cpp/cuda/integration_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_source_dq_strategy.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_resident_source_dq.py`; `tests/test_cuda_resident_stack.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_457_real_regression_summary.json` | Project-defined resident sampling kernel and GLASS median/MAD threshold semantics. Small all-pixel samples are tested against CPU numpy median/MAD; large-frame thresholds are explicitly approximate bounded-sample statistics. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime DQ/mask gate only. It does not change default image math because inline mode defaults to `off`; Gate457 proves the default real 200-light audit-map contract remains accepted at `36.073588 s` and `30.286452x` versus WBPP black-box timing. A later gate should replace sampled host sorting with all-GPU robust quantile/histogram reductions. |
| Resident CUDA histogram source-DQ threshold stats | clean-room resident CUDA histogram reductions plus GLASS robust scalar threshold contract | S2-Gate 458 resident CUDA histogram source-DQ threshold reductions | Adds resident CUDA min/max/count, value histogram, and absolute-deviation histogram launchers plus native `ResidentCalibratedStack.frame_histogram_robust_stats`, then switches opt-in `cosmetic_cuda` to prefer `threshold_source=cuda_resident_histogram_median_mad_scalar` before the existing CUDA threshold-apply kernel. | `cpp/cuda/integration_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_source_dq_strategy.py`; `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_resident_source_dq.py`; `tests/test_cuda_resident_stack.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_458_real_regression_summary.json` | Project-defined resident histogram kernels and GLASS median/MAD threshold semantics. Small CUDA fixtures compare approximate histogram thresholds against CPU numpy median/MAD within bin-width bounds; large-frame thresholds explicitly record histogram approximation metadata. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime DQ/mask gate only. It does not change default image math because inline mode defaults to `off`; Gate458 proves the default real 200-light audit-map contract remains accepted at `35.876992 s` internal run timing and `30.452414x` versus WBPP black-box timing. The compact histogram bin scan is still host-side, so a later gate should batch threshold extraction and move final quantile scan device-side. |
| Resident CUDA batched histogram source-DQ thresholds | clean-room resident CUDA batch histogram statistics plus GLASS robust scalar threshold contract | S2-Gate 459 resident CUDA batched histogram source-DQ thresholds | Adds native `ResidentCalibratedStack.frames_histogram_robust_stats`, which computes histogram median/MAD threshold stats for a batch of resident calibrated frames while reusing device work buffers, and updates resident batch calibration to prefetch `cosmetic_cuda` threshold infos once per batch before per-frame CUDA threshold application. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_cuda.py` | `tests/test_resident_source_dq.py`; `tests/test_cuda_resident_stack.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_459_real_regression_summary.json` | Project-defined batch execution over Gate458 histogram kernels and GLASS median/MAD threshold semantics. Native CUDA tests compare batch output against CPU numpy median/MAD within bin-width bounds; CLI tests verify batch native-method provenance and buffer-reuse evidence. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime DQ/mask performance gate only. It does not change default image math because inline mode defaults to `off`; Gate459 proves the default real 200-light audit-map contract remains accepted at `36.545063 s` internal run timing and `29.895721x` versus WBPP black-box timing. The batch method still synchronizes and scans compact histogram bins per frame, so future work should move quantile scans and threshold application toward fully batched device-side execution. |
| Resident CUDA batched source-DQ threshold apply | clean-room resident CUDA batch threshold application over GLASS scalar threshold contract | S2-Gate 460 resident CUDA batched source-DQ threshold apply | Adds native `ResidentCalibratedStack.apply_cosmetic_threshold_mask_frames`, which applies already computed low/high cosmetic thresholds to a batch of resident calibrated frames in one kernel launch and one synchronization, returning aggregate and per-frame hot/cold/nonfinite counts. Resident batch calibration now uses this batch route for opt-in `cosmetic_cuda` after Gate459 batch histogram threshold extraction. | `cpp/cuda/integration_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_cuda.py` | `tests/test_resident_source_dq.py`; `tests/test_cuda_resident_stack.py`; `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_460_real_regression_summary.json` | Project-defined CUDA threshold-mask kernel and GLASS median/MAD threshold semantics. Synthetic CUDA tests verify batched hot/cold/nonfinite exclusion and integration weight-map effects; CLI tests verify batch native-method provenance and single-kernel/single-sync evidence. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime DQ/mask performance gate only. It does not change default image math because inline mode defaults to `off`; Gate460 proves the default real 200-light audit-map contract remains accepted at `36.230598 s` internal run timing and `30.155202x` versus WBPP black-box timing. The remaining DQ-side gap is host-side compact histogram quantile scanning; broader runtime optimization should also target resident registration/warp orchestration. |
| Resident triangle translation warp batch closure | clean-room resident registration/warp routing over GLASS-owned CUDA matrix warp surfaces | S2-Gate 461 resident triangle translation warp batch closure | Keeps translation-like matrices in the resident matrix batch helper when native batch warp is available, and defers non-pixel-refine `similarity_cuda_triangle` warp application until the registration loop can submit accepted frames as one batch. Per-frame registration warnings now record `matrix_lanczos3_batch`/`matrix_bilinear_batch` provenance for these frames. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py`; `tests/test_gpu_warp_vs_cpu.py`; `tests/test_cuda_resident_stack.py`; `runs/checkpoints/s2_gate_461_real_regression_summary.json` | Project-defined runtime routing over existing GLASS CUDA matrix warp kernels and existing triangle-registration matrix estimates. The gate does not change star detection, descriptor fitting, matrix estimation, rejection, or pixel interpolation math. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime registration/warp performance gate. Gate461 proves the real 200-light path now batches 192 triangle warp applications with `native_loop_batched_inverse_one_sync`, reducing the measured warp segment from Gate460's `4.386194 s` to `3.566975 s` while passing acceptance at `36.563496 s` internal timing and `29.880649x` versus WBPP black-box timing. The default dispatch remains loop-batched, so the next optimization should harden the chunked scatter batch warp path before promotion. |
| Resident chunked warp default promotion | clean-room resident runtime default over GLASS-owned CUDA chunked matrix warp surfaces | S2-Gate 462 resident chunked warp default promotion | Promotes high-level resident warp batch dispatch from `loop` to `chunked` in `glass run`, `glass audit`, and the resident engine. The default `similarity_cuda_triangle` path now records `native_chunked_batch_warp_scatter_one_sync`, chunk workspace bytes, and warp/coverage/scatter launch counts without requiring an explicit CLI flag. | `src/glass/cli.py`; `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py`; `tests/test_gpu_warp_vs_cpu.py`; `tests/test_cuda_resident_stack.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_462_real_regression_summary.json` | Project-defined runtime default change over existing GLASS CUDA chunked matrix warp kernels. The low-level Python wrapper still defaults to loop for direct API compatibility; the high-level pipeline default is chunked. The gate does not change star detection, descriptor fitting, matrix estimation, rejection, source-DQ, or interpolation math. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime registration/warp default-promotion gate with real compute impact. Gate462 proves the no-explicit-dispatch 200-light default path uses `warp_batch_dispatch=chunked`, batches 192 warps in 24 chunks of 8 frames, passes acceptance at `36.018981 s` internal timing and `30.332368x` versus WBPP black-box timing, and keeps compare metrics inside contract. Chunked dispatch uses extra temporary workspace, so later work should surface chunk capacity and VRAM fallback in memory planning/reporting. |
| Resident chunked warp workspace memory planning | clean-room resident VRAM model over GLASS-owned CUDA chunked matrix warp surfaces | S2-Gate 463 chunked warp workspace memory planning | Extends resident CUDA memory estimates and reports so default chunked matrix warp workspace is counted in peak VRAM. The artifact now records preferred and observed chunk capacity, planned and observed workspace bytes, peak memory with and without chunked warp workspace, and structured `triangle_warp_batch_dispatch`. | `src/glass/engine/resident_cuda.py`; `src/glass/report/html_report.py` | `tests/test_resident_cuda_run.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_463_real_regression_summary.json` | Project-defined memory-planning model mirroring the GLASS native chunked warp allocator's preferred `min(frame_count, 8)` capacity and fallback-observed capacity. The gate does not change calibration, star detection, descriptor fitting, matrix estimation, warp interpolation, rejection, source-DQ, or integration math. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime planning/reporting gate with direct resident scheduling value. Gate463 proves the 200-light default path records `triangle_warp_batch_dispatch=chunked`, plans and observes 8-frame chunk workspace at `2466048352` bytes, raises peak VRAM evidence from `47.311736 GiB` to `49.608422 GiB`, and still passes acceptance at `37.830311 s` internal timing and `28.880043x` versus WBPP black-box timing. |
| Resident VRAM admission scheduler | clean-room resident pre-run scheduler over GLASS-owned memory model and CUDA chunked matrix warp surfaces | S2-Gate 464 resident VRAM admission scheduler | Adds `resident_memory_admission.json` before resident CUDA allocation, estimating per-light-group peak VRAM from calibrated-stack, output-map, and chunked-warp workspace byte terms; explicit `--vram-budget-gb` can block before allocation, while detected device-total safety budget is recorded as non-blocking evidence. | `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_resident_cuda_run.py`; `tests/test_cli_smoke.py`; `tests/test_gpu_warp_vs_cpu.py`; `tests/test_cuda_resident_stack.py`; `runs/checkpoints/s2_gate_464_real_regression_summary.json` | Project-defined pre-run admission policy over GLASS metadata, resident exclusion semantics, and the Gate463 memory model. It does not change calibration, star detection, descriptor fitting, transform estimation, warp interpolation, rejection, source-DQ, or integration pixel math. | Validation consumes GLASS synthetic fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime scheduling gate with direct resident stability value. Gate464 proves the 200-light path records `planned_active_frame_count=193`, `planned_warp_frame_count=192`, estimated peak `49.608422 GiB`, budget `86.032617 GiB`, and `resident_full_frame` admission before allocation, then passes resident, pipeline, StackEngine, and acceptance contracts at `37.538072 s` internal timing and `29.104877x` versus WBPP black-box timing. Reduced chunk capacity is still a recommendation; a later scheduler gate should enforce the selected capacity in native dispatch. |
| Admission-selected chunk capacity execution | clean-room resident scheduler execution over GLASS-owned memory model and CUDA matrix warp surfaces | S2-Gate 465 admission-selected chunk capacity execution | Changes resident memory admission so explicit budgets can pass with reduced chunked-warp capacity, then carries that selected capacity into resident matrix-warp execution by splitting Python-level batch submissions only for reduced-capacity runs. The default full-capacity path remains the native one-call chunked dispatcher. | `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_resident_cuda_run.py`; `tests/test_cli_smoke.py`; `tests/test_gpu_warp_vs_cpu.py`; `tests/test_cuda_resident_stack.py`; `runs/checkpoints/s2_gate_465_real_regression_summary.json` | Project-defined runtime scheduling over GLASS admission artifacts and GLASS CUDA warp wrappers. The gate does not change calibration, star detection, descriptor fitting, transform estimation, warp interpolation, rejection, source-DQ, or integration pixel math. | Validation consumes GLASS synthetic/fake-stack fixtures, GLASS-generated resident artifacts, and user-owned M38 H-alpha 200-light output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input image directories, change registration/rejection math, create package releases, or require a calibrated+DQ cache. | Runtime scheduling gate. Gate465 proves reduced-capacity admission is passed to resident execution in tests while the 200-light default path remains `native_chunked_batch_warp_scatter_one_sync` with native chunk frames `8`, chunk count `24`, peak `49.608422 GiB`, and acceptance speedup `30.261113x` versus WBPP black-box timing. Reduced-capacity execution currently uses Python-level batch splitting; a later native backend gate should expose max chunk capacity directly in CUDA bindings. |
| Native chunk-capacity warp dispatch | clean-room native CUDA scheduler control over GLASS resident matrix warp kernels | S2-Gate 466 native chunk-capacity warp dispatch | Adds native `max_chunk_capacity_frames` to resident bilinear and Lanczos3 matrix batch warp bindings, clamps chunked workspace capacity inside the native allocator, and lets admission-selected reduced capacity execute as one Python/native call instead of Python-level batch splitting. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py` | `tests/test_gpu_warp_vs_cpu.py`; `tests/test_resident_cuda_run.py`; `tests/test_cuda_resident_stack.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_466_native_capacity_summary.json` | Project-defined native scheduling over GLASS CUDA matrix warp kernels and GLASS resident admission artifacts. The gate does not change calibration, star detection, descriptor fitting, transform estimation, warp interpolation equations, rejection, source-DQ, or integration pixel math. | Validation consumes GLASS synthetic CUDA resident stacks, fake-stack resident engine fixtures, broader GLASS resident/CLI tests, and the existing user-owned M38 H-alpha 200-light Gate465 baseline. It does not inspect external implementation source, use proprietary behavior, modify input image directories, create package releases, or require a calibrated+DQ cache. | Native backend scheduling gate. Focused CUDA tests prove five-frame bilinear and Lanczos3 batches with `max_chunk_capacity_frames=2` use explicit-capacity native chunking, three chunks, two-frame output/map workspace, and CPU-matching output. Full pytest passed with `1101 passed`; the real 200-light default path remains Gate465's accepted `native_preferred` baseline because C: free space was too low for another large run. |
| Admission-to-native chunk-capacity artifact contract | clean-room resident runtime artifact contract over GLASS admission and native warp timing metadata | S2-Gate 467 admission-to-native chunk-capacity artifact contract | Records the native-reported matrix-warp `batch_capacity_source` and `batch_max_chunk_capacity_frames` in resident registration artifacts, while updating memory-admission wording to state that reduced capacity is passed to native chunked dispatch and native OOM fallback remains the boundary. | `src/glass/engine/resident_cuda.py` | `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_467_native_capacity_contract_summary.json` | Project-defined artifact contract over Gate464-Gate466 resident admission and native timing fields. The gate does not change calibration, star detection, descriptor fitting, transform estimation, warp interpolation equations, rejection, source-DQ, integration pixel math, or CUDA allocation behavior. | Validation consumes GLASS admission unit tests, a GLASS-generated small resident CUDA triangle-alignment run, and the existing user-owned M38 H-alpha 200-light Gate465 baseline for default performance context. It does not inspect external implementation source, use proprietary behavior, modify input image directories, create package releases, or require a calibrated+DQ cache. | Runtime evidence-chain gate. Focused tests prove admission text no longer describes reduced capacity as allocator-driven and default native-preferred CUDA runs now emit both high-level and native capacity-source fields. Full pytest passed with `1101 passed`; a new 200-light run was skipped because C: free space was too low for a large output directory. |
| Resident chunked warp metadata upload reuse | clean-room native CUDA scheduling optimization over GLASS resident matrix warp kernels | S2-Gate 468 resident chunked warp metadata upload reuse | Changes native chunked bilinear and Lanczos3 matrix batch warp dispatch to prepare and upload the full frame-index/inverse-matrix metadata batch once, then reuse device pointer offsets for each chunk while keeping output and coverage scratch chunk-capacity bounded. | `cpp/src/native_bindings.cpp`; `src/glass/engine/resident_cuda.py` | `tests/test_gpu_warp_vs_cpu.py`; `tests/test_resident_cuda_run.py`; `tests/test_cuda_resident_stack.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_468_chunk_metadata_upload_summary.json` | Project-defined native scheduling optimization over existing GLASS CUDA matrix warp kernels. The gate does not change calibration, star detection, descriptor fitting, transform estimation, interpolation equations, rejection, source-DQ, or integration pixel math. | Validation consumes GLASS synthetic CUDA resident stacks, a small GLASS-generated resident CUDA triangle-alignment run, broader resident/CLI/CUDA tests, and the existing user-owned M38 H-alpha Gate465 baseline for real-data context. It does not inspect external implementation source, use proprietary behavior, modify input directories, create package releases, or require a calibrated+DQ cache. | Performance gate. Focused CUDA tests prove five-frame/two-capacity bilinear and Lanczos3 batches still use three warp chunks but now perform one index upload and one inverse upload while matching CPU reference output. Full pytest passed with `1102 passed`; a new 200-light run was skipped because C: free space was too low, but the same native rule would reduce the Gate465 24-chunk metadata uploads to one per metadata kind. |
| Resident chunked warp fused postprocess | clean-room native CUDA postprocess fusion over GLASS resident matrix warp scratch buffers | S2-Gate 469 resident chunked warp fused postprocess | Adds a fused CUDA postprocess kernel that writes chunked warp scratch output back to the resident stack and accumulates coverage in one launch, replacing separate coverage-reduce and scatter launches for chunked bilinear and Lanczos3 matrix warp dispatch. | `cpp/cuda/warp_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass/engine/resident_cuda.py` | `tests/test_gpu_warp_vs_cpu.py`; `tests/test_resident_cuda_run.py`; `tests/test_cuda_resident_stack.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_469_fused_warp_postprocess_summary.json` | Project-defined CUDA kernel fusion over existing GLASS warp scratch buffers. The gate does not change calibration, star detection, descriptor fitting, transform estimation, interpolation equations, rejection, source-DQ, integration pixel math, or frame admission. | Validation consumes GLASS synthetic CUDA resident stacks, a small GLASS-generated resident CUDA triangle-alignment run, broader resident/CLI/CUDA tests, and the existing user-owned M38 H-alpha Gate465 baseline for real-data context. It does not inspect external implementation source, use proprietary behavior, modify input directories, create package releases, or require a calibrated+DQ cache. | Performance gate. Focused tests prove one-chunk and three-chunk bilinear/Lanczos3 paths now report `postprocess_mode=fused_scatter_reduce`, zero independent coverage/scatter launches, one fused postprocess launch per chunk, and CPU-matching output. Full pytest passed with `1102 passed`; a new 200-light run was skipped because C: free space was too low, but the same native rule would reduce Gate465 24-chunk postprocess launches from 48 to 24. |
| Resident fused matrix dispatch benchmark | clean-room synthetic CUDA benchmark over GLASS resident dispatch routes | S2-Gate 470 resident fused matrix dispatch benchmark | Adds a deterministic synthetic resident benchmark that compares chunked matrix warp into the resident stack plus resident mean integration against fused matrix-warped integration without registered-stack writeback. | `benchmarks/bench_resident_fused_matrix_dispatch.py` | `tests/test_benchmarks.py`; `runs/checkpoints/s2_gate_470_fused_matrix_dispatch_benchmark.json` | Project-defined synthetic mono frames, project-defined translation matrices, and existing GLASS CUDA resident stack/fused integration APIs. The benchmark does not introduce new interpolation, registration, rejection, or weighting formulas. | Validation consumes only GLASS-generated synthetic arrays and GLASS-owned CUDA kernels. It does not inspect external implementation source, use proprietary behavior, modify input directories, read PixInsight outputs, or require a calibrated+DQ cache. | Performance-validation gate. The benchmark keeps host upload outside the timed dispatch comparison and records timing, throughput, native timing payloads, and master/weight agreement. It provides synthetic evidence for prioritizing fused matrix integration as the next 200-light production optimization once disk space is available. |
| Fused throughput runtime preset | clean-room resident runtime preset over GLASS scheduling and dispatch knobs | S2-Gate 471 fused throughput preset | Adds opt-in `--resident-runtime-preset throughput-v2-fused`, inheriting throughput-v1 prefetch/H2D/calibration scheduling while setting `resident_integration_dispatch=auto` unless the user explicitly overrides that dispatch option. | `src/glass/cli.py`; `src/glass/report/benchmark_contract.py` | `tests/test_cli_smoke.py`; `tests/test_resident_cuda_run.py`; `tests/test_acceptance_audit.py`; `runs/checkpoints/s2_gate_471_status.md` | Project-defined runtime preset based on Gate470 GLASS synthetic fused-dispatch evidence and earlier throughput-v1 real-data scheduling evidence. The preset selects among existing GLASS dispatch routes; it does not add new calibration, registration, interpolation, rejection, weighting, or DQ formulas. | Validation consumes GLASS CLI argument state, GLASS synthetic resident CUDA smoke fixtures, and GLASS acceptance-contract fixtures. It does not inspect external implementation source, use proprietary behavior, modify input directories, read PixInsight outputs, or create release artifacts. | Runtime candidate gate. The default remains `throughput-v1`; `throughput-v2-fused` is only an explicit A/B candidate until a 200-light real-data run proves speed, frame accounting, DQ maps, acceptance, and compare agreement. |
| Real 200-light WBPP/GLASS A/B return | clean-room real-data regression over GLASS artifacts and user-generated WBPP black-box outputs | S2-Gate 472 real 200-light A/B return | Runs the M38 H-alpha `200` light, `20` bias, `20` dark, `20` flat resident CUDA parity path after freeing C: space, compares the GLASS master against the user-generated WBPP FastIntegration master, and records acceptance-audit plus speedup summaries. | `docs/phase2_algorithm_hardening.md`; `runs/checkpoints/s2_gate_472_status.md` | Real commands recorded in `runs/checkpoints/s2_gate_472_status.md`; `glass acceptance-audit`; `benchmarks/summarize_wbpp_speedup.py`; `glass compare` | Experiment-derived validation over existing GLASS runtime knobs: resident CUDA, throughput-v1, triangle registration, Lanczos3 warp, winsorized sigma rejection, no weighting, no LN, and flat floor `0.05`. No runtime defaults or algorithms changed. | Validation consumes GLASS-generated run artifacts under `C:\glass_runs\phase2_ab_200_wbpp_parity_20260622_223410`, staged user image inputs under `C:\gpwbpp_runs\final_m38_h_200\input`, and user-generated WBPP black-box outputs/timing under `C:\gpwbpp_runs\final_m38_h_200\pixinsight_wbpp_blackbox`. It does not inspect external implementation source, modify input directories, or create package releases. | Real regression gate. The contended run passed acceptance with `18.413825453037212x` speedup versus WBPP black-box timing, `193` active frames, DQ/sample-accounting closure, coverage fraction `0.960532609259836`, RMS `0.0017794216505176163`, and p99 absolute difference `0.00042621337808668863`. Because another GPU process was active, a clean idle-GPU rerun remains required before using this as final performance evidence. |
| Fused integration memory admission | clean-room resident VRAM scheduler model over GLASS dispatch routes | S2-Gate 473 fused integration admission model | Makes pre-run resident memory admission resolve the effective integration dispatch and omit chunked registered-stack warp workspace only when the route is effective `fused_matrix`; CLI admission artifacts now include the requested/effective integration dispatch and selection reason. | `src/glass/engine/resident_cuda.py`; `src/glass/cli.py` | `tests/test_resident_cuda_run.py`; `runs/checkpoints/s2_gate_473_real_plan_admission_compare.json`; `runs/checkpoints/s2_gate_473_status.md` | Project-defined memory-planning model that mirrors existing GLASS resident engine dispatch policy. It does not introduce new calibration, registration, interpolation, rejection, weighting, DQ, or integration formulas. | Validation consumes GLASS synthetic/unit fixtures, a GLASS-generated small CUDA fused-dispatch smoke run, and the user-staged M38 H-alpha real 200-light processing plan for metadata-only admission comparison. It does not inspect external implementation source, use proprietary behavior, modify input directories, read PixInsight implementation source, or launch a large timed run. | Scheduler gate for the next real A/B. On the M38 200-light plan, stack/Lanczos3 admission estimates `49.60843022540212 GiB` peak with `199` planned warp frames and `2466056756` bytes of chunked workspace; auto/bilinear fused admission estimates `47.3117358982563 GiB` with zero planned warp frames and zero chunked workspace. |
| Resident 200-light A/B matrix runner | clean-room execution matrix over GLASS resident CUDA commands and user-generated WBPP black-box outputs | S2-Gate 474 resident 200-light A/B matrix runner | Adds `glass resident-ab-matrix-plan` and `glass resident-ab-matrix-execute` to generate and optionally execute a readiness-guarded two-variant real-data matrix: `throughput-v1` Lanczos3 registered-stack parity baseline versus `throughput-v2-fused` bilinear resident candidate, followed by WBPP-reference compare, baseline compare, acceptance-audit, speedup summary, and report generation. | `src/glass/report/resident_ab_matrix_plan.py`; `src/glass/cli.py` | `tests/test_resident_ab_matrix_plan.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_474_ab_matrix_plan.json`; `runs/checkpoints/s2_gate_474_ab_matrix_execution_dry_run.json` | Project-defined run orchestration contract over existing GLASS resident CUDA settings, acceptance thresholds, benchmark profile, and compare/report commands. It does not introduce new calibration, registration, interpolation, rejection, weighting, DQ, integration formulas, or CUDA kernels. | Validation consumes GLASS command metadata, the user-staged M38 H-alpha 200-light plan/manifest, and user-generated WBPP black-box timing/reference paths. It does not inspect external implementation source, use proprietary behavior, modify input directories, or launch a heavy run while the GPU is busy. | Execution-readiness gate. The generated plan recorded RTX PRO 6000 Blackwell free VRAM `78987 MiB`, GPU utilization `100%`, C: target free space `65.19125747680664 GiB`, and therefore blocked non-dry-run execution until a clean GPU window. Dry-run execution proved both planned variants and all comparison/acceptance/report steps are reproducible for the next real A/B. |
| Resident A/B live readiness recheck | clean-room execution-safety contract over GLASS resident A/B plans | S2-Gate 475 resident A/B live readiness recheck | Makes `glass resident-ab-matrix-execute` recheck GPU and target-disk readiness immediately before non-dry-run execution, using the live sample as the execution gate while keeping dry-run deterministic and preserving an explicit `--no-readiness-recheck` escape hatch. | `src/glass/report/resident_ab_matrix_plan.py`; `src/glass/cli.py` | `tests/test_resident_ab_matrix_plan.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_475_live_recheck_execution_blocked.json`; `runs/checkpoints/s2_gate_475_status.md` | Project-defined execution safety policy over GLASS-owned A/B plan artifacts, nvidia-smi GPU telemetry, and target disk free-space checks. It does not introduce new calibration, registration, interpolation, rejection, weighting, DQ, integration formulas, CUDA kernels, or runtime pixel math. | Validation consumes GLASS synthetic command-plan fixtures plus the user-staged M38 H-alpha 200-light plan path and user-generated WBPP black-box reference paths. The real non-dry-run execution was live-rechecked and blocked before heavy commands because an unrelated GPU workload kept utilization above threshold. It does not inspect external implementation source, use proprietary behavior, modify input directories, or read image pixels. | Execution-safety gate for the next real A/B. A blocked plan sampled GPU utilization `55%`; the live executor rechecked at `64%`, recorded `execution_readiness.source=live_recheck`, and launched zero variant steps, preventing a contaminated 200-light benchmark. |
| Clean resident 200-light A/B execution | clean-room real-data benchmark over GLASS resident CUDA variants and user-generated WBPP black-box output | S2-Gate 476 clean resident 200-light A/B execution | Adds wait-ready execution controls, consecutive-ready gating, command executable replacement, and launch-error capture to `glass resident-ab-matrix-execute`, then runs the real M38 H-alpha 200-light matrix: `throughput-v1` Lanczos3 registered-stack baseline versus `throughput-v2-fused` bilinear candidate. | `src/glass/report/resident_ab_matrix_plan.py`; `src/glass/cli.py` | `tests/test_resident_ab_matrix_plan.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_476_ab_matrix_execution_real.json`; `runs/checkpoints/s2_gate_476_real_ab_summary.json`; `runs/checkpoints/s2_gate_476_status.md` | Project-defined execution automation and benchmark policy over existing GLASS resident CUDA knobs. The gate does not introduce new calibration, registration, interpolation, rejection, weighting, DQ, integration formulas, or CUDA kernels. | Validation consumes GLASS artifacts from `C:\glass_runs\phase2_s2_gate475_ab_matrix_real`, the user-staged M38 H-alpha 200-light plan/manifest, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, modify input directories, or read proprietary implementation behavior. | Clean real benchmark gate. After six consecutive ready samples at `0%` GPU utilization, baseline passed acceptance at `30.953057600010652 s` and `35.29670684293315x` versus WBPP; fused bilinear passed at `30.661248599993996 s` and `35.63263239058744x`, saving about `2.296694040298464 GiB` estimated peak VRAM. Candidate-vs-baseline RMS is `4.5849533818006405` ADU, so the fused bilinear route remains opt-in rather than default. |
| Resident I/O preset probe | clean-room resident scheduling preset over existing GLASS I/O/calibration knobs | S2-Gate 477 resident I/O preset probe | Adds opt-in `--resident-runtime-preset throughput-v3-io`, using deeper prefetch and a wider calibration wave to probe the Gate476 resident light read/upload/calibration bottleneck without changing the default `throughput-v1` route. | `src/glass/cli.py` | `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_477_io_probe_summary.json`; `runs/checkpoints/s2_gate_477_status.md` | Project-defined scheduling preset over existing GLASS resident CUDA controls. It does not introduce new calibration formulas, registration math, interpolation, rejection, weighting, DQ semantics, integration formulas, CUDA kernels, or default behavior. | Validation consumes GLASS artifacts from `C:\glass_runs\phase2_s2_gate477_io_probe` plus the Gate476 baseline output. The v3 preset master was compared against the Gate476 Lanczos3 baseline and produced RMS/p99/max absolute difference `0.0`, proving scheduling-only equivalence for this run. It does not inspect external implementation source, modify input directories, or read proprietary behavior. | Real I/O probe gate. Explicit deeper prefetch runs improved total time from Gate476 `30.953057600010652 s` to as low as `30.14285220002057 s`, but the actual `throughput-v3-io` preset repeat was neutral at `30.96547570003895 s`; therefore the preset remains experimental/opt-in and is not promoted to default. |
| Resident grid catalog multi-stream batch | clean-room CUDA scheduling optimization over GLASS resident grid catalog kernels | S2-Gate 488 resident grid catalog multi-stream batch | Adds stream-aware launch wrappers for resident grid-top-NMS catalog, deterministic catalog, centroid refine, and frame-sum kernels, then dispatches independent moving-frame catalog scratch across up to four CUDA streams with explicit stream-phase synchronization and artifact evidence. | `cpp/cuda/star_detect_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `src/glass/report/html_report.py` | `tests/test_cuda_resident_stack.py`; `tests/test_gpu_star_detect.py`; `tests/test_resident_cuda_run.py`; `tests/test_cli_smoke.py`; `runs/checkpoints/s2_gate_488_status.md` | Project-defined CUDA scheduling over existing GLASS resident star catalog kernels. The gate does not change star detection thresholds, NMS ranking, centroid formula, descriptor fitting, transform estimation, warp interpolation, rejection, DQ semantics, or integration pixel math. | Validation consumes focused GLASS CUDA/resident tests, full pytest, a real user-owned M38 H-alpha 200-light run under `C:\glass_runs\phase2_s2_gate488_catalog_multistream_ab_real`, and user-generated WBPP black-box timing/output artifacts for compare and speedup. It does not inspect external implementation source, modify input directories, or import proprietary behavior. | Real performance gate. The warm 200-light total improved from Gate487 `21.647297300049104 s` to `19.822146899998188 s`, WBPP speedup improved to `55.11718813869248x`, and the Gate488 master matched Gate487 with RMS/p99/max absolute difference `0.0`. Catalog timing changed to `batch_multistream_bulk_download_centroid_multistream` with batch size `199`, stream count `4`, sync phases `3`, native total `0.2573601 s`, and native sync `0.2153649 s`. |
| Resident runtime default stack dispatch proof | clean-room runtime default hardening over GLASS resident CLI defaults | S2-Gate 489 resident runtime default stack dispatch proof | Makes the default `throughput-v1` resident runtime preset explicitly apply `resident_integration_dispatch=stack`, so default run artifacts prove the conservative StackEngine-compatible resident stack route instead of relying on parser defaults. | `src/glass/cli.py`; `tests/test_cli_smoke.py` | `tests/test_cli_smoke.py`; real default-route run under `C:\glass_runs\phase2_s2_gate489_runtime_default_stack_ab_real`; `runs/checkpoints/s2_gate_489_status.md` | Project-defined default-route policy. The gate does not change calibration formulas, registration math, interpolation, rejection, DQ semantics, integration formulas, CUDA kernels, or output pixels. | Validation consumes focused GLASS CLI tests, full pytest, a real user-owned M38 H-alpha 200-light run without explicit `--backend`, `--memory-mode`, `--resident-runtime-preset`, or `--resident-integration-dispatch`, and user-generated WBPP black-box timing/output artifacts. It also runs GLASS StackEngine and pipeline contracts on the default-route output. | Runtime-default proof gate. The default run resolved `auto/resident` to `cuda/resident`, applied `throughput-v1` with `resident_integration_dispatch=stack`, completed in `19.54827229998773 s`, passed StackEngine and pixel-verifying pipeline contracts, matched Gate488 with RMS/p99/max absolute difference `0.0`, and measured `55.889389263351205x` speedup versus WBPP. |
| Post-cleanup real 200-light A/B regression | clean-room workspace maintenance and real-data regression over GLASS artifacts | S2-Gate 490 post-cleanup real 200-light A/B regression | Reclaims generated C: run space, preserves inputs and black-box references, and reruns the resident CUDA default stack route plus compare/contract/acceptance evidence. | `runs/checkpoints/s2_gate_490_status.md`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | Full pytest; real default run under `C:\glass_runs\phase2_s2_gate490_post_cleanup_ab_real`; `glass pipeline-contract`; `glass stack-engine-contract`; `glass compare`; `glass speedup-summary`; `glass acceptance-audit` | Project-defined cleanup and regression policy. It does not change calibration formulas, registration math, interpolation, rejection, DQ semantics, integration formulas, CUDA kernels, runtime defaults, or output pixels. | Validation consumes GLASS artifacts, user-staged M38 H-alpha 200-light inputs/plans, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, modify input directories, or delete preserved reference/master-cache data. | Real regression gate. The post-cleanup run completed in `19.786300399980973 s`, kept `193 / 200` active frames, matched Gate489 with RMS/p99/max `0.0`, matched WBPP within RMS `0.0017794216505176163` and p99 `0.00042621337808668863` at coverage fraction `0.960532609259836`, and measured `55.21704300016847x` speedup versus WBPP. |
| Resident source-DQ degenerate active-frame guard | clean-room DQ/source-mask contract over GLASS resident artifacts | S2-Gate 491 resident source-DQ degenerate active-frame guard | Adds a pipeline-contract resident source-DQ execution check, surfaces it in HTML, and adds `active_frame_count_not_degenerate` to the resident result contract so multi-frame resident outputs cannot silently collapse to a single active frame. | `src/glass/report/pipeline_contract.py`; `src/glass/report/html_report.py`; `src/glass/report/resident_result_contract.py`; `tests/test_pipeline_contract.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | `tests/test_pipeline_contract.py`; `tests/test_resident_source_dq.py`; full pytest; real source-DQ diagnostic and default A/B run under `C:\glass_runs\phase2_s2_gate491_source_dq_contract_ab_real` | Project-defined DQ contract policy over GLASS source-DQ/resident result artifacts. It does not change calibration formulas, star detection, registration transform math, warp interpolation, rejection formulas, integration formulas, CUDA kernels, or the production/default route pixels. | Validation consumes GLASS source-DQ execution artifacts, resident result contracts, current GLASS 200-light run artifacts, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, proprietary behavior, or input image directories outside normal read-only benchmark inputs. | Contract-hardening gate. The diagnostic `cosmetic_cuda` source-DQ run now fails on `active_frame_count_not_degenerate` after collapsing to `1 / 200` active frames, while the default 200-light A/B remains green: `19.793096599983983 s`, `193 / 200` active frames, Gate490 RMS/p99/max `0.0`, WBPP RMS `0.0017794216505176163`, p99 `0.00042621337808668863`, coverage `0.960532609259836`, and `55.19808355812724x` speedup. |
| Registration-safe resident cosmetic CUDA source-DQ ordering | clean-room resident execution-order fix over GLASS source-DQ and registration artifacts | S2-Gate 492 defer cosmetic_cuda source-DQ until post-registration/pre-warp | Keeps `cosmetic_cuda` threshold statistics at calibrated-frame time, but defers the destructive resident threshold application until each frame has completed registration and immediately before its warp, with a final post-registration flush for reference, failed, excluded, and fused-matrix frames. | `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused resident CUDA source-DQ tests; full pytest; real default A/B and real `cosmetic_cuda` diagnostic under `C:\glass_runs\phase2_s2_gate492_deferred_source_dq_ab_real` | Project-defined execution ordering over GLASS resident CUDA stack state. It does not change threshold formulas, star-detection thresholds, registration transform math, warp interpolation, rejection formulas, integration formulas, CUDA kernels, or the default route when inline source-DQ is off. | Validation consumes GLASS-generated synthetic fixtures, current GLASS resident artifacts, the user-owned M38 H-alpha 200-light run, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, proprietary behavior, or modify input image directories. | Runtime ordering gate. The real `cosmetic_cuda` diagnostic no longer collapses after source-DQ: registration status is `192 ok`, `1 reference`, `7 excluded`, coverage active frames remain `193 / 200`, all 200 source-DQ rows are applied with `post_registration_pre_warp`, and pending count is `0`. The default A/B remains green at `19.737044300010893 s`, `193 / 200` active frames, Gate491 RMS/p99/max `0.0`, WBPP RMS `0.0017794216505176163`, p99 `0.00042621337808668863`, coverage `0.960532609259836`, and `55.354843582095874x` speedup. |
| Conservative resident cosmetic CUDA source-DQ high-fraction guard | clean-room DQ/mask safety policy over GLASS resident threshold rows and CUDA count-only kernels | S2-Gate 493 conservative `cosmetic_cuda` source-DQ guard | Adds native count-only threshold kernels and wrappers for resident frames/batches, preflights opt-in `cosmetic_cuda` threshold rows before destructive application, skips frames whose would-invalid fraction exceeds the default `0.0001`, and records would-invalid/guarded-skip counts in source-DQ summaries and resident artifacts. | `cpp/cuda/integration_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_cuda.py`; `src/glass/engine/resident_source_dq_strategy.py`; `src/glass/cli.py`; `tests/test_cuda_resident_stack.py`; `tests/test_resident_source_dq.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused CUDA/source-DQ/CLI tests; real default A/B and real guarded `cosmetic_cuda` diagnostic under `C:\glass_runs\phase2_s2_gate493_guard001_ab_real` | Project-defined conservative guard over GLASS-owned resident source-DQ semantics. The count-only kernels use the same scalar low/high threshold contract as existing `cosmetic_cuda`, but do not modify pixels; skipped frames keep calibrated resident samples unchanged. | Validation consumes GLASS-generated fixtures, current GLASS resident artifacts, the user-owned M38 H-alpha 200-light run, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, use proprietary behavior, or modify input image directories. | DQ/mask safety gate. The default production route remains source-DQ off and pixel-identical to Gate492, completing in `17.58118659997126 s` with `62.14262011198868x` speedup versus WBPP. The guarded diagnostic skipped `200/200` would-high-fraction rows, recorded `96,803,833` would-invalid samples, applied `0` source-DQ invalid samples, passed the pipeline contract, and matched the default master with RMS/p99/max `0.0`. |
| Structure-aware inline cosmetic source-DQ CPU baseline | clean-room CPU defect detector over GLASS DQMask semantics | S2-Gate 494 isolated-defect source-DQ baseline | Adds `detect_isolated_cosmetic_defects`, which marks hot/cold candidates only when they are strong global and local outliers and lack same-sign support in the 8-neighborhood, then routes resident `--resident-inline-source-dq cosmetic` through that detector while recording `inline_structure_cosmetic_source_dq` provenance. | `src/glass/cpu/cosmetic.py`; `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_source_dq_strategy.py`; `src/glass/engine/resident_cuda.py`; `tests/test_cpu_calibration.py`; `tests/test_resident_source_dq.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused CPU/source-DQ/resident CLI tests; real default regression under `C:\glass_runs\phase2_s2_gate494_structure_cosmetic_ab_real` | Project-defined isolated-defect heuristic. It uses robust global median/MAD scale, local 8-neighbor median residuals, and same-sign neighbor support to protect compact star-like structure while flagging isolated defects. | Validation consumes GLASS-generated synthetic fixtures, current GLASS resident artifacts, and the user-owned M38 H-alpha 200-light run for default-path regression. It does not inspect external implementation source, use proprietary behavior, or modify input image directories. | CPU baseline DQ/mask gate. The opt-in CPU `cosmetic` route is safer for stars; default production output remains unchanged, with Gate494 vs Gate493 RMS/p99/max `0.0` and default runtime `17.611496199970134 s`. CUDA `cosmetic_cuda` still needs a resident GPU port of this structure-aware model before it can apply useful masks on real light frames. |
| Resident CUDA isolated cosmetic source-DQ | clean-room CUDA port of GLASS-owned CPU baseline | S2-Gate 495 resident CUDA isolated-defect source-DQ | Ports the Gate494 isolated-defect detector to resident CUDA for opt-in `cosmetic_cuda`, adding single-frame and batched count/apply kernels that classify global+local hot/cold candidates, protect same-sign neighbor-supported structure, set only selected invalid samples to NaN, and record candidate/protected counts. | `cpp/cuda/integration_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_source_dq.py`; `src/glass/engine/resident_source_dq_strategy.py`; `src/glass/engine/resident_cuda.py`; `tests/test_cuda_resident_stack.py`; `tests/test_resident_source_dq.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused CUDA/source-DQ/CLI tests; full pytest; real default regression under `C:\glass_runs\phase2_s2_gate495_isolated_cuda_ab_real`; Gate495-vs-Gate494 compare; GLASS-vs-WBPP compare, speedup summary, and acceptance audit | Project-defined CUDA implementation of the Gate494 robust median/MAD, local 8-neighbor median residual, and same-sign neighbor support heuristic. | Validation consumes GLASS-generated synthetic fixtures, current GLASS resident artifacts, the user-owned M38 H-alpha 200-light run, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, use proprietary behavior, modify input directories, or copy any external algorithm implementation. | CUDA DQ/mask hardening gate. The default route remains source-DQ off and matched Gate494 with RMS/p99/max `0.0` while completing in `17.566973500011954 s`; WBPP comparison passed with RMS `0.0017794216505176163`, p99 `0.00042621337808668863`, coverage fraction `0.960532609259836`, and `62.192898509197185x` speedup. The opt-in `cosmetic_cuda` route is now structure-aware but remains a conservative isolated-defect detector rather than a complete bad-pixel-map or semantic star classifier. |
| Resident stack minimal diagnostic download skip | clean-room CUDA transfer policy over GLASS resident stack outputs | S2-Gate 496 resident stack minimal output transfer | Extends registered-stack resident sigma/winsorized integration with `download_mode=master_weight`, so `--resident-output-maps minimal` downloads only the final master and internal weight map while skipping coverage, low-rejection, high-rejection, and geometric-coverage diagnostic map downloads. Audit/science modes keep the full diagnostic maps. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/algorithm_sources.md` | focused resident CUDA minimal-output test; full pytest; real 200-light A/B validation recorded in `runs/checkpoints/s2_gate_496_status.md` | Project-defined resident transfer scheduling over existing GLASS CUDA stack kernels. | Validation consumes GLASS synthetic resident fixtures, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, alter integration formulas, change accepted-frame decisions, modify input directories, or use proprietary behavior. | Runtime transfer optimization. Pixel math and stack dispatch remain unchanged; minimal mode intentionally omits diagnostic maps and should be used for speed probes, while `audit` remains the evidence-rich map mode. |
| Resident minimal master-only output transfer | clean-room CUDA transfer and contract policy over GLASS resident stack outputs | S2-Gate 497 resident minimal master-only speed path | Adds `download_mode=master_only` to resident stack and fused matrix integration wrappers so `--resident-output-maps minimal` downloads only the final master and does not allocate or copy a host weight map that is not written. Also aligns the resident result contract with minimal output-map policy so skipped DQ/geometric maps are not false-negative contract failures. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `src/glass/report/resident_result_contract.py`; `tests/test_resident_cuda_run.py`; `tests/test_resident_result_contract.py`; `benchmarks/bench_resident_fused_matrix_dispatch.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused resident CUDA and contract tests; full pytest; real 200-light minimal A/B validation under `C:\glass_runs\phase2_s2_gate497_master_only_minimal_ab_real` | Project-defined output-transfer policy and policy-aware contract logic over GLASS artifacts. The CUDA kernels still compute the device weight map internally; only the unused host-side download/allocation is skipped in minimal mode. | Validation consumes GLASS synthetic fixtures, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, alter integration formulas, change accepted-frame decisions, modify input directories, or use proprietary behavior. | Runtime speed-path optimization. The real 200-light minimal run completed in `7.6039500999613665 s`, matched Gate496 minimal with RMS/p99/max `0.0`, matched WBPP within RMS `0.0017794216505176163` and p99 `0.00042621337808668863` at coverage fraction `0.960532609259836`, and measured `143.6807166850754x` speedup versus WBPP. Audit/science remain the full diagnostic-map modes. |
| Resident minimal warp coverage accumulator skip | clean-room CUDA scheduling/output-policy optimization over GLASS resident matrix warp | S2-Gate 498 resident minimal warp coverage accumulator skip | Adds `track_coverage` to resident batch matrix warp wrappers and disables resident geometric warp coverage accumulator updates in `--resident-output-maps minimal`, while keeping per-frame batch coverage scratch for warp/scatter correctness. Audit/science and legacy calls keep tracking enabled. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused resident matrix-batch tests; CUDA import/device/smoke tests; native micro-check; full pytest; real 200-light A/B validation under `C:\glass_runs\phase2_s2_gate498_skip_warpcoverage_ab_real` | Project-defined resident CUDA scheduling policy over GLASS-owned warp kernels and output-map modes. It does not change calibration formulas, star detection, registration transform estimation, warp interpolation samples, rejection formulas, integration formulas, accepted-frame decisions, or public audit/science outputs. | Validation consumes GLASS synthetic/unit tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, alter input image directories, or use proprietary behavior. | Runtime speed-path optimization. The real minimal run completed in `7.653901700046845 s`, used `scatter_only_no_coverage_accumulator`, matched Gate497 bitwise with RMS/p99/max `0.0`, preserved the established WBPP scaled coverage-190 metrics by bitwise equivalence, and measured `142.74301432344149x` speedup versus WBPP. |
| Resident minimal batch warp coverage scratch skip | clean-room CUDA workspace/kernel output-policy optimization over GLASS resident batch matrix warp | S2-Gate 499 resident minimal batch warp coverage scratch skip | Makes batch bilinear/Lanczos3 warp kernels accept `batch_coverage=nullptr` and skips allocating/writing per-frame batch coverage scratch when `track_coverage=false`, preserving audit/science tracking and speed-mode warped pixels. | `cpp/cuda/warp_kernels.cu`; `cpp/src/native_bindings.cpp`; `tests/test_gpu_warp_vs_cpu.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused CUDA warp tests; full pytest; real 200-light A/B validation under `C:\glass_runs\phase2_s2_gate499_no_coverage_scratch_ab_real` | Project-defined CUDA workspace/scheduling policy over existing GLASS warp kernels. It does not change calibration formulas, star detection, registration transform estimation, interpolation formulas, rejection formulas, integration formulas, accepted-frame decisions, or public audit/science outputs. | Validation consumes GLASS CUDA tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/output artifacts. It does not inspect external implementation source, alter input image directories, or use proprietary behavior. | Runtime speed-path optimization. The real minimal run completed in `7.620175199990626 s`, reduced batch warp coverage workspace bytes from `493209600` to `0`, matched Gate498 bitwise with RMS/p99/max `0.0`, preserved the established WBPP scaled coverage-190 metrics by bitwise equivalence, and measured `143.37478749850055x` speedup versus WBPP. |
| Resident v3 I/O preset default | clean-room resident CUDA scheduling optimization over GLASS-owned timing and comparison artifacts | S2-Gate 500 resident v3 I/O default | Promotes the resident CUDA default runtime preset to `throughput-v3-io`, increasing prefetch depth/workers and calibration batch/wave size while preserving explicit `throughput-v1` as a lower-memory fallback. It also teaches benchmark-contract artifact matching to recognize v3-io from `resident_io_pipeline`. | `src/glass/cli.py`; `src/glass/report/benchmark_contract.py`; `src/glass/report/default_promotion_manifest.py`; `src/glass/report/windows_release_matrix.py`; `tests/test_cli_smoke.py`; `tests/test_acceptance_audit.py`; `tests/test_windows_release_matrix.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused CLI/acceptance/release-matrix tests; full pytest; same-window real 200-light v1/v3 A/B; default no-preset real 200-light run under `C:\glass_runs\phase2_s2_gate500_io_prefetch_ab_real` | Project-defined runtime scheduling policy derived from GLASS real-run timing artifacts. The preset changes only prefetch, pinned-ring H2D, calibration batch/wave, and callback release scheduling. | Validation consumes GLASS-generated run artifacts, the user-staged M38 H-alpha 200-light plan/manifest, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, modify input directories, change scientific formulas, or use proprietary behavior. | Runtime performance gate. Same-window explicit v3-io averaged `6.800686 s` versus v1 `7.610491 s`, reducing read-wait from about `2.289033 s` to `0.978885 s`. The new no-preset default recorded `throughput-v3-io`, completed in `7.274755600024946 s`, matched Gate499 bitwise with RMS/p99/max `0.0`, and measured `150.184x` speedup by GLASS run timing (`143.171x` by conservative shell wall compare) versus WBPP. |
| Direct simple FITS output writer | clean-room FITS serialization over GLASS output arrays | S2-Gate 501 direct simple-primary FITS writer | Adds a GLASS-owned direct writer for 2D simple primary FITS images with native FITS dtypes (`uint8`, `int16`, `int32`, `float32`, `float64`), big-endian storage, FITS block padding, and atomic temp-file replacement. Unsupported shapes/dtypes continue to use Astropy `PrimaryHDU`. Resident output artifacts record `writer_backend`. | `src/glass/io/fits_io.py`; `src/glass/engine/resident_cuda.py`; `tests/test_fits_io.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused FITS roundtrip/header/padding tests; resident output storage tests; full pytest; real 200-light default run under `C:\glass_runs\phase2_s2_gate501_direct_fits_writer_ab_real` | Project-defined FITS file serialization using public FITS layout rules. It does not change pixel values, calibration formulas, registration, warp, rejection, weighting, DQ semantics, or frame admission. | Validation consumes GLASS arrays, FITS roundtrip tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, or modify input directories. | Output-stage performance gate. The real default v3-io run used `writer_backend=direct_simple_primary`, reduced master output write timing to `0.548847800004296 s` from Gate500's `0.68125790002523 s`, matched Gate500 bitwise with RMS/p99/max `0.0`, and measured `152.8641040841657x` speedup by GLASS run timing versus WBPP. |
| Explicit resident warp chunk capacity | clean-room resident CUDA scheduler control over GLASS memory admission and matrix-warp dispatch | S2-Gate 502 explicit resident warp chunk capacity | Adds `--resident-warp-chunk-capacity-frames` to `glass run` and `glass audit`, routes the requested capacity through resident memory admission, records requested/preferred/selected capacity source fields, and passes the selected explicit or reduced capacity into resident execution. | `src/glass/cli.py`; `src/glass/engine/resident_cuda.py`; `tests/test_cli_smoke.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused resident admission/CLI tests; full pytest; real 200-light default/chunk16/chunk32/chunk64 A/B under `C:\glass_runs\phase2_s2_gate502_warp_chunk_capacity_ab_real\runs_20260623_051320` | Project-defined runtime scheduler control over existing GLASS native chunked matrix warp wrappers. It does not change calibration formulas, star detection, registration transform estimation, warp interpolation, rejection, weighting, DQ semantics, integration formulas, accepted-frame decisions, or default behavior when the option is omitted. | Validation consumes GLASS unit tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, or modify input directories. | Runtime control and negative-promotion gate. On the 200-light benchmark, chunk8 default completed in `7.12994339998113 s`, chunk16 in `7.12517720006872 s`, chunk32 in `7.1467631999985315 s`, and chunk64 in `7.218323500012048 s`; all candidate masters matched chunk8 bitwise and Gate501 bitwise. Larger chunks are safe but not materially faster, so the default remains native-preferred chunk8. |
| Batched triangle descriptor generation | clean-room CUDA batching of GLASS triangle asterism descriptors | S2-Gate 503 batched triangle descriptor generation | Adds native `triangle_asterism_descriptors_batch_f32`, Python wrapper/fallback, and resident `similarity_cuda_triangle` routing so moving-catalog descriptor generation can run as one padded batch kernel/sync/download before the existing batch descriptor-fit stage. | `cpp/cuda/registration_kernels.cu`; `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `tests/test_gpu_registration_search.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | CUDA batch-vs-single descriptor tests; resident triangle smoke; full pytest; real 200-light Gate503 run and repeat under `C:\glass_runs\phase2_s2_gate503_descriptor_batch_ab_real\runs_20260623_052608` | Project-defined batching over the existing GLASS triangle descriptor formula and CPU post-sort/dedup contract. It does not change star catalog selection, descriptor geometry, descriptor sorting/dedup rules, descriptor-fit scoring, transform estimation, warp interpolation, rejection, weighting, DQ semantics, integration formulas, or accepted-frame decisions. | Validation consumes GLASS CUDA tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, or modify input directories. | Resident registration performance gate. On the 200-light benchmark, moving descriptor generation fell from Gate502 `0.1319461001548916 s` to `0.05534540000371635 s` / `0.05633970000781119 s`, and the registration component fell from `2.0864001998257167 s` to about `2.00 s`; both Gate503 masters matched Gate502 bitwise with RMS/p99/max `0.0`. End-to-end timing was masked by output-write variance, so this is recorded as a component optimization rather than a default-speed claim. |
| Chunked direct simple FITS output writer | clean-room FITS serialization and output-stage memory policy over GLASS output arrays | S2-Gate 504 chunked direct FITS writer | Changes the direct simple-primary FITS writer to serialize eligible 2D outputs in bounded big-endian row chunks, records `writer_profile` in resident artifacts, and keeps unsupported shapes/dtypes on the Astropy fallback path. | `src/glass/io/fits_io.py`; `src/glass/engine/resident_cuda.py`; `tests/test_fits_io.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused FITS chunk/profile tests; full pytest; real 200-light Gate504 run/repeat and WBPP compare under `C:\glass_runs\phase2_s2_gate504_chunked_fits_writer_ab_real\runs_20260623_053419` | Project-defined FITS serialization using public FITS layout rules and GLASS-owned output arrays. It does not change pixel math, calibration formulas, star detection, registration, warp interpolation, rejection, weighting, DQ semantics, frame admission, or output-map policy. | Validation consumes GLASS unit tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, or modify input directories. | Output-stage performance and memory-pressure gate. Gate504 wrote the 246,604,800-byte master as `15` chunks, reduced master output write timing from Gate503 `0.6129282000474632 s` / `0.7726107999915257 s` to `0.08645619999151677 s` / `0.08730260003358126 s`, matched Gate503 bitwise with RMS/p99/max `0.0`, and measured `165.1433863147144x` speedup versus the WBPP black-box elapsed time on the same 200-light benchmark. |
| Native host DQ map contract fast path | clean-room host-side native scan over GLASS resident output arrays | S2-Gate 519 native host DQ map contract fast path | Adds `_glass_cuda_native.resident_dq_map_host_f32`, `glass_cuda` wrappers, and resident routing so DQ flags, summary counts, post-rejection coverage stats, geometric coverage stats, and low/high rejection-map stats are produced in one native host pass when the optional CUDA extension is available. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md` | focused DQ parity tests; full pytest; real 200-light warm/full A/B under `C:\glass_runs\phase2_s2_gate519_native_dq_ab\runs_20260623_100321`; GLASS-vs-WBPP compare; Gate519-vs-Gate518 bitwise compare | Project-defined DQ bit construction and stats aggregation over GLASS-owned resident master, weight, coverage, geometric coverage, and rejection maps. The native path preserves the existing Python fallback contract and uses the same GLASS DQ flag values. | Validation consumes GLASS synthetic/unit arrays, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, alter input directories, change calibration formulas, registration math, warp interpolation, winsorized rejection math, or accepted-frame decisions. | DQ/mask contract performance gate. Gate519 keeps science pixels bitwise identical to Gate518 and full-run/warm-run masters bitwise identical, marks DQ stats with `stats_backend=native_host`, and records 200-light speedups of `100.96148226060053x` warm internal and `71.62662202554446x` full internal versus the WBPP black-box `1092.541 s`. The next bottlenecks are light-pipeline Python orchestration/I/O overlap and resident registration/warp batching. |
| Adaptive native DQ dispatch | clean-room runtime dispatch policy over GLASS native build metadata and DQ fallback contract | S2-Gate 520 adaptive resident DQ native dispatch | Adds an optimized-build capability probe for `_glass_cuda_native.resident_dq_map_host_f32`, makes `glass_cuda.resident_dq_map_host_f32_preferred()` choose native only when the native scanner is optimized by default, and adds `GLASS_RESIDENT_DQ_NATIVE_HOST` overrides for diagnostics. Debug or unknown native builds keep the Python vectorized fallback. | `cpp/src/native_bindings.cpp`; `src/glass_cuda.py`; `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md`; `runs/checkpoints/s2_gate_520_status.md`; `runs/checkpoints/s2_gate_520_adaptive_dq_dispatch_summary.json` | cProfile over Gate520 current path; native rebuild; focused ruff and DQ dispatch tests; full pytest; real 200-light warm/full A/B under `C:\glass_runs\phase2_s2_gate520_adaptive_dq_dispatch\runs_20260623_101337`; GLASS-vs-WBPP compare; Gate520-vs-Gate519 bitwise compare | Project-defined adaptive dispatch policy: optimized native host DQ is preferred for Release-like builds, while Debug/unoptimized native builds use the already validated Python fallback unless explicitly overridden. | Validation consumes GLASS profile data, GLASS run artifacts, current GLASS 200-light outputs, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, modify input directories, change DQ bit definitions, calibration, registration, warp, winsorized rejection, integration formulas, or accepted-frame decisions. | Runtime regression recovery gate. The local Debug native build reports `dq_native_preferred=false`, keeps Python fallback stats, restores warm-repeat shell time from Gate519 `11.1992665 s` to `8.173170299999999 s`, and full-run shell time from `15.6334588 s` to `12.7043354 s`. Gate520 matches Gate519 and its own full/warm outputs bitwise, and measures `140.22103678886648x` warm internal / `88.6449295280004x` full internal speedup versus the WBPP black-box `1092.541 s`. |
| Resident Python DQ finite count-map fast path | clean-room Python fallback optimization over GLASS-owned resident count maps | S2-Gate 521 resident Python DQ count-map fast path | Adds a finite integer count-map fast path to the Python resident DQ fallback for GLASS-generated coverage, geometric coverage, and low/high rejection maps. Direct helper calls keep strict NaN and fractional-count semantics by default; the resident pipeline opts into the fast path only for maps produced by GLASS resident CUDA kernels. | `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md`; `runs/checkpoints/s2_gate_521_status.md`; `runs/checkpoints/s2_gate_521_python_dq_fastpath_summary.json` | focused DQ equivalence tests; cProfile before/after; real 200-light warm/full A/B under `C:\glass_runs\phase2_s2_gate521_python_dq_fastpath\runs_20260623_102649`; resident result contract; DQ pixel closure; GLASS-vs-WBPP compare; Gate521-vs-Gate520 bitwise compare | Project-defined DQ/count-map optimization based on the resident CUDA output contract that these maps are finite integer-valued counts. It does not change DQ flag definitions, output pixels, calibration, registration, warp, winsorized rejection, integration formulas, accepted-frame decisions, or strict fallback behavior for arbitrary arrays. | Validation consumes GLASS profile data, GLASS unit tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, or modify input directories. | Runtime DQ/mask contract performance gate. The profiled `_resident_dq_map` fallback dropped from `2.0067628 s` to `1.3696953 s`; warm 200-light internal time improved from Gate520 `7.791562700003851 s` to `6.949569999938831 s`, and full internal time improved from `12.324912499985658 s` to `11.440295899985358 s`. Gate521 matches Gate520 and its own full/warm outputs bitwise while recording `157.20987054013648x` warm internal speedup versus WBPP. |
| Resident DQ int16 runtime map | clean-room DQ bitfield memory contract over GLASS-owned resident output maps | S2-Gate 522 resident DQ int16 runtime map | Adds a checked DQ runtime dtype for the resident Python fallback, keeps direct helper calls on `uint32` by default, routes the resident pipeline DQ bitfield through `int16` to match the FITS DQ artifact dtype, and removes count-map rounding from the DQ output writer path. | `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md`; `runs/checkpoints/s2_gate_522_status.md`; `runs/checkpoints/s2_gate_522_dq_int16_runtime_summary.json` | focused DQ dtype/equivalence tests; cProfile after int16 routing; real 200-light warm/full A/B under `C:\glass_runs\phase2_s2_gate522_dq_int16_runtime\runs_20260623_104044`; resident result contract; DQ pixel closure; GLASS-vs-WBPP compare; Gate522-vs-Gate521 bitwise compare | Project-defined DQ bitfield storage policy derived from GLASS DQ flag values and the existing `int16` FITS DQ artifact contract. It does not change DQ flag definitions, calibration, registration, warp, winsorized rejection, integration formulas, accepted-frame decisions, output master pixels, or strict fallback behavior for arbitrary arrays. | Validation consumes GLASS profile data, GLASS unit tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, or modify input directories. | DQ/mask memory-contract gate. The runtime DQ map for the `9600x6422` benchmark drops from `246604800` bytes as `uint32` to `123302400` bytes as `int16`, saving about `117.59 MiB`; `_resident_dq_map` profiles at `1.3252872 s` versus Gate521 `1.3696953 s`, and output writing profiles at `0.2833296 s` versus Gate521 `0.388811 s`. Gate522 matches Gate521 and its own full/warm outputs bitwise, while GLASS-vs-WBPP robust-fit RMS/P99 remain `0.0015009512947433384` / `0.00034034321741462114`. End-to-end timing is treated as variance rather than a new speedup claim. |
| Resident output diagnostics sampled percentiles | clean-room audit-path diagnostics optimization over GLASS-owned resident output arrays | S2-Gate 523 resident output diagnostics sampled percentiles | Keeps exact output-diagnostic percentiles for small arrays, uses deterministic stride-sampled percentiles for large resident output diagnostics, preserves full-frame exact min/max/mean/std and clipping counts, and records percentile approximation metadata in artifacts. | `src/glass/engine/resident_cuda.py`; `tests/test_cuda_resident_stack.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md`; `runs/checkpoints/s2_gate_523_status.md`; `runs/checkpoints/s2_gate_523_output_diagnostics_sample_summary.json` | focused output-diagnostics tests; cProfile before/after; real 200-light warm/full A/B under `C:\glass_runs\phase2_s2_gate523_output_diagnostics_sample\runs_20260623_105417`; GLASS-vs-WBPP compare; Gate523-vs-Gate522 bitwise compare | Project-defined diagnostic sampling policy over GLASS output artifacts. It does not change output master pixels, DQ bits, coverage/rejection maps, calibration formulas, registration, warp, winsorized rejection, frame admission, or integration formulas. | Validation consumes GLASS profile data, GLASS unit tests, current GLASS 200-light artifacts, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, or modify input directories. | Audit-path performance gate. `_output_diagnostics` profile time dropped from about `0.828 s` to `0.265952 s`, and percentile time dropped from about `0.457 s` to `0.012514 s`. Warm 200-light shell time improved from the immediately preceding current-main measurement `7.4455019 s` to `6.9960698 s`; Gate523 matches Gate522 and its own full/warm outputs bitwise, while GLASS-vs-WBPP robust-fit RMS/P99 remain `0.0015009512947433384` / `0.00034034321741462114`. |
| Resident throughput-v3 prefetch32 default | clean-room resident I/O scheduling retune over GLASS runtime knobs | S2-Gate 524 throughput-v3 prefetch32 default | Retunes the default `throughput-v3-io` resident runtime preset from 48 to 32 prefetch frames while keeping 16 workers and the existing calibration batch/wave/release settings. Benchmark-contract expectations and CLI tests now treat 32 frames as the v3 default. | `src/glass/cli.py`; `src/glass/report/benchmark_contract.py`; `tests/test_cli_smoke.py`; `tests/test_acceptance_audit.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md`; `runs/checkpoints/s2_gate_524_status.md`; `runs/checkpoints/s2_gate_524_prefetch32_default_summary.json` | focused CLI/acceptance tests; same-window 200-light prefetch-depth probe under `C:\glass_runs\phase2_s2_gate524_prefetch_depth_probe\runs_20260623_110004`; default-path real 200-light warm/full validation under `C:\glass_runs\phase2_s2_gate524_prefetch32_default\runs_20260623_110233`; GLASS-vs-WBPP compare; Gate524-vs-Gate523 bitwise compare | Project-defined runtime scheduling default derived from measured GLASS artifacts. It changes only resident prefetch depth and related contract expectations; it does not change CUDA kernels, calibration formulas, registration, warp, rejection, weighting, DQ semantics, frame admission, integration formulas, or output pixels. | Validation consumes GLASS run artifacts, current GLASS 200-light outputs, and user-generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, or modify input directories. | Runtime scheduling gate. Same-window 32-slot runs averaged `6.67240465 s` shell versus 48-slot `7.2235844 s`, while reducing pinned host prefetch memory from `5.512046813964844 GiB` to `3.6746978759765625 GiB`. The new default run records `resident_prefetch_frames=32`, matches Gate523 and its own full/warm outputs bitwise, and measures `165.00201362863976x` warm-shell speedup versus WBPP. |
| Resident triangle translation-refine default bypass | clean-room resident registration scheduling policy over GLASS triangle artifacts | S2-Gate 525 translation-refine default bypass | Changes the resident `similarity_cuda_triangle` default so the Python-side triangle translation-refine post-step is not implicitly enabled by `transform_model=translation`; the previous path remains available when the plan explicitly sets `cuda_triangle_translation_refine: true`. Artifacts and per-frame warnings record `triangle_translation_refine_policy_source`. | `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md`; `runs/checkpoints/s2_gate_525_status.md`; `runs/checkpoints/s2_gate_525_translation_refine_bypass_summary.json` | focused resident triangle tests; real 200-light default/off A/B under `C:\glass_runs\phase2_s2_gate525_translation_refine_probe\runs_20260623_111012`; post-change default validation under `C:\glass_runs\phase2_s2_gate525_translation_refine_default\runs_20260623_111012`; Gate525-vs-Gate524 bitwise map compare | Project-defined runtime policy derived from GLASS timing artifacts showing that the default 200-light path attempted translation refine for 199 non-reference frames, applied it to 0 frames, and rejected/skipped every attempt. It changes scheduling only, not matrix fitting, interpolation, rejection, weighting, DQ semantics, frame admission, or output pixels. | Validation consumes GLASS-generated artifacts, current GLASS 200-light outputs, and previously generated WBPP black-box timing/reference outputs. It does not inspect external implementation source, use proprietary behavior, modify input directories, change CUDA kernels, or copy external algorithms. | Resident registration performance gate. The post-change default records `triangle_translation_refine=false`, policy source `default_similarity_triangle_off`, drops the measured registration component from Gate524 `1.9681131000539125 s` to `1.90622570034715 s`, and keeps master/weight/coverage/rejection/DQ maps bitwise identical to Gate524. |
| Resident DQ valid-map fast path | clean-room DQ/mask contract optimization over GLASS-owned resident maps | S2-Gate 526 resident DQ valid-map fast path | Adds guarded resident DQ Python fast paths for the verified pipeline case where output diagnostics prove finite master pixels and positive weights, while GLASS-generated coverage/rejection maps are finite nonnegative count maps. The strict DQ helper path remains unchanged for arbitrary arrays. | `src/glass/engine/resident_cuda.py`; `tests/test_resident_cuda_run.py`; `docs/phase2_algorithm_hardening.md`; `docs/algorithm_sources.md`; `runs/checkpoints/s2_gate_526_status.md`; `runs/checkpoints/s2_gate_526_dq_valid_map_fastpath_summary.json` | focused DQ map tests; before/after cProfile under `C:\glass_runs\phase2_s2_gate526_profile_current\runs_20260623_120000` and `C:\glass_runs\phase2_s2_gate526_dq_fastpath_profile\runs_20260623_120000`; real 200-light Gate526-vs-Gate525 bitwise map compare | Project-defined optimization derived from GLASS profile data and GLASS DQ map invariants. It changes only the Python fallback's scan/allocation strategy when existing diagnostics prove the fast-path preconditions; it does not change DQ flag values, coverage semantics, rejection maps, registration, warp, integration, or output pixels. | Validation consumes GLASS unit tests, GLASS-generated resident artifacts, current GLASS 200-light outputs, and prior user-generated WBPP black-box comparison evidence. It does not inspect external implementation source, use proprietary behavior, modify input directories, or change CUDA kernels. | DQ/mask runtime gate. `_resident_dq_map_python` cumulative profile time drops from `1.3944242 s` to `0.9936101 s`; the real 200-light run drops from Gate525 shell `6.75901 s` to `6.290154 s`, while master/weight/coverage/rejection/DQ maps remain bitwise identical. |

## Update Rule

When adding or modifying an algorithm:

1. Add or update an entry in this file.
2. Link the implementation file and tests.
3. State whether defaults are project-defined, experiment-derived, or cited.
4. Record any external data or black-box experiment used for validation.
5. Keep public user-facing claims proportional to measured evidence.
